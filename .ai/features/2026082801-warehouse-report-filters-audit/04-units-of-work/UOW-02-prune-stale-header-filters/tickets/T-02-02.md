---
id: T-02-02
uow: UOW-02
title: Test store cho việc dọn bộ lọc
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-02-01]
blocks: []
verifies: [AC-05, AC-06, AC-07]
touches:
  - apps/backoffice-web/src/store/page-stores/report/report.store.test.ts   # new
assumptions: [A-04]
---

# T-02-02 — Test store cho việc dọn bộ lọc

## Context
`apps/backoffice-web/package.json` khai `"test": "echo test"`, nhưng vitest **chạy được** qua
`npx vitest run` và repo đã có test thật ở `src/lib/*.test.ts`. Dùng đúng đường đó, đừng kết
luận app này không test được.

## Implementation notes
- Dựng store bằng `createReportStore(buildInitialReportState(...))`, không mount React
- Ca CHAIN và ca SINGLE tách riêng: `appliedRequest` khác nhau giữa hai nhánh
- Ca hồi quy cho nhóm khác: `PERIOD_COMPARE_*` sống sót khi đổi report type trong nhóm Lợi nhuận

## Done when
- [x] Test: `brand` bị xoá khi đổi sang báo cáo không khai dòng `BRAND`
- [x] Test: `PRODUCT_GROUP` được giữ khi cả hai báo cáo cùng khai dòng đó
- [x] Test: `SKU` sống sót qua mọi lần đổi report type
- [x] Test: 4 line `PERIOD_COMPARE_*` sống sót
- [x] Test: nhánh CHAIN chốt `appliedRequest.filters` đã dọn
- [x] `npx vitest run src/store/page-stores/report` xanh
