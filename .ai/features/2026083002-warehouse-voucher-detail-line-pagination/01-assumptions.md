---
feature: warehouse-voucher-detail-line-pagination
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| A-01 | Phân trang chỉ áp cho chế độ xem chi tiết, không áp cho chế độ tạo và chế độ sửa | high | yes | Phạm vi gấp đôi; phải giải bài toán giữ dòng chưa lưu qua các trang | confirmed | Akenzy chọn "Cả nhập + xuất, chỉ chế độ xem" ngày 2026-08-30 |
| A-02 | Phân trang phải là server-side, payload chỉ mang một trang | high | yes | UOW-02 và UOW-03 viết lại theo hướng cắt trang ở client | confirmed | Akenzy chọn "Server — endpoint trả lines theo trang" ngày 2026-08-30 |
| A-03 | ~~Hai endpoint `/:id/lines` sẵn có đủ dùng, không cần dựng endpoint mới~~ **BÁC BỎ 2026-09-03 — xem A-13** | high | yes | Phải thiết kế và dựng thêm hai endpoint; phạm vi lớn hơn hẳn | superseded | Kiểm mã 2026-08-30: `goods-issue.controller.ts:228` và `goods-receipt.controller.ts:91` đều nhận `PaginationQueryDto` và trả `items/page/pageSize/hasMore/total`; quan hệ `item` và `location` khai `eager: true` trên cả hai entity dòng nên dữ liệu trả về đã đủ mã, tên, ĐVT và vị trí |
| A-04 | Dòng phiếu xuất đang được sắp theo thứ tự tuỳ ý chứ không phải thứ tự nhập | high | yes | Nếu sai thì UOW-01 là công vô ích và phải bỏ cả migration | confirmed | Kiểm mã 2026-08-30: `goods-issue.service.ts:796` sắp `order: { id: 'ASC' }`, mà `id` khai `@PrimaryGeneratedColumn('uuid')` sinh UUID v4 ngẫu nhiên; `GoodsIssueLineEntity` không có cột `createdAt` cũng không có cột thứ tự nào khác |
| A-05 | ~~Phía phiếu nhập đã sắp đúng thứ tự nhập, không cần migration~~ **BÁC BỎ 2026-09-03 — xem A-14.** Vẫn đúng về mặt sự kiện (`created_at` cho ra đúng thứ tự nhập) nhưng kết luận "không cần migration" bị Akenzy đảo | high | yes | Phạm vi migration gấp đôi | superseded | Kiểm mã 2026-08-30: `GoodsReceiptLineEntity` có `@CreateDateColumn({ name: 'created_at' })` và `goods-receipt.service.ts` sắp `order: { createdAt: 'ASC' }` |
| A-06 | Thêm cột thứ tự cho `goods_issue_lines` phải viết migration tay, không dùng `migration:generate` | high | yes | `migration:generate` sinh drift lớn trên repo này và phá batch migration | confirmed | Quy ước sẵn có của repo, đã ghi trong bộ nhớ dự án và lặp lại nhiều lần: trích câu lệnh CREATE rồi viết tay |
| A-07 | ~~Backfill thứ tự dòng phiếu xuất phải dùng thứ tự hiện hành làm gốc vì không còn nguồn nào khôi phục được thứ tự nhập gốc~~ **BÁC BỎ 2026-09-03 — xem A-20** | medium | yes | Dòng của phiếu cũ hiện sai thứ tự vĩnh viễn nhưng ổn định | confirmed | `goods_issue_lines` không có `created_at`, không có cột thứ tự, và `stock_ledger_entries` ghi cả phiếu trong một `recordBatchMovements` nên `posted_at` giống nhau. Không tồn tại nguồn nào phục hồi được thứ tự gõ ban đầu; chỉ phiếu tạo sau khi có cột mới là đúng thứ tự thật |
| A-08 | Panel chi tiết cuộn vô hạn vẫn chạy đúng sau khi đổi thứ tự sắp xếp | medium | no | Panel đó nhảy thứ tự một lần sau khi backfill; sửa cục bộ | confirmed | Verify live ở T-06-04 trên `prod_3008`: cuộn tới hết `XK000018` (120 dòng), hai dòng cuối `NON240` rồi `NON280` — đúng thứ tự `line_no`, không trùng, dừng đúng chỗ. Hoá ra có **hai** panel chứ không phải một: `PurchaseOrdersPage.tsx:981` bên phiếu nhập cũng vậy, cũng đã chuyển và cũng đã kiểm (`NK000038`, dòng cuối `DNGUAB066-N-44`) |
| A-13 | Phải dựng endpoint `POST .../:id/lines/search` theo mẫu V2 và **xoá** `GET /:id/lines` | high | yes | Nếu giữ cả hai thì tồn tại hai đường đọc dòng song song lâu dài, đúng loại phân đôi ADR-04 đã cảnh báo | confirmed | Akenzy chọn "Thay thế: POST search V2 là đường duy nhất" ngày 2026-09-03. Kiểm mã cùng ngày: `GET /:id/lines` chỉ có một consumer khác là panel cuộn vô hạn `GoodsIssuePage.tsx:879`, nên chi phí chuyển đổi là một chỗ |
| A-14 | `goods_receipt_lines` cũng nhận cột `line_no`, đối xứng với `goods_issue_lines` | high | yes | Nếu sai thì thừa một migration rủi ro cao trên bảng đang chạy | confirmed | Akenzy chọn "Thêm line_no cho phiếu nhập (đối xứng ADR-01)" ngày 2026-09-03, biết rõ `created_at` đang cho ra đúng thứ tự |
| A-15 | Lọc phía server chỉ mở cho Mã SKU, Tên hàng hóa, Số lượng, Đơn giá, Thành tiền | high | yes | Handler phải join thêm `locations` + kho; ba ô lọc nữa phải chạy được | confirmed | Akenzy chọn "Chỉ mặt hàng + số" ngày 2026-09-03. Hệ quả bắt buộc: ô lọc của Kho / Vị trí / ĐVT phải tắt tường minh ở chế độ xem |
| A-16 | Ba số ở chân lưới khi đang lọc tính trên **tập đã lọc**, không phải toàn phiếu | high | yes | Handler tính SUM trên điều kiện khác; chân lưới đổi ý nghĩa | confirmed | Akenzy chọn "Theo tập đã lọc" ngày 2026-09-03. Đảo lại quy ước ghi trong `line-item-grid.tsx:107` ("totals are document-wide"), nên phải nói rõ trong ADR-08 |
| A-17 | `≤` của lưới ánh xạ `CompareOperator.LTE`, `*` của lưới ánh xạ `StringOperator.CONTAINS` | high | no | Ô lọc chạy khác thứ ký hiệu trên header quảng cáo | confirmed | Kiểm mã 2026-09-03: `line-item-grid.tsx:189-241` chỉ có hai luật — `≤` so sánh số, còn lại là substring không phân biệt hoa thường; `common/filters/filter.dto.ts` có đúng hai toán tử tương ứng |
| A-20 | Thứ tự vật lý của hàng (`ctid`) giữ được thứ tự nhập gốc, và là nguồn tốt nhất còn lại cho backfill | high | yes | Backfill đóng băng nhiễu cho ~99% dòng phiếu nhập và 100% dòng phiếu xuất | confirmed | Đo trên `prod_3008` ngày 2026-09-03. Tỉ lệ cặp dòng liền kề có mã hàng tăng dần: phiếu nhập **94,5 %** theo `ctid` so với **49,8 %** theo `id` (162.149 cặp); phiếu xuất **82,6 %** so với **50,7 %** (6.452 cặp). 49–51 % là tung đồng xu. Akenzy chốt "Đổi sang ctid" cùng ngày |
| A-21 | `created_at` của `goods_receipt_lines` **không** phân biệt được thứ tự dòng trong phiếu | high | yes | ADR-05 mất hoàn toàn lập luận; backfill theo `created_at, id` thoái hoá về `id` ngẫu nhiên | confirmed | Đo trên `prod_3008` 2026-09-03: 463/627 phiếu có `created_at` trùng giữa các dòng, chiếm 162.612/162.776 dòng. 164 phiếu "sạch" còn lại đều là phiếu **một dòng**, nên không có phiếu nhiều dòng nào mà `created_at` phân biệt được. Phiếu lớn nhất: **5.000 dòng cùng một `created_at`** |
| A-22 | Không đường ghi nào cập nhật một dòng tại chỗ, nên `ctid` không bị xáo bởi UPDATE | high | no | `ctid` sai lệch ở những dòng từng bị sửa | confirmed | Rà đường ghi ở T-01-02 và T-04-02: sửa phiếu **xoá hết dòng rồi chèn lại** cả bộ, không có `UPDATE` từng dòng ở cả hai bảng |
| A-23 | Migration backfill là cửa một chiều: chạy lại sau `down()` cho thứ tự kém hơn | high | no | Ai đó revert–rerun trên production và làm hỏng thứ tự vừa phục hồi, không có cảnh báo nào | confirmed | Đo trên `prod_3008` 2026-09-03: lần chạy đầu 94,5 %, lần thứ hai sau revert 92,5 %, 161.731/162.776 dòng đổi số. Nguyên nhân: chính bước backfill `UPDATE` mọi hàng nên `ctid` sau đó phản ánh thứ tự của lần UPDATE chứ không còn là thứ tự chèn. Đã ghi vào comment cả hai migration và vào phần hệ quả của ADR-09 |
| A-18 | Cột Thành tiền lọc theo `quantity * unit_price`, không theo cột `line_total` | high | no | Lọc ra tập khác thứ đang hiện trên lưới ở những dòng có `line_total` lệch | confirmed | Kiểm mã 2026-09-03 khi làm T-05-01: `line_total` được ghi bằng đúng tích đó ở **mọi** đường ghi phiếu xuất (`goods-issue.service.ts:233, :361, :500`), và handler v2 uỷ quyền về `create` nên đi qua cùng mã. Hai giá trị trùng nhau theo cấu tạo, chỉ lệch ở phép làm tròn 2 chữ số. Chọn `quantity * unit_price` vì đó là thứ cột lưới hiện và là định nghĩa "Tổng tiền" mà search danh sách phiếu đang dùng — dùng cột lưu trữ sẽ thành ba định nghĩa chỉ tình cờ trùng nhau |
| A-09 | Cỡ trang mặc định 50 dòng là hợp lý cho lưới trong dialog | low | no | Đổi một hằng số | confirmed | Dùng thật trên phiếu 118–120 dòng ở `prod_3008`: 3 trang, chuyển trang mượt, một trang ≈ 85 KB. Không có dấu hiệu nào cần đổi. Lưu ý số đo T-03-03: cỡ trang lớn hơn sẽ đẩy điểm hoà vốn thời gian ra xa hơn chứ không gần lại, vì chi phí cố định nằm ở số round-trip chứ không ở số byte |
| A-10 | Endpoint in và xuất Excel không đi qua `/:id/lines` nên không bị ảnh hưởng | high | no | Phiếu in thiếu dòng; phát hiện ngay ở G4 | confirmed | Chứng minh hai lần. T-03-03: 4 test khoá `getPrintPayload` gọi `getById` **không** kèm `includeLines=false`, và phiếu 120 dòng cho payload đủ 120. T-06-04: **xoá hẳn** `GET /:id/lines` và `getLines` khỏi cả hai service mà `jest` (3543 pass) lẫn `pnpm build` vẫn sạch — nếu đường in có phụ thuộc thì nó đã gãy ngay tại đó |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
| --- | --- | --- | --- |
| A-11 | Phải dựng endpoint phân trang mới cho dòng phiếu, như lựa chọn "server-side" hàm ý | Cả hai endpoint đã tồn tại và đã có consumer đang chạy trong sản phẩm | Phần backend thu về gần bằng không. UOW-02 và UOW-03 chỉ còn là nối dây phía web. Việc nặng nhất chuyển sang UOW-01, thứ tự dòng phiếu xuất |
| A-12 | Thứ tự dòng là chuyện phụ, để lại sau cũng được | Cắt trang biến thứ tự tuỳ ý từ chỗ dễ bỏ qua thành lỗi thấy rõ ngay trang 1 | UOW-01 được đưa vào phạm vi và trở thành tiền đề của UOW-02, thay vì tách ra feature riêng |
| A-19 | Ô lọc trên header lưới là chuyện của lưới, phân trang không đụng tới | Lưới chạy ở chế độ **không kiểm soát** và tự lọc trên `rows` nó nhận được. Phân trang làm `rows` teo lại còn một trang, nên ô lọc lặng lẽ đổi từ "tìm cả phiếu" thành "tìm trong 50 dòng" | Đây là hồi quy do chính feature này gây ra, không phải tính năng mới. UOW-04..06 được đưa vào phạm vi ngày 2026-09-03 |
