---
feature: inventory-qa-defects
blocking_open: 0
---

# Assumption register — inventory-qa-defects

Mọi ô "Resolution" phải ghi **ai** chốt và **khi nào**. Giả định chưa chốt mà `Blocking = yes`
sẽ chặn G1 — đó là điều kiện cố ý.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Dòng dư và dòng trùng ở Tổng hợp tồn kho đến từ khối "pending-only" ghép sau phân trang, không phải từ vị từ `search` hay fan-out của join. | high | no | Nếu sai, UOW-01 vá nhầm chỗ và `TXV6079` vẫn còn. | **resolved** | Đọc mã và xác minh trực tiếp (Claude, 03/09/2026): `stock-summary.service.ts:313` bật khối khi `branchId && !storageId && page === 1`; truy vấn `:402-440` chỉ có 4 mệnh đề WHERE (org, chi nhánh đích, `IN_PROGRESS`, chưa xoá) — không `search`, không bộ lọc cột, không `is_active`; ghép vào `data` tại `:575-618` với khoá trùng `groupKey:storageId`; chốt chặn `:944-967` chỉ loại khi có tồn **tại đúng kho đích**. Vị từ `search` (`:1091-1095`) chỉ OR trên hai cột cùng một dòng nên không thể kéo `TXV6079` về. Postgres cục bộ đang tắt nên **chưa** đối chiếu với dữ liệu thật — T-01-01 phải tái hiện trước khi sửa. |
| A-02 | "Ẩn vị trí ngưng hoạt động ở mọi nơi" nghĩa là **cả hai** cột: `locations.is_active = false` và `stock_balances.is_tracked = false`, áp cho mọi ô chọn và mọi auto-fill ở cả backoffice lẫn POS. | high | yes | Chọn sai cột thì hoặc không sửa được lỗi đã báo (E03.01 vẫn `is_active = true`), hoặc giấu mất tồn kho thật. Đổi phạm vi là cắt lại UOW-04. | **resolved** | Akenzy chốt 03/09/2026, chọn "Cả hai, ở mọi ô chọn + auto-fill" sau khi được nêu rõ hai cột khác nhau thế nào và vì sao ẩn nhầm cột là nguy hiểm. Trang quản trị và chứng từ đã lưu giữ nguyên hành vi hiện tại. |
| A-03 | Khối "sắp nhận về" phải **chịu cùng bộ lọc** như dòng thường, chứ không bị bỏ hẳn khi có bộ lọc. | high | yes | Bỏ hẳn thì gõ đúng mã một SKU đang trên đường về sẽ không thấy dòng nào — dễ bị hiểu là hết hàng. Đổi hướng là viết lại UOW-01. | **resolved** | Akenzy chốt 03/09/2026, chọn "Áp cùng bộ lọc cho khối này", kèm sửa khoá chống trùng, số dòng mỗi trang và tổng footer. |
| A-04 | Sửa `counterpartyNameSql` ở **cả 4 chỗ dùng** (Nhập kho, Xuất kho, Chuyển kho, `TRANSPORTER_NAME_SUBQUERY`), không chỉ màn hình QA đã báo. | high | no | Nếu bó lại một chỗ thì đúng lỗi này sẽ được báo lại thêm hai lần nữa. | **resolved** | Akenzy chốt 03/09/2026, chọn "Sửa cả 4 chỗ". |
| A-05 | **Không** xoá liên kết `item_storage_locations` khi Ngừng theo dõi, và **không** migration dọn liên kết cũ. Chỉ chặn ở chỗ đọc. | high | yes | Hệ quả được chấp nhận có chủ ý: liên kết hỏng vẫn nằm trong DB; mọi chỗ đọc viết sau này phải tự nhớ guard, nếu quên thì lỗi tái diễn ở màn hình mới. | **resolved** | Akenzy chốt 03/09/2026, chọn "Chỉ chặn ở chỗ đọc" sau khi được nêu rõ đánh đổi. Ghi vào Out of scope của `00-intent.md`. |
| A-06 | Guard đúng cho nhánh (a) của bộ giải vị trí là "**chưa có** dòng balance **hoặc** `is_tracked = true`", **không** phải `AND sb.is_tracked = true`. | high | yes | Nếu dùng inner join `is_tracked = true`, mặt hàng đã gán kệ nhưng **chưa từng nhận hàng** sẽ mất auto-fill — một lỗi mới nặng hơn lỗi đang sửa, và sẽ đổi hình UOW-04. | **resolved** | Bằng chứng trong chính repo (Claude, 03/09/2026): `isBalanceTracked` (`inventory-location-stock.service.ts:1109-1119`) trả `!balance \|\| balance.isTracked`, và comment `:1103-1108` nói rõ "A mapping with no balance row yet (assigned but never received) stays usable". T-04-02 phải có test cho đúng ca này. |
| A-07 | Không thể thêm `loc.is_active = true` **vô điều kiện** vào `getBalances`; phải là bộ lọc tuỳ chọn + trả `location.isActive` trong DTO, rồi để từng picker tự truyền. | high | yes | `GET /inventory/stock/balances` phục vụ **cả** picker In tem mã lẫn trang "Chi tiết vị trí". Lọc vô điều kiện sẽ làm trang quản trị không còn thấy vị trí đã ngừng — vi phạm chính điều A-02 loại trừ. | **resolved** | Chốt (Claude, 03/09/2026) từ hai call-site đã xác minh: `ItemLocationDetailsQuery.ts:46-51` mặc định `isTracked=true` và bỏ hẳn tham số khi chọn "Tất cả" — tức trang này **cần** đọc được dòng đã ngừng. Thành ADR-02. |
| A-08 | Ở In tem mã, dòng không có `locationCode` xếp **cuối** ở cả hai chiều, cùng quy tắc với dòng trống đang được ghim cuối. | medium | no | Nếu người dùng muốn khác thì đổi một dòng trong comparator; không đụng thiết kế. | **resolved** | Chốt (Claude, 03/09/2026): `locationCode` rỗng rất phổ biến — chế độ chuỗi, gõ tay, và `resolveRowsLocations` chỉ điền khi `source === "preferred" \| "stock"` (`InventoryItemBarcodesPage.tsx:289-301`). Sắp tăng dần mà không ghim thì cả khối rỗng dồn lên đầu, che mất dữ liệu thật. Theo đúng mẫu `isEmptyRow` đang ghim cuối (`:74-84`). |
| A-09 | Khi chế độ chuỗi ẩn cột Vị trí, trạng thái sort `{key:"locationCode"}` còn treo phải được xoá. | medium | no | Nếu không, lưới vẫn âm thầm đảo dòng mà không còn mũi tên nào để giải thích — người dùng không hiểu vì sao thứ tự lạ. | **resolved** | Chốt (Claude, 03/09/2026): cột bị lọc bỏ tại `BarcodeLabelGrid.tsx:357-365` theo `hideLocationColumns`, trong khi `sort` sống ở state trang (`InventoryItemBarcodesPage.tsx:124`) nên không tự dọn. T-03-02 xử lý. |
| A-10 | Ngoại lệ `includeUntracked=true` ở Chuyển kho tạm (POS) là **cố ý** và phải giữ nguyên. | high | no | Nếu siết luôn chỗ này thì mất đường dọn hàng ở kho tạm — đúng thứ tính năng đó sinh ra để làm. | **resolved** | Bằng chứng: `use-fast-stock-transfer-product-picker.ts:56-58` có comment "Chuyển kho tạm cần thấy cả chi tiết đã ngừng theo dõi để dọn hàng"; backend `pos-catalog.service.ts` nhận cờ này có chủ đích. Ghi vào Out of scope. |
| A-11 | `.ai/architecture.md` (sinh 03/08/2026, `repo_root` còn trỏ tên cũ `jack-erp`) vẫn dùng được làm nguồn cho `touches`. | high | no | Nếu bản đồ lệch thì `lint-touches` bắt được, vì nó kiểm sự tồn tại thật trên đĩa. | **resolved** | Chốt (Claude, 03/09/2026): mọi đường dẫn `touches` của feature này được lấy từ discovery trực tiếp và sẽ được `aidlc lint-touches --repo .` kiểm lại, nên bản đồ cũ không phải là nguồn rủi ro ở đây. Không sinh lại để khỏi ghi đè phần Akenzy đã sửa tay. |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|-----------------|----------------------|-------------|
| A-12 | Lỗi 500 cột "Đối tượng" là do trường không có trong whitelist bộ lọc, hoặc do cột đa hình không lọc trực tiếp được. | Trường **có** trong DTO (`goods-receipt-search-v2.dto.ts:49-53`) nên không phải 400/whitelist. Tính đa hình cũng không phải vấn đề: `FilterBuilder.applyString` nhận biểu thức SQL tuỳ ý (`filter.builder.ts:20-49`). Nguyên nhân thật là lệch kiểu `uuid = varchar` trong nhánh `employee`. | UOW-02 không đụng gì tới DTO hay whitelist; sửa gọn trong `counterparty-name.util.ts` + 1 chỗ độc lập ở transfer. |
| A-13 | "Ngưng theo dõi" trong ảnh QA là nhãn suy ra từ việc hết tồn (SL = 0). | Là cột lưu thật: `stock_balances.is_tracked` (`stock-balance.entity.ts:32-37`), có comment mô tả đúng nghĩa "ngừng theo dõi vị trí này". SL = 0 chỉ là hệ quả thường gặp, không phải định nghĩa. | UOW-04 lọc theo `is_tracked` chứ không theo `quantity > 0` — lọc theo số lượng sẽ ẩn nhầm kệ đang theo dõi mà tạm hết hàng. |
