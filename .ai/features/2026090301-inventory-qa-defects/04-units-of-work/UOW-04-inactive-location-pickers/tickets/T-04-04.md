---
id: T-04-04
uow: UOW-04
title: CrudFormDialog chỉ tìm vị trí đang hoạt động
layer: frontend
type: feature
estimate: 1h
status: done
depends_on: []
blocks: []
verifies: [AC-20]
touches:
  - apps/backoffice-web/src/components/crud/CrudFormDialog.tsx
assumptions: [A-02]
---

# T-04-04 — Một tham số bị bỏ quên

## Context

`CrudFormDialog.tsx:83-93`:

```ts
const searchLocations = async (query: string) => {
  const params = new URLSearchParams({ page: "1", pageSize: "8", search: query.trim() });
  const { data } = await apiClient.get<PaginatedResponse<InventoryLocation>>(
    `/inventory/locations?${params}`,
  );
  return data.data;
};
```

`searchStorages` ngay bên trên (`:70-80`) **có** truyền `activeOnly: "true"`. Đây là bỏ sót, không
phải lựa chọn — và `GET /inventory/locations` chỉ lọc khi được yêu cầu
(`inventory-location.service.ts:779-783`).

Ticket nhỏ nhất của feature; để riêng vì nó không phụ thuộc gì và có thể chạy song song.

## Implementation notes

- Thêm `activeOnly: "true"` vào `params`, khớp đúng cách `searchStorages` đang làm.
- Không đổi gì khác trong file. Đây là form dùng chung cho toàn bộ nền tảng CRUD chung, nên mọi
  thay đổi ngoài phạm vi sẽ lan rất rộng.

## Done when

- [x] Ô chọn vị trí trong form CRUD chung chỉ hiện vị trí `is_active = true`
- [x] `git diff` của file này chỉ có đúng thay đổi trên

## Kết quả (Claude, 03/09/2026)

Thêm đúng một dòng `activeOnly: "true"` vào `searchLocations`, khớp cách `searchStorages` ngay bên
trên đã làm. `git diff --stat` = **1 file, 1 insertion(+)**.

Kiểm phía server trước khi sửa: `inventory-location.service.ts:779-783` xử lý `activeOnly` bằng
**hai** mệnh đề, không phải một —

```ts
if (query.activeOnly) {
  qb.andWhere('location.isActive = true');
  // Vị trí thuộc kho đã ngừng hoạt động cũng bị ẩn khỏi picker.
  qb.andWhere('storage.isActive = true');
}
```

Nghĩa là một tham số này phủ cả vị trí ngưng hoạt động lẫn vị trí nằm trong kho đã ngưng hoạt động,
đủ cho AC-20 mà không cần thêm gì. Không có thay đổi nào khác trong file.
