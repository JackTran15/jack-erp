---
id: T-02-02
uow: UOW-02
title: Test hội tụ và giữ cảnh báo cho hàng thật sự thiếu tồn
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-02-01]
blocks: [T-01-04]
verifies: [AC-07, AC-08]
touches:
  - apps/pos-web/src/stores/common/checkout-session.store.test.ts
assumptions: [A-08, A-09]
---

# T-02-02 — Chốt rào an toàn cho cảnh báo tồn

## Context
Rủi ro của T-02-01 không phải "không sửa được lỗi" mà là "sửa quá tay": tắt luôn cảnh báo cho
hàng thật sự thiếu. Ticket này khoá hai đầu còn lại.

## Implementation notes
- Test trên store thật, không mock: `openDraftInNewSession` rồi `syncPurchaseCartOnHand`.
- Ca 1 — item có trong catalog với `sellableQuantity` 1, giỏ ghi SL 3 → sau sync
  `maxQty === 1`, `onHandUnknown === false`, và `getOversellSaleLines` vẫn trả dòng đó.
- Ca 2 — item KHÔNG có trong catalog → sau sync `onHandUnknown` vẫn `true`, và
  `selectUnknownOnHandLineCount` giữ nguyên (chứng minh không lặp vô hạn).
- Ca 3 — item có `sellableQuantity` null (payload BE cũ) → vẫn `onHandUnknown === true`.
- Ca 4 — dòng `isReturnCredit` không bị đếm và không bị đổi `maxQty`.

## Done when
- [x] Ca 1 — tồn showroom 4, giỏ ghi 5 → sau sync `maxQty === 4`, `onHandUnknown === false`,
      `computeOversellLines` vẫn trả dòng đó (cảnh báo KHÔNG bị tắt oan)
- [x] Ca 2 — item không có trong catalog → `onHandUnknown` vẫn `true` và
      `selectUnknownOnHandLineCount` đứng yên qua hai lần sync (chứng minh không lặp vô hạn)
- [x] Ca 3 — `sellableQuantity` null (payload BE cũ) → vẫn `onHandUnknown === true` (test có sẵn)
- [x] Ca 4 — dòng `isReturnCredit` không bị đếm
- [x] Bổ sung: tồn đủ (giỏ 3 / tồn 4) → `computeOversellLines` rỗng
- [x] `npx vitest run src/stores/common/checkout-session.store.test.ts` — 17/17 xanh
