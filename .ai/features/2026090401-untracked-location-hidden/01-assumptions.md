---
feature: untracked-location-hidden
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|------------|------------|----------|----------------------|--------|------------|
| A-01 | Bộ lọc `isTracked` ở `GET /inventory/locations/:id/stock-items` là **tuỳ chọn** (`undefined` = tất cả); FE tự truyền mặc định `true`. Không mặc định `true` ở backend. | high | yes | Nếu mặc định ở backend thì "Tất cả" không còn biểu diễn được bằng một tham số boolean, phải đẻ thêm sentinel `'all'` hoặc cờ thứ hai — lệch hẳn khỏi endpoint anh em mà cùng một trang đang dùng. | **resolved** | Thành ADR-01. Chốt từ ba bằng chứng: (1) endpoint anh em `GET /inventory/stock/balances` đã dùng đúng hình này (`stock-ledger.controller.ts:107-114`, "Bỏ trống = tất cả"); (2) trang "Chi tiết vị trí" gọi **cả hai** endpoint tuỳ chế độ, nên hai hợp đồng lệch nhau là bẫy; (3) tiền lệ A-07 đợt `2026090301` đã bác bỏ lọc vô điều kiện cho đúng họ endpoint này. |
| A-02 | `HAS_ITEMS_SQL` siết `sb.is_tracked = true` **vô điều kiện** — khác A-01, ở đây không cần tuỳ chọn. | high | yes | Nếu để tuỳ chọn thì phải thêm tham số vào `LocationSearchV2Dto` mà không ai gọi, và cột "Xếp hàng hoá" mất định nghĩa duy nhất. | **resolved** | Chốt (Claude, 04/09/2026): `hasItems` vừa là projection vừa là predicate của bộ lọc cột (`search-locations-v2.handler.ts:70-71,74`) — hai vế phải cùng một định nghĩa, nên nó là **định nghĩa nghiệp vụ** chứ không phải bộ lọc. Không consumer nào cần "Đã xếp kể cả dòng đã ngừng": chỉ có một call-site FE (`ItemLocationsPage.tsx:215`). |
| A-03 | "Đã xếp" chỉ xét `is_tracked`, **không** xét `quantity > 0`. Vị trí còn dòng đang theo dõi với số lượng 0 vẫn là "Đã xếp". | high | yes | Nếu xét số lượng thì mọi kệ đang theo dõi mà tạm hết hàng biến mất khỏi "Đã xếp" và khỏi hộp thoại chi tiết — lỗi mới nặng hơn lỗi đang sửa. | **resolved** | Akenzy chốt 04/09/2026 khi được hỏi thẳng, chọn "Vẫn Đã xếp — chỉ xét is_tracked". Trùng tiền lệ A-13 của `2026090301` đã chốt cùng nguyên tắc cho D4. |
| A-04 | Không xoá `stock_balances`, không xoá `item_storage_locations`, không migration dọn dữ liệu cũ. Chỉ lọc ở chỗ đọc. | high | yes | Nếu xoá thì "bật lại theo dõi" mất nghĩa: không còn dòng để hiện, và ngưỡng min/max của cặp đó (`item_stock_thresholds`) mồ côi. Không đảo ngược được. | **resolved** | Akenzy chốt 04/09/2026, giữ nguyên A-05 của `2026090301` sau khi được nêu lại đánh đổi. Ghi vào Out of scope của `00-intent.md`. |
| A-05 | Hộp thoại `LocationStockItemsDialog` cần bộ lọc trạng thái ba trạng thái (Đang theo dõi / Ngừng theo dõi / Tất cả), mặc định "Đang theo dõi". | high | yes | Không có nó thì nút xoá dòng sẵn có trong hộp thoại (`LocationStockItemsDialog.tsx:185`) không còn với tới được dòng đã ngừng, và người dùng mất một đường dọn dữ liệu mà không được báo. | **resolved** | Akenzy chốt 04/09/2026, chọn "Có — thêm bộ lọc trạng thái trong hộp thoại". |
| A-06 | `InventoryLocationService.listLocations` (`:817-829`) phải sửa cùng đợt dù backoffice không gọi nó. | medium | no | Nếu bỏ qua: một endpoint sống (`inventory-location.controller.ts:439`) trả `hasItems` theo định nghĩa cũ, và màn hình nào dùng nó sau này sẽ tái hiện đúng lỗi #21. Nếu sửa mà sai: 1 dòng, revert được. | **resolved** | Chốt (Claude, 04/09/2026): đây chính là kịch bản A-05 đợt trước cảnh báo — "mọi chỗ đọc viết sau này phải tự nhớ guard, nếu quên thì lỗi tái diễn ở màn hình mới". Chi phí 1 dòng `.andWhere`, nên không có lý do hoãn. T-01-02. |
| A-07 | `StockByLocationItemDto` thiếu `isTracked` là lỗ hổng tài liệu, không phải lỗi runtime. | high | no | Chỉ ảnh hưởng `packages/api-client` sinh ra và `/docs`; FE hiện dùng type viết tay từ `@erp/shared-interfaces` nên không vỡ. | **resolved** | Bằng chứng (Claude, 04/09/2026): `toItem` (`inventory-location-stock.service.ts:688`) **đã** trả `isTracked: sb.isTracked`, và `packages/shared-interfaces/src/inventory/stock-by-location.ts:28` đã khai — chỉ riêng DTO Swagger (`stock-by-location.response.dto.ts:10-31`) thiếu. T-02-02. |
| A-08 | Nhánh `BELOW_MIN` (`getBelowMinStock`) tự thừa hưởng bộ lọc mới, không cần sửa riêng. | medium | no | Nếu sai thì bộ lọc "dưới định mức" vẫn lọt dòng đã ngừng — một ca lọt lưới nhỏ, sửa thêm 0 dòng vì cùng `where`. | **resolved** | Đã đọc mã (Claude, 04/09/2026): `getStockByLocation` dựng `where = this.buildWhere(...)` **một lần** ở `:362` rồi truyền y nguyên vào `getBelowMinStock({ where, ... })` (`:366-375`), và nhánh đó gọi `find({ where: args.where })` (`:452`). Sửa trong `buildWhere` là phủ cả hai nhánh. T-02-01 phải có test cho nhánh BELOW_MIN. |
| A-09 | Ở chế độ xem một vị trí, cột "Trạng thái" đã có sẵn — chỉ đổi `filterKind` từ `"none"` sang `"select"`, không thêm cột mới. | medium | no | Nếu sai thì phải thêm cột, đổi bề rộng lưới; vẫn gọn trong T-03-01. | **resolved** | Đã đọc mã (Claude, 04/09/2026): `buildLocationStockItemColumns` (`ItemLocationDetailsColumns.tsx:163-173`) đã render `ActiveStatusBadge` với nhãn "Đang theo dõi"/"Ngừng theo dõi", chỉ đặt `filterKind: "none"`. `STATUS_FILTER_OPTIONS` (`:14-17`) đã tồn tại nhưng là `const` cục bộ — T-03-01 export nó thay vì nhân bản. |
| A-10 | Phải chạy `pnpm openapi:generate` và **build lại API** trước khi verify. | high | no | Nếu quên: FE gửi `isTracked` mà `dist/` cũ chưa khai trong DTO ⇒ `forbidNonWhitelisted` trả **400**, và verify cho đỏ giả — lỗi nằm ở server cũ chứ không ở tính năng. | **resolved** | Bẫy đã dính một lần 31/08/2026, được ghi lại nguyên văn trong `.ai/aidlc.yaml` (khối `local-backoffice-verify`). Đưa thành mục bắt buộc trong Definition of done của UOW-02. |
| A-13 | **Không thể** ngừng theo dõi một dòng còn tồn: `setBalanceTracking` chặn với `Chỉ được ngừng theo dõi khi tồn = 0`. Nên trong thực tế mọi dòng `is_tracked = false` đều có `quantity = 0`. | high | no | Không đổi thiết kế, nhưng đổi **hình dạng fixture**: một test dựng dòng đã ngừng với tồn > 0 là dựng một trạng thái hệ thống không cho phép, và sẽ đỏ ở bước dựng chứ không ở bước kiểm. | **resolved** | Phát hiện lúc chạy e2e (Claude, 04/09/2026): `stock-ledger.service.ts:686-711` truy vấn `WHERE sb.quantity > 0` rồi ném `BadRequestException`. Hệ quả tốt cho phép kiểm: cả hai vị trí trong fixture đều để tồn 0, nên **khác biệt duy nhất giữa chúng là `is_tracked`** — chứng minh trực tiếp rằng quy tắc không dựa vào số lượng (A-03). Cũng giải thích vì sao lỗi #21 gây nhầm: "Chưa xếp" và "hết hàng" gần như luôn trùng nhau trên dữ liệu thật. |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|-----------------|----------------------|-------------|
| A-11 | Trang "Chi tiết vị trí" hỏng ở cả hai chế độ, nên phải sửa `listStockBalances` luôn. | Chế độ chung **đã đúng**: `buildQuery` (`ItemLocationDetailsQuery.ts:47-50`) mặc định `extra.isTracked = true` khi bộ lọc chưa đặt, và `stock-ledger.service.ts:498-499` áp đúng. Chỉ chế độ xem một vị trí (`isLocationDetail`) sai, vì `locationParams` (`ItemLocationDetailsPage.tsx:104-128`) chỉ mang `page/pageSize/sortBy/sortOrder/itemCode/itemName`. | UOW-03 thu lại còn **một** ticket FE, không đụng `listStockBalances` cũng không đụng `stock-ledger.service.ts`. AC-11 đổi thành phép kiểm **không hồi quy** cho chế độ chung. |
| A-12 | Có thể sửa gọn ở FE: tải về rồi lọc `row.isTracked` trước khi render. | Không được. Cả hai endpoint **phân trang ở server** (`skip/take` ở `:407-408`, `offset/limit` ở `search-locations-v2.handler.ts:78-79`) và trả `meta.total` từ `findAndCount`. Lọc sau khi phân trang cho ra trang thiếu dòng và tổng sai — đúng lớp lỗi D1 của đợt `2026090301` (ghép dòng vào trang đã phân trang). | Mọi thay đổi lọc nằm ở **SQL**, không ở React. Thành ADR-03; FE chỉ truyền tham số. |
