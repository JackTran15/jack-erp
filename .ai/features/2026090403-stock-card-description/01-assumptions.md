---
feature: stock-card-description
blocking_open: 0
---

# Assumption register

| ID   | Assumption | Confidence | Blocking | Blast radius if wrong | Status    | Resolution |
| ---- | ---------- | ---------- | -------- | ---------------------- | --------- | ---------- |
| A-01 | Khi chứng từ gốc không có Diễn giải (rỗng/null) — kể cả hoá đơn bán/trả hàng vốn không có cột nào tương ứng — ô Diễn giải để trống, không fallback về chuỗi máy sinh hay nhãn loại chứng từ | high | yes | Nếu fallback về chuỗi cũ, một phần dòng vẫn hiện chuỗi máy sinh, hành vi không nhất quán | confirmed | Xác nhận bởi Akenzy, 2026-09-04, qua AskUserQuestion: "Diễn giải gốc, rỗng thì để trống" |
| A-02 | Phạm vi sửa áp dụng cho toàn bộ 13 `reference_type` có bảng chứng từ gốc trong `REFERENCE_DOCUMENT_TABLES`, không chỉ GOODS_RECEIPT/GOODS_ISSUE | high | yes | Nếu chỉ sửa 2 loại, cột vẫn không nhất quán cho TRANSFER/ADJUSTMENT/STOCK_TAKE/PURCHASE_ORDER | confirmed | Xác nhận bởi Akenzy, 2026-09-04, qua AskUserQuestion: "Tất cả loại chứng từ có bảng gốc" |
| A-03 | Cột Diễn giải nguồn cho từng loại chứng từ: GOODS_RECEIPT→`goods_receipts.description`, GOODS_ISSUE→`goods_issues.notes`, TRANSFER/TRANSFER_REVERSAL/TRANSFER_EDIT_REVERSAL/LOCATION_CHANGE→`stock_transfers.notes`, STOCK_TAKE→`COALESCE(purpose, conclusion, notes)`, PURCHASE/PURCHASE_ORDER→`purchase_orders.notes`, INVOICE*→không có cột, luôn NULL | high | yes | Chọn sai cột sẽ hiển thị nhầm nội dung khác (vd Lý do thay vì Diễn giải) | confirmed | Xác nhận bằng đọc trực tiếp entity + form binding: `GoodsReceiptFormDialog.tsx` field "Diễn giải"→`description`; `StockTransferPage.tsx:528-531` field "Diễn giải"→`notes`; `StockTakesPage.tsx:483-486` field "Diễn giải"→`purpose ?? conclusion ?? notes`; `PurchaseOrdersPage.tsx` label "Diễn giải" nhưng entity chỉ có `notes` (không có `description`) nên dùng `notes` |
| A-04 | ADJUSTMENT dùng `stock_adjustments.reason_description` ("Human-readable explanation") làm Diễn giải, không phải `notes` ("Additional notes") — không có trang backoffice nào hiện đang bind field này với nhãn "Diễn giải" để đối chiếu | medium | no | Nếu sai cột, dòng ADJUSTMENT trong thẻ kho hiện nhầm "Ghi chú thêm" thay vì "giải thích lý do" — chỉ ảnh hưởng 1/13 loại chứng từ, không ảnh hưởng kiến trúc | confirmed | Xác nhận bởi Akenzy, 2026-09-04, qua AskUserQuestion: "Xác nhận `reason_description` như hiện tại" |
| A-05 | Đổi nguồn dữ liệu cho bộ lọc `dto.description` (search theo Diễn giải) sang cùng cột đã resolve, thay vì giữ nguyên lọc trên `stock_ledger_entries.notes` | high | yes | Nếu không đổi, ô lọc "Diễn giải" trên UI sẽ lọc theo chuỗi máy sinh cũ trong khi cột hiển thị đã đổi — người dùng gõ đúng nội dung thấy trên màn hình nhưng không lọc ra dòng đó | confirmed | Suy luận trực tiếp từ yêu cầu "hiển thị đúng" — lọc và hiển thị phải cùng nguồn để nhất quán; không cần hỏi thêm vì đây là hệ quả kỹ thuật bắt buộc của A-01..A-03, không phải lựa chọn sản phẩm độc lập |
