---
id: T-01-01
uow: UOW-01
title: descriptionSql() + REFERENCE_DESCRIPTION_TABLES trong stock-ledger-reference.constants.ts
layer: shared
type: feature
estimate: 1h
status: done
depends_on: []
blocks: [T-01-02]
verifies: []
touches:
  - apps/api/src/modules/inventory/ledger/stock-ledger-reference.constants.ts
assumptions: [A-02, A-03, A-04]
---

# T-01-01 — descriptionSql() + REFERENCE_DESCRIPTION_TABLES

## Context

`documentNumberSql()` (cùng file) đã giải bài toán "tra ngược chứng từ gốc theo
`reference_type` + `reference_id`" bằng `CASE ... WHEN '<type>' THEN (SELECT
<col> FROM <table> d WHERE d.id = <alias>.reference_id) ... END`. Cần một hàm
tương tự cho Diễn giải, với cột nguồn khác nhau theo từng loại chứng từ (xem
A-03/A-04 trong `01-assumptions.md`).

## Implementation notes

- Thêm map mới ngay dưới `REFERENCE_DOCUMENT_TABLES`:
  ```ts
  export const REFERENCE_DESCRIPTION_TABLES: Record<
    string,
    { table: string; columns: string[] }
  > = {
    GOODS_RECEIPT: { table: "goods_receipts", columns: ["description"] },
    GOODS_ISSUE: { table: "goods_issues", columns: ["notes"] },
    TRANSFER: { table: "stock_transfers", columns: ["notes"] },
    TRANSFER_REVERSAL: { table: "stock_transfers", columns: ["notes"] },
    TRANSFER_EDIT_REVERSAL: { table: "stock_transfers", columns: ["notes"] },
    LOCATION_CHANGE: { table: "stock_transfers", columns: ["notes"] },
    ADJUSTMENT: { table: "stock_adjustments", columns: ["reason_description"] },
    STOCK_TAKE: { table: "stock_takes", columns: ["purpose", "conclusion", "notes"] },
    PURCHASE: { table: "purchase_orders", columns: ["notes"] },
    PURCHASE_ORDER: { table: "purchase_orders", columns: ["notes"] },
    // INVOICE / INVOICE_CANCEL / RETURN_INVOICE: bảng invoices không có cột
    // diễn giải — cố ý không liệt kê, rơi vào nhánh ELSE NULL.
  };
  ```
- Thêm hàm, đặt ngay dưới `documentNumberSql`:
  ```ts
  export function descriptionSql(alias = "sle"): string {
    const arms = Object.entries(REFERENCE_DESCRIPTION_TABLES).map(
      ([referenceType, { table, columns }]) => {
        const colExpr =
          columns.length > 1
            ? `COALESCE(${columns.map((c) => `d.${c}`).join(", ")})`
            : `d.${columns[0]}`;
        return `WHEN '${referenceType}' THEN (SELECT ${colExpr} FROM ${table} d WHERE d.id = ${alias}.reference_id)`;
      },
    );
    return `CASE ${alias}.reference_type ${arms.join(" ")} ELSE NULL END`;
  }
  ```
- Không đổi `documentNumberSql`, `REFERENCE_DOCUMENT_TABLES`, `REFERENCE_TYPE_LABELS`, hay `resolveReferenceLabel`.
- Không lọc `deleted_at` trong subquery — nhất quán với `documentNumberSql` (xem Error taxonomy trong `03-logical-design.md`).

## Done when

- [x] `REFERENCE_DESCRIPTION_TABLES` liệt kê đủ 10 `reference_type` có cột diễn giải nguồn (không liệt kê 3 loại INVOICE*)
- [x] `descriptionSql(alias)` sinh đúng SQL dạng CASE cho cả cột đơn (`d.description`) và COALESCE nhiều cột (STOCK_TAKE)
- [x] `pnpm --filter @erp/api build` không có lỗi type mới
