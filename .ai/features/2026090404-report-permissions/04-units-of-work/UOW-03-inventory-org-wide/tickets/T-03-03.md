---
id: T-03-03
uow: UOW-03
title: Bỏ kiểm tra anchor branch ở drill-down điều chuyển
layer: service
type: refactor
estimate: 2h
status: done
depends_on: [T-03-01]
blocks: [T-03-04, T-04-01]
verifies: [AC-10]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/transfer-document-detail.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/transfer-difference-detail.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/transfer-by-store.report.ts
assumptions: [A-10]
---

# T-03-03 — Bỏ anchor check ở drill-down

## Context
`resolveTransferPair` bắt buộc chân được bấm phải nằm trong tập gán. Khi báo cáo
cha đã org-wide, giữ nguyên nghĩa là user thấy dòng L0 của một cặp cửa hàng rồi
403 ngay khi bấm xuống — bất nhất, và người dùng không hiểu vì sao.

## Implementation notes
- Bỏ `permittedBranchIds(actor).has(anchor)`; giữ kiểm tra cả hai chân thuộc
  cùng tổ chức (400 nếu không).
- `transfer-by-store.resolveScope`: bỏ kẹp tương tự, giữ `findOne` theo org.
- Kiểm tra vẫn nằm ngoài `buildData` để `countRows` (đường export) không đi vòng.
- Dọn import `ForbiddenException` đã mồ côi ở hai file.

## Done when
- [x] Drill-down mở được cho mọi cặp cửa hàng trong tổ chức
- [x] Cửa hàng ngoài tổ chức vẫn 400 ở cả `buildData` và `countRows`
