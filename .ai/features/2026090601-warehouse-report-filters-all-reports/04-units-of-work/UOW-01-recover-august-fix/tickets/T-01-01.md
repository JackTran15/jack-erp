---
id: T-01-01
uow: UOW-01
title: Cherry-pick 1cb20a60 lên main, gỡ 3 conflict
layer: infra
type: chore
estimate: 2h
status: done
depends_on: []
blocks: [T-01-02]
verifies: [AC-01]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/stock-by-store-pivot.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/stock-by-store-pivot.report.spec.ts
  - .ai/aidlc.yaml
assumptions: [A-04]
---

# T-01-01 — Cherry-pick 1cb20a60 lên main, gỡ 3 conflict

## Context
`1cb20a60` (nhánh local `filter-report-warehouse`) là bản sửa D1–D5 đã qua G5 ngày 2026-08-29,
12/12 ticket. Nó chưa bao giờ được merge, nên `main` vẫn nguyên lỗi. Commit cha của nó
(`94477a78`) trùng nội dung với `658dc79e` đã có trên `main`, nên cherry-pick **chỉ** commit này
cho đúng phần delta D1–D5.

Đã dựng thử trong worktree tách rời: 29 file add + 22 modify merge sạch, đúng 3 conflict.

## Implementation notes
- `git cherry-pick 1cb20a60`
- Conflict 1 — `.ai/aidlc.yaml`: giữ bản `main` (đã có 3 environment backoffice), không lấy bản cũ.
- Conflict 2 — `stock-by-store-pivot.report.ts`, khối `import`: **giữ `resolveOrgWideBranchIds` của
  `main`** (`#240` đổi báo cáo này thành org-wide) và **thêm** `resolveDescendantCategoryIds`
  + `ItemCategoryEntity` của nhánh. Không mang `resolveInventoryBranchIds` trở lại.
- Conflict 3 — spec cùng file: gộp cả hai — giữ `branchRepo` + `ORG_BRANCHES` của `main`, thêm
  tham số thứ ba `categories` vào constructor.
- Không sửa gì ngoài ba chỗ trên. Mọi thay đổi hành vi khác thuộc UoW sau.

## Done when
- [x] `git status` không còn file conflict
- [x] `pnpm build` xanh
- [x] `git show --stat HEAD` cho thấy đủ 29 file add
- [x] `resolveDescendantCategoryIds` có mặt trong `report-scope.util.ts`

## Kết quả

- Cherry-pick `1cb20a60` → `fd209a64` trên nhánh `fix/warehouse-report-filters-all-reports`.
  54 file, **29 file add**, 2803 chèn / 118 xoá.
- Ba conflict đã gỡ đúng như T-01-01 dự kiến:
  - `.ai/aidlc.yaml` — giữ bản `main` (3 environment backoffice).
  - `stock-by-store-pivot.report.ts` — giữ `resolveOrgWideBranchIds` của `#240`, thêm
    `resolveDescendantCategoryIds` + `ItemCategoryEntity`. `permittedBranchIds` và
    `resolveInventoryBranchIds` không còn chỗ dùng nào (0 hit) nên **không** mang trở lại.
  - Spec cùng file — gộp: giữ `branchRepo` + `ORG_BRANCHES` của `main`, thêm tham số thứ ba.
- **Hai chỗ nữa git merge sạch nhưng vỡ kiểu**, không nằm trong ba conflict và chỉ lộ ra khi
  chạy `tsc`: `stock-by-store-pivot.report.spec.ts:250` (helper thứ hai của `main`) và
  `transfer-summary-by-counterpart.report.spec.ts:39` (file của `#248` dựng
  `DocumentDetailReport`, mà constructor lớp đó vừa nhận thêm `categories`). Cả hai chỉ thiếu
  đối số thứ ba. Ghi lại vì "3 conflict" của A-04 đếm theo git, không theo trình biên dịch.
- `tsc --noEmit` xanh cho cả `@erp/api` lẫn `@erp/backoffice-web`; `nest build` exit 0.

