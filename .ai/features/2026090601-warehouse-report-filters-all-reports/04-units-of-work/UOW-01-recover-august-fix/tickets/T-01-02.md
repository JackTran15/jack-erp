---
id: T-01-02
uow: UOW-01
title: Chạy lại bộ test của 1cb20a60 trên main, sửa chỗ lệch
layer: test
type: test
estimate: 3h
status: done
depends_on: [T-01-01]
blocks: [T-01-03, T-04-02]
verifies: [AC-04]
touches:
  - apps/api/src/modules/inventory-reports/report/column-filterability.spec.ts   # new; đến cùng cherry-pick T-01-01
  - apps/api/src/modules/inventory-reports/report/report-scope.util.spec.ts   # new; đến cùng cherry-pick T-01-01
  - apps/api/src/modules/inventory-reports/report/report-data.util.spec.ts
  - apps/backoffice-web/src/store/page-stores/report/report.store.test.ts   # new; đến cùng cherry-pick T-01-01
assumptions: [A-05]
---

# T-01-02 — Chạy lại bộ test của 1cb20a60 trên main, sửa chỗ lệch

## Context
`main` đã đi tiếp 13 commit kể từ điểm rẽ, trong đó `#240` (stock-by-store org-wide),
`#246` (lọc theo mức vị trí) và `#248` (drill-down điều chuyển) đều chạm vùng này. A-05 giả định
bản sửa cũ vẫn đúng — ticket này là chỗ đóng giả định đó.

## Implementation notes
- `pnpm --filter @erp/api test` và `npx vitest run` trong `apps/backoffice-web`.
- Test đỏ thì sửa **nguyên nhân**, không sửa kỳ vọng, trừ khi kỳ vọng cũ mô tả hành vi mà `#240`
  đã cố ý đổi (stock-by-store org-wide) — khi đó ghi lý do ngay cạnh test.
- Biết trước: `auth.service.spec.ts` có 2 test TTL đỏ sẵn không liên quan; `backoffice-web`
  không có jsdom nên 3 test print đỏ sẵn. Ghi nhận, không tính là hồi quy.

## Done when
- [x] `pnpm --filter @erp/api test` xanh, trừ đúng các test đỏ sẵn đã liệt kê
- [x] `npx vitest run` ở `backoffice-web` không đỏ thêm so với baseline
- [x] Mỗi kỳ vọng bị sửa đều có một dòng lý do

## Kết quả

**Không có chỗ lệch nào để sửa.** A-05 đóng: bản sửa tháng 8 vẫn đúng sau 13 commit của `main`.

### `@erp/api` — `npx jest --silent`, 2026-09-06

```
Test Suites: 1 failed, 313 passed, 314 total
Tests:       2 failed, 1 skipped, 3671 passed, 3674 total
Time:        16.083 s
```

Đúng hai test đỏ sẵn đã liệt kê, cả hai trong `auth.service.spec.ts › token TTL`:
- `login() honors a JWT_ACCESS_TTL override from config` — DI hỏng, thiếu
  `BranchEntityRepository` ở index [8] trong `RootTestModule`.
- `switchBranch() mints tokens carrying the configured TTL` — `ForbiddenException: Branch not
  assigned to user`.

Không dính `inventory-reports`. **0 test đỏ trong vùng feature.** Cả 4 spec đến cùng cherry-pick
đều xanh: `report-scope.util.spec.ts`, `report-data.util.spec.ts`, `column-filterability.spec.ts`,
và toàn bộ `report/reports/*.spec.ts`.

### `@erp/backoffice-web` — `npx vitest run`, 2026-09-06

```
Test Suites: 2 failed, 19 passed, 21 total
Tests:       3 failed, 78 passed, 81 total
```

Đúng ba test đỏ sẵn, cả ba trong `src/lib/print/print-html-document.test.ts` (không có jsdom):
`removes the iframe…`, `leaves no stray iframe…`, `resolves without throwing outside a browser…`.
`report.store.test.ts` — file mới đến cùng cherry-pick — **7/7 xanh**.

(Bảng đếm "2 failed suite" vì báo cáo JSON của vitest tính cả suite bị bỏ qua; chỉ một file có
test đỏ.)

### Kiểm chéo: cherry-pick không nuốt `#240`

Test xanh chưa đủ để đóng A-05, vì spec `stock-by-store-pivot` đến từ chính nhánh cũ. Đọc thẳng
mã nguồn — **cả hai hành vi cùng tồn tại** trong `stock-by-store-pivot.report.ts`:

| dòng | gọi | thuộc về |
|------|-----|----------|
| 144, 196 | `resolveOrgWideBranchIds(...)` | `#240` (`main`) |
| 222 | `resolveDescendantCategoryIds(...)` | D1 (nhánh tháng 8) |

Không bên nào đè bên nào.

### Baseline chốt cho các ticket sau

Bốn con số này là mốc so sánh cho T-01-03 trở đi; đỏ thêm ngoài danh sách dưới đây là hồi quy:
- api: 2 đỏ, cả hai `auth.service.spec.ts › token TTL`
- backoffice-web: 3 đỏ, cả ba `print-html-document.test.ts`
