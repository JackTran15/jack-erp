---
id: T-01-03
uow: UOW-01
title: Probe dựng lại D1–D5 trên erp_dev_3008
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-01-02]
blocks: [T-02-01, T-05-01]
verifies: [AC-01, AC-02, AC-03]
touches:
  - .ai/features/2026090601-warehouse-report-filters-all-reports/evidence/probe-after-recover.md   # new
assumptions: [A-05]
---

# T-01-03 — Probe dựng lại D1–D5 trên erp_dev_3008

## Context
Số liệu tháng 8 đo trên `erp_dev`; stack hiện chạy `erp_dev_3008` nên mọi con số cũ hết so
sánh được. Baseline mới đã ghi ở `evidence/probe-2026-09-06.md`.

## Implementation notes
- Đăng nhập bằng `.ai/credentials.env`, chi nhánh `c3bf1922-…`, kỳ 2026-01-01 → 2026-12-31.
- Đo lại đúng bốn dòng của E1: không lọc / PHỤ KIỆN / GIÀY DÉP / Giày nam.
- `POST /reports/inventory/search` trả **201** — coi 2xx là thành công, đừng so `== 200`.
- Curl trả tên hàng có ký tự điều khiển: ghi ra file bằng `-o` rồi `json.loads(raw, strict=False)`.

## Done when
- [x] GIÀY DÉP > 0 và bằng tổng các nhóm lá bên dưới
- [x] PHỤ KIỆN > 0
- [x] Giày nam vẫn đúng 2205
- [x] Bảng trước/sau ghi vào `evidence/probe-after-recover.md`

## Kết quả

Đo trên **`:4100`** dựng riêng từ build mới của chính checkout này — không dùng `:4000`, vì tiến
trình ở đó nạp mã trước khi build lại (không phải watch mode). Chi tiết + bảng số:
`evidence/probe-after-recover.md`.

| Bộ lọc | trước | sau |
|---|---|---|
| không lọc | 9593 | 9593 |
| PHỤ KIỆN (cha) | **0** | **664** |
| GIÀY DÉP (cha) | **0** | **8918** |
| Giày nam (lá) | 2205 | 2205 |

`GIÀY DÉP` = 8918 = **đúng** tổng 11 nhóm lá hỏi riêng. Duyệt hết 8918 dòng: 10 nhóm khác nhau,
**0 dòng ngoài cây**, `rows_walked == total`.

Hai bẫy khi dựng lại probe, đã ghi vào evidence:
1. `columnFilters` là **mảng** `ColumnFilterDto[]`, gửi `{}` ⇒ 400 với thông báo nói về `col`.
2. Baseline 9593/2205 đo **org-wide**, không phải chi nhánh HCM như dòng ghi chú trong
   `probe-2026-09-06.md`. `X-Branch-Id` không thu hẹp báo cáo này; `filters.store` mới làm
   (HCM ⇒ 1734). So sai shape sẽ ra "hồi quy" giả.

Giới hạn đã ghi rõ: cây nhóm trên `erp_dev_3008` **chỉ sâu 1 cấp**, nên probe không phân biệt
được "mọi hậu duệ" với "chỉ con trực tiếp". Phần đó do `report-scope.util.spec.ts:26` (cây 3 cấp)
và `:58` (cây có chu trình) khoá.

**A-05 đóng.**
