---
feature: inventory-item-stock-status-utilities
blocking_open: 0
---

# Assumption register

Bốn dòng đầu là câu trả lời trực tiếp của Akenzy trong vòng hỏi ngày 2026-09-06; phần còn
lại là những chỗ tôi tự quyết sau khi đo dữ liệu thật trên `erp_dev_3008`, ghi ra đây để
người khác lật lại được.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| A-01 | Tổng tồn để xét "hết hàng" cộng theo **chi nhánh đang chọn**, không phải toàn tổ chức | high | yes | Sai phạm vi ⇒ sai toàn bộ tập kết quả (1.605 vs 1.015 nhóm ở HCM). Phải thêm `branchId` vào DTO + handler + CTE | confirmed | Akenzy chốt 2026-09-06: "Chi nhánh đang chọn (X-Branch-Id)". Số đo kèm theo: HCM 1.605, Cần Thơ 1.216, toàn tổ chức 1.015 / 2.554 nhóm |
| A-02 | Bulk "Ngừng kinh doanh" gặp hàng đang ở Showroom thì **bỏ qua dòng đó và làm tiếp phần còn lại**, trả về danh sách mã bị bỏ qua | high | yes | Quyết định ngược ⇒ `setActiveStatus` giữ nguyên `throw`, contract trả về khác, UI khác | confirmed | Akenzy chốt 2026-09-06: "Bỏ qua phần bị chặn, làm phần còn lại". Kéo theo phải sửa `item-crud.service.ts:223-243` từ `throw` sang trả `skipped[]` |
| A-03 | "Trạng thái hết hàng" là **nút lọc bật/tắt**, không thêm cột số tồn vào lưới | high | yes | Thêm cột ⇒ đổi row DTO, cấu hình cột, và cả `ENTITY_COLUMN_CONFIGS` | confirmed | Akenzy chốt 2026-09-06: "Chỉ là nút lọc bật/tắt" |
| A-04 | Tiện ích chỉ tác động lên **các dòng đang tick** (phạm vi trang hiện tại), không có chế độ "áp dụng cho toàn bộ kết quả lọc" | high | yes | Chế độ "toàn bộ kết quả" cần endpoint nhận điều kiện lọc thay vì danh sách id — khác hẳn contract | confirmed | Akenzy chốt 2026-09-06: "Chỉ các dòng đã tick (trang hiện tại)" |
| A-05 | Tồn lấy từ **`stock_balances`** (số dư hiện tại), không dựng lại từ `stock_ledger_entries` | medium | no | Lệch với màn "Tổng hợp tồn kho" ở đúng những mặt hàng có chứng từ đã huỷ: `stock_balances` **không** loại chứng từ huỷ, còn báo cáo theo ledger thì có (`EXCLUDE_VOIDED_DOCS_SQL`, `stock-summary.service.ts:22-34`). Đổi nguồn = sửa một biểu thức SQL | resolved | Đã cài theo `stock_balances`. **Đây là lựa chọn của tôi, chưa phải điều Akenzy xác nhận** — cần ratify. Hệ quả đã biết và chấp nhận: lệch với báo cáo theo ledger ở đúng những mặt hàng có chứng từ đã huỷ. Đổi nguồn = sửa một biểu thức trong `stockTotalColumn` |
| A-06 | Khoanh chi nhánh bằng cột **`sb.branch_id`** (phi chuẩn hoá) chứ không join `locations → storages` để lấy `storage.branch_id` (được ghi chú là "authoritative") | high | no | Nếu dữ liệu prod có kho bị chuyển chi nhánh sau import thì hai cột lệch nhau và tập kết quả sai theo | resolved | Giữ `sb.branch_id`. Đo lại lúc dựng: **0/164.481** dòng lệch so với `storage.branch_id` trên `erp_dev_3008`. Unit spec khẳng định SQL **không** ép `::uuid` (mutation test: thêm cast ⇒ 1 test đỏ) |
| A-07 | Có áp bộ lọc `sb.is_tracked = true AND loc.is_active AND storage.is_active` cho khớp màn Tổng hợp tồn kho | medium | no | Hàng nằm ở vị trí đã ngừng theo dõi sẽ bị coi như không tồn tại ⇒ báo "hết hàng" trong khi kho vẫn còn | resolved | Có áp `is_tracked` + `loc.is_active` + `st.is_active`. Đo: **0 nhóm** đổi kết quả trên dữ liệu dev, nên không có rủi ro thực tế đã quan sát được |
| A-08 | Bulk đổi trạng thái ghi `items.is_active` cho **mọi biến thể** trong nhóm, đồng thời đồng bộ `products.is_active` cho dòng `type:'product'` | medium | no | Không đồng bộ ⇒ nới rộng thêm chỗ trôi dữ liệu đã có (56 sản phẩm đang lệch giữa hai cờ trên `erp_dev_3008`) | resolved | Đã đồng bộ, kèm một chốt chặn không có trong kế hoạch ban đầu: **chỉ đồng bộ `products.is_active` khi không có biến thể nào bị bỏ qua**. Chạy thật: nhóm 12 biến thể ⇒ `products.is_active=false`; nhóm bị bỏ qua 2/14 ⇒ cờ sản phẩm **giữ nguyên true**. Không có chốt này thì nhóm còn hàng bán sẽ bị đánh dấu ngừng kinh doanh |
| A-09 | Bộ lọc hết hàng **kết hợp AND** với các bộ lọc cột đang có, không thay thế chúng | high | no | Người dùng lọc thương hiệu rồi bật hết hàng sẽ mất bộ lọc thương hiệu | resolved | Đúng. Chứng minh trên trình duyệt: lọc SKU `AK1` + bật hết hàng = **19**, tắt = **25**, ô lọc SKU giữ nguyên. e2e cũng có ca riêng |
| A-10 | Sau khi "Ngừng kinh doanh", các dòng vừa đổi **vẫn hiển thị** trên lưới | high | no | Nếu bị ẩn, dòng biến mất ngay sau thao tác và người dùng tưởng đã xoá nhầm | resolved | Đúng. Sau khi chuyển 2 dòng sang ngừng kinh doanh, cả 2 vẫn nằm trên lưới với trạng thái mới (`includeInactive: true` mặc định) |
| A-11 | Trạng thái bật/tắt của nút "Trạng thái hết hàng" **không** được ghi nhớ qua reload hay qua điều hướng | medium | no | Người dùng reload rồi tưởng vẫn đang lọc, đọc nhầm danh sách | resolved | Đúng, và quan sát được: đổi chi nhánh ⇒ `BranchSelector` reload trang ⇒ nút toggle về tắt. Đây là hành vi có chủ đích chứ không phải lỗi |
| A-12 | Menu Tiện ích bị ẩn/vô hiệu với tài khoản không có `inventory.write` (vd vai trò SALES chỉ có `inventory.read`) | high | no | Vai trò chỉ-đọc bấm được rồi nhận 403 | resolved | Menu ẩn khi thiếu `inventory.write` (`usePermissionCheck`). Chặn phía máy chủ đã kiểm bằng tài khoản thật `staff-hcm@erp.local` (vai trò Nhân viên bán hàng): `POST /inventory/items/set-active-status` trả **403** |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
| --- | --- | --- | --- |
| A-13 | Handler `search-inventory-items-v2` gộp nhóm **trong RAM** bằng JS (theo ghi chú cũ trong bộ nhớ dự án) | Handler **đẩy toàn bộ xuống SQL**: CTE `combined` = nhánh product `UNION ALL` nhánh orphan, và docstring nói rõ "the org's full item set is never loaded into memory" (`search-inventory-items-v2.handler.ts:17-24`) | Bỏ hướng "nạp `stock_balances` lên RAM rồi cộng". Tổng tồn cộng thẳng trong CTE bằng subquery tương quan, theo đúng khuôn `PIVOT_TOTAL_SQL` (`stock-balance-pivot.service.ts:57-70`). Ghi chú bộ nhớ dự án cần sửa lại |
| A-14 | Hai nút "Tiện ích" và "Trạng thái hết hàng" còn **chưa có** trên thanh công cụ, phải dựng mới | Cả hai **đã có sẵn**, đã đăng ký nhãn + icon trong `TOOLBAR_REGISTRY`, mỗi nút gọi `soon(...)` bắn toast "đang được triển khai" (`crud-entity-toolbar.ts:105-113`) | Phạm vi FE thu hẹp còn thay thân hàm `onClick`; không đụng `PageToolbar` ngoài việc bổ sung thể hiện trạng thái bật cho nút toggle |
| A-15 | Cần viết mới nghiệp vụ bulk đổi trạng thái ở backend | `InventoryInventoryItemCrudService.setActiveStatus(ids, isActive, actor)` đã tồn tại đầy đủ kèm quy tắc Showroom và xoá cache POS (`item-crud.service.ts:214-252`), nhưng **không có caller nào trong toàn monorepo** | Bỏ ticket "viết service bulk"; thay bằng "mở route + đổi `throw` thành `skipped[]`". Tiết kiệm một UoW |
| A-16 | Có thể tái dùng nghĩa `StockStateFilter.OUT_OF_STOCK` sẵn có cho "hết hàng" | Enum đó định nghĩa `OUT_OF_STOCK` là `SUM(sb.quantity) = 0` **đúng bằng 0**, còn `NEGATIVE` là một trạng thái tách riêng (`stock-summary.service.ts:1143-1149`). Yêu cầu của người dùng là `≤ 0`, tức hợp của hai trạng thái đó. Ngoài ra repo có **hai** enum khác nhau cùng tên `StockStateFilter` (`stock-summary-query.dto.ts:37` và `packages/shared-interfaces/src/inventory/stock-by-location.ts:3`) | Không tái dùng tên `OUT_OF_STOCK`. Dùng cờ boolean riêng `outOfStock` và ghi rõ trong ADR-03 rằng ngưỡng `≤ 0` khác với `= 0` của màn Tổng hợp tồn kho. Trên dev chênh nhau 3 nhóm (1.015 vs 1.012) |
