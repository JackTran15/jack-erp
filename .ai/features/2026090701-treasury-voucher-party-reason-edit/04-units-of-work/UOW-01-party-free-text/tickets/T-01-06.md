---
id: T-01-06
uow: UOW-01
title: Test đối tượng tự nhập cho cả 4 loại phiếu
layer: test
type: test
estimate: 3h
status: done
depends_on: [T-01-01, T-01-02, T-01-04]
blocks: []
verifies: [AC-03, AC-06]
tests:
  - apps/api/src/modules/accounting/cash-vouchers/shared/voucher-party.spec.ts
touches:
  - apps/api/src/modules/accounting/cash-vouchers/shared/voucher-party.spec.ts
  - apps/api/test/e2e/treasury-voucher-party.e2e-spec.ts   # new; cạnh các e2e treasury sẵn có
assumptions: [A-12]
---

# T-01-06 — Test đối tượng tự nhập cho cả 4 loại phiếu

## Context

Bốn loại phiếu nằm ở hai module với hai enum song song, nên rủi ro thật là sửa một bên quên bên
kia. Một bảng test chạy cùng kịch bản qua cả bốn là cách rẻ nhất để chặn.

## Implementation notes

- Bảng `[cash-receipt, cash-payment, bank-receipt, bank-payment]`, mỗi dòng chạy: tạo với
  `OTHER` + tên → đọc lại → sửa tên → đọc lại
- Kiểm cả AC-03: hàng trong lưới v2 hiện đúng tên. Nhớ thứ tự COALESCE ở
  `search-cash-vouchers-v2.handler.ts:53-58` — `payer_name` thắng `partner_name_snapshot`, nên
  fixture phải để trống "Người nộp" mới thấy được cột Đối tượng lấy từ đâu
- E2E cần env DB tường minh và `erp_test` dựng sẵn; teardown Kafka treo có thể làm suite báo đỏ
  giả — đọc output test thật, đừng tin mỗi exit code

## Done when

- [x] Bốn loại phiếu cùng qua một bảng test, không nhánh riêng cho loại nào
- [x] Có test khẳng định `partner_id` NULL khi kind là `OTHER`
- [x] Có test cho lưới v2 hiện đúng tên tự nhập
- [x] `pnpm --filter @erp/api test` xanh

## Sửa lúc thi công

- 12/12 xanh (`treasury-voucher-party.e2e-spec.ts`, 105s), bảng chạy cả 4 loại phiếu qua HTTP thật.
- Thêm một ca không có trong kế hoạch nhưng cần thiết: gửi field sai chính tả (`partnerNmae`) và
  đòi **400**. Không có ca này thì hai ca kia có thể xanh vì `forbidNonWhitelisted` âm thầm loại
  field lạ, chứ không phải vì DTO thật sự khai `partnerName` — tức là xanh giả.
- Ca "cắt khoảng trắng" gộp vào ca đầu: gửi `'  Nguyễn Văn A  '` và đòi lưu đúng `'Nguyễn Văn A'`.
- **AC-03 (lưới v2 hiện tên) KHÔNG kiểm ở đây.** Thứ tự COALESCE ở
  `search-cash-vouchers-v2.handler.ts:53-58` cho `payer_name` thắng `partner_name_snapshot`, nên
  ca này cần fixture để trống "Người nộp" mới nói lên điều gì; đã có test đơn vị ở tầng handler.
- Dựng tài khoản tiền gửi theo đúng công thức của `deposit-recon-lock.e2e-spec.ts` (1121 + banks +
  deposit_accounts), không phát minh lại.
- Log có 4 dòng `Bad Request Exception` — đó là 400 CỐ Ý của ca sai chính tả, không phải lỗi.
