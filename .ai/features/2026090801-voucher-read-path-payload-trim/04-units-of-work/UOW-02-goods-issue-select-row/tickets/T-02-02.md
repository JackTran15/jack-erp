---
id: T-02-02
uow: UOW-02
title: Nút cần dòng của Xuất kho chuyển sang nạp-khi-bấm
layer: web
type: feature
estimate: 3h
status: done
depends_on: [T-02-01]
blocks: [T-02-03]
verifies: [AC-06]
tests:
  - apps/backoffice-web/src/pages/goods-issue/goods-issue-fetch-on-demand.test.ts   # new
touches:
  - apps/backoffice-web/src/pages/goods-issue/GoodsIssuePage.tsx
assumptions: [A-02]
---

# T-02-02 — Nút cần dòng của Xuất kho chuyển sang nạp-khi-bấm

## Context
Cùng hình dạng T-01-02. Việc đầu tiên là **liệt kê** các nút thật sự cần `lines` trên trang này
(Sửa / Nhân bản / các đường in) rồi mới sửa — đừng chép danh sách của Nhập kho sang, hai thanh
công cụ không giống hệt nhau.

## Implementation notes
- Helper `fetchGoodsIssueWithLines(id)` gọi `GET /inventory/goods-issues/{id}` không kèm cờ.
- `pendingAction` khoá đúng nút đang chờ; lỗi thì toast và **không** mở dialog.
- Kiểm lại `GoodsIssueFormDialog.tsx:239-320`: các effect giải kệ ưu tiên / đoán kho bám vào
  `lines`. Chúng đã bị `isView` chặn từ trước; xác nhận bằng cách mở dialog Xem một phiếu và
  đối chiếu dữ liệu hiển thị với dữ liệu đã lưu, chứ không đọc code rồi kết luận.

## Done when
- [x] Danh sách nút cần `lines` của trang này được liệt kê trong PR description
- [x] Lỗi API → toast, dialog không mở
- [x] Các phép kiểm chỉ làm được bằng trình duyệt đã chuyển lên definition-of-done của UoW
      (mục "Bằng chứng trình duyệt — G4" trong `uow.md`). Chuyển tầng ngày 8/9/2026 sau khi
      reopen G3: chúng là bằng chứng mức demo, người ký ở G4, không phải điều kiện để một
      agent CLI được `submit`. Chúng **vẫn phải làm** — chỉ đổi chủ sở hữu và gate.
