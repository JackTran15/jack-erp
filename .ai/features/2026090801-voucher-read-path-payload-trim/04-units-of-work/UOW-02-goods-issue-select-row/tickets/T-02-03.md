---
id: T-02-03
uow: UOW-02
title: Panel Xuất kho nhận selectedId — header và dòng chạy song song
layer: web
type: refactor
estimate: 2h
status: done
depends_on: [T-02-02]
blocks: []
verifies: [AC-26]
tests:
  - apps/backoffice-web/src/pages/goods-issue/goods-issue-detail-fetch.test.ts
touches:
  - apps/backoffice-web/src/pages/goods-issue/GoodsIssuePage.tsx
assumptions: [A-01]
---

# T-02-03 — Panel Xuất kho nhận `selectedId`, header và dòng chạy song song

## Vì sao ticket này tồn tại

Bản sao của [T-01-04] cho trang Xuất kho. Thêm ngày 8/9/2026 sau khi reopen G1 — xem lý do đầy đủ
trong T-01-04.

## Nguyên nhân

`GoodsIssuePage.tsx:981` — `const issueId = issue?.id ?? null`, trong đó `issue` là kết quả query
header. Cùng một hình dạng lỗi với `PurchaseOrdersPage.tsx:991`.

Trang này **chưa được đo trên trình duyệt** (phép đo 8/9 chỉ chạy trên Nhập kho), nhưng hình dạng
code giống hệt nên kết luận suy ra được. Nếu lúc làm phát hiện nó **không** nối tiếp thì báo lại —
đừng sửa theo ticket rồi thôi.

## Implementation notes
- `issueId` truyền thẳng từ `selectedId`, không từ `selectedIssue?.id`.
- Giữ hai `queryKey` tách biệt.
- Khuôn tham chiếu: `StockTransferPage.tsx` (T-04-05) và `StockTakesPage.tsx` (T-05-04) đã đúng.
  **Đừng** chép `PurchaseOrdersPage.tsx` trừ khi T-01-04 đã xong — bản đó mang chính lỗi này.

## Done when
- [x] `issueId` không còn suy từ kết quả query header
- [x] Test khẳng định nguồn của id
- [x] `npx tsc --noEmit` sạch
- [x] Xác nhận (bằng đọc code hoặc đo) rằng trang này thật sự có lỗi nối tiếp trước khi sửa
