---
id: T-05-02
uow: UOW-05
title: DTO + query + handler search-stock-take-lines-v2
layer: application
type: feature
estimate: 4h
status: done
depends_on: [T-05-01]
blocks: [T-05-03]
verifies: [AC-18]
tests:
  - apps/api/src/modules/inventory/stock-take/queries/search-stock-take-lines-v2.handler.spec.ts   # new
touches:
  - apps/api/src/modules/inventory/stock-take/dto/stock-take-line-search-v2.dto.ts   # new
  - apps/api/src/modules/inventory/stock-take/queries/search-stock-take-lines-v2.query.ts   # new
  - apps/api/src/modules/inventory/stock-take/queries/search-stock-take-lines-v2.handler.ts   # new
assumptions: [A-05]
---

# T-05-02 — DTO + query + handler `search-stock-take-lines-v2`

## Context
Khuôn: `goods-receipt/queries/search-goods-receipt-lines-v2.{query,handler}.ts`. Module
`stock-take` chưa có thư mục `queries/` và `dto/` — tạo mới theo đúng quy ước module NestJS của repo.

## Implementation notes
- Request `{ page?, limit? }`; response `{ data, page, limit, total }`; sắp `line_no ASC`.
- 404 trước, phân trang sau. Phạm vi: `StockTakeService.getById` gọi
  `findOrFail(id, actor.organizationId, actor.branchId)` (`stock-take.service.ts:754-756`) — route
  có `@RequireBranchScope()`, nên vị từ phải gồm **cả** chi nhánh, khác hai module kia.
- `StockTakeLineEntity` eager `item` + `location` (`:90-94`); chọn tường minh thay vì dựa `eager`.
- Panel kiểm kê hiển thị cả số đếm và chênh lệch — đối chiếu `StockTakeDetailPanel.tsx` để biết
  đủ cột trước khi chọn field trả về.

## Done when
- [x] Spec: 1 000 dòng, phân trang đúng theo `line_no`, `total` đúng
- [x] Spec: id ngoài phạm vi tổ chức **hoặc** ngoài chi nhánh → `NotFoundException`
- [x] Dòng trả về đủ cho mọi cột `StockTakeDetailPanel` đang vẽ
