# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Gộp lookup + search thành **một** endpoint `GET /pos/branches/:id/catalog/search` trả `{ exact, suggestions }`; hai endpoint cũ giữ nguyên cho consumer khác | high | yes | Nếu sai: phải quay lại 2 round-trip, hỏng US-01 | resolved | Akenzy chốt 2026-09-08 — phương án "Gộp 1 endpoint" |
| A-02 | Cắt payload bằng `?limit` (mặc định 20, trần 100) + `?view=suggest` bỏ `locations[]`/`quantityOnHand`; **mặc định giữ nguyên shape đầy đủ** | high | yes | Nếu sai: FastStockTransfer mất `locations[]` → `catalogLocationsForLine` trả rỗng → không chọn được kho nguồn | resolved | Akenzy chốt 2026-09-08 — phương án "limit + view=suggest" |
| A-03 | `minChars` của ô tìm hàng checkout nâng 1 → 3 | high | yes | Nếu sai: `%2%` vẫn quét 21 024 dòng mỗi keystroke, AC-04 không đạt | resolved | Akenzy chốt 2026-09-08 — phương án "Nâng lên 3" |
| A-04 | Đường Enter (`onSubmitQuery`) **không** bị `minChars` chặn, nên quét/nhập mã ngắn vẫn auto-add được | high | yes | Nếu sai: nâng minChars=3 sẽ giết luôn auto-add cho SKU 1–2 ký tự | resolved | Đã đọc `PosSearchPopover.tsx:265,362` (gate `runSearch`) vs `:451` (`onSubmitQuery` gọi thẳng, không gate). Xác nhận bằng code, không phải suy đoán |
| A-05 | `item_barcodes.code` cần index trigram; `products.code` nên có luôn cho cùng nhánh `OR` | high | no | Nếu sai: nhánh `b.code ILIKE` vẫn quét 41 714 dòng (18 ms) kể cả term ≥3 ký tự | resolved | Đo trên `erp_dev_3008`: seq scan 18,1 ms. Build index GIN: 240 ms, 1 296 kB — rẻ, không cần `CONCURRENTLY` |
| A-06 | `/catalog/lookup` không thiếu index; chỉ cần viết lại `i.code = $3 OR b.code = $3` thành `UNION` hai nhánh | high | no | Nếu sai: thêm index thừa vào bảng 41 K dòng mà không nhanh hơn | resolved | Đo: dạng `OR` 25,3 ms (seq scan 21 024 + 21 023); dạng `UNION` 1,1 ms (2 index scan). Không thêm index nào |
| A-07 | Endpoint mới đi theo chuẩn CQRS + controller riêng (`PosCatalogSearchController`) như [[feedback_cqrs_standard_dedicated_controller]], không nhét thêm route vào `pos.controller.ts` | medium | no | Nếu sai: phải gỡ controller + module wiring, ~2 ticket | resolved | ADR-04. Ranh giới trong CLAUDE.md ("CQRS cho query multi-join động") không phủ rõ ca này, nhưng feedback của Akenzy nói rõ "report/search → full CQRS + 1 dedicated controller" — theo feedback |
| A-08 | `sellableQuantity` (cảnh báo bán vượt tồn) **phải** giữ trong `view=suggest`; chỉ `locations[]` và `quantityOnHand` bị cắt | high | no | Nếu sai: cảnh báo tồn ở giỏ hàng sai sau khi thêm từ dropdown — hồi quy [[project_pos_stock_warning_temp_warehouse]] | resolved | `addProductByItem` nhận nguyên `PosCatalogLine`; `sellableQuantity` là cơ sở cảnh báo, `locations[]` thì không consumer nào của checkout đọc (`use-checkout-cart-actions.ts` 0 match) |
| A-09 | `stagedStock.getBranchDelta(branchId, orgId)` (kho tạm) vẫn phải chạy cho đường suggest vì `sellableQuantity` phụ thuộc nó | medium | no | Nếu sai: hoặc cảnh báo tồn sai, hoặc gánh thêm một truy vấn không cần | resolved | Đo 2026-09-08 trên `erp_dev_3008`: **0,435 ms**, xa ngưỡng 5 ms → **không cache**. `searchCatalog` gọi đúng 1 lần cho cả hai nhánh (spec khẳng định), nên chi phí này không nhân lên |
| A-10 | Chỉ POS checkout (`ProductSearchInput`) chuyển sang endpoint mới ở feature này; `FastStockTransferProductSearchInput` giữ nguyên đường cũ | high | no | Nếu sai: phạm vi phình sang màn chuyển kho nhanh, thêm ~4 ticket | resolved | Out of scope trong `00-intent.md`; endpoint cũ không đổi hợp đồng nên chuyển kho nhanh không bị ảnh hưởng |
