---
id: T-02-04
uow: UOW-02
title: Chứng minh Chuyển kho nhanh không hồi quy
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-02-03]
blocks: []
verifies: [AC-11]
tests:
  - apps/pos-web/src/lib/page-libs/fast-stock-transfer/fast-stock-transfer-pickers.test.ts   # new
touches:
  - apps/pos-web/src/lib/page-libs/fast-stock-transfer/fast-stock-transfer-pickers.test.ts   # new
assumptions: [A-02, A-10]
---

# T-02-04 — Chuyển kho nhanh không hồi quy

## Context
`locations[]` là thứ duy nhất trong `PosCatalogLine` mà feature này có thể vô tình
làm rỗng. Consumer thật: `fast-stock-transfer-pickers.ts:20` (`catalogLocationsForLine`)
và `picker-cache.ts:59`.

`pos-web` **chạy được** `npx vitest run` dù `package.json` ghi `echo test` —
[[reference_pos_web_vitest_works.md]]. Không có RTL/jsdom nên chỉ test được hàm thuần;
`catalogLocationsForLine` là hàm thuần nên test được. Có một số test đỏ sẵn từ trước —
đọc kết quả theo tên file, đừng đọc theo exit code.

## Implementation notes
- Test `catalogLocationsForLine` với: line có `locations` không rỗng; line rỗng
  `locations` nhưng có `defaultLocationId`; line rỗng cả hai.
- Phần UI phải verify bằng trình duyệt, không phải vitest: chụp màn hình ô chọn kho
  nguồn ở màn Chuyển kho nhanh, trước và sau.
- Chạy `npx vitest run` từ `apps/pos-web`, **không** qua rtk (rtk từng nuốt output
  tiếng Việt) — [[reference_pos_web_vitest_works.md]].

## Done when
- [x] 3 ca của `catalogLocationsForLine` xanh
- [x] ~~Ảnh chụp ô chọn kho nguồn~~ — **KHÔNG chụp được**, org có dữ liệu là bản restore prod không có mật khẩu. Thay bằng: `EXCEPT` hai chiều (T-02-03) + response API còn nguyên `locations[]`. Ảnh UI chuyển sang T-04-04
- [x] Danh sách test đỏ sẵn không dài thêm dòng nào (3 → 3)

## Bằng chứng (2026-09-08)

### vitest (`apps/pos-web`, `npx vitest run`, không qua rtk)

| | tổng | pass | fail |
|---|---|---|---|
| Trước ticket | 166 | 163 | 3 |
| Sau ticket | 169 | 166 | **3** |

Ba test đỏ đều nằm trong `src/lib/common/api-axios.test.ts` (refresh-token 401),
đỏ sẵn từ trước, không liên quan. **Danh sách đỏ không dài thêm dòng nào.**

### API thật (`erp_dev_3008`, org `f1000000…` "My Company", 20 692 item, chi nhánh `c3bf1922…`)

`GET /pos/branches/:id/catalog?search=235` sau khi đã đổi sang UNION:

- 200, 87 phần tử, 25 024 B, 15 ms
- Khoá của mỗi phần tử: `code, defaultLocationId, itemId, locations, name, productId,
  quantityOnHand, sellableQuantity, sellingPrice, unit` — **`locations` còn nguyên**
- 9/87 phần tử có `locations` không rỗng, ví dụ
  `[{locationId: 6bfb85d9…, name: "DEFAULT", quantity: 1}]`

Đó là thứ `catalogLocationsForLine` đọc, nên Chuyển kho nhanh không thể hồi quy.

### Điểm chưa làm được — nói thẳng

**Chưa chụp được màn hình ô chọn kho nguồn.** Tài khoản duy nhất đăng nhập được
(`admin@erp.local`) thuộc org `f1000000…`; org mang dữ liệu đo hiệu năng
(`e60e5f49…` "MT", 21 024 item POS-visible) là bản restore prod và không có mật khẩu
nào dùng được. Tạo user trong org đó để chụp ảnh là ghi vào bản restore prod — không
tự ý làm.

Thay vào đó, không-hồi-quy được chứng minh ở hai tầng chặt hơn ảnh chụp:
`EXCEPT` hai chiều cho tập item giống hệt trên 3 term (T-02-03), và response thật
của API còn nguyên `locations[]` (ở trên). Ảnh chụp UI để lại cho T-04-04, nơi có
phiên POS thật.
