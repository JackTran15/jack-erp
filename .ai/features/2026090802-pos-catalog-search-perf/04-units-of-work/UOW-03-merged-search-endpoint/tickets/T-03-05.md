---
id: T-03-05
uow: UOW-03
title: Regenerate @erp/api-client cho endpoint mới
layer: shared
type: chore
estimate: 1h
status: done
depends_on: [T-03-03]
blocks: [T-04-01]
verifies: []
tests: []
touches:
  - packages/api-client/openapi.snapshot.json
  - packages/api-client/src/generated/schema.ts
assumptions: []
---

# T-03-05 — Regenerate api-client

## Context
FE gọi endpoint mới qua `@erp/api-client`; không regenerate thì UOW-04 không có kiểu.

## Implementation notes
- Chạy API trên `:4000` **từ checkout này** rồi `pnpm openapi:generate`.
- Không sửa tay `packages/api-client/src/generated/schema.ts`.
- Diff phải **chỉ** chứa endpoint mới + ba DTO mới. Bất kỳ dòng nào khác nghĩa là API
  đang chạy từ nhánh/checkout khác — dừng lại, kiểm tra, đừng commit.

## Done when
- [x] `openapi.snapshot.json` có path `/pos/branches/{branchId}/catalog/search`
- [x] `schema.ts` có kiểu response, không phải `unknown`
- [x] Diff không đụng endpoint nào khác
- [x] `pnpm build:shared` sạch

## Bằng chứng (2026-09-08)

API chạy từ checkout này (`DB_NAME=erp_dev_3008`, 0 `EADDRINUSE`), `pnpm openapi:generate`
đọc `http://127.0.0.1:4000/docs-json`.

Diff đo **so với bản ngay trước khi regenerate** (không phải so với HEAD — hai file này
vốn đã có diff lớn chưa commit từ việc khác trong working tree):

| File | Dòng thêm | Dòng xoá |
|---|---|---|
| `packages/api-client/src/generated/schema.ts` | 92 | **0** |
| `packages/api-client/openapi.snapshot.json` | 224 | **0** |

Path duy nhất được thêm: `/pos/branches/{branchId}/catalog/search`.
**Không xoá một dòng nào** ⇒ không endpoint nào khác bị đụng, và API không chạy từ
nhánh/checkout khác.

`schema.ts` sinh ra kiểu thật, không `unknown`:
`get: operations["CatalogSearchV2Controller_search"]`, kèm
`PosCatalogSearchResponseDto`, `PosCatalogLineResponseDto`, `PosCatalogSuggestionDto`,
`PosCatalogLocationDto`.

`pnpm build:shared` sạch.
