---
feature: stock-transfer-print-export
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID   | Assumption                                                                                                                                   | Confidence | Blocking | Blast radius if wrong                                                          | Status   | Resolution |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------ | -------- | ---------- |
| A-01 | "Phiếu chuyển kho" trong báo lỗi là trang **Chuyển kho** (`/inventory/stock-transfers`, `StockTransferEntity`), không phải Lệnh điều chuyển (đã in được) | high       | yes      | Toàn bộ feature nhắm sai màn hình                                              | resolved | Chốt theo plan đã duyệt 2026-09-08 (Loc Tran): nút In/Xuất khẩu chết nằm ở `StockTransferPage.tsx:1167` |
| A-02 | Bố cục phiếu theo file mẫu MISA: cột STT, Mã SKU, Tên hàng hóa, Kho xuất, Vị trí xuất, Kho nhập, Vị trí nhập, ĐVT, Số lượng, Đơn giá, Thành tiền, (Giá bán, Thành tiền giá bán, Ghi chú ẩn trên giấy) + dòng "Số tiền viết bằng chữ" | high       | yes      | Cột sai ⇒ sửa mapper + spec (1 ticket)                                          | resolved | Chốt theo plan đã duyệt 2026-09-08 (Loc Tran): "làm theo structure hiện tại, cho giống phiếu Nhập, Xuất" + 2 file mẫu đính kèm |
| A-03 | Thêm `VoucherKind.STOCK_TRANSFER` thay vì dùng lại `TRANSFER_ORDER` — hai bảng route FE khóa theo kind, hai chứng từ có route khác nhau       | high       | yes      | Nếu dùng chung kind, FE không phân biệt được route ⇒ phải đổi chữ ký hàm          | resolved | Chốt theo plan đã duyệt 2026-09-08 (Loc Tran); xem ADR-01 |
| A-04 | Dòng "Người vận chuyển" in tên **Đối tượng** (`counterparty.name`), rơi về `transporter.fullName` (trường legacy) khi phiếu cũ không có đối tượng | medium     | no       | Sai nhãn/giá trị một dòng thông tin; sửa mapper 10 phút                          | resolved | Đã áp dụng đúng vậy: payload thật CK004523 in "Người vận chuyển: Cao Thị Diễm Hằng" (đối tượng); spec phủ fallback transporter → "—". Chủ sở hữu có thể đổi nhãn/giá trị sau (1 dòng mapper) |
| A-05 | Cột "Vị trí xuất/nhập" in **tên** vị trí (`LocationEntity.name`) như phiếu Nhập/Xuất đang làm, không in mã                                    | medium     | no       | Người dùng muốn mã ⇒ đổi một biểu thức trong mapper                              | resolved | Đã áp dụng: in `LocationEntity.name` như `goods-issue-print.mapper.ts:72`; bản in CK004523 hiện "999" / "Mặc định". Đổi sang mã là một biểu thức |
| A-06 | Đầu phiếu lấy chi nhánh **xuất** (`sourceBranchId`); kho nhận thể hiện theo từng dòng ở cột "Kho nhập", không thêm dòng "Đến" như Lệnh điều chuyển | medium     | no       | Phiếu liên chi nhánh thiếu dòng chi nhánh nhận ở đầu phiếu; thêm 1 info row       | resolved | Đã áp dụng theo file mẫu MISA (không có dòng "Đến"); kho nhận nằm ở cột "Kho nhập" từng dòng. Thêm info row nếu chủ sở hữu muốn |
| A-07 | Ngày trên phiếu = `transferredAt ?? createdAt` (cùng trường cột "Ngày" của danh sách)                                                          | high       | no       | Ngày lệch với lưới                                                             | resolved | Đã áp dụng `transferredAt ?? createdAt` — cùng trường cột Ngày ở `StockTransferPage.tsx:483`; spec phủ cả hai nhánh |
| A-08 | Route mới scope theo org (không `@RequireBranchScope`) như Lệnh điều chuyển, vì phiếu chuyển kho có thể liên chi nhánh và `getById` hiện đã org-scoped | high       | no       | Nếu cần chặn theo chi nhánh: thêm guard + 1 case e2e                            | resolved | Đã áp dụng (ADR-02); e2e `voucher-print-payload` có case cùng org, chi nhánh khác → 200 và khác org → 404 |
| A-09 | Không cần migration — mọi trường cần in đã có trên `StockTransferEntity` / `StockTransferLineEntity` (eager `item`, `sourceStorage`, `destinationStorage`, `sourceLocation`, `destinationLocation`, `unitPrice`, `lineValue`) | high       | no       | Nếu thiếu quan hệ eager ⇒ thêm `relations` ở `findOrFail`                         | resolved | Đúng: route chạy trên dev với `getById` sẵn có, mọi quan hệ eager; không thêm `relations`, không migration |

## Rejected assumptions

| ID   | What we assumed                                                              | What is actually true                                                                              | Consequence                                              |
| ---- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| A-R1 | Phiếu chuyển kho chưa in được vì nền tảng in chứng từ chưa có                  | Nền tảng đã xong (`export-print`, UOW-03/08/10/11/12/15); chỉ thiếu mapper + route + bật nút        | Phạm vi co lại còn 1 UoW, không có ticket nào ở renderer  |
| A-R2 | Phiếu chuyển kho thiếu Đơn giá / Vị trí nhập như Lệnh điều chuyển              | `StockTransferLineEntity` có `unitPrice`, `lineValue`, `destinationStorage`, `destinationLocation`  | In đủ mọi cột của file mẫu MISA, có "Số tiền viết bằng chữ" |
