---
id: T-01-01
uow: UOW-01
title: VoucherKind.STOCK_TRANSFER + bề rộng cột destPosition
layer: shared
type: feature
estimate: 1h
status: done
depends_on: []
blocks: [T-01-02, T-01-04]
verifies: [AC-01]
touches:
  - packages/shared-interfaces/src/printing/voucher-payload.ts
  - apps/api/src/modules/inventory/voucher-column-width.const.ts
assumptions: [A-03]
---

# T-01-01 — VoucherKind.STOCK_TRANSFER + bề rộng cột destPosition

## Context
ADR-01. FE dispatch route theo `VoucherKind`; renderer không switch theo kind nên chỉ cần thêm
giá trị. `VOUCHER_COLUMN_WIDTHS` thiếu khóa cho cột "Vị trí nhập".

## Implementation notes
- Thêm `STOCK_TRANSFER = 'STOCK_TRANSFER'` sau `TRANSFER_ORDER`; sửa comment "7 voucher kinds" → 8.
- Thêm `destPosition: 13` cạnh `sourcePosition`.
- `pnpm build:shared` để API/web thấy enum mới.

## Done when
- [x] `VoucherKind.STOCK_TRANSFER` compile ở cả `@erp/api` và `@erp/backoffice-web`
- [x] `VOUCHER_COLUMN_WIDTHS.destPosition` tồn tại
