---
id: T-01-02
uow: UOW-01
title: mapStockTransferToVoucherPayload + spec
layer: application
type: feature
estimate: 3h
status: done
depends_on: [T-01-01]
blocks: [T-01-03]
verifies: [AC-01, AC-02, AC-03]
touches:
  - apps/api/src/modules/inventory/transfer/stock-transfer-print.mapper.ts   # new; theo goods-issue-print.mapper.ts
  - apps/api/src/modules/inventory/transfer/stock-transfer-print.mapper.spec.ts   # new
assumptions: [A-02, A-04, A-05, A-06, A-07, A-09]
---

# T-01-02 — mapStockTransferToVoucherPayload + spec

## Context
Mapper thuần, chữ ký `(transfer: StockTransferEntity, sourceBranch: DocumentBranchInfo | null)`.
Chép `goods-issue-print.mapper.ts` (cột tiền, `amountInWordsVi`, cột ẩn) và
`transfer-order-print.mapper.ts` (cột kho xuất/nhập, tiêu đề).

## Implementation notes
- `LINE_COLUMNS`: stt, sku, name(span 4), sourceWarehouse, sourcePosition, destWarehouse, destPosition, uom, quantity ("Số lượng"), unitPrice, lineTotal, salePrice(hidden), saleTotal(hidden), note(hidden); width từ `VOUCHER_COLUMN_WIDTHS`.
- Giá trị dòng: `line.sourceStorage?.name`, `line.sourceLocation?.name`, `line.destinationStorage?.name`, `line.destinationLocation?.name`, `Number(line.unitPrice ?? 0)`, `lineTotal = line.lineValue != null ? Number(line.lineValue) : unitPrice * quantity`.
- `info`: "Người vận chuyển" = `counterparty?.name ?? transporter?.fullName ?? '—'`; "Diễn giải" = `notes ?? '—'`.
- `docDate = toLongVietnameseDate(transferredAt ?? createdAt)`, `totalsLabel 'Tổng'`, `amountInWords` chỉ khi có dòng, `signatures` 5 nhãn chung.

## Done when
- [x] Spec: title/kind/paper; thứ tự nhãn cột; giá trị dòng (AC-02); totals + amountInWords; fallback transporter; phiếu rỗng → totals null, không amountInWords (AC-03)
- [x] Không import `@nestjs/*` hay typeorm runtime trong mapper
