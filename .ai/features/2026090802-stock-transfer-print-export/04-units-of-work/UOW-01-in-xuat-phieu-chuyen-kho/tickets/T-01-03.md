---
id: T-01-03
uow: UOW-01
title: Route print-payload + export cho phiếu chuyển kho, sinh lại api-client
layer: api
type: feature
estimate: 2h
status: done
depends_on: [T-01-02]
blocks: [T-01-04, T-01-05]
verifies: [AC-01, AC-04]
touches:
  - apps/api/src/modules/inventory/transfer/stock-transfer.service.ts
  - apps/api/src/modules/inventory/transfer/stock-transfer.controller.ts
  - packages/api-client/src/generated/schema.ts
  - packages/api-client/openapi.snapshot.json
assumptions: [A-08, A-09]
---

# T-01-03 — Route print-payload + export cho phiếu chuyển kho

## Context
ADR-02. Chép nguyên `transfer-order.controller.ts:424-448` và `transfer-order.service.ts:298-323`.

## Implementation notes
- Service: `getPrintPayload(id, actor)` = `getById(id, actor.organizationId)` → `loadVoucherBranch(this.dataSource.manager, transfer.sourceBranchId, actor.organizationId)` → mapper.
- Controller: `@Get(':id/print-payload')` và `@Get(':id/export')` (`@HttpCode(200)`, `@Res()`), cùng `@RequirePermission('inventory.transfer.read')`; export dùng `ExportPipeline(StaticRowsFetcher, VoucherXlsxWriter, HttpResponseSink)` + `voucherToReportDocument`.
- API chạy trên :4000 → `pnpm openapi:generate`; commit hai file sinh ra.

## Done when
- [x] `curl` print-payload trên dev trả 200 với title PHIẾU CHUYỂN KHO
- [x] `curl` export trả 200, spreadsheetml, filename `phieu-chuyen-kho.xlsx`, file mở được bằng openpyxl
- [x] `schema.ts` có `StockTransferController_getPrintPayload` và `StockTransferController_export`
