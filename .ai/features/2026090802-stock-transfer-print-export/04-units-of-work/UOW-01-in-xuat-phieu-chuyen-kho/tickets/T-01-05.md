---
id: T-01-05
uow: UOW-01
title: E2E cách ly org cho route print-payload / export phiếu chuyển kho
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-01-03]
blocks: []
verifies: [AC-05, AC-06]
touches:
  - apps/api/test/e2e/voucher-print-payload.e2e-spec.ts
assumptions: [A-08]
---

# T-01-05 — E2E cách ly org cho route phiếu chuyển kho

## Context
Suite sẵn có lái 3 route bằng một bảng `describe.each`; thêm phiếu chuyển kho là thêm một hàng.

## Implementation notes
- Seed bằng SQL vào `stock_transfers` (organization_id, branch_id, source_branch_id, destination_branch_id, status POSTED, document_number) và `stock_transfer_lines` (item_id, source_storage_id, destination_storage_id, quantity, unit_price, line_value).
- Thêm hàng `stock transfer` vào bảng 404-khác-org (không lộ `TEST-`/tên hàng) và case "org-scoped only" (chi nhánh khác cùng org → 200); thêm 200 cho chủ sở hữu ở cả print-payload và export.

## Kết quả chạy (2026-09-08)
`pnpm --filter @erp/api test:e2e -- voucher-print-payload`: 6 pass / 5 fail. **Toàn bộ case phiếu
chuyển kho xanh** (owner 200 + xlsx stream, 2 hàng khác org → 404 không lộ trường, cùng org khác chi
nhánh → 200) và case Lệnh điều chuyển xanh. 5 case đỏ là các hàng **Nhập kho / Xuất kho có sẵn**:
route `goods-receipts` / `goods-issues` trả **403** cho chính user seed — lỗi có sẵn ở HEAD (spec này
chưa từng xanh sau khi 2 module đó đổi: seed thiếu `line_no`, cột audit không tồn tại, và giờ là
quyền). Đã sửa 3 lỗi seed schema để suite chạy được; lỗi 403 thuộc module NK/XK, ngoài phạm vi
feature này — ghi thành việc riêng.

## Done when
- [x] `pnpm --filter @erp/api test:e2e -- voucher-print-payload`: mọi case phiếu chuyển kho xanh, đọc output thật (không chỉ exit code); 5 case NK/XK đỏ 403 từ trước, xem "Kết quả chạy"
- [x] Không hàng nào của 3 route cũ bị đổi (chỉ sửa seed cho khớp schema hiện tại: `line_no`, bỏ cột audit không có trên `goods_issue_lines`)
