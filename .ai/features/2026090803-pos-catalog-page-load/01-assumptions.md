# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Làm tươi tồn giỏ bằng endpoint mới `POST /pos/branches/:id/catalog/stock` nhận `itemIds[]` | high | yes | Nếu sai: quay lại N request/giỏ hoặc lại tải toàn catalog | resolved | Akenzy chốt 2026-09-08 — "Endpoint mới theo itemIds" |
| A-02 | `addProductByQuery` và `openForQuery` hỏi server (`/catalog/search?view=suggest&limit=2`) rồi quyết theo số dòng | high | yes | Nếu sai: vẫn cần mảng catalog đầy đủ trên client, feature vô nghĩa | resolved | Akenzy chốt 2026-09-08 — "Hỏi server, đếm kết quả" |
| A-03 | `addProductByCatalogCard` + `handleCatalogSelect` được **xoá** | high | yes | Nếu sai: `catalog` còn consumer, `useCatalogQuery` không gỡ được | resolved | Akenzy chốt 2026-09-08 — "Xoá". Đã xác nhận là code chết: grep toàn `apps/pos-web/src` chỉ ra định nghĩa + dòng return, 0 lời gọi |
| A-04 | Bỏ `disabled={catalogLoading}` ở ô tìm; `CatalogErrorAlert` đọc lỗi của `/catalog/products` | high | no | Nếu sai: mất chỉ báo lỗi tải, hoặc ô tìm bật khi chưa sẵn sàng | resolved | Akenzy chốt 2026-09-08 — "Bỏ khoá, trỏ error sang lưới" |
| A-05 | `limit=2` là đủ cho `addProductByQuery`: chỉ cần phân biệt 0 / 1 / ≥2 | high | no | Nếu sai: đếm sai ca "nhiều kết quả" | resolved | Ba nhánh của `addProductByQuery` (`use-checkout-cart-actions.ts:73-88`) chỉ rẽ theo `length === 1`, `=== 0`, `else`. Dòng thứ ba trở đi không bao giờ được đọc |
| A-06 | Ngữ nghĩa khớp **đổi**: client `matchesCatalogQuery` chỉ so `name`/`code`; server so thêm mã vạch + `products.code`/`name` | medium | no | Nếu sai: một chuỗi trước khớp 1 nay khớp nhiều → "Nhiều kết quả" thay vì thêm được hàng | resolved | ADR-03. Đây là **mở rộng** tập khớp, và là hành vi đúng hơn: cùng chuỗi đó gõ vào ô tìm đã ra dropdown theo luật server từ feature trước, nên Enter khớp luật khác là bất nhất |
| A-07 | `POST /catalog/stock` trả shape `PosCatalogLine` đầy đủ (có `locations[]`) | medium | no | Nếu sai: `syncPurchaseCartOnHand` thiếu trường | resolved | `syncPurchaseCartOnHand` chỉ đọc `sellableQuantity` qua `readSellableOnHand`; nhưng shape đầy đủ không tốn gì ở quy mô 1–20 item, và giữ nó cho consumer sau. Không thêm `view=suggest` cho endpoint này |
| A-08 | Ngưỡng `≤ 100 kB` payload lúc mở trang gồm cả `/catalog/products?pageSize=30` | medium | no | Nếu sai: success signal đo nhầm thứ | resolved | Đo ở T-04-01: chỉ còn `/catalog/products` (~20 kB đo trên Cà Mau) và `/catalog/stock` (0 nếu giỏ rỗng) |
| A-09 | Giỏ rỗng ⇒ **không** gọi `/catalog/stock` | high | no | Nếu sai: 1 request thừa mỗi lần mở trang | resolved | ADR-02: hook bail khi `itemIds.length === 0`. Đây là ca thường gặp nhất (mở trang với giỏ trống) |
| A-10 | `POST /catalog/stock` gác bằng `inventory.read`, giống ba endpoint catalog còn lại | high | no | Nếu sai: thu ngân hoặc nhân viên kho mất quyền đọc tồn | resolved | Cùng lý do đã ghi ở `pos.controller.ts:45-47`: đọc catalog không phải là bán hàng |
