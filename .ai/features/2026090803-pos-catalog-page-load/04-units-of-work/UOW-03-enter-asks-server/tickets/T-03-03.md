---
id: T-03-03
uow: UOW-03
title: Xoá addProductByCatalogCard + handleCatalogSelect
layer: web
type: chore
estimate: 2h
status: done
depends_on: [T-03-01, T-03-02]
blocks: [T-04-01]
verifies: [AC-08]
tests: []
touches:
  - apps/pos-web/src/hooks/page-hooks/checkout/use-checkout-cart-actions.ts
  - apps/pos-web/src/hooks/page-hooks/checkout/use-checkout-session-cart.ts
assumptions: [A-03]
---

# T-03-03 — Xoá đường code chết

## Context
`addProductByCatalogCard` (`use-checkout-cart-actions.ts:34,90,111`) và
`handleCatalogSelect` (`use-checkout-session-cart.ts:262,472`) được khai báo, định
nghĩa và trả ra, nhưng **không component nào gọi**. `ProductCard.tsx:20` bấm vào thì
gọi `openForCatalogCard`, đi thẳng theo `kind` + `id` của card.

Chúng cũng là lý do **duy nhất** mảng `catalog` thô còn bị truyền ra khỏi
`useCheckoutCatalog` — nên xoá chúng là điều kiện để UOW-04 gỡ được `useCatalogQuery`.

`CLAUDE.md` cấm tự ý xoá code chết có sẵn; đây là quyết định của Akenzy (2026-09-08),
ghi ở A-03 và ADR-05.

## Implementation notes
- Xoá cả khai báo trong `UseCheckoutCartActionsResult` và dòng trong object trả về.
- Xoá `handleCatalogSelect` khỏi `use-checkout-session-cart.ts` và khỏi object trả về.
- Gỡ luôn các import thành mồ côi theo (`CatalogProduct` nếu không còn dùng) — kiểm
  bằng `tsc --noUnusedLocals`, đó là thứ duy nhất bắt được import mồ côi ở repo này
  (không có linter thật, mọi workspace đều `echo lint`).
- **Chỉ** xoá hai hàm này. Không dọn dẹp gì khác trong hai file.

## Done when
- [x] `grep -rn "addProductByCatalogCard\|handleCatalogSelect" apps/pos-web/src` → rỗng
- [x] `tsc --noEmit` sạch
- [x] `tsc --noEmit --noUnusedLocals` không báo import mồ côi mới ở hai file này
- [x] Bấm vào card sản phẩm ở lưới vẫn mở dialog biến thể như cũ

## Bằng chứng (2026-09-08)

```
grep -rn "addProductByCatalogCard|handleCatalogSelect" apps/pos-web/src
→ (rỗng)
```

Đã xoá: khai báo trong `UseCheckoutCartActionsResult`, thân hàm, dòng trong object
trả về của cả hai hook.

`tsc --noUnusedLocals` bắt bốn import thành mồ côi theo — `useCheckoutCatalog` và
`CatalogProduct` (cart-actions), `PosCatalogLine` và `CatalogProduct` (session-cart)
— đã gỡ cả bốn. Đây là công cụ duy nhất bắt được import thừa ở repo này: không có
linter thật, mọi workspace đều `echo lint`.

**Còn một cảnh báo KHÔNG phải của tôi:** `use-checkout-session-cart.ts:34 'cartError'
is declared but its value is never read` — có sẵn từ trước, `git diff` không chạm dòng
đó. Không đụng (CLAUDE.md: không tự dọn code chết có sẵn), ghi lại để bạn biết.

`tsc --noEmit` sạch. Bấm card sản phẩm ở lưới vẫn mở dialog biến thể — đường đó đi
`openForCatalogCard`, không liên quan tới hai hàm vừa xoá.
