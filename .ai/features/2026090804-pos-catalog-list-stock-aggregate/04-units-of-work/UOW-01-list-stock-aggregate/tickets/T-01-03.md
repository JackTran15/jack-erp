---
id: T-01-03
uow: UOW-01
title: Đo lại trước/sau trên erp_dev_3008
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-01-02]
blocks: []
verifies: [AC-07, AC-08]
tests: []
touches:
  - apps/api/scripts/bench-catalog-stock.js   # new; chưa có thư mục scripts cho bench
  - .ai/features/2026090804-pos-catalog-list-stock-aggregate/07-verification.md  # new
assumptions: [A-03]
---

# T-01-03 — Đo lại trước/sau

## Context

Phần Problem dựng trên số đo, nên phần kết cũng phải là số đo — không phải "trông có vẻ
nhanh hơn". Hai mốc đã có sẵn giá trị trước:

| Mốc | Trước | Ngưỡng đạt |
| --- | --- | --- |
| AC-07 — nạp tồn của `listProducts`, chi nhánh `71230276…` | 91.0 ms | ≤ 25 ms |
| AC-08 — `findOne` pos-settings khi 4 lượt list chạy song song | 83.3 ms | ≤ 25 ms |

Mốc "trước" đo bằng `AppDataSource` từ `apps/api/dist`, DB `erp_dev_3008`,
org `e60e5f49-304d-4eb1-9735-3a2d10ba288f`, chi nhánh
`71230276-ba6d-4293-a40f-2dae480a46d4`, sau một lượt chạy làm ấm.

## Implementation notes

- Script chạy được lại, không phải đoạn dán một lần: `apps/api/scripts/bench-catalog-stock.js`.
- Nó phải đo **cả hai** đường trong cùng một lần chạy để so sánh có nghĩa: đường cũ
  (`balanceRepo.find` + `locationRepo.find` + `storageRepo.find`) và đường mới
  (truy vấn gộp) — đường cũ vẫn còn nguyên trong code nên đo trực tiếp được.
- Phần AC-08: bắn N lượt đường mới song song bằng `Promise.all`, kèm một
  `orgRepo.findOne` trong cùng mẻ, in ra thời gian của từng lượt.
- Kết quả ghi vào `07-verification.md` theo đúng hình dạng bảng ở trên, kèm ngày và tên DB.
- Script đọc mật khẩu từ env (`PGPASSWORD` / `DB_PASS`), không hard-code.

## Done when

- [x] Script chạy được bằng một lệnh và in cả cột trước lẫn cột sau
- [x] AC-07 đạt: ≤ 25 ms (mốc 91.0 ms)
- [x] AC-08 đạt: ≤ 25 ms (mốc 83.3 ms)
- [x] `07-verification.md` có bảng trước/sau, ngày đo, tên DB, org và branch
- [x] Nếu một mốc không đạt, ghi số thật vào `07-verification.md` và mở lại ticket thay
      vì hạ ngưỡng
