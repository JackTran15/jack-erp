---
feature: 2026090804-treasury-voucher-party-address-print-export
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|---|---|---|---|---|---|---|
| A-01 | Không thêm dropdown "Loại đối tượng" vào phiếu. Ô Tên luôn gõ được; gõ tên mà bỏ trống ô Mã ⇒ lưu `partnerType = OTHER`; chọn từ kính lúp ⇒ lưu theo loại danh mục. | high | yes | Bố cục ô Đối tượng ở 4 dialog + `VoucherPartnerFields` + `mapPartnerFields`; UOW-01 viết lại | confirmed | Akenzy chốt 2026-09-08 ("Nếu không chọn select type thì mặc định là khác, user chỉ cần fill tên thôi") |
| A-02 | Chọn đối tượng từ danh mục rồi sửa lại ô Tên thì **giữ** `partner_id` và ghi đè `partner_name_snapshot` bằng tên gõ tay. Phiếu vẫn quy về đúng khách hàng cho công nợ, nhưng tên in ra có thể khác tên danh mục. | high | yes | Luật ghi snapshot ở 4 service + `mapPartnerFields`; đổi ý sau G3 phải sửa cả BE lẫn FE | confirmed | Akenzy chốt 2026-09-08 |
| A-03 | Địa chỉ gõ tay thắng địa chỉ danh mục và đóng băng lên phiếu — `dto.address ?? partner?.address`, đúng luật `bank-receipts.service.ts:199` đang chạy. Áp cho cả đối tượng danh mục lẫn đối tượng "Khác", trên cả 4 loại phiếu. | high | yes | DTO + service tiền mặt (UOW-02); nếu sai thì ô Địa chỉ phải thành read-only, đảo ngược cả UoW | confirmed | Akenzy chốt 2026-09-08 |
| A-04 | Phạm vi In/Xuất lần này: In A5 cho 4 loại phiếu + Xuất .xlsx từng phiếu + Xuất .xlsx danh sách Thu chi. **Không** làm mẫu 80mm. | high | yes | Số UoW và ước lượng; 80mm sẽ là một UoW riêng | confirmed | Akenzy chốt 2026-09-08 |
| A-05 | Cột "Đối tượng" trên lưới Thu chi giữ nguyên thứ tự `COALESCE(payer_name, partner_name_snapshot)` ở `search-cash-vouchers-v2.handler.ts:53-58`. Tên gõ tay chỉ hiện khi ô "Người nộp"/"Người nhận" trống. | medium | no | Một mệnh đề SQL trong 2 nhánh UNION + fixture test tương ứng | pending | — |
| A-06 | Gõ tên vào ô Đối tượng **không** tự điền sang ô "Người nộp"/"Người nhận" — hai ô độc lập. (Chọn từ danh mục thì vẫn auto-fill như hiện nay: `setPersonName(prev => prev.trim() \|\| p.partnerName)`.) | medium | no | Một handler ở mỗi dialog; hệ quả nhìn thấy được là A-05 | pending | — |
| A-07 | Nút In và Xuất khẩu chỉ hiện khi phiếu **đã lưu** (có `id`), ẩn ở chế độ tạo mới. | high | no | Điều kiện render ở 4 dialog | pending | — |
| A-08 | In/Xuất từng phiếu dùng lại đúng quyền đọc phiếu đó — `accounting.{cash_receipt,cash_payment,bank_receipt,bank_payment}.read` — không thêm permission mới, không seed RBAC. | high | no | Nếu sai thì phải thêm 4 permission + migration seed + gán vai trò | pending | — |
| A-09 | File .xlsx từng phiếu quỹ dùng `VoucherXlsxWriter` như chứng từ kho, **không** đòi khớp một file mẫu MISA riêng. Repo hiện không có `examples/` (thư mục UOW-10 từng tham chiếu đã không còn), nên không có gì để đối chiếu byte-level. | medium | no | Định dạng cột/độ rộng file xuất; sửa được sau mà không đụng route | pending | — |
| A-10 | Xuất danh sách Thu chi lấy **toàn bộ** kết quả theo bộ lọc đang áp dụng, không chỉ trang hiện tại, và đi qua `ExportPipeline` như báo cáo kho — không dùng lại kiểu buffer cũ của `deposit-ledger/export`. | medium | no | UOW-05: nguồn dữ liệu và cách phân trang khi xuất | pending | — |
| A-11 | Feature này build tiếp trên nhánh `feat/treasury-voucher-party-reason-edit` (nơi có `c57f92e7`), không phải trên `main`. | high | no | Toàn bộ UOW-01 mất nền; nếu build trên `main` thì phải làm lại cả feature 2026090701 | pending | — |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|---|---|---|---|
| A-R1 | "Nhập tên đối tượng chưa work" nghĩa là chức năng chưa được viết. | Toàn bộ đường dây đã viết xong ở `c57f92e7` — DTO, enum Postgres, service, mapper FE, ô nhập tay. Thiếu **duy nhất** một đường vào UI tìm-thấy-được: không dialog nào có dropdown loại, và lối duy nhất là mở modal *tìm kiếm danh mục* rồi đổi bộ lọc sang "Khác". **Đính chính 2026-09-08:** bản đầu của dòng này nói lối đó bắn `type=other` rồi nuốt 400 im lặng — SAI, `onChange` của select chặn free-text trước `loadPage` (`VoucherEntitySearchModal.tsx:303-320`) và trả về form đúng trạng thái cần. Lỗi thật là **khả năng tìm thấy**, không phải lối cụt. | UOW-01 thu từ "dựng chức năng đối tượng tự nhập" xuống "mở đường vào + hợp nhất luật suy loại", ~1d thay vì ~3d. Không migration, không DTO mới. Riêng phiếu tiền gửi vẫn có lỗi cứng — xem A-R5. |
| A-R2 | Địa chỉ hỏng ở cả 4 loại phiếu. | Chỉ 2 phiếu **tiền mặt** hỏng. Phiếu tiền gửi có `address` trong DTO và ưu tiên `dto.address ?? partner?.address` từ trước. | UOW-02 chỉ đụng nhánh tiền mặt; luật ghi lấy nguyên từ phiếu tiền gửi thay vì tự nghĩ ra (A-03). |
| A-R3 | In/Xuất phiếu quỹ phải dựng từ đầu (ước lượng cũ của `export-print` UOW-04: 1.5d, rủi ro chính là hàm đọc số thành chữ tiếng Việt). | `VoucherKind` **đã** khai sẵn 4 giá trị treasury; `VoucherPrintPayload.paper` đã có `'A5'` với chú thích "A5 for treasury"; `amount-in-words.util.ts` đã tồn tại kèm spec; `renderVoucherHtml` đã tham số hoá khổ giấy; `ExportPipeline` + `VoucherXlsxWriter` + `voucherToReportDocument` đã chạy thật cho 3 chứng từ kho. | Rủi ro "đọc số thành chữ" của UOW-04 cũ **biến mất**. Việc còn lại là 4 mapper + 8 route + nối nút. `export-print` UOW-04 bị thay thế bởi UOW-03/UOW-04 ở đây. |
| A-R6 | Phiếu tiền gửi đã ghi địa chỉ **đúng hoàn toàn**, nên chỉ cần chép luật sang phiếu tiền mặt (ADR-03). | Đúng về *cấu trúc*, sai về *xử lý rỗng*. `bank-receipts.service.ts` / `bank-payments.service.ts` dùng `dto.address ?? receipt.partnerAddressSnapshot` — **không** `.trim()`, **không** `\|\| null`. Hệ quả: gửi `address: ''` để xoá trắng thì cột lưu chuỗi rỗng chứ không phải `NULL`, và khoảng trắng đầu/cuối được lưu nguyên. Chưa test nào bắt vì chưa ai viết ca đó. | T-02-02 chép **cấu trúc** của phiếu tiền gửi nhưng lấy cách xử lý rỗng từ tiền lệ `partnerName` của T-01-05 (`.trim() \|\| null`) — nếu chép nguyên xi thì AC-09 đỏ. Phía tiền gửi **vẫn còn** lệch; nằm ngoài phạm vi khiếu nại của người dùng nên chưa sửa, cần Akenzy quyết có mở ticket đồng bộ hay không. |
| A-R5 | Đường gửi tên gõ tay đã xong ở cả 4 loại phiếu, chỉ thiếu đường vào UI. | Chỉ đúng với **2 phiếu tiền mặt**. Hai dialog tiền gửi dựng body **inline** trong `handleSave` và **không hề gửi `partnerName`** (`DepositReceiptVoucherDialog.tsx:553-575`, `DepositPaymentVoucherDialog.tsx:660-745`); FE type `CreateBankReceiptBody` (`bank-vouchers.types.ts:143-160`) cũng không khai trường đó. Backend thì có — `/docs-json` xác nhận `partnerName` trên cả `CreateBankReceiptDto` lẫn `CreateBankPaymentDto`. | UOW-01 phải có ticket riêng cho nhánh tiền gửi (T-01-03): thêm trường vào 2 FE type + suy `partnerType` trong 2 body inline. Nếu bỏ sót, AC-06 sẽ xanh ở tiền mặt và đỏ im lặng ở tiền gửi — đúng kiểu bug mà feature này đang đi vá. |
| A-R4 | Người dùng muốn xuất khẩu Sổ chi tiết tiền mặt (nút stub ở `LedgerCashPage.tsx:252`). | Người dùng nói "phiếu thu, phiếu chi" ⇒ trang danh sách **Thu chi** (`TreasuryCashReceiptsPage`, tab `receipts-expenses`), trang này hiện **không có nút xuất nào cả**. `LedgerCashPage` là tab *Sổ chi tiết*, một màn khác. | UOW-05 nhắm `TreasuryCashReceiptsPage`. Nút stub ở `LedgerCashPage` để nguyên; nó vẫn thuộc `export-print` UOW-05 và ghi vào Out of scope. |
