# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | "Người nộp/nhận" trên lưới = `payer_name`/`payee_name` (người trực tiếp giao/nhận tiền), KHÔNG phải `staff_id`/`collected_by`/`paid_by` (nhân viên nội bộ thu/chi) | high | yes | Cột mới hiện sai người; sổ quỹ vốn đã có cột nhân viên riêng nên sẽ trùng lặp | resolved | Akenzy 2026-09-09 chốt phương án "không thêm cột Nhân viên thu/chi"; ảnh 2 cho thấy phiếu tách rõ "Người nhận" (`payee_name`) khỏi "Nhân viên chi" (`staff_id`); `voucher-party.ts:16-19` đã ánh xạ `personName -> payee_name/payer_name` |
| A-02 | Cột "Đối tượng nộp/nhận" KHÔNG fallback sang tên người khi phiếu không có đối tượng danh mục — để trống | high | yes | Toàn bộ hình dạng SQL và kỳ vọng test; nếu sai thì hai cột trùng giá trị trên phiếu gõ tay | resolved | Akenzy 2026-09-09: "Để trống, tách hẳn" |
| A-03 | Phạm vi là cả 4 màn hình (Thu chi tiền mặt, Thu chi tiền gửi, Sổ quỹ tiền mặt, Sổ tiền gửi) | high | yes | Số UoW và số câu SQL phải sửa | resolved | Akenzy 2026-09-09 chọn cả 4 |
| A-04 | Cột mới lọc được ở server (`StringFilterDto`, cùng đường `applyString` như `counterparty`), không phải cột chỉ-hiển-thị | medium | no | Người dùng lọc được cột này hay không; thêm 1 trường DTO mỗi endpoint | resolved | ADR-03 (2026-09-09): dùng lại `applyString` sẵn có của từng handler; cột mới lọc server-side như `counterparty` |
| A-05 | Chỉ **một** đường xuất khẩu bị ảnh hưởng: `EXPORT_COLUMNS` của `cash-voucher-v2.controller.ts`. Thu chi tiền gửi KHÔNG có nút xuất; "Xuất khẩu" của Sổ quỹ tiền mặt là stub `toast.info` (`LedgerCashPage.tsx:251`); export Sổ tiền gửi (`deposit-ledger.service.ts:529-535`) vốn không có cột đối tượng | high | no | Nếu sai thì thiếu cột trong 1–3 file Excel nữa | resolved | Đã đọc cả 4 đường ngày 2026-09-09; ghi lại để không đi tìm lại |
| A-06 | Cột "Người nộp/nhận" đặt ngay sau "Đối tượng nộp/nhận" trên cả 4 lưới | medium | no | Thứ tự cột hiển thị và thứ tự cột trong file Excel | resolved | ADR-06 (2026-09-09): liền kề cột đối tượng, cả trên lưới lẫn trong `EXPORT_COLUMNS` |
| A-07 | Không backfill dữ liệu cũ. Phiếu cũ chỉ điền `payer_name` sẽ có cột Đối tượng trống | high | no | Người dùng có thể coi ô trống là hồi quy | resolved | ADR-04 (2026-09-09): không migration, không backfill — ô trống là kết quả đúng |
| A-08 | Nhãn cột mới là "Người nộp/nhận" — một nhãn duy nhất cho lưới gộp cả thu lẫn chi, cùng lối đặt với nhãn "Đối tượng nộp/nhận" sẵn có và với `personLabel` trong `treasury-print-labels.ts` | medium | no | Chỉ là chuỗi hiển thị, sửa rẻ | resolved | ADR-06 (2026-09-09): nhãn "Người nộp/nhận", khớp `personLabel` của `treasury-print-labels.ts` |
| A-09 | Giữ nguyên khoá `counterparty` cho cột Đối tượng thay vì đổi tên thành `partnerName`; chỉ đổi *nghĩa* của nó thành "chỉ đối tác". Nhãn hiển thị không đổi nên bộ lọc người dùng đang gõ vẫn trỏ đúng cột họ thấy | medium | no | Nếu đổi tên thì phải sửa thêm 4 DTO + 4 kiểu FE + 2 adapter, không đổi hành vi | resolved | ADR-01 (2026-09-09): giữ khoá `counterparty`, đặt tên cột mới là `personName` theo `VoucherPartySnapshot` |
| A-10 | Sổ tiền gửi (`LedgerDepositPage.tsx:370-373`) dùng nhãn "Đối tượng" ngắn hơn 3 lưới kia; giữ nguyên nhãn cũ ở đó và thêm cột "Người nộp/nhận" bên cạnh | low | no | Nhãn không đồng bộ giữa 4 màn; thuần hiển thị | resolved | ADR-05 (2026-09-09): giữ nhãn "Đối tượng" cũ ở sổ tiền gửi, chỉ thêm cột mới bên cạnh |
