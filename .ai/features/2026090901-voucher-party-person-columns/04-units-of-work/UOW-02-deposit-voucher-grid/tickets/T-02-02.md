---
id: T-02-02
uow: UOW-02
title: Lưới Thu chi tiền gửi hiện hai cột Đối tượng và Người
layer: web
type: feature
estimate: 2h
status: done
depends_on: [T-02-01]
blocks: []
verifies: [AC-06, AC-07]
touches:
  - apps/backoffice-web/src/pages/treasury/deposit/receipts-expenses/receipt-deposit.types.ts
  - apps/backoffice-web/src/pages/treasury/deposit/receipts-expenses/receipt-deposit.utils.ts
  - apps/backoffice-web/src/pages/treasury/deposit/receipts-expenses/useReceiptDepositTableColumns.tsx
assumptions: [A-06, A-08]
---

# T-02-02 — Hai cột trên lưới Thu chi tiền gửi

## Implementation notes

- `receipt-deposit.types.ts`: thêm `personName: string` vào cả hai interface có
  `counterparty` (`:22` hàng thô, `:41` hàng lưới).
- `receipt-deposit.utils.ts:28`: thêm `personName: row.personName ?? ""` cạnh dòng
  `counterparty` sẵn có — giữ nguyên lối `?? ""` của file này.
- `useReceiptDepositTableColumns.tsx`: chèn `TableColumn` mới ngay sau `counterparty`
  (`:107-112`), nhãn "Người nộp/nhận", `width: 160`, cùng mặc định filter với cột bên cạnh.
- Lưới này có thêm cột "Số tài khoản" mà lưới tiền mặt không có; đừng chèn nhầm vị trí.

## Done when

- [x] `personName` khai bắt buộc trên cả hai interface; adapter điền nó
- [x] Cột mới nằm ngay sau `counterparty`, trước "Lý do"
- [x] `npx tsc --noEmit` và `pnpm --filter @erp/backoffice-web build` xanh

AC-06/AC-07 quan sát trên trình duyệt — bằng chứng G4 của UOW-02.
