---
feature: partner-catalog-api
blocking_open: 0
---

# Assumption register

Số liệu trong bảng đo trên `erp_dev_3008` (snapshot 30/08) ngày 2026-09-09:
62 nhóm hàng (56 có cha), 4.731 product, 41.718 item, 8.268 attribute definition,
28.839 option, 80.616 item attribute value.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|---|---|---|---|---|---|---|
| A-01 | Catalog trả về là của `actor.organizationId` suy từ API key; đối tác không cần và không được chọn tổ chức khác | high | no | Thêm tham số `organizationId` vào cả 3 hợp đồng + kiểm tra quyền chéo tổ chức | resolved | Đúng như giả định: mọi truy vấn lọc `actor.organizationId` suy từ key; không route nào nhận `organizationId`. e2e: mã chỉ có ở tổ chức khác → 404; tổ chức khác trùng mã không bao giờ được trả về |
| A-02 | **Đối tác chấp nhận nhận mã màu nội bộ.** `product_attribute_options.value_label` của `Color` là 35 mã 1–3 ký tự (`B`, `BA`, `BO`, `CC`, `D`, `Đ`, `N`, `ND`, `X`, `XD`, …), không có tên tiếng Việt và **không có mã hex**. Storefront tham chiếu vẽ 17 ô màu — không dựng được từ dữ liệu hiện có | high | yes | Nếu cần tên/hex: thêm bảng ánh xạ màu + migration + màn nhập liệu ⇒ thêm hẳn 1 UoW và đổi hình dạng `colors[]` trong cả 3 response | resolved | Akenzy chốt 2026-09-09: **trả mã thô**, đối tác tự giữ bảng tên+hex phía họ. `colors[]` là mảng string mã. Không thêm bảng, không migration |
| A-03 | Tên chiều thuộc tính là `"Color"` / `"Size"` (tiếng Anh). Không có bảng master: `product_attribute_definitions` là **theo từng product**, tên do người dùng tự nhập; trên erp_dev chỉ tồn tại đúng 2 tên này | medium | no | Tổ chức nhập `"Màu sắc"` sẽ khiến bộ lọc **câm lặng không khớp** (0 kết quả, không lỗi). Giảm nhẹ: so khớp không phân biệt hoa thường + danh sách bí danh trong hằng số | resolved | Đúng trên dữ liệu: erp_dev và erp_dev_3008 chỉ có `Color`/`Size`, và `item-crud.service.ts` ghi cứng hai tên này khi tạo; bí danh tiếng Việt giữ trong `partner-catalog.constants.ts`. Tên người dùng tự gõ khác các bí danh vẫn không khớp — chấp nhận |
| A-04 | **"Sắp xếp theo mức độ phổ biến" cần định nghĩa.** Không có cột đếm lượt bán trên `products`/`items`; nguồn duy nhất là gộp `invoice_items`, tốn kém và phụ thuộc khoảng thời gian | high | yes | Chọn "gộp invoice_items" ⇒ thêm 1 ticket query nặng + có thể cần bảng tổng hợp/index; chọn "bỏ" ⇒ hợp đồng chỉ còn 3 kiểu sắp xếp và đối tác phải sửa UI | resolved | Akenzy chốt 2026-09-09: **bỏ `popular`**. Hợp đồng chỉ nhận `newest` \| `price_asc` \| `price_desc`; giá trị khác trả 400 |
| A-05 | **Quyền riêng cho bề mặt đối tác.** 3 endpoint dùng quyền mới `partner.catalog.read`, không tái dùng `inventory.read` | high | yes | Nếu tái dùng `inventory.read`, key đối tác **vẫn gọi được** `/v2/inventory-items/search` và đọc `purchasePrice` ⇒ mục tiêu bảo mật của feature sụp. Nếu dùng quyền mới thì phải seed quyền + tạo role đối tác + gán khi cấp key | resolved | Akenzy chốt 2026-09-09: **quyền mới `partner.catalog.read`** + role "Đối tác" chỉ mang quyền này. Không gỡ `purchasePrice` khỏi DTO nội bộ (giữ backoffice/POS nguyên vẹn) |
| A-06 | Sản phẩm hiển thị cho đối tác khi có ít nhất một item `is_active = true`. `is_pos_visible` **không** dùng làm điều kiện hiển thị (đó là cờ của màn POS) | medium | no | Đối tác thấy hàng lẽ ra chỉ bán tại quầy, hoặc mất hàng lẽ ra bán online | resolved | Đúng như triển khai: chỉ `items.is_active`; `is_pos_visible` không tham gia (handler, e2e AC-18) |
| A-07 | Item có `selling_price = 0` (dữ liệu test, ví dụ `A02-D-39`) vẫn nằm trong kết quả; API không tự ẩn | medium | no | Storefront hiển thị "0 đ" | resolved | Giữ nguyên: item giá 0 vẫn được trả về, API không tự ẩn; chưa có yêu cầu nào đổi |
| A-08 | Nhóm hàng của một product suy từ item con (`items.category_id`), vì `products` không có cột nhóm. Lọc theo nhóm = "có ≥1 item thuộc nhánh nhóm đó"; trường `category` trong response lấy theo item có `code` nhỏ nhất | medium | no | Product có item rải nhiều nhóm sẽ hiện ở nhiều nhánh menu | resolved | Đúng như triển khai: lọc nhóm = có ≥1 item trong nhánh (AC-06, e2e); `categoryId` của dòng lấy theo item có mã nhỏ nhất; nhóm không thu hẹp dòng (A-19) |
| A-09 | Tồn kho trả về là **cờ boolean `inStock`**, không trả số lượng. Tính bằng `SUM(stock_balances.quantity) > 0` trên mọi `branch_id` mà key được phép (chốt của chủ sở hữu: phạm vi toàn tổ chức) | high | no | Đối tác cần số lượng thật để hiện "còn 3 đôi" ⇒ thêm 1 trường và một quyết định về lộ dữ liệu thương mại | resolved | Đúng, đổi cách tính: vẫn là cờ boolean, không trả số lượng, tồn > 0 trên các chi nhánh key được thấy. Từ T-06-03 lấy từ một `LEFT JOIN` trên tập item còn tồn (ADR-08); dữ liệu thật: true + false = tổng, 0 lỗi |
| A-10 | Đối tác phân trang bằng `page`/`limit` như mọi endpoint v2 khác (`{ data, total, page, limit }`), không cần cursor | high | no | Danh mục 4.731 product không đủ lớn để offset thành vấn đề; nếu sai thì đổi hợp đồng phân trang | resolved | Đúng: `page`/`limit` với `{ data, total, page, limit }`; trang 100 trên MT p95 82 ms, không cần cursor (`07-performance.md`) |
| A-11 | `items.variant_label` (dạng `"38 · BA"`) dùng trực tiếp làm nhãn biến thể, không ghép lại từ option | high | no | Nhãn lệch với thuộc tính nếu dữ liệu cũ không đồng bộ; giảm nhẹ bằng cách trả kèm cả `attributes[]` có cấu trúc | resolved | Đúng: `variantLabel` trả nguyên trạng, kèm `attributes` có cấu trúc (AC-15, e2e) |
| A-12 | Bản đồ kiến trúc ký 2026-08-03 thiếu `modules/api-key` và `modules/mobile`. Đã bổ sung 2 dòng vào bảng module trong Phase 0 thay vì sinh lại (giữ phần người viết tay) | high | no | `touches` trỏ vào module không có trong bản đồ ⇒ `lint-touches` báo lỗi | resolved | Bổ sung 2 dòng vào `.ai/architecture.md` ngày 2026-09-09; không đụng các mục viết tay |
| A-13 | Dữ liệu option có rác: `Size` chứa `35,` và `36,`; `Color` chứa `D,`. API trả nguyên trạng, không dọn | high | no | Danh sách facet của đối tác có giá trị rác; dọn dữ liệu là việc khác | resolved | Đúng như giả định: giá trị rác (`35,`, `36,`, `D,`) trả nguyên trạng; dọn dữ liệu là việc khác |
| A-14 | Phản ánh 2026-09-13 "lọc màu/size trả sai hoặc 0 sản phẩm" là hệ quả của A-15 (dòng mô tả mọi biến thể), không phải lỗi chọn product. Không tái hiện được lỗi SQL: `colors=['BA']` + `sizes=['39']` ra 52–54 sản phẩm trên erp_dev và erp_dev_3008 (SQL chạy lại 2026-09-13; qua HTTP ở T-03-03, mã module không đổi từ đó), mọi tên chiều đúng là `Color`/`Size` (`item-crud.service.ts:841` ghi cứng hai tên này), 0 biến thể có thuộc tính mà không nhận ra chiều. Chưa có request body thật của đối tác | medium | no | Nếu lỗi nằm ở chỗ khác — mã chữ thường `ba`, `sizes` gửi dạng số (400), tên chiều khác trên môi trường của đối tác — UOW-06 không sửa được nó; thêm 1 ticket khi có payload, không làm lại ticket nào | resolved | Không tái hiện được lỗi chọn sai sản phẩm trên erp_dev/erp_dev_3008. Phần phản ánh giải thích được bằng việc dòng mô tả mọi biến thể — đã sửa theo A-15, kiểm bằng e2e 17/17 và cURL 37/37. Chưa nhận payload của đối tác; nếu còn báo sai, mở feature mới kèm request body |
| A-15 | Khi có bộ lọc mức biến thể (`colors`, `sizes`, `priceFrom`, `priceTo`, `inStock`), `priceMin`/`priceMax`/`colors`/`sizes`/`inStock` của dòng tính trên đúng các biến thể khớp; sort giá theo giá đã thu hẹp | high | yes | Giữ gộp trên mọi biến thể ⇒ lọc BA vẫn hiện D, BA/39 hết hàng vẫn báo còn (47/106 trên erp_dev_3008) | resolved | Akenzy chốt 2026-09-13: thu hẹp về biến thể khớp. ADR-07 |
| A-16 | Bộ lọc `inStock` khớp trên CÙNG biến thể với màu/size/giá: `true` = có biến thể khớp mọi bộ lọc còn tồn; `false` = không biến thể khớp nào còn tồn | high | yes | Đánh giá trên cả product ⇒ "BA size 39 còn hàng" trả product mà đôi BA/39 đã hết | resolved | Akenzy chốt 2026-09-13: cùng biến thể. ADR-08 |
| A-17 | Chi tiết tra theo `products.code`, khớp chính xác, thay hẳn `products.id`; không nhận UUID, không nhận SKU biến thể. Product không có mã không mở được chi tiết (1 product bán được trên erp_dev_3008, 0 trên erp_dev) | high | yes | Nhận thêm SKU biến thể ⇒ va chạm mã (1 SKU biến thể trùng mã một product khác trên erp_dev_3008); nhận UUID ⇒ hai khoá cho một route | resolved | Akenzy chốt 2026-09-13: chỉ `products.code`. Route `:productId` chưa từng live nên không phá hợp đồng. ADR-09 |
| A-18 | Ghi chú "filter size or color not working" đặt dưới mục API Detail nói về endpoint danh sách: route chi tiết chưa đăng ký trên `main` (T-04-03 todo, không commit nào trên mọi nhánh nối nó), nên không thể quan sát lỗi lọc ở đó. Endpoint chi tiết không nhận bộ lọc | high | no | Nếu đối tác muốn chi tiết nhận `color`/`size` để thu hẹp `variants` ⇒ thêm tham số + 1 ticket ở UOW-04 | resolved | Không có thông tin ngược lại: endpoint chi tiết không nhận bộ lọc (docs §7), không yêu cầu thêm sau khi route theo mã chạy |
| A-19 | `keyword` và `categoryId` là bộ lọc mức product: chọn product, không thu hẹp dòng | medium | no | Product có biến thể rải nhiều nhóm sẽ hiện giá/màu của biến thể ngoài nhóm đang lọc; sửa = thêm vị từ nhóm vào tập "khớp" ở T-06-01 | resolved | Đúng như triển khai và có test: keyword/categoryId chọn product, không thu hẹp dòng (e2e 'lets keyword and categoryId select the product without narrowing its row'; unit guard) |
| A-20 | `products.code` an toàn làm path segment: 0/4.731 mã active trên erp_dev_3008 (0/2.322 trên erp_dev) chứa ký tự ngoài `[A-Za-z0-9._~-]`; 0 cặp trùng khi so không phân biệt hoa thường; `UQ_products_org_code` bảo đảm duy nhất trong tổ chức | high | no | Mã chứa `/` hoặc khoảng trắng ⇒ đối tác phải percent-encode; thêm ghi chú docs, không đổi code | resolved | Đúng trên dữ liệu: 0 mã active cần percent-encode; docs §5 ghi cách xử lý nếu về sau có |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|---|---|---|---|
| A-90 | Có sẵn bảng ảnh sản phẩm để trả `images[]` | Không có cột ảnh, không có bảng media, không có module upload trong toàn repo. Tiền lệ duy nhất là `imageUrl: null` viết cứng ở `pos-catalog-product.response.dto.ts:23-27` | `images` luôn trả `[]`; ghi vào Out of scope của `00-intent.md` theo chỉ đạo của chủ sở hữu |
| A-91 | Giá bán nằm trên `products` nên một product có một giá | Giá nằm trên `items.selling_price`; một product có nhiều item giá khác nhau | Response danh sách phải trả `priceMin`/`priceMax` (theo tiền lệ `minPrice`/`maxPrice` của POS), không phải một số |
| A-92 | Nhóm hàng gắn trên `products` | Gắn trên `items.category_id`; `products` không có cột nhóm nào | Sinh ra A-08 và khiến lọc theo nhóm phải đi qua item |
| A-93 | Có thể tái dùng `POST /v2/inventory-items/search` cho đối tác, chỉ cần thêm bộ lọc | `InventoryItemGroupRowDto:141` trả `purchasePrice` (giá vốn trung bình) và DTO này dùng chung với backoffice | Phải dựng bề mặt riêng — xem ADR-01 |
