---
id: T-01-01
uow: UOW-01
title: Scaffold module partner-catalog + wiring CQRS
layer: api
type: chore
estimate: 2h
status: done
depends_on: []
blocks: [T-01-03, T-01-04, T-02-01]
verifies: []
tests:
  - apps/api/src/modules/partner-catalog/partner-catalog.module.spec.ts
touches:
  - apps/api/src/modules/partner-catalog/partner-catalog.module.ts   # new; theo khuôn modules/<feature>/ của repo
  - apps/api/src/modules/partner-catalog/partner-catalog.constants.ts   # new
  - apps/api/src/app.module.ts
assumptions: [A-01]
---

# T-01-01 — Scaffold module partner-catalog + wiring CQRS

## Context

Module chỉ-đọc: không entity, không migration. Cố ý làm trước và làm mỏng để 4 slice sau
cắm vào mà không tranh file.

## Implementation notes

- `@Module({ imports: [CqrsModule, TypeOrmModule.forFeature([...])] })`. Entity cần đọc:
  `ItemCategoryEntity`, `ProductEntity`, `ItemEntity`, `StockBalanceEntity`,
  `ProductAttributeDefinitionEntity`, `ProductAttributeOptionEntity`, `ItemAttributeValueEntity`.
- `partner-catalog.constants.ts` giữ `PARTNER_CATALOG_PERMISSION = 'partner.catalog.read'` và tên
  hai chiều thuộc tính (`ATTRIBUTE_COLOR = 'Color'`, `ATTRIBUTE_SIZE = 'Size'`). A-03: tên do người
  dùng nhập, không có bảng master — nên so khớp không phân biệt hoa thường và để một chỗ, sau này
  thêm bí danh không phải sửa rải rác.
- Đăng ký `PartnerCatalogModule` trong `app.module.ts`. KHÔNG thêm `APP_GUARD` nào.
- Chưa có controller ở ticket này; module rỗng vẫn phải khởi động được.

## Done when

- [x] `pnpm --filter @erp/api build` xanh
- [x] Test khởi tạo module đi qua (module compile được trong `Test.createTestingModule`)
- [x] Không `@Public()` ở bất kỳ file nào trong module
- [x] Không import gì từ `modules/inventory` ngoài entity
