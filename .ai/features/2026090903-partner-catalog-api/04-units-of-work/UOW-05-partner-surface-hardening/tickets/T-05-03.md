---
id: T-05-03
uow: UOW-05
title: Sinh lại OpenAPI client và commit snapshot
layer: shared
type: chore
estimate: 3h
status: todo
depends_on: [T-04-03]
blocks: []
verifies: []
tests: []
touches:
  - packages/api-client/src/generated/schema.ts
  - packages/api-client/openapi.snapshot.json
assumptions: []
---

# T-05-03 — Sinh lại OpenAPI client và commit snapshot

## Context

Quy ước repo: sửa endpoint xong thì chạy `pnpm openapi:generate` và commit cả hai file sinh
ra. Đây cũng chính là tài liệu đối tác đọc.

## Implementation notes

- Bật API rồi chạy `pnpm openapi:generate`. **Không** sửa tay `schema.ts`.
- **Bẫy đã dính:** phải sinh từ một build riêng (ví dụ cổng `:4100`), vì `:4000` có thể đang được phục
  vụ bởi checkout khác trỏ DB khác — khi đó snapshot sinh ra từ mã nguồn không phải mã mình vừa sửa.
- Kiểm trong `schema.ts`: cả 3 endpoint có kiểu response cụ thể, không phải `unknown`. Đây đúng chỗ
  nhiều controller v2 cũ trong repo bị rỗng schema.
- Kiểm `packages/api-client/openapi.snapshot.json` chỉ chứa thay đổi của 3 endpoint mới — diff phình ra chỗ khác nghĩa là
  snapshot đang sinh từ nhánh hoặc DB sai.

## Done when

- [ ] `pnpm openapi:generate` chạy từ build riêng; ghi rõ cổng đã dùng
- [ ] 3 endpoint xuất hiện trong `schema.ts` với kiểu response cụ thể, không `unknown`
- [ ] Diff của `openapi.snapshot.json` chỉ đụng 3 endpoint mới
- [ ] `schema.ts` không bị sửa tay
