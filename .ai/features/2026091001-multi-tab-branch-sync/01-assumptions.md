# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Sự kiện `storage` **không** bắn ở chính tab đã ghi localStorage, nên tab vừa bấm đổi chi nhánh không tự bật dialog của mình | high | no | Tab nào đổi chi nhánh cũng tự hỏi lại chính nó → mỗi lần đổi là hai bước thay vì một | resolved | Đúng theo đặc tả HTML: `storage` chỉ bắn ở **document khác** cùng origin. Vẫn thêm chốt thứ hai ở tầng thiết kế: tab đang đổi tự cập nhật baseline trước khi ghi (ADR-03), nên kể cả trình duyệt nào bắn sai thì cũng không hiện dialog |
| A-02 | "Lệch" phải đo bằng **thay đổi sau khi tab khởi tạo**, không phải "khác với zustand đang có". Baseline chốt lúc tab tải xong; chỉ thay đổi xảy ra *sau* đó mới là lệch | high | **yes** | Đo sai chiều là hỏng cả hai đầu: (a) tab mới mở/đăng nhập xong luôn bật dialog giả vì store khởi tạo trước khi có phiên; (b) ca hợp lệ ở `PosLocationIndicator.tsx:38-57` — đăng nhập mới, JWT mang chi nhánh mặc định khác chi nhánh đã persist — bị dialog chặn, người dùng phải bấm mới bán được hàng | resolved | Chốt 2026-09-10: detector giữ `baselineRef` chốt tại lần chạy đầu (backoffice: `localStorage.active_branch_id`; POS: `branchId` trong `pos_access_token`), rồi so **giá trị hiện tại vs baseline**. Bằng nhau ⇒ không lệch, bất kể zustand đang hiện gì. Ca (a) và (b) đều rơi vào "bằng baseline" nên đi đúng đường cũ, không đụng dialog |
| A-03 | Backoffice phải lấy **`localStorage.active_branch_id`** làm "chi nhánh mới", không đọc được từ JWT | high | no | Chọn nhầm nguồn thì detector không bao giờ thấy lệch ở backoffice | resolved | Access token của backoffice nằm **trong bộ nhớ** (`lib/access-token.ts`), và jti của TAB A đã bị `switchBranch` thu hồi (`auth.service.ts:226`) nên nó cũng không còn dùng được. Tín hiệu dùng chung duy nhất là `active_branch_id`, được `persistSwitchBranchResponse` ghi ngay khi đổi (`auth-storage.ts:47`) |
| A-04 | POS phải lấy `branchId` trong `pos_access_token` (qua `parseAccessTokenPayload`) làm "chi nhánh mới" — không lấy khoá persist `pos-branch` | high | no | Lấy `pos-branch` thì đo được ý muốn của tab kia chứ không phải chi nhánh mà server thực sự đang trả dữ liệu | resolved | Token POS nằm ở `localStorage.pos_access_token`, **dùng chung mọi tab** (`pos-web/src/lib/common/api-axios.ts:21`), và `@Actor` ưu tiên `jwt` (`actor-context.decorator.ts:33`) — nên JWT chính là chi nhánh của dữ liệu. `parseAccessTokenPayload` đã có sẵn và `PosLocationIndicator` đang dùng đúng cách đó |
| A-05 | Tên chi nhánh hiển thị trong dialog lấy từ `useMyBranches()` (backoffice) / `useMyBranchesQuery()` (POS); nếu danh sách chưa tải xong thì **hoãn** hiện dialog chứ không hiện id thô | medium | no | Dialog hiện `9f3c…-uuid` thay vì "Chi nhánh Hồ Chí Minh" — người dùng không biết mình đang chọn gì | resolved | **Đúng, và đã cài đúng như vậy.** `BranchDriftDialog.tsx:38` và `PosBranchDriftDialog.tsx:32` cùng `return null` khi chưa giải được tên từ `useMyBranches()` / `useMyBranchesQuery()`. Danh sách tải xong thì lần render sau mở dialog, nên không có trạng thái nào hiện uuid. Akenzy xác nhận trên máy local 2026-09-10 |
| A-06 | Ở backoffice, `window.location.reload()` là cách áp chi nhánh mới — giống hệt `moveTo` đang làm (`BranchSelector.tsx:52`) — chứ không đi invalidate từng query | high | no | Invalidate thủ công sót một query → vẫn còn dữ liệu chi nhánh cũ trên màn hình, đúng cái bug đang sửa | resolved | **Giữ nguyên reload ở backoffice, và bỏ luôn cả lần ghi thừa.** `active_branch_id` đã là chi nhánh mới — chính là chỗ detector đọc ra — nên nhánh "dùng chi nhánh mới" chỉ gọi `window.location.reload()` (`BranchDriftDialog.tsx:47`), không `setActiveBranch` lại. POS thì ngược lại, không reload: nó `setBranch` + `resetCheckoutSelections()` + `queryClient.clear()`, vì `queryClient.clear()` ở POS đã là đường dọn cache sẵn có của `handleChange` |
| A-07 | `PosDialog` cần thêm **một prop opt-in** để tắt nút X và chặn Esc/click nền; mặc định giữ nguyên nên mọi nơi đang dùng không đổi hành vi | high | no | Không có prop thì dialog POS vẫn hiện nút X; nếu để `onClose` rỗng thì nút X thành nút chết — tệ hơn là không có | resolved | Đọc `PosDialog.tsx:84-91`: `<Dialog open onOpenChange={(next) => !next && onClose()}>` và `<DialogContent …>` **không** truyền `showCloseButton`, nên mặc định `true` (`packages/ui/src/components/dialog.tsx:41`) → có nút X. Thêm `dismissible?: boolean` (mặc định `true`) là thay đổi nhỏ nhất và cộng thêm, không phá 20+ chỗ dùng |
| A-08 | Backoffice **không cần sửa `@erp/ui`** để có dialog không đóng được | high | no | Sửa package dùng chung cho một màn hình → phải build lại `@erp/ui` và ảnh hưởng cả POS | resolved | `DialogContent` đã có `showCloseButton` (`packages/ui/src/components/dialog.tsx:33`) và spread `...props` xuống `DialogPrimitive.Content` (`:57`), nên `onEscapeKeyDown`/`onInteractOutside` của Radix truyền thẳng được. Bỏ `onOpenChange` là hết đường đóng |
| A-09 | Không có test runner ở frontend — nghiệm thu bằng `tsc --noEmit`, `build` và demo trên trình duyệt thật | high | no | Viết `*.test.ts` rồi tưởng đã có bằng chứng, trong khi không có gì chạy nó | resolved | Kiểm 2026-09-10: `vitest` không có trong `node_modules/.bin` ở root lẫn `apps/pos-web`; `"test": "echo test"` ở cả hai `package.json`. Các file `*.test.ts` sẵn có (`http.test.ts`, `useImportableTransferOrderCount.test.ts`) hiện không chạy được |
| A-10 | Ở chế độ "Chuỗi cửa hàng" (`isChain`) vẫn phát hiện lệch và vẫn bật dialog như thường | medium | no | Người dùng đang xem báo cáo chuỗi bị hỏi một câu có vẻ không liên quan; hoặc ngược lại, bỏ qua chế độ chuỗi thì báo cáo chuỗi vẫn ngầm đổi phạm vi chi nhánh mà không ai biết | resolved | **Đúng: chế độ chuỗi vẫn bật dialog.** `useBranchDrift` không đọc `isChain` ở bất kỳ đâu — nó chỉ so `active_branch_id` với baseline. Đây là chủ ý: báo cáo chuỗi vẫn gửi `X-Branch-Id` và vẫn chạy dưới JWT của phiên, nên chi nhánh của phiên đổi là phạm vi dữ liệu đổi, bất kể vỏ đang hiện "Chuỗi cửa hàng" |
| A-11 | Ở POS, chọn "Dùng chi nhánh mới" **không** cần gọi `switch-branch`: token dùng chung đã mang chi nhánh mới rồi, chỉ cần `setBranch` + `resetCheckoutSelections()` + `queryClient.clear()` | medium | no | Gọi thừa một lần `switch-branch` sẽ **xoay jti lần nữa** và làm chính tab vừa đổi (TAB B) rơi vào lệch → ping-pong không do người dùng gây ra | resolved | **Đúng, và đây là lý do nhánh "dùng chi nhánh mới" không gọi `switch-branch` ở cả hai app.** Token dùng chung đã mang chi nhánh mới; gọi thêm sẽ xoay jti lần nữa và đẩy chính tab vừa đổi vào lệch. Akenzy đếm request trên Network 2026-09-10: nhánh này 0 request, nhánh "quay về" đúng 1 |
| A-12 | Đăng xuất ở tab khác (`refresh_token`/`pos_refresh_token` bị xoá) phải đi đường phiên hết hạn sẵn có, **không** bật dialog chi nhánh | high | no | Người dùng đã đăng xuất lại bị hỏi chọn chi nhánh; hoặc dialog che mất màn đăng nhập | resolved | **Đúng, và cài bằng cách tắt hẳn detector chứ không lọc ở tầng dialog.** `readSessionBranch()` trả `null` khi `hasRefreshToken()` false (`useBranchDrift.ts:36`) / khi không còn `pos_refresh_token` (`use-branch-drift.ts:41`), nên `drifted` không bao giờ true sau khi tab khác đăng xuất. Tab đi tiếp đường 401 → `/login` sẵn có |

## Cách gỡ A-05, A-10, A-11, A-12

Tất cả đều là hành vi quan sát được trên trình duyệt, không phải câu hỏi kiến trúc. Chúng
được gỡ ở Demo script của UOW-01/UOW-02 (mục nào cũng có bước tương ứng) chứ không cần
hỏi thêm ai.
