---
feature: report-multi-location-column
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| A-01 | Tập vị trí của một mặt hàng = **hợp** của kệ ưu tiên (`item_storage_locations`) và mọi kệ đang có tồn (`stock_balances.quantity > 0`), chứ không phải "ưu tiên thắng trước, hết mới xét tồn" như hiện nay | high | yes | Toàn bộ nội dung cột ở 4 báo cáo; viết lại `resolveItemWarehouseLocations` và mọi test của nó | confirmed | akenzy chốt 2026-09-10: "Hợp cả hai: kệ ưu tiên + mọi kệ đang có tồn" |
| A-02 | Không giới hạn số cặp kho–vị trí trong ô; xử lý chuỗi dài bằng cách nới độ rộng cột và dựa vào tooltip `title=` đã có (`ReportPageTableView.tsx:524-528`) | high | yes | Nếu sai thì phải thêm logic cắt "+N" và tách riêng đường xuất Excel để không cắt dữ liệu | confirmed | akenzy chốt 2026-09-10: "Không giới hạn, chỉ nới rộng cột" |
| A-03 | Định dạng ô: cột "Mã vị trí" chỉ chứa **mã vị trí**; cột "Tên vị trí" chỉ chứa **tên vị trí**, không tiền tố tên kho; nhiều kệ nối bằng `", "` | high | yes | Định dạng hiển thị của 4 báo cáo và mọi assert trong test | confirmed | akenzy chốt 2026-09-10 là `Mã kho-Mã vị trí` cho cả hai cột. **Đảo lần một 2026-09-12**: bỏ tiền tố ở ô mã, tên kho lui về ô tên (ADR-05). **Đảo lần hai 2026-09-12** sau khi xem lại Tổng hợp nhập xuất tồn kho: *"Hiện tại Tên vị trí đang show Kho + Vị trí, nó chỉ nên show vị trí 1; vị trí 2, ..."* — tên kho ra khỏi nốt ô tên. Ví dụ mới: `A101, A201` và `Kệ A101, Kệ A201`. Không cột nào của 4 báo cáo còn nêu kho — xem ADR-06 |
| A-04 | Sửa cả 4 báo cáo dùng chung `resolveItemWarehouseLocations`, không chỉ Tổng hợp nhập xuất tồn kho | high | yes | Phạm vi feature: 1 UoW hay 2; số ticket; số báo cáo phải hồi quy | confirmed | akenzy chốt 2026-09-10: "Cả 4 báo cáo dùng chung hàm này" |
| A-05 | Không thêm bộ lọc/tìm kiếm theo vị trí; chỉ cần dropdown "Kho" hiện có tiếp tục trả đúng tập dòng | high | yes | Nếu sai thì phải đẩy việc tính vị trí xuống SQL theo mẫu `temp-warehouse-report.service.ts:502-561`, đổi hẳn kiến trúc lời giải | confirmed | akenzy chốt 2026-09-10: "Giữ dropdown lọc Kho chạy đúng như cũ" |
| A-06 | `storages.code` luôn có giá trị trên dữ liệu thật, nên tiền tố `Mã kho-` không bao giờ rỗng | high | no | Ô hiện `-A101` trông như lỗi. Phòng thủ: `code` rỗng thì bỏ luôn tiền tố và dấu gạch | resolved | Đã kiểm mã nguồn: cả hai đường tạo đều sinh mã `WHxxxxxx` (`inventory-location.service.ts:454`, `storage-crud.service.ts:107-124`) và `1784800000001-BackfillStorageCode.ts` đã lấp mọi dòng cũ. **Hết hiệu lực 2026-09-12 (ADR-06):** không ô nào còn tiền tố kho — `storageCode` chỉ còn là khoá phá hoà trong sắp xếp, `storageName` không còn được hiển thị. Nhánh phòng thủ đã bỏ ở T-01-05 và không cần dựng lại |
| A-07 | Thứ tự các kệ trong ô là tất định: sắp theo **mã vị trí**, rồi tên vị trí, rồi mã kho | medium | no | Ô đổi thứ tự giữa hai lần tải cùng bộ lọc; test snapshot chập chờn | resolved | akenzy chốt 2026-09-10 là `(mã kho, mã vị trí)`. **Đổi 2026-09-12 (ADR-06)** khi kho ra khỏi cả hai ô: khoá chính phải là thứ người dùng nhìn thấy, nếu không đúng dòng QA báo sẽ đọc ra `S01.01, A01.01`. Mã kho giữ lại làm khoá phá hoà cuối để hai kệ khác kho trùng cả mã lẫn tên vẫn tất định (AC-14) |
| A-08 | `showroomFallback` giữ nguyên ngữ nghĩa hiện tại: chỉ dùng kệ showroom khi mặt hàng **không** nằm trên bất kỳ kệ kho lưu trữ nào — không trộn showroom vào cùng danh sách với kệ kho | medium | no | Cột của Tổng hợp nhập xuất tồn kho lẫn kệ showroom vào, mâu thuẫn với lý do nêu ở `item-warehouse-location.util.ts:39-45` | resolved | **Đúng, giữ nguyên.** Test `never mixes in the showroom shelf when the item already has a warehouse shelf (A-08)` trong `item-warehouse-location.util.spec.ts`: mặt hàng đã có kệ kho lưu trữ thì vòng showroom không chạy. `resolveItemWarehouseLocations` chỉ gọi vòng hai cho tập `missing`. Không đổi dòng nào vì giả định này. |
| A-09 | Ba báo cáo doanh thu/lợi nhuận tiếp tục **không** truyền `showroomFallback`; chúng chỉ thừa hưởng việc bỏ giới hạn "một kệ" | high | no | Cột vị trí của báo cáo doanh thu hiện kệ showroom — đúng thứ mà `:39-45` cố tình loại | resolved | **Đúng.** Ba báo cáo không truyền cờ; test AC-08 của cả ba (`leaves the location empty for an item stocked only on the showroom`) xanh sau mọi lần đảo, kể cả sau khi thêm cột "Kho" — ô kho của ba báo cáo này cũng để trống, không lấy tên showroom. |
| A-10 | `profit-by-item.report.ts:222-229` chỉ lấy `loc.code`, nên nó tự nhận chuỗi đã nối mà không cần sửa gì thêm ngoài kiểu dữ liệu | medium | no | Báo cáo lợi nhuận hiện `[object Object]` hoặc chỉ một vị trí trong khi 3 báo cáo kia đã đủ | resolved | **Đúng lúc viết, đã hết đúng.** `profit-by-item.report.ts` khi đó `.map(([itemId, loc]) => [itemId, loc.code])` nên chỉ cần đúng kiểu chuỗi. T-03-03 phải bỏ hẳn `.map` đó để lấy được `storage`, nay giữ nguyên `Map<string, ItemWarehouseLocation>` và đọc `.code`/`.storage` tại chỗ gán — ngắn hơn bản cũ. Ngoài ra phát hiện `profit-by-item.aggregator.ts` có một `switch` chiếu ô riêng: thiếu `case` thì trả `null` chứ không đỏ biên dịch, tức A-10 che mất một chỗ phải sửa. |
| A-11 | Việc tra vị trí vẫn ở phạm vi **một trang** (`pageSize` mặc định 20, `stock-summary.report.ts:236`), nên bỏ chốt "first wins" không đổi bậc chi phí | high | no | Nếu một trang có mặt hàng nằm trên hàng trăm kệ thì 4 truy vấn tra vị trí phình ra; đường xuất Excel chịu ảnh hưởng nặng hơn vì chạy nhiều trang | resolved | **Đúng.** Test `resolves a page in exactly four repository queries when no preferred shelf needs an untracked check (A-11)` chốt con số 4; phạm vi vẫn là `result.data` của một trang. Thêm ô `storage` ở T-03-01 không thêm truy vấn nào — tên kho lấy từ `storageById` đã nạp sẵn. |
| A-12 | Cặp (mặt hàng, vị trí) bị đánh dấu "Ngừng theo dõi" (`stock_balances.is_tracked = false`) vẫn phải bị loại khỏi danh sách — loại đúng cặp đó, không loại cả mặt hàng | high | no | Lỗi đã sửa ở `2026090401-untracked-location-hidden` quay lại dưới dạng khác | resolved | **Đúng, quy tắc giữ nguyên ở dạng nhiều cặp.** Test `drops only the untracked pair, keeping the item on its other shelf (AC-04)`: cặp bị Ngừng theo dõi biến mất, mặt hàng vẫn trên báo cáo với kệ còn lại. Áp cho cả ba ô — ô "Kho" cũng mất tên kho của kệ bị ngừng nếu mặt hàng không còn kệ nào khác trong kho đó. |
| A-16 | Độ rộng cột vị trí của ba báo cáo doanh thu/lợi nhuận đến từ hai nơi độc lập: bảng dùng chung `REPORT_COLUMN_WIDTHS` (`report-column-widths.ts:37-39` — `location: 160`, `locationCode: 110`, `locationName: 160`) và `tableConfig.width` trong registry frontend của từng báo cáo. **Đã sửa sau T-02-03:** trên màn hình, bảng lấy độ rộng từ catalog cột backend (`ReportTableConfigSync.tsx:98-109` → `mapHeadersToTableConfig`, `invoice-report.api.ts:109`); registry frontend chỉ dùng khi lời gọi catalog lỗi hoặc rỗng — xem A-17. Vẫn nới cả hai nơi để đường fallback khớp, và việc nới chỉ chạm đúng ba báo cáo này | high | no | Nới một nơi thì màn hình và file xuất lệch nhau — hiện đã lệch sẵn: backend `locationName`/`location` 160 so với frontend 110; hoặc nới lan sang báo cáo khác | resolved | **Đúng, và đã dùng đúng như vậy ba lần.** Độ rộng đến từ hai nơi độc lập (`REPORT_COLUMN_WIDTHS` + `tableConfig.width` của registry FE); T-02-03, T-03-03 và T-03-04 đều sửa/giữ đồng thời cả hai. Trên màn hình bảng lấy từ catalog BE, registry chỉ là fallback (A-17) — nhưng vẫn phải khớp, nếu không hai đường đi cho hai bộ cột khác nhau. |
| A-18 | Ô "Mã vị trí" khử trùng theo mã — hai kệ khác kho trùng mã gộp còn **một** mục. Ô "Tên vị trí" **không** khử trùng: giữ đúng một mục cho mỗi kệ, kể cả khi hai kệ trùng tên | high | yes | Ô "Mã vị trí" hiện `999, 999` vô nghĩa; hoặc ngược lại, ô tên gộp mất dấu hiệu mặt hàng đang nằm trên nhiều kệ | confirmed | akenzy chốt 2026-09-12 cho ô mã: "hai kệ khác kho nhưng trùng mã thì gộp còn một". **Chốt lại 2026-09-12 cho ô tên** khi A-03 đảo lần hai: Akenzy được hỏi trực tiếp và chọn "Giữ đủ hai mục" — chấp nhận chuỗi `999, 999` để không mất số kệ thật. Hai ô nay lệch nhau về số mục, đó là chủ ý |
| A-19 | Kho quay lại bằng **một** cột "Kho" riêng, nội dung là **tên kho đã khử trùng** (`HANG SUA 2026, KHO SG`), thêm cho cả 4 báo cáo | high | yes | Số cột của 4 báo cáo; kiểu trả về của hàm dùng chung; 4 khai báo cột BE, bảng nhãn BE và 3 registry FE | confirmed | Akenzy chốt 2026-09-12 khi được hỏi trực tiếp. Loại **mã kho** (dữ liệu thật là `WHxxxxxx`, không đọc được) và loại **hai cột Mã kho + Tên kho** (4 cột cho một thông tin). Khử trùng vì một mặt hàng thường nằm nhiều kệ trong cùng một kho |
| A-20 | Độ rộng cột "Kho" mới và độ rộng thu lại của "Tên vị trí" phải chốt bằng **phép đo thật** ở 1440x900 trên `erp_dev_3008`, không ước lượng | high | yes | Cột bị CSS cắt (AC-12, AC-13) hoặc chiếm chỗ thừa của các cột số | confirmed | Akenzy chọn "Đo thật trên browser" 2026-09-12. Có tiền lệ: T-01-04 ước lượng `220` là đủ, phép đo bác bỏ (271px trong ô 259px) và phải nới lên `320` |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
| --- | --- | --- | --- |
| A-13 | Cột "Vị trí" do SQL của báo cáo sinh ra, nên sửa bằng `string_agg` trong CTE | Engine trả `NULL::text` cho cả hai cột ở grain `item` (`stock-period.service.ts:222-224`); giá trị được gắn bằng TypeScript sau khi query (`stock-summary.report.ts:239-248`) | Lời giải nằm ở `item-warehouse-location.util.ts`, không phải ở `stock-period.service.ts`. Không có ticket migration/SQL nào trong feature này |
| A-14 | Chỉ báo cáo Tổng hợp nhập xuất tồn kho bị ảnh hưởng | Hàm dùng chung có 4 nơi gọi, cả 4 đều hiển thị một vị trí duy nhất | Phạm vi mở rộng thành 4 báo cáo (A-04); thêm ticket hồi quy cho 3 báo cáo doanh thu/lợi nhuận |
| A-15 | `stock-quantity-detail` và `stock-summary-by-store` cũng cần sửa vì cùng họ "nhập xuất tồn" | Hai báo cáo này không có cột vị trí nào trong `COLUMNS` (`:44-77`, `:42-63`) và không gọi hàm dùng chung | Loại khỏi phạm vi, ghi rõ ở `00-intent.md` § Out of scope |
| A-17 | Registry frontend (`tableConfig.width`) là thứ quyết định độ rộng cột trên màn hình, còn bảng dùng chung backend chỉ phục vụ phía server/xuất file | Khi lời gọi catalog cột thành công — trường hợp bình thường — `ReportTableConfigSync.tsx` ghi cấu hình bảng từ header backend qua `mapHeadersToTableConfig` (`width: h.width ?? defaultReportColumnWidth(dataType)`); registry chỉ là fallback khi lời gọi lỗi hoặc trả rỗng | Không đổi mã: T-02-03 vẫn nới cả hai nơi. Đổi lý do: thay đổi `REPORT_COLUMN_WIDTHS` mới là thứ sửa AC-13 trên màn hình; sửa registry chỉ giữ đường fallback khớp. Implementer của T-02-03 phát hiện, đã kiểm lại mã ngày 2026-09-10 |
