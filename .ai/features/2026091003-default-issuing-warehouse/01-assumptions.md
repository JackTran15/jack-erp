---
feature: default-issuing-warehouse
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| A-01 | Dropdown chọn kho ở POS Kho tạm vẫn liệt kê **đủ** kho lưu trữ đang hoạt động của chi nhánh; chỉ đổi thứ tự để kho xuất mặc định lên đầu | high | yes | Nếu sai (rút xuống 2 kho) thì đổi hẳn hợp đồng của endpoint và chặn nghiệp vụ chuyển tới kho thứ ba | confirmed | akenzy chốt 2026-09-10: "Tất cả kho lưu trữ đang hoạt động, xếp kho xuất mặc định lên đầu" |
| A-02 | Một kho được phép vừa là kho xuất mặc định vừa là kho nhập mặc định; hai cờ độc lập, mỗi cờ một partial unique index riêng | high | yes | Nếu sai thì phải thêm validate chéo ở cả hai command và chi nhánh một kho không đặt được cả hai | confirmed | akenzy chốt 2026-09-10: "Được — hai cờ độc lập" |
| A-03 | Migration backfill kho xuất mặc định dùng **cùng kỹ thuật** với `1784500000000-AddStorageDefaultReceiving.ts:28-36` — `DISTINCT ON (branch_id)` sắp theo `created_at ASC, id ASC` — nhưng **đảo điều kiện lọc** thành `is_main_storage = false` và **thêm** `is_active = true` | high | yes | Nội dung migration; và POS có đổi hành vi ngay sau deploy hay phải chờ từng chi nhánh vào tick | confirmed | akenzy chốt 2026-09-10: "Có — backfill giống hệt kho nhập mặc định". Sau đó phát hiện một khác biệt bắt buộc: migration kho **nhập** chọn `is_main_storage = true` (kho backing showroom), trong khi POS Kho tạm lọc bỏ sạch `isMainStorage` (`use-fast-stock-transfer-data.ts:143-146`) — backfill y hệt sẽ gán cờ cho đúng cái kho mà POS không bao giờ thấy, và cả feature thành no-op. Xem ADR-04. **Lần lệch thứ hai**, akenzy chốt 2026-09-10 "Thêm is_active = true": không lọc thì kho cũ nhất đã ngừng hoạt động vẫn được gán cờ — đúng trạng thái chốt ngừng hoạt động (AC-05) cấm, và POS lọc bỏ nên chi nhánh coi như chưa có kho mặc định. Diễn tập chỉ-đọc trên `erp_dev_3008`: 23 chi nhánh có kho, cả hai biến thể đều gán 23, 0 kho ngừng hoạt động bị chọn — dữ liệu dev, không phải production (`erp_clone_prod` không còn trên :5433) |
| A-04 | Cờ mới bám đúng khuôn `isDefaultReceiving`: cột + partial unique index theo chi nhánh, command `SetDefaultIssuingWarehouseCommand` xoá-rồi-đặt trong một transaction, endpoint `POST /v2/inventory/storages/:id/set-default-issuing`, và bị strip khỏi PATCH chung | high | no | Nếu để cờ đi qua PATCH chung thì partial unique index bị vi phạm và request lỗi 500 thay vì lỗi nghiệp vụ | pending | Khuôn đã có sẵn và đang được cưỡng chế trong repo (`storage-crud.service.ts:150-156`); đi lệch khuôn là tự tạo đường vòng qua bất biến |
| A-05 | Chặn ngừng hoạt động kho đang là kho xuất mặc định, đối xứng với chốt đã có cho kho nhập mặc định (`storage-crud.service.ts:170-176`) | medium | no | Chi nhánh ngừng hoạt động kho xuất mặc định rồi POS quay về thứ tự tuỳ tiện mà không ai biết vì sao | pending | — |
| A-06 | Việc sắp xếp "mặc định lên đầu" làm ở **client POS**, không đổi thứ tự mặc định của `GET /inventory/storages` | high | no | Endpoint này có 18+ nơi gọi ở backoffice (`GoodsReceiptFormDialog`, `StockTransferPage`, `StockTakesPage`, …); đổi `ORDER BY` mặc định là đổi thứ tự dropdown của mọi màn đó, vượt xa phạm vi đã chốt ở `00-intent.md` | pending | Mẫu tương đương đã có ở backoffice: `StockTransferPage.tsx:937-948` sắp theo `isDefaultReceiving` phía client |
| A-07 | `GET /inventory/storages` trả nguyên `StorageEntity` (`findAndCount` không `select`), nên cột cờ mới tự có trong payload; chỉ cần nới kiểu `InventoryStorageOption` phía POS | high | no | Nếu payload bị lọc trường thì phải sửa thêm phía server | pending | Đã kiểm `inventory-location.service.ts:590-598` — không có `select`, trả cả thực thể |
| A-08 | Trạng thái hoạt động của showroom đọc từ `storages.is_active` của kho backing nó; `ShowroomEntity` không có và sẽ không có cột `isActive` riêng | high | no | Thêm cột thứ hai là tạo hai nguồn sự thật cho cùng một trạng thái | pending | Đã kiểm `showroom.entity.ts:9-25` — không có cột active nào; `storage_id` là FK duy nhất tới `storages` |
| A-09 | Tham số `activeOnly` thêm vào `GET /inventory/showrooms` là **tuỳ chọn, mặc định tắt**, để mọi nơi gọi hiện tại không đổi hành vi | high | no | Bật mặc định sẽ âm thầm giấu showroom ở các màn khác đang dựa vào danh sách đầy đủ | pending | Cùng khuôn với `activeOnly` của `listStorages` (`inventory-location.service.ts:586-588`) |
| A-10 | POS không cần thêm logic "tự chọn kho xuất mặc định": `pickDefaultStorageId` đã lấy `storages[0]` (`fast-stock-transfer-warehouse-defaults.ts:20-24`), nên khi danh sách được sắp lại thì phần tử đầu chính là kho xuất mặc định | medium | no | Nếu sai thì màn hình mở ra vẫn chọn sẵn kho cũ dù dropdown đã sắp đúng — người dùng coi như chưa sửa gì | pending | — |
| A-11 | Hai tab "Xuất đi" và "Trả lại" dùng chung một danh sách kho, chỉ hoán vai nguồn/đích, nên sửa một chỗ là áp cho cả hai tab | high | no | Phải sửa hai chỗ và dễ sót một tab | pending | Đã kiểm `use-fast-stock-transfer-data.ts:227-241` — cùng một mảng `storages`, chỉ đổi vị trí nguồn/đích theo `direction` |
| A-12 | Nửa yêu cầu "không load kho đã ngừng hoạt động" ở phía kho lưu trữ **đã xong**; chỉ còn phía showroom | high | no | Làm lại việc đã có, hoặc bỏ sót đúng nửa còn hỏng | pending | Đã kiểm: POS gửi `activeOnly=true` (`inventory.service.ts:40`), backend tôn trọng (`inventory-location.service.ts:586-588`); `listShowrooms` (`:658-681`) không lọc gì |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
| --- | --- | --- | --- |
| A-13 | `storages` có sẵn cột phân loại kho theo mục đích (kho xuất / kho nhập), nên chỉ cần lọc theo nó | Không có cột nào như vậy; chỉ có `is_main_storage` phân biệt kho backing showroom với kho lưu trữ thật | "chỉ nên lấy thông tin kho xuất kho nhận" không map được vào phân loại có sẵn — phải hỏi lại, và câu trả lời (A-01) là đổi thứ tự chứ không phải lọc |
| A-14 | Phải thêm bộ lọc "kho đang hoạt động" cho POS | POS đã gửi `activeOnly=true` và backend đã tôn trọng | Bỏ ticket đó; thay bằng ticket cho phía showroom (A-12) |
| A-15 | Sắp "mặc định lên đầu" ở server là gọn nhất vì chỉ sửa một chỗ | `GET /inventory/storages` có 18+ nơi gọi ở backoffice | Chuyển sang sắp ở client POS (A-06); server chỉ nhận thêm `activeOnly` cho showroom |
