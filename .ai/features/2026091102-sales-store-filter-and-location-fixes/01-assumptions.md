---
feature: sales-store-filter-and-location-fixes
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| A-01 | Mục 8 áp cho **cả 4** báo cáo nhóm Bán hàng (`daily_sales_summary`, `invoice_and_order_list`, `revenue_detail_by_invoice_and_product`, `revenue_by_product`), không chỉ "Doanh thu theo mặt hàng" — QA viết "Báo cáo > Bán hàng" | high | no | Bỏ bớt 3 registry khỏi UOW-01 và thu hẹp AC-01, AC-03; thiết kế không đổi | confirmed | Akenzy xác nhận ngày 11/09/2026 ("All 4 sales reports") |
| A-02 | Ở chế độ một chi nhánh, ghim về chi nhánh header cho **mọi** người dùng, kể cả người có quyền tổng hợp; muốn xem nhiều chi nhánh thì chuyển sang Chuỗi cửa hàng | high | no | Phải giữ dòng "Cửa hàng" cho người có quyền tổng hợp — ngược lời QA | confirmed | Akenzy chốt khi đóng plan ngày 11/09/2026: đúng yêu cầu QA mục 8. Đã kiểm trên trình duyệt: ở một chi nhánh, payload ghim chi nhánh header và 4 báo cáo không còn dòng "Cửa hàng"; Chuỗi cửa hàng vẫn có (AC-01, AC-05) |
| A-03 | Header chỉ cho chọn chi nhánh có trong `actor.branchIds`, nên `storeIds: [chi nhánh header]` không bao giờ bị 403 với người không có quyền tổng hợp | medium | no | Người dùng gặp toast 403 thay vì dữ liệu; phải thêm nhánh xử lý | moved | Chưa kiểm được: `.ai/credentials.env` không có tài khoản quản lý chi nhánh đăng nhập được (bước 8 demo UOW-01). Akenzy đóng plan ngày 11/09/2026; chuyển thành việc kiểm sau bằng tài khoản quản lý chi nhánh, chưa có plan |
| A-04 | Sửa cuộn ngay trong `MultiSelectChips` dùng chung, nên form Chương trình khuyến mãi được sửa theo | high | no | Nếu form khuyến mãi cố ý không cuộn thì phải tách prop | confirmed | Akenzy chốt khi đóng plan ngày 11/09/2026. Sửa cuộn nằm trong `MultiSelectChips` dùng chung; ô chọn cửa hàng đã cuộn được trên trình duyệt. Form Chương trình khuyến mãi chưa mở lại để kiểm |
| A-05 | Trên prod, các dòng Kho tạm hiện A01.01 có `source_location_id` = kệ thật, chỉ `notes` sai | medium | yes | Phiếu chuyển xuất sai kệ; phải sửa cả đường quét và dữ liệu prod | moved | Akenzy chọn tách mục 9 sang `2026091103-temp-warehouse-line-shelf` ngày 11/09/2026; tiếp tục ở đó dưới id A-01, vẫn chờ SQL trên prod |
| A-06 | Cột Vị trí trên POS hiện kệ đã quét của dòng, không phải kệ ưu tiên hiện tại | medium | yes | Đổi thiết kế Kho tạm sang tính kệ lúc đọc | moved | Akenzy chọn "kệ đã quét" ngày 11/09/2026; chuyển sang `2026091103-temp-warehouse-line-shelf` dưới id A-02 (confirmed) |
| A-07 | `temp_warehouse_lines.notes` không có chỗ đọc nào khác ngoài nhãn Vị trí trên POS | high | no | Cần script sửa `notes` | moved | Chuyển sang `2026091103-temp-warehouse-line-shelf` dưới id A-03, ngày 11/09/2026 |
| A-08 | Endpoint danh sách dòng kho tạm không khai báo response DTO cho OpenAPI | medium | no | Thêm bước regenerate OpenAPI | moved | Chuyển sang `2026091103-temp-warehouse-line-shelf` dưới id A-04, ngày 11/09/2026 |
| A-09 | Chặn ở `resolveAssignedLocation` dùng đúng quy tắc của `isBalanceTracked`: liên kết **chưa có** dòng tồn vẫn tính là đã xếp, vẫn chặn xếp sang kệ khác | high | no | Nếu muốn coi liên kết chưa có tồn là trống thì đổi AC-21 | confirmed | Akenzy chốt khi đóng plan ngày 11/09/2026: giữ quy tắc của `isBalanceTracked`; AC-21 có test đơn vị. Review còn ghi khoảng hở khi mặt hàng vẫn theo dõi ở kệ khác cùng kho — để làm sau |
| A-10 | Sửa resolver làm form Nhập kho (`GoodsReceiptFormDialog.tsx:1031`, gọi cùng endpoint) thôi tự điền kệ đã ngừng theo dõi — đúng mục tiêu UOW-04 của `2026090301-inventory-qa-defects` | high | no | Nếu có luồng dựa vào việc tự điền kệ đã ngừng thì phải tách endpoint | confirmed | Akenzy chốt khi đóng plan ngày 11/09/2026: đúng mục tiêu UOW-04 của `2026090301-inventory-qa-defects`. Bước 6 demo UOW-04 (form Nhập kho) chưa chạy trên trình duyệt |
| A-11 | T-02-03 của `2026091002-report-multi-location-column` đã commit phần code (a4ed8537 sửa độ rộng trong `report-revenue-by-product.registry.ts`), chỉ còn trạng thái ticket — sửa `:145-146` cùng file không đè lên nhau | medium | no | Phải đợi T-02-03 xong mới làm T-01-02 | confirmed | Akenzy chốt làm song song, không chờ T-02-03, ngày 11/09/2026; `git show a4ed8537` xác nhận file đã được sửa ở commit đó |
| A-12 | Mặt hàng chỉ nằm ở showroom vẫn trống Tên vị trí trên báo cáo doanh thu sau khi sửa, và QA chấp nhận | medium | no | Mở rộng sang `showroomFallback` — thuộc `2026091002`, không thuộc feature này | confirmed | Akenzy xác nhận ngày 11/09/2026 ("Showroom blank OK") |
| A-13 | Test hàm thuần của backoffice-web chạy bằng `rtk proxy npx --yes vitest run <file>` là bằng chứng hợp lệ. Trái tiền đề ADR-05 của `2026091003-default-issuing-warehouse` ("không có trình chạy test"): chạy thật ngày 11/09 cho `report.store.test.ts` 12/12, pos-web lib `fast-stock-transfer` 14/14 (vitest 5.0.0 tải qua npx) | high | no | Bỏ `tests:` của T-01-01, T-01-02 và nghiệm thu các AC đó bằng demo | confirmed | Akenzy xác nhận ngày 11/09/2026 ("Unit tests via npx") |
