---
id: T-01-03
uow: UOW-01
title: Dropdown mục thu/chi của 4 loại phiếu sắp theo display_order; demo trên backoffice local
layer: frontend
type: feature
estimate: 2h
status: done
depends_on: [T-01-02]
blocks: []
verifies: [AC-06]
tests: []
touches:
  - apps/backoffice-web/src/hooks/treasury/use-cash-voucher-categories.ts
assumptions: [A-05]
---

# T-01-03 — Dropdown theo thứ tự ảnh

## Context

`useCashVoucherCategories` (`use-cash-voucher-categories.ts`) gọi `GET /admin/entities/cash-voucher-categories/records`
không kèm `sortBy`, nên `BaseCrudService.applySorting` sắp theo `createdAt` ⇒ mục do migration chèn nằm cuối
dropdown. `display_order` hiện chỉ được báo cáo Kết quả kinh doanh dùng (`business-results.report.ts`,
`queryOtherCategories`).

Consumer của hook:

- 4 dialog render `<option>` theo thứ tự mảng: `PaymentVoucherDialog`, `ReceiptVoucherDialog`,
  `DepositPaymentVoucherDialog`, `DepositReceiptVoucherDialog`.
- 3 trang chỉ dùng `useCategoryNameMap` (map id → tên, không phụ thuộc thứ tự): `TreasuryCashReceiptsPage`,
  `TreasuryDepositReceiptsPage`, `LedgerCashPage`.

Depends on T-01-02 vì demo cần dữ liệu đã migrate; code không phụ thuộc.

## Implementation notes

- Thêm `sortBy: "displayOrder", sortOrder: "asc"` vào `query`. Hai tham số đã có kiểu trong
  `operations["CrudController_listRecords"]` ⇒ không regenerate api-client.
- `queryKey` giữ nguyên: thứ tự cố định cho mọi consumer, không phải bộ lọc.
- `pageSize: 100` vẫn đủ (tối đa 34 mục chi mặc định).
- Backoffice không có test runner (`"test": "echo test"`) ⇒ bằng chứng là `tsc` và demo.

## Done when

- [x] `pnpm --filter @erp/backoffice-web exec tsc --noEmit` sạch
- [x] ~~DevTools › Network: request mang `sortBy=displayOrder&sortOrder=asc`~~ → **đổi cách kiểm** (12/09):
      extension Chrome không bắt được request tới API và Resource Timing rỗng, nên bằng chứng là
      (a) module Vite đang phục vụ có `sortBy: "displayOrder"`, `sortOrder: "asc"`, và (b) 14 mục do
      migration chèn (`created_at` mới nhất) nằm **xen giữa** danh sách chứ không dồn xuống cuối —
      chỉ xảy ra khi sắp theo `display_order`
- [x] Phiếu chi tiền mặt: dropdown mục chi đúng thứ tự AC-06 — ảnh chụp
- [x] Phiếu chi tiền gửi và Phiếu thu tiền mặt: đúng thứ tự — ảnh chụp (kiểm thêm Phiếu thu tiền gửi)

## Kết quả 2026-09-11 (đang làm)

- `use-cash-voucher-categories.ts`: thêm `sortBy: "displayOrder", sortOrder: "asc"` vào `query`; `queryKey` giữ nguyên.
- `pnpm --filter @erp/backoffice-web exec tsc --noEmit`: exit 0, không output.
- API `:4000` và backoffice `:3000` đã bật (chạy nền). Chrome (org MT, header KHO SG) → Thêm mới › Phiếu chi tiền:
  dropdown "Mục chi" **trước migration** có 20 mục theo `display_order` cũ, từ "Tiền điện" đến "Chi trả nợ nhà cung cấp",
  "Phí ngân hàng". Đóng dialog, không lưu.
- Network: extension không bắt được request tới API (chỉ thấy file module Vite), Resource Timing cũng rỗng. Thay bằng:
  module Vite đang phục vụ `use-cash-voucher-categories.ts` có `sortBy: "displayOrder"` và `sortOrder: "asc"` (fetch từ
  trang). Bằng chứng hành vi là thứ tự sau migration: mục migration chèn (`created_at` mới) phải nằm giữa danh sách.
- `migration:run` trên `erp_dev_3008` bị bộ phân loại quyền của Claude Code chặn ("Modify Shared Resources"); chờ Akenzy tự chạy.
- Org MT không có dòng phiếu chi nào gắn `CHI_TIEN_NUOC` hoặc `CHI_CCDC` ⇒ bước 8 của Demo script: không có dữ liệu.
- Snapshot SQL trước migration: `scratchpad/categories-before.txt` (MT 9/20, My Company 9/19; id `CHI_TIEN_NUOC` MT
  `14545123-…`, `CHI_CCDC` MT `bcb53487-…`).

## Demo 2026-09-12 (Chrome của Akenzy, backoffice local :3000, DB `erp_dev_3008`, tổ chức MT)

Akenzy tự chạy `migration:run` (Claude bị chặn quyền). Sau migration, SQL xác nhận:
`migrations` có dòng 187 `BackfillCashVoucherCategoryOptions1789930000000`; MT và My Company đều **9 mục thu /
34 mục chi**; MT được chèn 14 dòng `created_by = 'migration'`, My Company 15 dòng (thêm `BANK_FEE`); `CHI_TIEN_NUOC`
thành "Tiền nước sinh hoạt" và `CHI_CCDC` thành "Mua đồ dùng, công cụ, dụng cụ" **giữ nguyên id** (`14545123-…`,
`bcb53487-…`) ⇒ AC-02 đúng trên dữ liệu thật. Snapshot: `scratchpad/categories-after.txt`.

| Dialog | Kết quả | Ảnh |
| --- | --- | --- |
| Phiếu chi tiền mặt | 34 mục chi, đúng thứ tự ảnh 1→6, "Chi trả nợ nhà cung cấp" và "Phí ngân hàng" cuối | `screenshot-1789181706920-0.jpg` |
| Phiếu thu tiền mặt | 9 mục thu, đúng thứ tự ảnh 7 | `screenshot-1789181764998-1.jpg` |
| Phiếu chi tiền gửi | 34 mục chi, cùng thứ tự | `screenshot-1789181858040-3.jpg` |
| Phiếu thu tiền gửi | 9 mục thu, cùng thứ tự | `screenshot-1789181884094-4.jpg` |

Danh sách đọc trực tiếp từ `<option>` của dropdown, không phải từ ảnh. Thứ tự mục chi: Tiền điện · Tiền điện thoại ·
Tiền internet · Tiền nước sinh hoạt · Tiền thuê cửa hàng · **Tiền vận chuyển** · **Mua dụng cụ sửa dép** · Tiền lương ·
Tiền thưởng · Tiền phụ cấp · **Ứng lương** · Mua đồ dùng, công cụ, dụng cụ · Tài sản cố định · **Mua máy móc thiết bị** ·
Chi khác · Chi tiếp khách · Mua văn phòng phẩm · Chi tạm ứng · **Thuê mướn khác** · **Chi vệ sinh môi trường** ·
**Chi lý do khác** · Chi mua hàng hóa · Chi chuyển tiền sang cửa hàng khác · Rút tiền gửi về nhập quỹ ·
Chi gửi tiền vào ngân hàng · **Chi ăn uống** · **Mua xăng dầu nhớt** · **Nạp VETC** · **Làm Hàng** ·
**Đồ dùng vệ sinh** · **Tiền ăn** · **Tiền nước uống** · Chi trả nợ nhà cung cấp · Phí ngân hàng (in đậm = 14 mục mới).

Hai điểm chệch so với Demo script: bước 8 (mở phiếu cũ đã gắn mục đổi tên) **không chạy được** — org MT không có
dòng phiếu nào gắn `CHI_TIEN_NUOC` hay `CHI_CCDC`; bước 7 đọc query string đổi sang cách kiểm ghi ở Done when.
Lần mở dialog Phiếu chi tiền gửi đầu tiên không mở được vì bấm vào mục menu đã đóng; mở lại bằng toạ độ thì được.
