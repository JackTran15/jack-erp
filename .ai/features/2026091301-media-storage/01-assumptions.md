---
feature: media-storage
blocking_open: 0
---

# Assumption register

A-01..A-04 do chủ sở hữu trả lời trong phiên planning 2026-09-13. Chín assumption blocking tiếp
theo (A-05, A-06, A-08, A-10, A-13, A-14, A-15, A-17, A-23) được đề xuất rồi chủ sở hữu trả lời
"Confirm" cùng ngày; A-15 sau đó bị thay bằng A-26 (xem Rejected). A-26 và 6 ADR được chủ sở hữu
chọn/chấp nhận cùng ngày. Các dòng `pending` còn lại **chưa ai xác nhận**. Mục `M…` trỏ tới
section "Feature surface — media storage" trong `.ai/architecture.md`.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|---|---|---|---|---|---|---|
| A-01 | Phạm vi v1 gồm: **ảnh hàng hoá**, **ảnh nhân viên**, **tài liệu đính kèm chứng từ**. Không gồm logo tổ chức/chi nhánh | high | yes | Mỗi đối tượng sở hữu thêm/bớt là một UoW | resolved | Akenzy chốt 2026-09-13: chọn 3 đối tượng trên, bỏ logo tổ chức/chi nhánh |
| A-02 | File đi **thẳng từ trình duyệt lên storage** bằng presigned POST có policy giới hạn kích thước và loại file; API chỉ cấp vé và xác nhận, không bao giờ nhận byte file | high | yes | Đổi sang multipart qua API: thêm `FileInterceptor`, giới hạn body, và phải xử lý lỗ hổng fingerprint idempotency với multipart (M6) | resolved | Akenzy chốt 2026-09-13: "Browser → storage". Ban đầu ghi "presigned PUT"; đổi thành presigned POST theo ADR-02, Akenzy chấp nhận cùng ngày |
| A-03 | Production chạy **MinIO community tự host**, dù repo đã archive (2026-04-25), không có binary/image mới, không bảo đảm vá bảo mật | high | yes | Nếu đổi engine: đổi hạ tầng và ticket compose/bootstrap; code app không đổi nhờ ADR-01 | resolved | Akenzy chốt 2026-09-13: "MinIO, accept the risk" sau khi được báo trạng thái archive |
| A-04 | **Ảnh hàng hoá công khai** (URL ổn định, không hết hạn, cache được); **ảnh nhân viên riêng tư** (presigned GET có hạn) | high | yes | Đổi chính sách đọc: đổi layout bucket/policy và hợp đồng URL trả về cho POS/đối tác | resolved | Akenzy chốt 2026-09-13: "Public products, private staff" |
| A-05 | **Tài liệu đính kèm chứng từ là riêng tư**, đọc bằng presigned GET có hạn như ảnh nhân viên. Câu hỏi A-04 không nhắc tới đính kèm | high | yes | Nếu công khai: chứng từ tài chính (phiếu thu/chi, ngân hàng) lộ cho bất kỳ ai có link; đổi bucket và luồng tải về | resolved | Akenzy xác nhận đề xuất 2026-09-13 |
| A-06 | **Trình duyệt với tới MinIO qua cùng origin** `erp.giaymt.com.vn`: nginx thêm một `location` cho mỗi bucket (ví dụ `/erp-media-public/`, `/erp-media-private/`) proxy nguyên path và giữ header `Host` sang MinIO. Không dùng hostname riêng. Đề xuất theo tên bucket vì presigned GET ký cả path; proxy dưới một path con khác sẽ làm lệch chữ ký. **Cần spike xác minh trên nginx thật** | low | yes | Nếu dùng hostname riêng: thêm CORS cho bucket, DNS, và cấu hình `publicEndpoint` khác; nếu path con không giữ được chữ ký, luồng đọc riêng tư phải đổi | resolved | Akenzy xác nhận đề xuất 2026-09-13. Spike trên nginx thật vẫn là điều kiện trong plan, không phải đã xác minh |
| A-07 | MinIO production chạy **cùng máy** với PM2/Postgres/Redis; người vận hành chịu trách nhiệm tạo credential, bucket (bằng lệnh bootstrap feature cung cấp), backup và cập nhật cấu hình nginx. Secret đặt ở cùng chỗ với `DB_PASS` hiện nay | low | no | Không làm lại code; nhưng **chặn phát hành** cho tới khi có người nhận việc vận hành. Được ghi ở Constraints | pending | — |
| A-08 | **Ảnh gắn theo sản phẩm** (`products`), dùng chung cho mọi biến thể/item. Không có ảnh theo màu/size. Căn cứ: POS `imageUrl` nằm trên dòng sản phẩm (`pos-catalog-product.response.dto.ts:27`), đối tác `images[]` cũng trên dòng sản phẩm (`partner-product-search.dto.ts:180`) | medium | yes | Nếu cần ảnh theo màu: bảng liên kết media đổi khoá (thêm item/option), UI chọn ảnh theo biến thể, và cả hai hợp đồng đọc phải biết chọn ảnh nào | resolved | Akenzy xác nhận đề xuất 2026-09-13. Phần bổ sung cho hàng không có biến thể: xem A-25 |
| A-09 | **Giới hạn file**: ảnh hàng hoá ≤ 2 MB, jpg/jpeg/png/gif/webp, ≤ 10 ảnh/sản phẩm (giữ nguyên `item-create/constants.ts:11-13`); ảnh nhân viên 1 ảnh ≤ 5 MB, jpg/jpeg/png/webp (giữ con số "< 5MB" UI đang hứa ở `EmployeeBasicInfoTab.tsx:146,168`); đính kèm ≤ 10 MB/file, ≤ 10 file/chứng từ, pdf/jpg/jpeg/png/webp/xls/xlsx/doc/docx/csv. **Không** cho svg/html. Server kiểm lại kích thước và content-type khi xác nhận, không tin client | medium | no | Đổi hằng số và thông báo lỗi; không đổi cấu trúc | pending | — |
| A-10 | **API đối tác bắt đầu trả URL ảnh** trong `images[]` (thứ tự tải lên). Hình dạng giữ nguyên `string[]` — DTO đã ghi rõ "so images can appear later without a breaking change" (`partner-product-search.dto.ts:177-178`); test khoá rỗng ở `partner-catalog-contract.spec.ts:152-156` được sửa | medium | yes | Nếu đối tác chưa được báo: storefront đang chạy bất ngờ nhận URL. Nếu giữ rỗng: bỏ 1 ticket, đối tác chờ feature khác | resolved | Akenzy xác nhận đề xuất 2026-09-13 |
| A-11 | POS `imageUrl` = URL công khai của **ảnh đầu tiên** của sản phẩm, `null` nếu không có ảnh. pos-web **không** hiển thị ảnh trong v1 (hiện không render ở đâu — M3) | high | no | Chọn sai ảnh đại diện: đổi một câu query; muốn POS hiển thị: thêm 1 UoW FE | pending | — |
| A-12 | Module mobile **tiếp tục không** trả ảnh (loại có chủ đích ở `mobile-sales-model-stock.response.dto.ts:9-13`, khoá bằng `mobile-sales-model-stock.spec.ts:119`) | high | no | App tư vấn bán hàng cần ảnh: thêm trường + sửa test khoá | pending | — |
| A-13 | Migration **đặt `photo_url = NULL`** cho mọi dòng `employee_profiles` có giá trị bắt đầu bằng `blob:`. Những giá trị này không hiển thị được ở bất kỳ đâu. Không đảo ngược được. Chưa đếm số dòng bị ảnh hưởng: 2026-09-13 thử đếm nhưng Postgres local không chạy | medium | yes | Xoá dữ liệu người dùng mà không hỏi; nếu giữ lại thì màn nhân viên tiếp tục hiện ảnh vỡ và cần xử lý `blob:` ở tầng đọc | resolved | Akenzy xác nhận đề xuất 2026-09-13. Ticket migration phải đếm và ghi số dòng bị ảnh hưởng trước khi chạy |
| A-14 | **Đính kèm v1 chỉ cho 7 bảng đã có cột `attachment_ids`**: goods_receipts, transfer_orders, stock_transfers, cash_receipts, cash_payments, bank_receipts, bank_payments (M3). Phiếu xuất kho, kiểm kê, kiểm quỹ có nhãn trên UI nhưng không có cột nên không làm | medium | yes | Thêm loại chứng từ: mỗi loại thêm migration + DTO + dialog, có thể thêm 1 UoW | resolved | Akenzy xác nhận đề xuất 2026-09-13 |
| A-16 | Giá trị `attachment_ids` hiện có trong DB **đều là `[]`** vì chưa từng có bảng nào để trỏ tới. 2026-09-13 thử đo nhưng Postgres local không chạy; đo lại trước khi ticket chứng từ đầu tiên bắt đầu. Lưu ý: hôm nay create/update của cả 7 loại đều nhận và lưu UUID bất kỳ (ví dụ `goods-receipt.service.ts:176,311-312`), nên về lý thuyết có thể đã có giá trị | medium | no | Nếu có UUID rác: `syncOwner` sẽ từ chối lần sửa tiếp theo của chứng từ đó với `MEDIA_NOT_FOUND`; cần migration dọn trước | pending | — |
| A-17 | **Không thêm khoá quyền `media.*` chung.** Xin vé tải lên / link tải về dùng quyền của chính đối tượng sở hữu: sửa hàng hoá, sửa hồ sơ nhân viên, sửa (tải lên) hoặc xem (tải về) từng loại chứng từ | medium | yes | Nếu cần khoá riêng: thêm seed + nhãn + gán vai trò + danh sách e2e (M7) ở mọi UoW; nếu dùng một khoá chung: người chỉ có quyền xem phiếu thu có thể tải file lên phiếu khác | resolved | Akenzy xác nhận đề xuất 2026-09-13. Khoá cụ thể liệt kê ở `03-logical-design.md` |
| A-18 | File đã tải lên nhưng **không được xác nhận trong 24 giờ** bị một `@Cron` hằng ngày xoá (cả object lẫn bản ghi). API chạy 1 tiến trình PM2 (M8) nên không cần khoá phân tán | medium | no | Nếu chạy nhiều instance: job chạy trùng (xoá hai lần, vô hại nhưng tốn) — thêm khoá Redis | pending | — |
| A-19 | **Xoá đối tượng sở hữu thì media của nó bị gỡ** (đánh dấu xoá, job dọn xoá object). Đề xuất ban đầu "xoá mềm thì giữ object" dựa trên một tiền đề sai: `items` xoá cứng (`DeletionPolicy.HARD`, `item-crud.service.ts:1925`) và `products` không có cột `deleted_at` (`product.entity.ts`, migration `1777500000000-ProductCatalogSchema.ts:8-21`), nên không có xoá mềm nào để giữ lại. Điểm móc có sẵn: `afterDelete` của `BaseCrudService.remove` (`base-crud.service.ts:180`) | medium | no | Nếu muốn giữ file sau khi xoá: dung lượng rác tăng dần, cần báo cáo dọn tay | pending | — |
| A-20 | pos-web **không tải lên** trong v1 | high | no | Nếu cần: pos-web phải có đường gửi file (hiện không có — M2) | pending | — |
| A-21 | Link riêng tư và byte file **đi qua HTTP thuần** cho tới khi production có HTTPS; rủi ro tương đương access token đang đi qua HTTP hôm nay | low | no | Không làm lại code; rủi ro nghe lén link riêng tư trong thời hạn URL. Cần chủ sở hữu ghi nhận | pending | — |
| A-22 | CI **không thêm service MinIO**. Client storage không kết nối khi khởi tạo; e2e thay provider storage bằng bản giả. Local dev thêm MinIO vào `docker-compose.yml` ở cổng 9000/9001 (chưa bị service nào trong repo chiếm — M5) | medium | no | Nếu muốn e2e chạy MinIO thật: thêm service vào `.github/workflows/ci.yml` và global-setup tạo bucket | pending | — |
| A-23 | Section "Feature surface — media storage" (M1–M12) thêm vào `.ai/architecture.md` ngày 2026-09-13 là **bản nháp chưa ai xác minh**. `aidlc check G0` chỉ đọc `verified_by` ở frontmatter (ký 2026-08-03), không phủ section mới | high | yes | `touches` của ticket suy từ section này; nếu sai, ticket trỏ vào file/luồng không tồn tại | resolved | Akenzy xác nhận section M1–M12 ngày 2026-09-13 (trả lời "Confirm"); ghi chú xác nhận đặt ở đầu section, không sửa frontmatter |
| A-24 | **Không xử lý ảnh phía server** (không resize, thumbnail, xoá EXIF). Lưu nguyên file. Ảnh chụp điện thoại có thể mang toạ độ GPS trong EXIF | medium | no | POS/đối tác tải ảnh gốc nặng; ảnh nhân viên có thể lộ vị trí chụp. Thêm xử lý sau là một consumer riêng | pending | — |
| A-25 | **Hàng hoá không có biến thể gắn ảnh vào chính dòng `items`.** Form tạo hàng hoá không có màu/size chỉ ghi một dòng `items`, không tạo `products` (`item-crud.service.ts:355-372`). POS đã coi đó là một thẻ sản phẩm với id `COALESCE(i.product_id, i.id)` (`pos-catalog-product.service.ts:480`). Vì vậy chủ sở hữu ảnh hàng hoá có hai loại: sản phẩm (có biến thể) và item đứng một mình. API đối tác chỉ trả dòng có `product_id` (`search-partner-products.handler.ts:118`) nên không bị ảnh hưởng | medium | no | Nếu hàng không biến thể không cần ảnh: bỏ một loại chủ sở hữu, form ẩn ô ảnh ở chế độ đó; nếu phải luôn có `products`: đổi luồng tạo hàng hoá, ngoài phạm vi feature | pending | — |
| A-26 | **Đính kèm theo đúng khả năng sửa của chứng từ.** Thêm và gỡ đính kèm được bất cứ khi nào endpoint sửa của chính chứng từ đó chấp nhận; ở trạng thái kết thúc (`CANCELLED`, `REVERSED`, `COMPLETED`) chính endpoint đó từ chối nên cả thêm lẫn gỡ đều bị khoá. Căn cứ: phiếu nhập kho `POSTED` vẫn sửa được (`goods-receipt.service.ts:267`); phiếu chuyển kho `POSTED` sửa bằng bút toán đảo (`stock-transfer.service.ts:552`); lệnh chuyển kho `IN_PROGRESS` chỉ cho sửa ghi chú và đính kèm (`transfer-order.service.ts:866-873,888`); phiếu thu/chi tiền mặt và ngân hàng đi qua `assertEditable` (`editable-voucher.util.ts:43-66`). Module media không có luật trạng thái riêng | medium | yes | Nếu cần khoá gỡ sau ghi sổ: thêm luật trạng thái theo từng loại chứng từ vào policy media và một mã lỗi mới | resolved | Akenzy chọn 2026-09-13: "Follow voucher editability", thay cho A-15 |
| A-27 | **File trong bucket công khai đọc được ẩn danh ngay khi tải lên**, trước khi được gắn vào hàng hoá, cho tới khi job dọn chạy (≤ 24 giờ cộng chu kỳ job). Người có `inventory.write` có thể dùng điều này để đặt một file ảnh bất kỳ trên origin của ERP. Chấp nhận vì người có quyền này là nhân viên nội bộ; giảm nhẹ bằng allowlist chỉ gồm MIME ảnh và `X-Content-Type-Options: nosniff` ở location công khai (T-05-02). Phát hiện ở security review T-01-02, 2026-09-13 | medium | no | Nếu không chấp nhận: tải vào bucket riêng tư rồi `CopyObject` sang bucket công khai lúc gắn — đổi `syncOwner` (T-01-05) và vé tải lên (T-01-04) | pending | — |
| A-28 | **Allowlist đính kèm giữ `.doc` / `.xls` (định dạng cũ, có thể chứa macro VBA) và `.csv` (công thức bị bảng tính thực thi khi mở).** Người lập chứng từ có thể đặt file độc cho kế toán mở. Chấp nhận vì tải về luôn là `Content-Disposition: attachment`, không hiển thị inline, và người tải lên là nhân viên có quyền lập chứng từ. Phát hiện ở security review T-01-02, 2026-09-13 | medium | no | Bỏ `application/msword` và `application/vnd.ms-excel` khỏi `media-owner-policies.ts` (chỉ giữ `.docx` / `.xlsx`) là đổi một hằng số và một dòng thông báo lỗi | pending | — |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|---|---|---|---|
| A-15 | Chứng từ đã ghi sổ/hoàn tất vẫn thêm được đính kèm nhưng không gỡ được; chứng từ nháp thêm/gỡ tự do (Akenzy xác nhận 2026-09-13) | Quy tắc được xác nhận dưới ràng buộc sai "chứng từ đã ghi sổ bất biến" (A-95). Khi được báo, Akenzy chọn A-26 cùng ngày | AC-16 viết lại theo A-26; bỏ mã lỗi `MEDIA_DETACH_FORBIDDEN`; policy media không còn cột quy tắc gỡ theo trạng thái |
| A-90 | MinIO community còn được bảo trì và vẫn phát hành Docker image | Repo `minio/minio` ghi "THIS REPOSITORY IS NO LONGER MAINTAINED", archive 2026-04-25; từ 10/2025 chỉ phân phối mã nguồn. SDK `minio` (npm) vẫn Apache-2.0 nhưng bản cuối publish 2026-02-27; `@aws-sdk/client-s3` publish 2026-09-11 | Chủ sở hữu vẫn giữ MinIO (A-03), ghi vào Constraints; ADR-01: code chỉ dùng S3 API chuẩn qua AWS SDK v3 |
| A-91 | Tải file đi qua API bằng multipart, như tiền lệ duy nhất là import Excel (M2) | Chủ sở hữu chọn trình duyệt → storage (A-02) | Không thêm `FileInterceptor`; lỗ hổng fingerprint idempotency với multipart (M6) nằm ngoài đường đi của feature này |
| A-92 | Ảnh nhân viên đã được lưu, chỉ thiếu chỗ chứa đẹp hơn | Đang lưu URL `blob:` chết (`EmployeeBasicInfoTab.tsx:93-97` → `user-form.ts:55`) | UoW ảnh nhân viên phải sửa luồng lưu **và** dọn dữ liệu cũ (A-13) |
| A-93 | Ảnh hàng hoá đã được gửi lên server, chỉ là server bỏ qua | `productImages` không bao giờ rời trình duyệt: chỉ được đọc ở bộ đếm `InventoryItemCreateForm.tsx:939` | UoW ảnh hàng hoá cần luồng lưu từ đầu, không phải chỉ bật một cờ |
| A-94 | Xoá đối tượng sở hữu là xoá mềm, nên có thể giữ object (bản đầu của A-19) | `items` xoá cứng; `products` không có cột `deleted_at` | A-19 viết lại: xoá chủ sở hữu thì gỡ media qua hook `afterDelete` |
| A-95 | Chứng từ đã ghi sổ bất biến (ràng buộc trong bản đầu `00-intent.md`, chép từ CLAUDE.md) | 6/7 loại chứng từ trong phạm vi vẫn sửa được sau ghi sổ; chỉ trạng thái kết thúc bị khoá | Dòng Constraints của `00-intent.md` được sửa; A-15 bị thay bằng A-26 |
