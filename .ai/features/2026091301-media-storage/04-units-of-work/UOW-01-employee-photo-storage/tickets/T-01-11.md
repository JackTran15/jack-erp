---
id: T-01-11
uow: UOW-01
title: E2E — storage giả cho AppModule và kiểm vé tải lên
layer: test
type: test
estimate: 3h
status: in_progress
depends_on: [T-01-04]
blocks: [T-02-03, T-04-09]
verifies: [AC-12, AC-19]
tests:
  - apps/api/test/e2e/media-upload.e2e-spec.ts
touches:
  - apps/api/test/e2e/setup/test-app.ts
  - apps/api/test/e2e/setup/fake-object-storage.ts   # new
  - apps/api/test/e2e/media-upload.e2e-spec.ts   # new; theo apps/api/test/e2e/inventory.e2e-spec.ts
assumptions: [A-22]
---

# T-01-11 — E2E — storage giả cho AppModule và kiểm vé tải lên

## Context

`test-app.ts:28-59` boot `AppModule` thật, chỉ override TypeORM; CI chỉ có Postgres và Redis
(`.github/workflows/ci.yml:17-40`). Muốn e2e đi qua luồng media mà không cần MinIO thì phải thay
`ObjectStorageService` bằng bản giả ở một chỗ duy nhất, để T-02-03 và T-04-09 dùng lại.

## Implementation notes

- `fake-object-storage.ts`: cài cùng các phương thức công khai của `ObjectStorageService`, lưu object
  trong `Map<key, { size, contentType }>`; có hàm `putFake(key, size, contentType)` để test giả lập
  việc trình duyệt đã tải file lên.
- `test-app.ts`: `.overrideProvider(ObjectStorageService).useValue(fake)` và export `fake` để spec dùng.
- `media-upload.e2e-spec.ts`:
  - Người dùng không có `iam.user.write` xin vé `EMPLOYEE_PROFILE` → 403 (kiểm danh sách khoá cứng ở
    `test-app.ts:135-166` để chọn tài khoản đúng). Sửa 2026-09-13 lúc implement: bản plan ghi
    `PRODUCT`/`inventory.write`, nhưng AC-12 là quyền sửa hồ sơ nhân viên, nên test theo `EMPLOYEE_PROFILE`.
  - Admin xin vé → 201 có `upload.fields`.
  - `putFake` sai kích thước rồi `complete` → 400 `MEDIA_INVALID`; đúng kích thước → 200 `UPLOADED`.
- Suite chạy nối tiếp, `forceExit` (CLAUDE.md): đọc output test thật, không chỉ dòng exit.

## Done when

- [ ] `pnpm --filter @erp/api test:e2e -- media-upload` xanh khi MinIO **không** chạy
  - 2026-09-14: suite từng bị đánh FAIL dù 4/4 test pass, vì `OutboxRelayService` chạy `pollOnce` đúng lúc
    `resetDatabase()` gọi `synchronize(true)`: bảng `outbox_messages` tạm vắng và promise lỗi không ai bắt.
    `resetDatabase` giờ tạm dừng relay trong lúc dựng lại schema. Chạy lại với `MEDIA_S3_ENDPOINT=http://127.0.0.1:1`
    → 1/1 suite, 4/4 test pass. Sau đó vòng sửa theo code review đã thêm ca 503 (AC-19), theo dõi bucket trong
    storage giả và reset storage giả cùng DB (compile sạch). Cần chạy lại; sẽ chạy trong lượt e2e đầy đủ trên
    nhánh, sau baseline.
- [ ] Các suite e2e sẵn có vẫn xanh sau khi sửa `test-app.ts`
  - 2026-09-14: đối chiếu 14 suite trên cây làm việc: 0 suite từ PASS sang FAIL, 3 suite từ FAIL sang PASS nhờ tạm
    dừng relay, 10 suite vẫn FAIL với lỗi không liên quan media. Lượt đối chiếu này không sạch: cây đã có thay đổi
    của feature, và API dev chạy song song dùng chung consumer group Kafka. Đang chạy toàn bộ e2e trên `main`
    (`743b485a`, trước feature, không có API dev chạy). 5 suite đầu cho thấy `mobile`, `checkout-saga`,
    `product-variants`, `deposit-recon-lock` đã FAIL sẵn trên `main`. Tiếp theo sẽ chạy toàn bộ trên nhánh và so
    từng suite.
- [x] Response lỗi có `code` ở top-level (`MEDIA_INVALID`), không nằm trong `details` (`media-upload.e2e-spec.ts` › "returns 400 MEDIA_INVALID (top-level code)…" — `res.body.code === 'MEDIA_INVALID'` và `res.body.details.code` là `undefined`, test pass)
