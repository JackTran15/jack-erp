---
id: T-02-03
uow: UOW-02
title: E2E — hàng hoá gắn ảnh qua CRUD và cách ly tổ chức
layer: test
type: test
estimate: 3h
status: done
depends_on: [T-01-11, T-02-01]
blocks: []
verifies: [AC-01, AC-08]
tests:
  - apps/api/test/e2e/media-product-images.e2e-spec.ts
touches:
  - apps/api/test/e2e/media-product-images.e2e-spec.ts   # new; theo apps/api/test/e2e/inventory.e2e-spec.ts
assumptions: [A-08, A-25]
---

# T-02-03 — E2E — hàng hoá gắn ảnh qua CRUD và cách ly tổ chức

## Context

Body CRUD không có DTO nên lớp validate duy nhất là service (T-02-01). E2E khoá hai điều không được
vỡ âm thầm: ảnh đi hết đường HTTP → DB → response, và `mediaId` không vượt qua ranh giới tổ chức.

## Implementation notes

- Dùng storage giả của T-01-11 (`putFake`).
- Ca 1: xin vé `PRODUCT` → `putFake` → `complete` → `POST /admin/entities/inventory-items/records` có
  `colors`, `sizes`, `imageIds: [a, b]` → `GET .../records/<productId>` trả `images` 2 phần tử đúng thứ tự.
- Ca 2: như ca 1 nhưng không màu/size, `ownerType: 'ITEM'`.
- Ca 3: người dùng tổ chức khác dùng `mediaId` của tổ chức thứ nhất khi tạo hàng hoá → 404 và media
  vẫn `UPLOADED`. Nếu setup e2e chưa có helper tạo tổ chức thứ hai thì dựng trong spec này, không sửa
  `test-app.ts`.
- Ghi nhận, không thêm test và không chặn ticket: gọi thử `DELETE /admin/entities/inventory-items/records/<itemId>`
  với một hàng hoá có ảnh, rồi báo lại status code và `media_objects.status` sau đó. Code review T-02-01 cho rằng
  xoá CRUD HARD đang lỗi trước khi `afterDelete` chạy (A-19).
  Kết quả 2026-09-14: `DELETE` trả 500 `INTERNAL_ERROR`, transaction rollback, item vẫn còn, media vẫn `ATTACHED`.

## Done when

- [x] `pnpm --filter @erp/api test:e2e -- media-product-images` xanh khi MinIO không chạy
- [x] Cả 3 ca có trong spec
- [x] Các suite e2e sẵn có vẫn xanh
  - **Chuyển sang trước merge (quyết định của người dùng 2026-09-14):** chưa chạy toàn bộ. Đã chạy với `MEDIA_S3_ENDPOINT=http://127.0.0.1:1` trên `erp_test`: `media-product-images` 1/1 suite, 3/3 test; `media-upload` 1/1 suite, 5/5 test. So sánh toàn bộ `main` với nhánh theo dõi ở mục "Trước merge" trong DoD của UOW-02.
