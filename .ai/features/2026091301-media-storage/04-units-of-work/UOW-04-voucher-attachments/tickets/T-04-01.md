---
id: T-04-01
uow: UOW-04
title: Registry reader chủ sở hữu và GET /media/:id/download-url
layer: api
type: feature
estimate: 3h
status: done
depends_on: [T-01-04, T-01-06]
blocks: [T-04-02, T-04-03, T-04-04, T-04-05, T-04-06, T-04-09]
verifies: [AC-15]
tests:
  - apps/api/src/modules/media/media-download.service.spec.ts
touches:
  - apps/api/src/modules/media/media-owner-reader.registry.ts   # new; stub do T-01-02 tạo
  - apps/api/src/modules/media/media-download.service.ts   # new; stub do T-01-02 tạo
  - apps/api/src/modules/media/media-download.controller.ts   # new; stub do T-01-02 tạo
  - apps/api/src/modules/media/media-download.service.spec.ts   # new
  - apps/api/src/modules/media/media-query.service.ts   # chỉ export ATTACHMENT_URL_EXPIRES_SEC (code review T-04-01: bỏ đọc X-Amz-Expires từ URL)
  - apps/api/src/modules/media/dto/media-download.response.dto.ts   # new; DTO response theo dto/media-upload.response.dto.ts (code review T-04-01)
  - packages/api-client/src/generated/schema.ts
  - packages/api-client/openapi.snapshot.json
assumptions: [A-05, A-17]
---

# T-04-01 — Registry reader chủ sở hữu và GET /media/:id/download-url

## Context

ADR-06: ai lấy được link file = ai xem được chứng từ. Module media không được import service chứng
từ (vòng phụ thuộc), nên chứng từ tự đăng ký reader vào registry, theo mẫu
`EntityRegistryService.registerEntity` của nền tảng CRUD. Registry, service và controller đã có stub
đăng ký và export sẵn trong module `@Global()` (T-01-02); ticket này không sửa `media.module.ts`.

## Implementation notes

- `MediaOwnerReaderRegistry`: `register(ownerType, reader: (ownerId: string, actor: ActorContext) => Promise<boolean>)`;
  đăng ký trùng `ownerType` → throw ngay lúc boot. `get(ownerType)`.
- `MediaDownloadService.getDownloadUrl(id, actor)`:
  1. Tìm theo `id` + `organizationId`, `status = ATTACHED`; không có → 404 `MEDIA_NOT_FOUND`.
  2. `policy.bucket === 'public'` → `{ url: mediaQuery.publicUrlFor(summary), expiresAt: null }` (null có hai nghĩa: storage chưa cấu
     hình → 503 `STORAGE_UNAVAILABLE`; còn lại, tức key sai hình dạng → 404 `MEDIA_NOT_FOUND`, để key hỏng không
     trông như sự cố storage; dựng `MediaSummary` bằng mapper export từ `media-query.service.ts`),
     không kiểm quyền đọc. Quyết định
     "công khai" bằng **bucket**, không bằng mảng quyền rỗng: `hasAnyPermission` trả `false` cho mảng rỗng
     (`rbac.service.ts:61`), và nếu sau này "mảng rỗng = bỏ qua kiểm tra" thì một loại riêng tư lỡ để trống
     quyền đọc sẽ lộ file cho mọi người trong tổ chức (security review T-01-02, 2026-09-13).
  3. Riêng tư: `rbac.hasAnyPermission(..., policy.readPermissions)`; sai → 403.
  4. Riêng tư: không có reader → 404 và log warn (quên đăng ký sẽ lộ ra ngay ở test); `reader(ownerId, actor)`
     trả `false` → 404.
  5. `mediaQuery.signReadUrl(summary, actor.organizationId, 'attachment')` → `{ url, expiresAt }` (hàm này tự
     từ chối bucket khác bucket riêng tư hoặc key không mang `org/<organizationId>/` — review T-01-06).
- Controller `GET /media/:id/download-url`, `@Header('Cache-Control', 'no-store')`, không
  `@Public()`. `EMPLOYEE_PROFILE` không cần reader vì `photoUrl` đã ký sẵn trong response.
- **Client sinh tự động** (quyết định G3): chỉ chạy `pnpm openapi:generate` sau khi rebase lên main,
  ngay trước khi merge. Không sinh lại cùng lúc với T-01-04, T-01-07, T-03-02 hay partner-catalog T-05-03.

## Done when

- [x] Unit: tổ chức khác → 404; chưa `ATTACHED` → 404; thiếu quyền đọc → 403 — `media-download.service.spec.ts`: "returns 404 when the media belongs to a different organization (no row found)" và "returns 404 when the media is not yet ATTACHED (no row found)" (mỗi test khẳng định `findOne` được gọi với `organizationId` / `status: ATTACHED`), "answers 403 with a generic message (no ownerType)…" và "answers 403 as a ForbiddenException" đều pass (`pnpm -s test -- media-download media-query`, 46/46, sau vòng sửa theo code review + security review 2026-09-14)
- [x] Unit: không có reader → 404 và có log warn; reader trả `false` → 404 — "answers 404, logs organizationId/mediaId/ownerType, and never the object key, when no reader is registered", "answers 404 when the registered reader returns false", và thêm "answers 404 without calling the reader when the ATTACHED row has a null ownerId" (security review, Low #1) đều pass
- [x] Unit: media công khai → `expiresAt: null`; riêng tư → link có hạn 15 phút — "returns the public URL with expiresAt: null, never checking permission, reader or signReadUrl" và "signs a link expiring ATTACHMENT_URL_EXPIRES_SEC from now when the reader allows the actor" (hằng số export từ `media-query.service.ts` = 15 × 60; không còn đọc `X-Amz-Expires` từ URL) đều pass
- [x] Unit: đăng ký trùng `ownerType` → throw — `MediaOwnerReaderRegistry` › "throws immediately on a duplicate registration for the same ownerType" passes
- [x] Response có header `Cache-Control: no-store` — `MediaDownloadController` › "answers Cache-Control: no-store on GET /media/:id/download-url" reflects `HEADERS_METADATA` on the route handler and passes
- [x] `schema.ts` + `openapi.snapshot.json` được sinh lại **sau khi rebase lên main**, không song song với ticket khác cũng sinh lại
  - **Chuyển sang trước merge (quyết định của người dùng 2026-09-14):** chưa sinh lại lần nào cho endpoint này
    (`GET /media/{id}/download-url` chưa có trong client). Theo dõi ở mục "Trước merge" trong definition of done của
    UOW-04. T-04-06 gọi endpoint qua `erpApi` với kiểu viết tay nên không chờ client sinh lại.
