---
id: T-04-04
uow: UOW-04
title: Phiếu thu và phiếu chi tiền mặt — đồng bộ attachmentIds, trả attachments, đăng ký reader
layer: service
type: feature
estimate: 4h
status: done
depends_on: [T-01-05, T-01-06, T-04-01]
blocks: [T-04-08, T-04-09]
verifies: [AC-14, AC-16]
tests:
  - apps/api/src/modules/accounting/cash-vouchers/cash-receipts/cash-receipts.service.spec.ts
  - apps/api/src/modules/accounting/cash-vouchers/cash-payments/cash-payments.service.spec.ts
touches:
  - apps/api/src/modules/accounting/cash-vouchers/cash-receipts/cash-receipts.service.ts
  - apps/api/src/modules/accounting/cash-vouchers/cash-payments/cash-payments.service.ts
  - apps/api/src/modules/accounting/cash-vouchers/cash-receipts/cash-receipts.service.spec.ts
  - apps/api/src/modules/accounting/cash-vouchers/cash-payments/cash-payments.service.spec.ts   # new; theo cash-receipts.service.spec.ts
assumptions: [A-14, A-16, A-26]
---

# T-04-04 — Phiếu thu và phiếu chi tiền mặt — đồng bộ attachmentIds, trả attachments, đăng ký reader

## Context

Hai service gần như đối xứng. Cả hai đã lưu `attachmentIds` khi tạo (`cash-receipts.service.ts:195`,
`cash-payments.service.ts:191`) và khi sửa (`:288`, `:284`, dạng `dto ?? existing`). Luật sửa nằm ở
`assertEditable` (`apps/api/src/modules/accounting/cash-vouchers/shared/editable-voucher.util.ts:43-66`),
gọi ở `:218` và `:214`: từ chối chứng từ không phải nhập tay, đã xoá, đã đảo.

## Implementation notes

- **Trước khi code**: đếm `attachment_ids <> '[]'::jsonb` trên `cash_receipts` và `cash_payments` ở DB
  local (A-16), ghi vào Done when; > 0 thì `aidlc block` và báo.
- Tạo: sau khi có id, `syncOwner('CASH_RECEIPT' | 'CASH_PAYMENT', id, dto.attachmentIds, actor, manager)`,
  ghi lại `attachmentIds` từ mảng trả về.
- Sửa: gọi **sau** `assertEditable`; giữ ngữ nghĩa `dto ?? existing` — không gửi `attachmentIds` thì
  truyền `undefined` để không đổi gì.
- Detail (entity + `sourceLink`/`linkedVoucher`, `:814-822`): thêm `attachments`.
- `onModuleInit`: đăng ký reader bằng đúng hàm đọc của endpoint detail.
- `cash-payments.service.spec.ts` chưa tồn tại: tạo theo cách `cash-receipts.service.spec.ts` dựng module test.

## Done when

- [x] Số dòng `attachment_ids <> '[]'` đã ghi — cash_receipts: 0, cash_payments: 0 (đếm trên `erp_dev` trước khi sửa code, đúng A-16)
- [x] Unit (cả hai service): tạo với id hợp lệ → `ATTACHED`; id tổ chức khác → 404 — "create — attachments (AC-14)" (cả hai service): `syncOwner` chạy trước `docNumbering.generate`/`cashService.recordMovement` (khẳng định bằng `invocationCallOrder`, sau bản sửa security review chuyển sync lên trước khi đúc số/hạch toán) rồi lưu mảng trả về; `MediaException(404)` từ `syncOwner` (id tổ chức khác) làm `create()` reject **trước khi** đúc số/hạch toán/ghi `attachmentIds` — pass
- [x] Unit: phiếu đã đảo (`REVERSED`) sửa đính kèm → bị `assertEditable` từ chối, `syncOwner` không được gọi (AC-16) — "update — attachments (AC-14, AC-16)" › REVERSED, non-manual (saga), stale revision, đã xoá — cả 4 nhánh từ chối của `assertEditable`/`assertRevisionMatches` đều chặn trước `syncOwner`, cho cả hai service — pass
- [x] Unit: sửa không gửi `attachmentIds` → danh sách không đổi — "update — attachments (AC-14, AC-16)" › "leaves the list unchanged when attachmentIds is omitted" (cả hai service) pass
- [x] Unit: detail có `attachments`; reader đã đăng ký cho cả hai loại — "getById — attachments (AC-14)" (map summary → `{id, fileName, contentType, size}`, mảng rỗng khi không có media), "onModuleInit — media reader" (đăng ký đúng `MediaOwnerType`, reader trả `true` khi `getById` thành công, `false` khi `NotFoundException`/`ForbiddenException`, rethrow lỗi khác) và "denies a branchless actor without ever calling getById" (mirror `BranchScopeGuard`: thiếu `branchId`/`branchId` không nằm trong `branchIds` → `false`, không gọi `getById`) đều pass cho cả hai service

**Vòng sửa theo security review (2026-09-14):**
- Khôi phục `cash-payments.service.spec.ts` về đúng nội dung tại HEAD (26 test) rồi mới thêm các khối test đính kèm/media lên trên — không mất/làm yếu test nào; `cash-receipts.service.spec.ts` được xác nhận qua `git diff HEAD --stat` chỉ có insertions (0 deletions).
- Đổi thứ tự `create()`: mint `voucherId` bằng `randomUUID()`, `syncOwner` chạy trước, chỉ sau đó mới gọi `createAndPostInternalInTx` (đúc số PT/PC, hạch toán). `insertPostedVoucher` nhận `args.id` tuỳ chọn và luôn ghi `attachmentIds: []` (bỏ trường `attachmentIds` khỏi `CashReceiptCreateAndPostArgs`/`CashPaymentCreateAndPostArgs` — không caller nào khác dùng, đã `grep` xác nhận).
- Reader của cả hai service chặn `!actor.branchId || !actor.branchIds?.includes(actor.branchId)` trước khi gọi `getById`, mirror đúng `BranchScopeGuard` (`@RequireBranchScope()` áp ở cấp controller, bao trùm cả `GET :id`).
- `setup()` trong cả hai spec nhận thêm tham số tuỳ chọn `dataSourceManager` (mặc định = `manager`, không đổi hành vi test cũ); các test create/update-attachments mới truyền một manager riêng biệt cho `dataSource.manager` để khẳng định "dùng đúng transaction manager" có thể fail thật.
- Kết quả: `pnpm -s test -- cash-vouchers`: 16 suites, 253/253 pass. Riêng hai spec của ticket: `pnpm -s test -- cash-receipts.service.spec cash-payments.service.spec`: 2 suites, 89/89 pass (receipts 51, payments 38 — payments ≥ 26 test gốc tại HEAD + 12 test mới). `tsc --noEmit` không có lỗi ở `accounting/cash-vouchers`.
