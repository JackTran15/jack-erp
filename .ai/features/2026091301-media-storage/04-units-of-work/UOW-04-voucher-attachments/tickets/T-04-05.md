---
id: T-04-05
uow: UOW-04
title: Phiếu thu và phiếu chi ngân hàng — đồng bộ attachmentIds, trả attachments, đăng ký reader
layer: service
type: feature
estimate: 4h
status: done
depends_on: [T-01-05, T-01-06, T-04-01]
blocks: [T-04-08]
verifies: [AC-14]
tests:
  - apps/api/src/modules/accounting/deposit-vouchers/bank-receipts/bank-receipts.service.spec.ts
  - apps/api/src/modules/accounting/deposit-vouchers/bank-payments/bank-payments.service.spec.ts
touches:
  - apps/api/src/modules/accounting/deposit-vouchers/bank-receipts/bank-receipts.service.ts
  - apps/api/src/modules/accounting/deposit-vouchers/bank-payments/bank-payments.service.ts
  - apps/api/src/modules/accounting/deposit-vouchers/bank-receipts/bank-receipts.service.spec.ts   # new; theo cash-receipts.service.spec.ts
  - apps/api/src/modules/accounting/deposit-vouchers/bank-payments/bank-payments.service.spec.ts   # new; theo cash-receipts.service.spec.ts
assumptions: [A-14, A-16, A-26]
---

# T-04-05 — Phiếu thu và phiếu chi ngân hàng — đồng bộ attachmentIds, trả attachments, đăng ký reader

## Context

Cùng khuôn với T-04-04. Tạo lưu `attachmentIds` ở `bank-receipts.service.ts:208` và
`bank-payments.service.ts:221`; sửa ở `:301` và `:323`; `assertEditable` gọi ở `:231` và `:244`.
Trạng thái có thêm `PENDING_APPROVAL` (`deposit-vouchers/enums.ts:7-12`) — không xử lý riêng, đi theo
đúng luật sửa sẵn có (A-26). Chưa có spec cho hai service này.

## Implementation notes

- **Trước khi code**: đếm `attachment_ids <> '[]'::jsonb` trên `bank_receipts` và `bank_payments`
  (A-16), ghi vào Done when; > 0 thì `aidlc block`.
- Tạo và sửa: như T-04-04 với `BANK_RECEIPT` / `BANK_PAYMENT`; sửa gọi `syncOwner` sau `assertEditable`.
- Detail (`bank-receipts` entity `:870-877`, `bank-payments` entity `:960-967`): thêm `attachments`.
- `onModuleInit`: đăng ký reader bằng đúng hàm đọc của endpoint detail.
- Hai spec mới dựng module test theo `cash-receipts.service.spec.ts`; chỉ phủ phần đính kèm.

## Done when

- [x] Số dòng `attachment_ids <> '[]'` đã ghi — bank_receipts: 0, bank_payments: 0 (psql trên erp_dev, 2026-09-14)
- [x] Unit (cả hai service): tạo với id hợp lệ → `ATTACHED`; id tổ chức khác → 404 — `bank-receipts.service.spec.ts` / `bank-payments.service.spec.ts` > `attachments (T-04-05, AC-14)` > "create: syncs media..." và "create: an attachment id from another organization rejects with 404 and nothing is persisted"
- [x] Unit: sửa chứng từ bị `assertEditable` từ chối → `syncOwner` không được gọi — cùng describe block > "update: a voucher assertEditable rejects never reaches syncOwner" (cả hai service)
- [x] Unit: detail có `attachments`; reader đã đăng ký cho cả hai loại — "getById returns attachments copied from MediaQueryService..." + describe `media reader (ADR-06)` (cả hai service)
