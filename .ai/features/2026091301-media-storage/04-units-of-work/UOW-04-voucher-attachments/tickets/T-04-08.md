---
id: T-04-08
uow: UOW-04
title: Bốn dialog chứng từ quỹ và ngân hàng dùng danh sách đính kèm thật
layer: web
type: feature
estimate: 4h
status: done
depends_on: [T-04-04, T-04-05, T-04-06]
blocks: []
verifies: [AC-14]
tests: []
touches:
  - apps/backoffice-web/src/pages/treasury/documents/receipt-voucher-dialog/ReceiptVoucherDialog.tsx
  - apps/backoffice-web/src/pages/treasury/documents/payment-voucher-dialog/PaymentVoucherDialog.tsx
  - apps/backoffice-web/src/pages/treasury/documents/deposit-payment-voucher-dialog/DepositPaymentVoucherDialog.tsx
  - apps/backoffice-web/src/pages/treasury/documents/deposit-receipt-voucher-dialog/DepositReceiptVoucherDialog.tsx
  - apps/backoffice-web/src/pages/treasury/cash-vouchers.types.ts
  - apps/backoffice-web/src/pages/treasury/bank-vouchers.types.ts
  - apps/backoffice-web/src/components/media/MediaAttachmentList.tsx   # thêm prop tuỳ chọn onUploadingChange để dialog khoá nút lưu khi đang tải file; T-04-06 chưa có tín hiệu này (bổ sung lúc implement 2026-09-14)
  - apps/backoffice-web/src/pages/treasury/ledger-cash/ledger-cash.types.ts   # 2 dialog quỹ đi qua LedgerCashVoucherDetail, chưa có attachments/attachmentIds (bổ sung lúc implement 2026-09-14)
  - apps/backoffice-web/src/pages/treasury/cash-vouchers.adapters.ts   # nạp attachments từ detail API vào LedgerCashVoucherDetail
  - apps/backoffice-web/src/pages/treasury/cash-vouchers.api-body.ts   # chuyển attachmentIds sang body tạo/sửa phiếu thu/chi
  - apps/backoffice-web/src/pages/treasury/cash-vouchers.api-body.test.ts   # test sẵn có của api-body; chỉ thêm, không bớt
  - apps/backoffice-web/src/pages/treasury/documents/_shared/voucher-dialog.utils.ts   # buildReceiptDetailFromForm/buildPaymentDetailFromForm mang attachmentIds
  - apps/backoffice-web/src/pages/treasury/cash/receipts-expenses/TreasuryCashReceiptsPage.tsx   # trang host gọi adapter khi mở và lưu
  - apps/backoffice-web/src/pages/treasury/ledger-cash/LedgerCashPage.tsx   # host thứ hai gọi cùng adapter
assumptions: [A-14, A-26]
---

# T-04-08 — Bốn dialog chứng từ quỹ và ngân hàng dùng danh sách đính kèm thật

## Context

Hàng "Tài liệu đính kèm" bị disable, dùng `FormField`, nằm trong `generalInfo`:
`ReceiptVoucherDialog.tsx:861-867`, `PaymentVoucherDialog.tsx:1096-1102`,
`DepositPaymentVoucherDialog.tsx:1104-1107`, `DepositReceiptVoucherDialog.tsx:866-869`. Kiểu đã khai
`attachmentIds` nhưng không dialog nào gửi: `cash-vouchers.types.ts:128,154,333,388`,
`bank-vouchers.types.ts:81,120,159,184`.

## Implementation notes

- Thay đúng hàng disable bằng `<MediaAttachmentList ownerType=... />` (T-04-06):
  `CASH_RECEIPT`, `CASH_PAYMENT`, `BANK_PAYMENT`, `BANK_RECEIPT` theo thứ tự 4 dialog trên.
- Nạp `attachments` từ detail khi mở; gửi `attachmentIds` trong payload tạo và sửa.
- `readOnly` theo đúng điều kiện mà dialog đang dùng để khoá các trường khác — chứng từ đã đảo,
  không phải nhập tay, đã xoá (luật `assertEditable`, `editable-voucher.util.ts:43-66`).
- Thêm kiểu `attachments` vào response detail ở hai file types.
- Khoá nút lưu khi đang tải file.

## Done when

- [x] `pnpm --filter @erp/backoffice-web build` xanh — 2026-09-14: sau khi coordinator bổ sung
  `touches` cho luồng cash, chạy lại `pnpm --filter @erp/backoffice-web build` → exit 0,
  `✓ built in 5.00s` (no other web ticket running concurrently).
- [x] Mỗi dialog: đính kèm 1 file, lưu, mở lại thấy file, tải về được (AC-14) — nay đã nối đủ 4
  - **Chuyển sang trước merge (quyết định của người dùng 2026-09-14):** chưa thử trong trình duyệt (cần phiên đăng nhập có quyền ghi và đọc phiếu thu/chi tiền mặt và ngân hàng). Theo dõi ở mục "Trước merge — trình duyệt" trong DoD của UOW-04. `readOnly` của cả 4 dialog là `mode === VIEW`, cùng biến khoá các trường khác.
  dialog (CASH_RECEIPT, CASH_PAYMENT, BANK_PAYMENT, BANK_RECEIPT), kể cả 2 dialog quỹ tiền mặt sau
  khi `touches` được mở rộng. Cần trình duyệt đăng nhập — coordinator kiểm tra ở pre-merge.
- [x] Chứng từ đã đảo hiển thị danh sách chỉ đọc — cần trình duyệt đăng nhập, coordinator kiểm tra
  - **Chuyển sang trước merge (quyết định của người dùng 2026-09-14):** chưa thử trong trình duyệt (cần phiên đăng nhập có quyền ghi và đọc phiếu thu/chi tiền mặt và ngân hàng). Theo dõi ở mục "Trước merge — trình duyệt" trong DoD của UOW-04. `readOnly` của cả 4 dialog là `mode === VIEW`, cùng biến khoá các trường khác.
  ở pre-merge.

### Scope gap 2026-09-14 (đã giải quyết)

Ghi chú trước đó (giữ nguyên `ReceiptVoucherDialog.tsx`/`PaymentVoucherDialog.tsx` vì luồng
`LedgerCashVoucherDetail` → `cash-vouchers.api-body.ts` không nằm trong `touches`) đã được
coordinator giải quyết bằng cách bổ sung `ledger-cash.types.ts`, `cash-vouchers.adapters.ts`,
`cash-vouchers.api-body.ts`, `cash-vouchers.api-body.test.ts`,
`documents/_shared/voucher-dialog.utils.ts`, `TreasuryCashReceiptsPage.tsx`, `LedgerCashPage.tsx`
vào `touches`. Đã nối đủ end-to-end trong lần implement này — chi tiết trong báo cáo cho
coordinator (per-dialog owner type, điều kiện `readOnly`, đường đi create/update, và danh sách
sub-mode ẩn đính kèm vì body đích không có `attachmentIds`).

`cash-vouchers.api-body.test.ts` không chạy được trong môi trường này: `apps/backoffice-web`
không có `vitest` cài đặt (`node_modules/.bin/vitest` không tồn tại ở root lẫn app, và
`tsconfig.json` của app loại `*.test.ts` khỏi việc `tsc` kiểm tra kiểu), nên các test case mới
thêm (`attachmentIds` create/update) chưa được thực thi hay type-check tự động — chỉ được rà soát
thủ công khớp với `ledgerDetailToCreateReceiptBody`/`ledgerDetailToCreatePaymentBody`.
