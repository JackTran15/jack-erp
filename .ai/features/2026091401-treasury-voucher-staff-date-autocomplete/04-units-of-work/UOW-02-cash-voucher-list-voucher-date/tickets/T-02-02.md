---
id: T-02-02
uow: UOW-02
title: Regenerate api-client sau khi đổi CashVoucherSearchV2Dto
layer: api
type: chore
estimate: 1h
status: done
depends_on: [T-02-01]
blocks: []
verifies: [AC-14]
tests: []
touches:
  - packages/api-client/openapi.snapshot.json
  - packages/api-client/src/generated/schema.ts
assumptions: [A-11]
---

# T-02-02 — Snapshot OpenAPI và schema sinh ra

## Context

`CashVoucherSearchV2Dto` có mặt trong `packages/api-client/src/generated/schema.ts` (3 chỗ). CLAUDE.md yêu cầu sau khi
đổi hợp đồng API thì chạy API và `pnpm openapi:generate`, commit cả snapshot lẫn `schema.ts`, không sửa tay file sinh
ra. Web hiện gọi route này bằng `apiClient.post` với kiểu cục bộ, nên T-02-03 không phụ thuộc ticket này — nhưng
snapshot phải khớp hợp đồng thật để lần generate sau không làm hỏng người khác.

Verifies AC-14 theo nghĩa hẹp: body xuất danh sách dùng cùng DTO, và DTO trong client sinh ra phải là DTO mới.

## Implementation notes

- `make dev-api` (API ở :4000, đã có code T-02-01), rồi `pnpm openapi:generate`.
- Diff chỉ nên đổi `createdAt` → `voucherDate` trong `CashVoucherSearchV2Dto` (và docblock). Có thay đổi ngoài phạm vi
  (do nhánh khác chưa generate) thì ghi lại trong summary, không tự dọn.

## Done when

- [x] `rtk proxy grep -n "voucherDate" packages/api-client/src/generated/schema.ts` thấy trường trong `CashVoucherSearchV2Dto`
- [x] Schema của `CashVoucherSearchV2Dto` không còn trường `createdAt`
- [x] `pnpm build:shared` xanh
- [x] Diff của hai file được đọc lại; thay đổi ngoài `CashVoucherSearchV2Dto` (nếu có) được ghi vào summary

## Summary

Chạy 14/09/2026 trên API dev (:4000) của nhánh `feat/treasury-voucher-staff-date-autocomplete`.

- `CashVoucherSearchV2Dto` (snapshot và `schema.ts`): có `voucherDate?: DateRangeFilterDto`, không còn `createdAt`.
  Các trường còn lại không đổi.
- `pnpm build:shared` exit 0.
- **Snapshot trên `main` đã cũ trước feature này.** So snapshot của HEAD với bản mới sau khi chuẩn hoá bằng `jq -S`:
  paths 493 → 527, schemas 488 → 554, không path nào bị xoá. Phần lệch ngoài phạm vi, **không** do nhánh này:
  - 34 path `/mobile/*` mới (vd `/mobile/inventory/products`, `/mobile/manager/invoices`) và 5 path `/mobile/*` đổi
    (`/mobile/customers`, `/mobile/invoices`, `/mobile/product-revenue`, `/mobile/products`, `/mobile/suppliers`) — từ PR
    mobile-integration (#268) đã merge mà chưa generate.
  - Schema mới ngoài Mobile*: `CancelSalesOrderDto`, `CreateSalesOrderDto`, `RejectSalesOrderDto`, `SalesOrderLineDto`.
  - Schema đổi: `CreateDocumentNumberRuleDto`, `DocumentNumberRuleEntity`, `GenerateDocumentNumberDto`,
    `InvoiceReportFilterDto`, `InvoiceSearchV2Dto`, `MobileCustomerPageDto`, `MobileCustomerResponseDto`,
    `SalesmanAssignmentEntity`.
  - Git diff thô: +27 829 / −20 364 dòng trên hai file. Không dọn tay (CLAUDE.md cấm sửa file sinh ra); khi commit nên
    tách phần này thành commit riêng hoặc để người giữ `api-client` quyết định.
