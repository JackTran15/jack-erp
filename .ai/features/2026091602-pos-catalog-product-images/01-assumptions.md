---
feature: pos-catalog-product-images
blocking_open: 0
---

# Assumption register

Ba câu hỏi được hỏi một lượt qua AskUserQuestion ngày 2026-09-16 (A-01, A-02, A-05); phần
còn lại được trả lời bằng đọc code hoặc bằng chứng đã có của media-storage.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Dialog chọn biến thể chỉ cần **ảnh đầu tiên** (`imageUrl` đã có); ô "Xem" mở đúng một ảnh cỡ lớn, không có gallery. Không đổi contract hai endpoint, không chạy `openapi:generate` | high | yes | Muốn gallery: thêm `images: string[]` vào `PosProductDetailDto` + service + snapshot + api-client — thành feature BE riêng, FE chỉ đổi lightbox nhận mảng | resolved | Akenzy chọn "Single image, FE-only" 2026-09-16 qua AskUserQuestion |
| A-02 | Chỉ màn **Bán hàng** (lưới catalog + dialog chọn biến thể). Picker của `FastStockTransferPage` dùng chung endpoint danh sách nhưng có `CatalogProduct` riêng (`interfaces/fast-stock-transfer.interface.ts`) và không đụng | high | yes | Nếu cần cả picker chuyển kho: thêm 1 ticket FE tương tự T-01-01 cho `use-fast-stock-transfer-data.ts` + card của nó | resolved | Akenzy chọn "Checkout only" 2026-09-16 qua AskUserQuestion |
| A-03 | Không vẽ thumbnail theo từng dòng trong `VariantTable`: `variants[].imageUrl` luôn là ảnh của mẫu mã cha (`pos-catalog-product.service.ts:641-661` gán cùng một `imageUrl` cho mọi biến thể; A-08 của media-storage) | high | no | Nếu về sau có ảnh theo màu/size, bảng biến thể cần cột ảnh — ngoài phạm vi | confirmed | Đọc `pos-catalog-product.service.ts:641-643` — `resolvePublicUrls([product.id])` một lần, chia cho mọi biến thể |
| A-04 | Ở production ảnh chỉ hiện khi nginx đã mở đường tới bucket công khai (T-05-02 của media-storage đang `blocked`). Feature này **không chờ** việc đó: URL chết → `onError` rơi về placeholder túi xám, người dùng thấy đúng như hôm nay | high | no | Nếu `MEDIA_PUBLIC_BASE_URL` prod trỏ sai host, mọi card là placeholder — không hỏng gì, chỉ chưa có ảnh; sửa env là xong | confirmed | `apps/api/.env.example:51-55` — URL công khai là tuyệt đối, cấu hình bằng env; `aidlc flow` media-storage: T-05-02 blocked 9.9d |
| A-05 | Card lưới: ảnh phủ kín ô 120px bằng `object-cover` (cắt giữa), badge giá vẫn đè góc dưới-trái | high | no | Nếu người dùng muốn thấy trọn ảnh: đổi một class sang `object-contain` | resolved | Akenzy chọn "object-cover" 2026-09-16 qua AskUserQuestion |
| A-06 | `<img src>` từ origin POS (`:3001` local, `jack-erp-pos.ducanhzed.com` prod) tải ảnh cross-origin từ `MEDIA_PUBLIC_BASE_URL` (`:3000/erp-media-public/...` local) mà không cần CORS: thẻ `<img>` không có `crossorigin` là "no-cors", trình duyệt không chặn | high | no | Nếu bucket trả `Content-Type` sai hoặc yêu cầu auth, ảnh không tải → placeholder (A-04); không có lỗi JS | confirmed | pos-web `vite.config.ts` không có proxy nào; media-storage đã chứng minh backoffice (`:3000`) hiện ảnh qua cùng URL (evidence S3/S12 của `2026091601-product-image-utilities`) |
| A-07 | `PosDialog` lồng trong `PosDialog` (ảnh cỡ lớn mở trên dialog chọn biến thể) chạy đúng: Esc chỉ đóng lớp trên cùng, đóng ảnh trả focus về ô "Xem", dialog chọn biến thể còn nguyên state | high | no | Nếu Esc đóng cả hai: đổi lightbox thành overlay inline trong `PosDialog.Body` của dialog chọn biến thể thay vì `PosDialog` thứ hai | confirmed | Radix `DismissableLayer` (`@radix-ui/react-dismissable-layer@1.1.12/dist/index.mjs:60-62`): `isHighestLayer` mới nhận `onEscapeKeyDown`; `PosDialog.tsx:115-124` `onCloseAutoFocus` trả focus về `previousFocusRef` (phần tử đang focus lúc mở) |
| A-08 | Fixture của media-storage đủ để demo: `AAA-MEDIA-A` (mẫu mã, 3 ảnh), `AAA-MEDIA-B` (hàng lẻ, 1 ảnh), `AAA-MEDIA-C` (không ảnh) có mặt trong `GET /pos/branches/{HCM}/catalog/products?search=AIDLC` với `imageUrl` đúng | high | no | Nếu fixture bị xoá: chạy lại `verify-fixtures.py --reset` của media-storage trước khi verify | confirmed | `verify-fixtures.py:305-306` (AC-05 của media-storage) đã kiểm chính query này; `evidence/api-checks.json` ghi kết quả |
| A-09 | Ô chưa có ảnh (`imageUrl === null`) giữ nguyên `ShoppingBagIcon` trên nền xám như hôm nay — không thêm chữ "Chưa có ảnh", không đổi màu nền | high | no | Thuần thẩm mỹ; đổi sau nếu muốn | confirmed | Ảnh 1 và 2 trong yêu cầu là trạng thái hiện tại; yêu cầu chỉ là "hiển thị ảnh", không đổi placeholder |
| A-10 | Không có skeleton/shimmer khi ảnh đang tải: nền xám sẵn có của ô chính là trạng thái tải; ảnh `loading="lazy"` hiện dần | high | no | Nếu thấy nhấp nháy: thêm `opacity` transition sau `onLoad` — 2 dòng | confirmed | Quyết định thiết kế, ghi ở `03-logical-design.md` ADR-01 |
| A-11 | Ô "Xem" khi **không** có ảnh không mở gì (không phải mở dialog trống); khi có ảnh là `<button>` mở ảnh cỡ lớn. Nhãn "Xem" giữ nguyên ở cả hai trạng thái để bố cục không nhảy | medium | no | Nếu chủ sở hữu muốn ẩn "Xem" khi không có ảnh: đổi một điều kiện render | confirmed | Suy từ Sapo (ô "Xem" là nút xem ảnh sản phẩm); Akenzy đã chọn phương án có lightbox (A-01) nên "Xem" phải làm gì đó khi có ảnh |
| A-12 | Verify trình duyệt chạy được trên env `local-pos` với dữ liệu `erp_dev`: `post_login` ghim chi nhánh theo `${LOCAL_POS_BRANCH_ID}`, và fixture media-storage nằm ở Hồ Chí Minh `c3bf1922-3a2e-42d9-b00d-a7129efe592c` (org `f1000000-…-0001`) | high | no | Giá trị hiện tại trong `.ai/credentials.env` là `20000000-0000-4000-8000-000000000003` — **không tồn tại** trong `erp_dev`, `erp_dev_3008` hay `erp_test` (đo 2026-09-16 qua `docker exec erp-postgres psql`), nên radio chọn chi nhánh không khớp và phiên bị chụp với `branchId` null. T-01-03 phải đặt lại biến này về id HCM trước khi `capture-session.py`; file không commit nên chỉ là việc của máy chạy verify | confirmed | Đo trực tiếp trên `erp_dev` 2026-09-16: `branches` có `c3bf1922-…` = "Hồ Chí Minh"; fixture `AAA-MEDIA-A` (2 ảnh hiện tại, 3 sau `--reset`), `AAA-MEDIA-B` (1 ảnh), `AAA-MEDIA-C` (0 ảnh) đều có mặt |
