---
id: T-01-03
uow: UOW-01
title: 07-verification.md + evidence trên local-pos
layer: test
type: test
estimate: 1h
status: done
depends_on: [T-01-01, T-01-02]
blocks: []
verifies: [AC-01, AC-02, AC-04, AC-05, AC-06]
touches:
  - .ai/features/2026091602-pos-catalog-product-images/07-verification.md   # new
  - .ai/features/2026091602-pos-catalog-product-images/capture-pos-session.py   # new — chụp phiên local-pos (recipe form của runner không điền được form 3 ô)
tests: []
assumptions: [A-08, A-12]
---

# T-01-03 — 07-verification.md + evidence trên local-pos

## Context

pos-web không có bộ chạy test (`"test": "echo test"`), nên bằng chứng cho AC-01/02/04/05/06 là
`aidlc-verify` trên env `local-pos` (`.ai/aidlc.yaml` → `verify.environments.local-pos`,
`post_login` ghim chi nhánh theo `${LOCAL_POS_BRANCH_ID}`). Fixture là của media-storage
(`AAA-MEDIA-A/B/C`, HCM `c3bf1922-3a2e-42d9-b00d-a7129efe592c` trên `erp_dev`).
`LOCAL_POS_BRANCH_ID` hiện trỏ `20000000-…-0003`, không tồn tại trong `erp_dev` (A-12).

## Implementation notes

- Đặt `LOCAL_POS_BRANCH_ID=c3bf1922-3a2e-42d9-b00d-a7129efe592c` trong `.ai/credentials.env`
  (không commit); API chạy `DB_NAME=erp_dev`; MinIO local chạy; POS `:3001`.
- Trước khi chạy: `python3 .ai/features/2026091301-media-storage/verify-fixtures.py --reset`
  rồi `~/.venvs/aidlc-verify/bin/python .ai/capture-session.py` (nếu script chỉ chụp
  backoffice, chụp POS theo cách runner `form` của `local-pos` — xem comment trong `aidlc.yaml`).
- `07-verification.md` frontmatter `environments: [local-pos]`, `viewports: [desktop]`. Steps
  (selector kiểm lại trên DOM thật trước khi ghi):
  - S1 — tìm `AIDLC`: `count [role="listitem"] = 3; count [role="listitem"] img[src*="/erp-media-public/"] = 2` (AC-01, AC-02)
  - S2 — card A có ảnh **và** badge: `count [role="listitem"]:has(img) :text("750.000")`-kiểu assert theo giá thật của A (AC-01)
  - S3 — bấm card A → `wait [role="dialog"] img[src*="/erp-media-public/"]`; `text=Xem` (AC-04)
  - S4 — bấm `button[aria-label^="Xem ảnh"]` → `count [role="dialog"] = 2`; ảnh lớn `img.object-contain` (AC-05)
  - S5 — `press Escape` (nếu DSL có; nếu không, click nút đóng của dialog trên cùng) → `count [role="dialog"] = 1`; bảng biến thể còn (AC-05)
  - S6 — đóng dialog, bấm card C → `count [role="dialog"] button[aria-label^="Xem ảnh"] = 0`; `text=Xem` (AC-06)
- AC-03 (chặn URL ảnh) không diễn đạt được bằng DSL của runner → ghi kết quả làm bằng tay
  (Demo script bước 6) vào phần "Not verified here" kèm ngày.
- Chạy `aidlc-verify .ai/features/2026091602-pos-catalog-product-images --write` rồi
  `aidlc-evidence …`; `08-evidence.md` phải `status: pass`.
- Đếm request `GET /pos/` trước/sau (tab Network) khi mở màn Bán hàng và khi mở dialog — ghi
  hai cặp số vào ticket này (DoD "không thêm request").

## Done when

- [x] `07-verification.md` có S1–S6 với selector đã kiểm trên DOM thật
  - 2026-09-16: hai selector phải đổi sau lần chạy đầu — ô tìm không dùng được `input[placeholder=…]` vì `fill` cắt ở `=` (dùng `div:has(> :text("TƯ VẤN BÁN HÀNG")) input`); chờ lưới còn đúng 3 card (`:nth-child(3):last-child`) vì `AAA-MEDIA-*` xếp đầu trang chưa lọc; badge đếm theo `span.rounded-full`. Ghi lý do trong file.
- [x] `08-evidence.md` `status: pass`, 6/6 bước trên `local-pos`
  - 2026-09-16: `aidlc-verify --write` → `pass — 6/6 steps · chromium 151.0.7922.34 · commit cd19ec94`; `aidlc-evidence` → PASS, 5/5 AC evidenced, AC-03 out of browser scope. Phiên chụp bằng `capture-pos-session.py` (mới, cùng thư mục) vì `LOCAL_POS_*` trong `credentials.env` trỏ org/chi nhánh không tồn tại (A-12) — **không** sửa `credentials.env`.
- [x] AC-03 ghi kết quả kiểm tay (ngày, kết quả) trong "Not verified here"
  - 2026-09-16: Playwright `page.route` chặn `**/erp-media-public/**` → 3 card, 0 `<img>`, 0 toast; ghi trong `07-verification.md`.
- [x] Số request `GET /pos/` trước/sau ghi trong ticket, bằng nhau
  - 2026-09-16 (Playwright, đếm request tới `:4000`): mở màn Bán hàng + tìm `AIDLC` = **12** request API, tất cả là endpoint có sẵn (`/branches/me`, `/pos/…/catalog/products` ×2, `/catalog/search`, `/salesmen`, `/customers`, `/pos-settings`, `/payment-accounts`, `/admin/users/me`, `/v2/invoices/drafts/search`, `/v2/promotions/evaluate`, `/v2/inventory/item-categories/tree`); mở dialog A = **1** (`GET …/catalog/products/:id`); mở ảnh lớn = **0**. Ảnh: 2 request tới `localhost:3000/erp-media-public/...` **không có header Authorization**; dialog + viewer dùng lại cache trình duyệt (0 request). Diff của feature không thêm `useQuery`/service/`http.` nào nên "trước" không thể khác: mọi request API trên là của code có sẵn.
