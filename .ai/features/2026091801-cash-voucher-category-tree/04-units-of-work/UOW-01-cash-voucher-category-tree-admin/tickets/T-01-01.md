---
id: T-01-01
uow: UOW-01
title: Cột parent_group_id, luật cha/con trong CashVoucherCategoriesService, config field Mục cha + nhãn Thu/Chi
layer: service
type: feature
estimate: 3h
status: done
depends_on: []
blocks: [T-01-02]
verifies: [AC-05, AC-06, AC-09]
tests:
  - apps/api/src/modules/accounting/cash-vouchers/cash-voucher-categories/cash-voucher-categories.service.spec.ts
touches:
  - apps/api/src/database/migrations/1790030000000-AddCashVoucherCategoryParentGroup.ts   # new; matches 1782400000000-ExtendItemCategory.ts
  - apps/api/src/modules/accounting/cash-vouchers/cash-voucher-categories/cash-voucher-category.entity.ts
  - apps/api/src/modules/accounting/cash-vouchers/cash-voucher-categories/cash-voucher-categories.service.ts
  - apps/api/src/modules/accounting/cash-vouchers/cash-voucher-categories/cash-voucher-categories.service.spec.ts   # new; matches inventory/location/item-category-crud.service.spec.ts
assumptions: [A-01, A-02, A-04, A-05, A-08]
---

# T-01-01 — Schema + luật cha/con

## Context

`CashVoucherCategoriesService` (`cash-voucher-categories.service.ts`) hiện là `BaseCrudService` không override gì;
`DeletionPolicy.SOFT`. Khuôn để chép:

- Cột + FK + index: `inventory/location/item-category.entity.ts:26-40` và migration
  `1782400000000-ExtendItemCategory.ts:14-57` (FK `ON DELETE SET NULL`, bọc `DO $$ … EXCEPTION WHEN duplicate_object`).
- `normalizePayload` (''→`null`, **không** `undefined` vì `repository.merge` bỏ qua `undefined`) và `assertNoCycle`
  với tập `seen`: `inventory/location/supplier-group-crud.service.ts:49-128`.
- Hook có sẵn: `beforeCreate(payload, actor)`, `beforeUpdate(id, payload, actor)`, `beforeDelete(id, actor)` —
  `crud/base-crud.service.ts:187-242`; `remove()` gọi `beforeDelete` trước khi xoá mềm (`:146-181`).

## Implementation notes

- Migration `1790030000000-AddCashVoucherCategoryParentGroup`: `ALTER TABLE "cash_voucher_categories" ADD COLUMN
  "parent_group_id" uuid`; FK `FK_cash_voucher_categories_parent_group` → `cash_voucher_categories(id) ON DELETE SET
  NULL`; `CREATE INDEX IDX_cash_voucher_categories_parent_group ON … (parent_group_id)`. `down()` bỏ index, FK, cột.
  Không migration dữ liệu (A-05).
- Entity: `@Column({ name: 'parent_group_id', type: 'uuid', nullable: true }) parentGroupId?: string | null;`
  `@ManyToOne(() => CashVoucherCategoryEntity, { nullable: true }) @JoinColumn({ name: 'parent_group_id' }) parent?`.
- Service:
  - `beforeCreate`: normalize; nếu có `parentGroupId` → `loadParent` (`findOne({ id, organizationId })`, soft-delete
    filter mặc định của TypeORM đã loại dòng xoá mềm) → thiếu ⇒ `BadRequestException('Mục cha không tồn tại')`;
    `parent.direction !== payload.direction` ⇒ `'Mục cha phải cùng loại Thu/Chi'`.
  - `beforeUpdate`: normalize; `parentGroupId === id` ⇒ `'Mục không thể là mục cha của chính nó'`; `loadParent`;
    `assertNoCycle(id, parentGroupId, actor)` ⇒ `'Không thể chọn mục con làm mục cha'`; loại hiệu lực =
    `payload.direction ?? existing.direction`, so với cha; nếu `payload.direction` khác `existing.direction` và
    `hasChildren(id)` ⇒ `'Không thể đổi loại của mục đang có mục con'`.
  - `beforeDelete`: `hasChildren(id)` (đếm dòng `parentGroupId = id`, chưa xoá mềm, cùng org) ⇒
    `'Không thể xóa mục đang có mục con'` (A-04).
  - Thông điệp đúng chuỗi ở bảng Error taxonomy của `03-logical-design.md` (T-01-04 và demo dựa vào chúng).
- `CASH_VOUCHER_CATEGORY_ENTITY_CONFIG`: chèn `{ key: 'parentGroupId', label: 'Mục cha', type: 'relation',
  relationEntity: 'cash-voucher-categories', hideInList: true }` sau `direction`; thêm
  `enumLabels: { IN: 'Thu', OUT: 'Chi' }` vào `direction`. Không đổi permissions/scoping/deletion.
- Spec theo khuôn `item-category-crud.service.spec.ts` (repo mock qua `getRepositoryToken`, `DataSource` mock):
  cha khác org, cha đã xoá mềm, tự làm cha, vòng 2 tầng và 3 tầng, cha khác loại (create và update), đổi loại khi có
  con, xoá khi có con, xoá lá thành công, `''`→`null` đưa lên gốc.

## Done when

- [x] Migration `up` tạo cột/FK/index; `down` bỏ đúng ba thứ; `pnpm migration:generate` sau `migration:run` không sinh câu nào nhắc tới `parent_group_id`, FK hay index mới (AC-09)
  - Tiêu chí gốc là "không ra diff"; Akenzy đổi ngày 18/09/2026 vì repo đã lệch entity ↔ migration từ trước (~4 100 câu trên DB sạch: FK đổi sang tên băm, COMMENT cột `BaseEntity`, UNIQUE ↔ index — cùng kiểu trên `inventory_item_categories`), không thuộc feature này.
  - `up`/`down`/`up` lại trên DB tạm `erp_test_migr` (tất cả migration đã áp): cột `parent_group_id uuid NULL`, FK `ON DELETE SET NULL`, index đúng tên; sau `down` cả ba biến mất (SQL đếm 0/0/0).
  - Sau khi ghim tên index/FK và `onDelete` trên entity, `grep parent_group_id\|FK_cash_voucher_categories_parent_group\|IDX_cash_voucher_categories_parent_group` trên file generate: 0 dòng.
- [x] Spec: 10 trường hợp trên xanh — `pnpm --filter @erp/api test -- cash-voucher-categories.service.spec.ts` (AC-05, AC-06) — 20/20 (18 luật + 2 config), 18/09/2026
- [x] `GET /admin/entities/cash-voucher-categories` trả field `parentGroupId` (relation, hideInList) và `direction.enumLabels` Thu/Chi — `CrudController.getEntityConfig` trả thẳng `CASH_VOUCHER_CATEGORY_ENTITY_CONFIG` (`crud.controller.ts:53-58`), spec assert trên hằng số đó; HTTP thật kiểm ở demo UOW-01 bước 3–4
- [x] `pnpm --filter @erp/api test` toàn bộ xanh; `npx tsc --noEmit -p apps/api/tsconfig.json` sạch — 408 suite / 5 651 test xanh (1 skipped có sẵn), tsc không lỗi, 18/09/2026

## Kết quả 2026-09-18

- Migration `1790030000000-AddCashVoucherCategoryParentGroup`: cột, FK `ON DELETE SET NULL`, index; `down` gỡ cả ba. Không migration dữ liệu.
- Entity: `parentGroupId` + `@ManyToOne parent`; tên index và FK ghim bằng `@Index(...)` và `JoinColumn.foreignKeyConstraintName`, `onDelete: 'SET NULL'` — để `migration:generate` không đòi đổi tên/đổi hành vi FK (TypeORM mặc định sinh `FK_<hash>` và `NO ACTION`).
- Service: `beforeCreate`/`beforeUpdate`/`beforeDelete` theo `03-logical-design.md`; `''`→`null` đã do `BaseCrudService.normalizeBlankValues` lo vì field là `relation`. Thông điệp đúng bảng Error taxonomy.
- Config: field `parentGroupId` (relation, `hideInList`), `direction.enumLabels` Thu/Chi.
- Lệch entity ↔ migration có sẵn của repo: `migration:generate` trên DB sạch sinh ~4 100 câu không liên quan feature này (ghi ở ô 1). DB tạm `erp_test_migr` đã xoá sau khi Akenzy chọn chạy `migration:run` trên `erp_dev_3008` (12 migration, 18/09/2026); cả hai org 9 thu / 34 chi, `parent_group_id` đều null.
- `erp_dev` còn 10 và `erp_dev_3008` còn 12 migration chờ (kể cả migration này) — chưa chạy, chờ Akenzy (Constraints, `00-intent.md`).
