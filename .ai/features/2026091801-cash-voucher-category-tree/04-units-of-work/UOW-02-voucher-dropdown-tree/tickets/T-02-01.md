---
id: T-02-01
uow: UOW-02
title: useCashVoucherCategories lấy từ endpoint cây; 4 dialog thụt lề option theo depth
layer: web
type: feature
estimate: 2.5h
status: todo
depends_on: [T-01-02, T-01-03]
blocks: []
verifies: [AC-08]
tests: []
touches:
  - apps/backoffice-web/src/hooks/treasury/use-cash-voucher-categories.ts
  - apps/backoffice-web/src/pages/treasury/cash-vouchers.types.ts
  - apps/backoffice-web/src/pages/treasury/documents/receipt-voucher-dialog/ReceiptVoucherDialog.tsx
  - apps/backoffice-web/src/pages/treasury/documents/payment-voucher-dialog/PaymentVoucherDialog.tsx
  - apps/backoffice-web/src/pages/treasury/documents/deposit-receipt-voucher-dialog/DepositReceiptVoucherDialog.tsx
  - apps/backoffice-web/src/pages/treasury/documents/deposit-payment-voucher-dialog/DepositPaymentVoucherDialog.tsx
assumptions: [A-03, A-06, A-11, A-12]
---

# T-02-01 — Dropdown theo cây

## Context

`use-cash-voucher-categories.ts:13-44` gọi `GET /admin/entities/cash-voucher-categories/records?pageSize=100&sortBy=
displayOrder&filters={direction}`; `useCategoryNameMap` (`:46-57`) và ba trang lưới/sổ quỹ dựa vào nó.
Bốn dialog render `<select>` thuần: `ReceiptVoucherDialog.tsx:509-536` (Mục thu), `PaymentVoucherDialog.tsx:579-606`
(Mục chi), `DepositReceiptVoucherDialog.tsx` (~`:478`), `DepositPaymentVoucherDialog.tsx:582-608` — option
`{c.name}`; `getValue` hiện tên ở chế độ xem. `flattenCategoryTree` generic có từ T-01-03.

## Implementation notes

- Hook: `erpApi.POST("/v2/cash-voucher-categories/tree", { body: direction ? { direction } : {} })` →
  `flattenCategoryTree(res.data)` → map thành `CashVoucherCategory` kèm `depth: row.__depth`, `parentGroupId`. Giữ
  `queryKey: treasuryQueryKeys.cashVoucherCategories(direction)`, `staleTime: 5 * 60_000`, **không** lọc `isActive`
  (A-11). Export `formatCategoryOptionLabel(c) = " ".repeat((c.depth ?? 0) * 3) + c.name`.
- `cash-vouchers.types.ts`: `CashVoucherCategory` thêm `parentGroupId?: string | null; depth?: number; displayOrder?: number`.
- 4 dialog: chỉ đổi text của `<option>` thành `formatCategoryOptionLabel(c)`; `getValue`, `onChange` (ghi `categoryId` +
  `category` = `c.name` gốc) không đổi. `DepositPaymentVoucherDialog.tsx:495,505` (tên cứng cho rút tiền/chuyển
  cửa hàng) không đụng.

## Done when

- [ ] Phiếu chi tiền mặt / tiền gửi: option con thụt lề dưới cha, chọn được cả hai; lưu và mở lại hiện đúng tên không thụt lề (AC-08)
- [ ] Phiếu thu tiền mặt / tiền gửi: cây mục thu; lưới Thu chi tiền mặt, Thu chi tiền gửi, Sổ quỹ tiền mặt vẫn hiện tên mục phiếu cũ kể cả mục đã tắt (AC-08)
- [ ] Network: dialog gọi `POST /v2/cash-voucher-categories/tree`, không còn `GET …/records` từ hook này
- [ ] `npx tsc --noEmit -p apps/backoffice-web/tsconfig.json` sạch
