---
id: T-01-05
uow: UOW-01
title: e2e geo.e2e-spec.ts — provinces, ranh giới xác thực, đếm dữ liệu
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-01-03, T-01-04]
blocks: [T-02-02]
verifies: [AC-01, AC-02, AC-03, AC-04, AC-11]
tests:
  - apps/api/test/e2e/geo.e2e-spec.ts
touches:
  - apps/api/test/e2e/geo.e2e-spec.ts                 # new; khung theo partner-catalog.e2e-spec.ts
assumptions: [A-01, A-06, A-07]
---

# T-01-05 — e2e provinces

## Context
`resetDatabase()` (`test/e2e/setup/test-app.ts:85`) xoá dữ liệu migration → spec tự nạp bằng
`upsertGeoDataset(ds, dataset)` với dataset đọc từ `src/database/migrations/data/geo-2026/`. Mint API
key theo đúng `partner-catalog.e2e-spec.ts:336-367` (`ensurePermission` + vai "Đối tác" + POST
`/admin/entities/api-keys/records` → `rawKey`). Chạy với `OUTBOX_RELAY_DISABLED=1`.

## Implementation notes
- `beforeAll`: `createTestApp()` → `resetDatabase(app)` → `seedBaseData(...)` → `upsertGeoDataset(app.get(DataSource), readDataset())`
  → mint `partnerKey` cho vai chỉ có `partner.catalog.read`.
- Mô tả theo AC: `describe('AC-01 danh sách')`, … để `aidlc-test-runner` map được.
- AC-01: 35 phần tử; kiểm `data[0].code === '2026_91'` (An Giang — đầu bảng theo tên không dấu) và
  Hà Nội trước Hải Phòng; mỗi phần tử có đủ 5 field, `effectiveFrom === '2026-01-01'`.
- AC-02: `q=ha noi` → `[2026_01]`; `q=%` → `[]`; `q=_` → `[]`.
- AC-03: `2026_79` → `mergedFrom.length === 3`; `1995_02` → 404.
- AC-04, phần provinces: không header → 401 ở `/v2/geo/provinces`; `X-Api-Key` không `X-Branch-Id`
  → 200, `deepEqual` với body JWT. Phần wards của AC-04 thuộc T-02-02 (route chưa tồn tại thì Nest trả
  404 trước khi tới guard, nên assert ở đây sẽ sai lý do).
- AC-11 (phần đếm): `SELECT count(*)` hai bảng qua `DataSource` sau khi nạp = 35 / 14428, `is_current` 3321.
  Phần `migration:run`/`revert` của AC-11 kiểm tay ở T-01-03 (e2e không chạy migration sau reset).

## Done when
- [x] `OUTBOX_RELAY_DISABLED=1 pnpm --filter @erp/api test:e2e -- geo.e2e-spec.ts`: 6/6 ✓ (đọc từng dòng ✓). Phát hiện: `erp_test` thiếu extension `unaccent` (tạo trước migration 1782500000000, `synchronize` không tạo extension) → spec `CREATE EXTENSION IF NOT EXISTS unaccent` trong `beforeAll`
- [x] Mỗi AC-01..04 (provinces) và AC-11 có `it(...)` mang mã AC trong tên
- [x] Không sửa file test nào khác (`git status`: chỉ `geo.e2e-spec.ts` mới)
