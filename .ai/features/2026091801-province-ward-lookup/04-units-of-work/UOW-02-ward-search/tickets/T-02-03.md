---
id: T-02-03
uow: UOW-02
title: Sinh lại @erp/api-client (openapi snapshot + schema.ts) với 4 route geo
layer: api
type: chore
estimate: 1h
status: done
depends_on: [T-02-01]
blocks: []
verifies: [AC-01, AC-05]
tests: []
touches:
  - packages/api-client/openapi.snapshot.json          # sinh bởi pnpm openapi:generate
  - packages/api-client/src/generated/schema.ts        # sinh — không sửa tay
assumptions: [A-04, A-10]
---

# T-02-03 — Sinh lại api-client

## Context
Quy ước repo (CLAUDE.md): sau khi đổi endpoint, chạy API rồi `pnpm openapi:generate`, commit snapshot và
`schema.ts`. Không client nào dùng ngay (A-10) nhưng hợp đồng phải có mặt để feature FE sau dùng được.

## Implementation notes
- API chạy trên :4000 với đủ 4 route (T-02-01 đã xong) → ở gốc repo `pnpm openapi:generate`.
- Kiểm `git diff --stat packages/api-client`: chỉ hai file; diff chứa `/v2/geo/provinces`, `/v2/geo/provinces/{code}`,
  `/v2/geo/wards`, `/v2/geo/wards/{code}` và các schema `ProvinceDto`, `ProvinceListResponseDto`, `WardDto`,
  `WardSearchResponseDto`. Nếu diff lẫn thay đổi của endpoint khác (snapshot cũ), ghi rõ trong PR — không cắt tay.
- `pnpm --filter @erp/api-client build` rồi `pnpm --filter @erp/backoffice-web build` (hoặc `tsc --noEmit`) xanh.

## Done when
- [x] `OPENAPI_URL=http://127.0.0.1:4177/docs-json pnpm openapi:generate` (API erp2 chạy trên :4177 vì :4000 là checkout erp3): snapshot +367/−2, `schema.ts` +234; 4 path `/v2/geo/*` + schema Province*/Ward*. **Diff lẫn drift cũ**: `MobileRevenueCategory…Dto.required` thêm `quantity` — snapshot đã lệch từ trước, nay tự khớp lại; ghi trong PR, không cắt tay
- [x] `@erp/api-client` build xanh; `tsc --noEmit` backoffice-web exit 0, pos-web exit 0
- [x] `git diff --stat packages/api-client` = đúng hai file sinh
