---
feature: cash-fund-reports
blocking_open: 0
---

# Assumption register

Trình duyệt (Claude in Chrome) không kết nối được trong phiên lập kế hoạch 2026-09-18, nên
đặc tả tham chiếu lấy từ 12 ảnh chụp MShopKeeper mà chủ sở hữu gửi. Bốn câu hỏi cốt lõi + 3
câu theo phát hiện từ repo được hỏi một lượt qua AskUserQuestion (A-06, A-17, A-18); ba giả
định chặn (A-01, A-02, A-03) được hỏi ở lượt bổ sung duy nhất. Phần còn lại trả lời bằng đọc
code.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Ngày xếp chứng từ vào kỳ báo cáo là **ngày chứng từ** — `cash_receipts.voucher_date`, `cash_payments.voucher_date`, `bank_receipts.doc_date`, `bank_payments.doc_date` — đúng "Ngày chứng từ" của MShopKeeper. Không dùng `posted_at` (quy ước của `profit-report/business-results.report.ts:319-320`) hay `cash_movements.created_at` (quy ước của `cash-ledger.service.ts:154`). Hệ quả chấp nhận: một phiếu ghi lùi ngày làm mục III ở đây lệch với "Chi khác" của Kết quả kinh doanh cùng tháng | high | yes | Đổi cột ngày trong `CashFundPeriodService` (một chỗ), mọi spec số liệu phải tính lại; đối chiếu MShopKeeper hỏng với phiếu nhập muộn | resolved | Akenzy chọn "voucherDate / docDate" 2026-09-18 qua AskUserQuestion → ADR-02 |
| A-02 | Hai dòng cố định của "Tình hình thu chi": **Thu từ bán hàng** = phiếu thu có `purpose IN (POS_SALE, DEBT_COLLECTION)` (tương đương `reference_type IN (INVOICE, INVOICE_DEBT, RECEIVABLE, INVOICE_KEPT_CHANGE, RETURN_CANCEL)`); **Chi mua hàng hóa** = phiếu chi có `purpose IN (PURCHASE, SUPPLIER_PAYMENT)`. Mọi dòng chứng từ còn lại gom theo `category_id` của dòng; dòng không có mục → "Thu khác" / "Chi khác" (gồm REFUND, fund swap, hai chân chuyển quỹ liên chi nhánh) | high | yes | Đổi tập `purpose` trong `CashFundPeriodService` (một hằng số) và spec; số I–IV không đổi, chỉ phân bổ giữa các dòng con | resolved | Akenzy chọn "Yes, as proposed" 2026-09-18 qua AskUserQuestion |
| A-03 | **Số dư đầu kỳ**: Tiền mặt = Σ(thu − chi) chứng từ tiền mặt POSTED có ngày chứng từ < `from` (`cash_accounts` không có cột số dư đầu kỳ — `cash/cash-account.entity.ts` chỉ có `balance` sống); Tiền gửi = `deposit_accounts.opening_balance` (khi `opening_date < from`) + Σ(thu − chi) chứng từ tiền gửi POSTED trước `from`. Theo cấu trúc, IV của kỳ trước = I của kỳ sau | high | yes | Thêm cột `opening_balance`/`opening_date` cho `cash_accounts` + migration + UI nhập → thêm 1 ticket entity/migration/FE vào UOW-01 và một bước nhập liệu trước khi đối chiếu | resolved | Akenzy chọn "computed from vouchers" 2026-09-18 qua AskUserQuestion → ADR-03. T-01-05 lúc đầu cộng `opening_balance` **không điều kiện** (theo Sổ tiền gửi, BR-LEDG-02); e2e T-01-07 cho thấy kỳ 2025 vì thế có 1.000.000 tiền gửi trước ngày mở tài khoản, sai AC-06 và sai giả định này. **T-01-08 sửa lại đúng A-03**: cộng khi `opening_date <= from` (`<=` chứ không `<`: tài khoản mở đúng ngày `from` có số dư từ đầu ngày; `<` làm mất 1.000.000 của tháng mở). Hệ quả: IV(kỳ trước) = I(kỳ sau) **trừ** ranh giới chứa `opening_date` — ngày số dư đầu được bơm vào; và báo cáo này lệch Sổ tiền gửi chỉ với kỳ bắt đầu trước ngày mở tài khoản |
| A-04 | **Phiếu bị đảo và phiếu đảo đều bị loại** ở cả 5 báo cáo: `status = REVERSED` (bản gốc) bị loại bởi lọc `status = POSTED`; phiếu đảo (`reference_type = REVERSAL`, sao chép dòng của bản gốc, `cash-receipts.service.ts reverse`) bị loại tường minh — cùng quy tắc `business-results.report.ts:303-306, 368-371`. Net bằng 0, và MShopKeeper không có khái niệm phiếu đảo (huỷ = xoá) nên đây là cách duy nhất khớp số | high | no | Nếu kế toán muốn thấy cả hai dòng trên Bảng kê: bỏ điều kiện `reference_type <> REVERSAL` cho #3/#5 (một dòng WHERE), giữ nguyên #2/#4/#6 | confirmed | Đọc `business-results.report.ts:290-330, 360-400`; đã nêu trong câu hỏi A-03 và được chấp nhận 2026-09-18 |
| A-05 | "Phương thức thanh toán" trên #3/#5 = **Tiền mặt** (phiếu ở `cash_*`) hoặc **Chuyển khoản** (phiếu ở `bank_*`); chứng từ tiền mặt không có cột phương thức (`cash-vouchers/enums.ts`, `payment_accounts.payment_method` chỉ dùng cho POS). Cột "Tên thẻ" của MShopKeeper bỏ vì không mô hình hoá | high | no | Nếu cần tách "Thẻ" khỏi "Chuyển khoản": đọc `deposit_accounts.kind` (nếu có) hay thêm cột — thay đổi nhỏ ở SELECT | confirmed | `cash-vouchers/enums.ts`, `payment-accounts/payment-account.entity.ts`; hỏi ở lượt out-of-scope 2026-09-18, chủ sở hữu không phản đối |
| A-06 | **Báo cáo #1 "Bảng kê biên bản bàn giao ca" tạm hoãn.** Repo chỉ có `pos_sessions` + `pos_session_reconciliations` (`pos/entities/`), không có bản ghi bàn giao; sẽ là feature riêng | high | yes | Không có: đã cắt khỏi phạm vi, 5 báo cáo còn lại không phụ thuộc | resolved | Akenzy: "current pause the report #1" 2026-09-18 qua AskUserQuestion |
| A-07 | In, "Sửa mẫu", hàng lọc theo cột đều **trong phạm vi** — câu hỏi out-of-scope không được đánh dấu mục nào; cả ba đều là năng lực sẵn có của report-core (`ColumnConfigDialog`, `ReportExportButtons`, `FilterHeaderCell`) nên không tốn ticket riêng | medium | no | Nếu chủ sở hữu muốn cắt: bỏ AC-19..21, giảm DoD của UOW-01; không đổi thiết kế | confirmed | AskUserQuestion 2026-09-18 trả về không chọn gì → hiểu là "tất cả trong phạm vi"; chi phí ≈ 0 |
| A-08 | Nhãn báo cáo theo đúng MShopKeeper ("Tình hình thu chi", "Bảng kê thu chi", "Chi tiền theo mục chi", "Bảng kê tiền chi theo mục chi", "Chi tiền theo thời gian"); 5 nhãn stub trong `report-type.constant.ts:451-456` được đổi. Nhãn nhóm: "Quỹ tiền" (thay "Quỹ tiền mặt" đang comment vì có cả tiền gửi) | high | no | Đổi chuỗi | confirmed | Tín hiệu thành công là khớp MShopKeeper; ảnh chụp 1–12 |
| A-09 | Kỳ báo cáo được FE resolve từ preset sang `from/to` (`PeriodSelect`, `packages/ui/src/components/period-filter.tsx:22-33`: Hôm nay … Năm trước, Khác); BE chỉ nhận `DateRangeFilterDto { from, to }` như mọi báo cáo tiền | high | no | — | confirmed | `store/page-stores/report/report.store.ts:69-75`; chỉ inventory-reports có preset ở BE |
| A-10 | Chuyển quỹ (`DEPOSIT_TRANSFER` / `CASH_TRANSFER`, `FUND_SWAP`) và chuyển liên chi nhánh (`INTER_BRANCH_IN/OUT`) hiện như phiếu thu/chi bình thường: chân chi ở quỹ nguồn, chân thu ở quỹ đích; trên "Tổng cộng" của #2 chúng triệt tiêu trong cùng chi nhánh | medium | no | Nếu MShopKeeper loại chuyển quỹ khỏi thu/chi: thêm điều kiện loại `purpose` — một hằng số, số I/IV không đổi | confirmed | Cách nhất quán duy nhất với số dư từng quỹ; không có bằng chứng ngược trong ảnh chụp |
| A-11 | `.ai/architecture.md` (ký 2026-08-03, > 30 ngày) được **tái sử dụng**, không sinh lại: các mục report-core (dòng 344-359) và cash/deposit-vouchers (101-107, 223-226) vẫn đúng với cây thư mục hôm nay | high | no | Đường dẫn `touches` sai → `aidlc lint-touches` bắt được | confirmed | Đối chiếu bằng `find`/`ls` 2026-09-18 (debt-report 46 file, report-core 40 file, cash-vouchers/, deposit-vouchers/) |
| A-12 | Nguồn của #4/#5/#6 là **dòng** phiếu chi `cash_payment_lines` + `bank_payment_lines` (`category_id`, `amount`, `description`) của phiếu POSTED, gom theo mục chi `cash_voucher_categories` (`direction = OUT`); bảng `expenses` (`accounting/expenses/`) không phải nguồn — nó là chứng từ chi phí dồn tích, khi thanh toán mới sinh `cash_payment` (`expenses.cash_payment_id`) | high | no | Nếu muốn tính cả chi phí chưa thanh toán: thêm nguồn thứ ba — nhưng đó là báo cáo chi phí, không phải quỹ tiền | confirmed | `expenses/expense.entity.ts` (`paymentMethod` CASH/BANK/PAYABLE, `cashPaymentId`); tên báo cáo là "Chi **tiền**" |
| A-13 | "Thống kê theo" của #6 = Ngày \| Tuần \| Tháng \| Quý \| Năm (`date_trunc`), mặc định Ngày; cần một filter line mới vì `STATISTIC_BY` hiện có map `statBy: ReportGroupBy { item, parent, group }` (nhóm hàng hoá) và `STAT_DATE_TYPE` là "ngày thống kê" của hoá đơn | medium | no | Thêm/bớt lựa chọn trong một `const` + một nhánh `date_trunc` | confirmed | Ảnh 11 chỉ thấy "Ngày" đang chọn; các mức còn lại suy từ preset kỳ báo cáo (tuần/tháng/quý/năm) của chính MShopKeeper |
| A-14 | Phạm vi chi nhánh: #2/#4/#6 theo chi nhánh đang chọn trên header (`activeBranchId`, FE gửi trong `filters.branchId`; **BE không được rơi về `actor.branchId`** — nó luôn là `branchIds[0]` của JWT nên chế độ Chuỗi sẽ không bao giờ hợp nhất; T-01-08 sửa), hoặc toàn tổ chức khi ở chế độ Chuỗi và có `reporting.cash.consolidated.read`; #3/#5 có `StoreScopeDto { scope: all \| group, storeIds }` và bị kẹp về `actor.branchIds` bởi `resolveReportBranchIds` (`report-core/report-query.util.ts`) khi không có quyền hợp nhất | high | no | Nếu muốn #2 cũng chọn nhiều cửa hàng: thêm filter line `STORE` vào registry của #2 — FE thuần | confirmed | Ảnh 5/9 có phụ đề "Xem theo cửa hàng" (#3/#5), ảnh 3/7/12 không có; chênh 239.245.000 vs 4.512.563.722 |
| A-15 | Mẫu cột ("Sửa mẫu") lưu theo **chuỗi / chi nhánh** (`TemplateScope = 'chain' \| 'branch'`, `report_templates`), không theo từng người dùng như MShopKeeper có thể đang làm | high | no | Nếu cần theo người dùng: việc của report-core, ngoài feature này | confirmed | `report-core/template-scope.ts`, `packages/shared-interfaces/src/invoice-report/template.ts:21` |
| A-16 | Cột "Loại chứng từ" trên #3 có 4 giá trị: Phiếu thu, Phiếu chi, Thu tiền gửi, Chi tiền gửi; lọc dropdown theo `documentKind` | high | no | Đổi nhãn / thêm loại | confirmed | 4 bảng nguồn; ảnh 5 chỉ thấy "Tất cả" |
| A-17 | Không có deadline, không có phụ thuộc ngoài; đối chiếu số liệu với MShopKeeper làm thủ công trên cùng bộ chứng từ (không có feature nhập lịch sử) | high | no | — | resolved | Akenzy chọn "None — ship when ready" 2026-09-18 |
| A-18 | Phiên lập kế hoạch dừng ở **G3**; G4 làm ở phiên sau, từng UoW; Akenzy uỷ quyền `pass G0..G3` khi `check` xanh | high | no | — | resolved | Akenzy chọn "Plan to G3 only" + "Pass as Akenzy when check is green" 2026-09-18 |
| A-19 | "Nhân viên thu/chi" = `staff_id` trên phiếu (nhân viên lập/thu/chi, đã autocomplete từ `2026091401-treasury-voucher-staff-date-autocomplete`); "Đối tượng nộp/nhận" = `partner_name_snapshot` (rơi về `payer_name`/`payee_name`); "Người nhận" (#5) = `payee_name`; "Mã đối tượng" = mã của `partner_id` theo `partner_type` (customer/supplier/employee) | medium | no | Đổi ánh xạ cột trong SELECT của #3/#5 | confirmed | `cash-receipt.entity.ts` / `cash-payment.entity.ts` / `bank-payment.entity.ts` (partner_*, payer/payee, staff_id); ảnh 9: cột Đối tượng và Người nhận có giá trị khác nhau ("Thầy Hà" / trống) |
| A-20 | "Số hóa đơn" (#3/#5, ẩn mặc định) = `document_number` của hoá đơn khi `reference_type IN (INVOICE, INVOICE_DEBT, INVOICE_KEPT_CHANGE, RETURN_CANCEL)`; "Tham chiếu" = `reference_type` + số chứng từ tham chiếu | medium | no | Cột ẩn mặc định, sai chỉ ảnh hưởng người bật lên | confirmed | Ảnh 6/10 (cột ẩn mặc định); entity `reference_type/reference_id` |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
| ---- | ---------------- | ----------------------------------------- | ----------------------------- |
| A-21 | `report-core/export/xlsx-style.ts` đọc `bold` trên từng dòng nên file xlsx của #2/#5 in được dòng đậm (ADR-04) | `XlsxStreamWriter` chỉ đậm header và dòng tổng; `bold`/`indentLevel` tới writer nhưng bị bỏ qua — Kết quả kinh doanh cũng vậy | ADR-04 sửa hệ quả 2026-09-18; xlsx của #2/#5 dòng đậm hiện thường; sửa writer là follow-up report-core, ngoài feature này |
| A-22 | `report_templates` có cột `domain` để tách mẫu của domain `cash` | Không có cột domain; mẫu phân biệt bằng `reportType` (khoá của `cash` là chuỗi riêng) | Chỉ sửa chữ ở 03-logical-design §Templates; code giống debt-report |
| A-23 | Bốn dispatcher trong `_api/` là mọi chỗ FE rẽ nhánh theo `backendSource` | Còn `ReportTableConfigSync.tsx:66-86` gọi `/reports/<source>/columns` — thiếu nhánh `cash` là cột catalog rơi về registry và search bị từ chối | T-01-06 thêm file này vào `touches` (G3 reopened 2026-09-18) |
| A-24 | Tiêu đề báo cáo có tooltip để ghi "theo ngày chứng từ" (ADR-02) và registry có `sortable: false` (ADR-04) | `ReportPageHeaderTitle` không có tooltip, `ReportTypeMetadata` không có trường; `ReportColumnTableConfig` không có `sortable` (bảng không sort theo cột) | Bỏ cả hai; ghi chú ngày chứng từ đi vào phụ đề tài liệu in/xuất (`cashFundFilterSummary`) |
