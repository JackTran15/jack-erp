---
id: T-01-07
uow: UOW-01
title: e2e domain `cash` + tái sinh api-client
layer: test
type: test
estimate: 2h
status: todo
depends_on: [T-01-01, T-01-04, T-01-05, T-01-06]
blocks: []
verifies: [AC-02, AC-03, AC-04, AC-06, AC-07, AC-19, AC-20, AC-21]
tests:
  - apps/api/test/e2e/cash-fund-report.e2e-spec.ts
touches:
  - apps/api/test/e2e/cash-fund-report.e2e-spec.ts        # new; matches apps/api/test/e2e/report-export.e2e-spec.ts
  - apps/api/test/e2e/setup/cash-fund-fixture.ts           # new; dựng fixture PT-01..PC-07 qua API; matches cash-vouchers-phase2.e2e-spec.ts (phần tạo phiếu); UOW-02/03 chỉ import
  - packages/api-client/src/generated/schema.ts           # generated by `pnpm openapi:generate`
  - packages/api-client/openapi.snapshot.json             # generated
assumptions: [A-17]
---

# T-01-07 — e2e + api-client

## Context

`cash-vouchers-phase2.e2e-spec.ts` biết cách tạo phiếu thu/chi POSTED qua API;
`deposit-fund.e2e-spec.ts` tạo tài khoản tiền gửi với `openingBalance`; `report-export.e2e-spec.ts`
kiểm header xlsx. Fixture của `02-requirements.md` dựng bằng các API đó (một tổ chức, hai
chi nhánh, PT-01..PC-07 kể cả đảo PC-05 qua endpoint reverse).

## Implementation notes

- Case: 403 với user không quyền (AC-02); search #2 kỳ 09/2026 → I/II/III/IV như fixture
  (AC-03, AC-04); kỳ 2025 → 9 dòng 0 (AC-06); user chi nhánh A ở chế độ chuỗi → không có số
  của B; admin hợp nhất → có (AC-07); export → 200, `content-type` xlsx, sheet có 4 header và
  dòng "IV." (AC-19); print-payload có `rows.length ≥ 9` (AC-20); template chain tạo/đọc/xoá (AC-21).
- Sau khi API chạy: `pnpm openapi:generate`; commit snapshot + schema (không sửa tay).
- `maxWorkers: 1`, `forceExit: true` — đọc output thật, không tin exit message (CLAUDE.md).

## Done when

- [ ] `pnpm --filter @erp/api test:e2e -- cash-fund-report` xanh, output đọc tay
- [ ] Chuyển từ T-01-03: `POST /reports/cash-fund/search` với người dùng đã đăng nhập nhưng không có `reporting.cash.read` → 403 (AC-02)
- [ ] Chuyển từ T-01-03: `filter-options?type=store` trả đúng chi nhánh actor được đọc (và mọi chi nhánh khi có quyền hợp nhất); `type=paymentMethod` trả đúng 2 lựa chọn Tiền mặt / Chuyển khoản
- [ ] `openapi.snapshot.json` có 9 path `/reports/cash-fund/*`; `schema.ts` tái sinh, không diff thủ công
- [ ] Thời gian search #2 trên fixture < 2 s (ghi số đo vào ticket)
