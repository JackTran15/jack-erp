---
id: T-02-02
uow: UOW-02
title: `filter-options?type=employee` cho domain `cash`
layer: api
type: feature
estimate: 2h
status: in_progress
depends_on: [T-01-03]
blocks: [T-02-03]
verifies: [AC-09]
tests:
  - apps/api/src/modules/reporting/cash-fund-report/queries/get-report-filter-options.handler.spec.ts
touches:
  - apps/api/src/modules/reporting/cash-fund-report/queries/get-report-filter-options.handler.ts   # new in T-01-03
  - apps/api/src/modules/reporting/cash-fund-report/queries/get-report-filter-options.handler.spec.ts   # new; matches invoice-report/queries/get-report-filter-options.handler.spec.ts nếu có, else debt-report
  - apps/api/src/modules/reporting/cash-fund-report/dto/report-filter-options-query.dto.ts   # new in T-01-03
assumptions: [A-19]
---

# T-02-02 — filter-options `employee`

## Context

`invoice-report/queries/get-report-filter-options.handler.ts:16-41` đã lấy nhân viên từ
`EmployeeProfileEntity` (`rbac/employee/`) theo phạm vi chi nhánh
(`EmployeeBranchScopeService`). Domain `cash` cần cùng danh sách nhưng chỉ nhân viên có
`staff_id` trên ít nhất một phiếu (tránh dropdown 200 người).

## Implementation notes

- Enum `ReportFilterOptionType` của domain thêm `employee`; handler: `SELECT DISTINCT staff_id`
  trên 4 bảng trong phạm vi → JOIN `employee_profiles` → `{ value: id, label: 'Mã - Tên' }`,
  `search` ILIKE, `limit` 50.
- Giữ `store` và `paymentMethod` như T-01-03; `expenseCategory` vẫn 400 (T-03-04 nối).

## Done when

- [x] Spec: chỉ nhân viên có phiếu; tôn trọng phạm vi chi nhánh; `search` lọc theo tên/mã
- [x] Không đổi handler filter-options của domain khác
