---
id: T-01-03
uow: UOW-01
title: e2e — nhóm hàng cha→con, round-trip và type bất biến
layer: test
type: test
estimate: 3h
status: done
depends_on: [T-01-02]
blocks: [T-01-04]
verifies: [AC-06, AC-07, AC-08]
tests:
  - apps/api/test/e2e/promotion-item-discount.e2e-spec.ts
touches:
  - apps/api/test/e2e/promotion-item-discount.e2e-spec.ts   # new (T-01-02)
assumptions: [A-10, A-12]
---

# T-01-03 — e2e: nhóm cha→con, round-trip, type bất biến

## Context
Ba rủi ro còn lại của việc bật lại một đường đã tắt lâu: chọn theo **nhóm hàng**
có còn với tới hàng ở nhóm con không, dữ liệu đọc lại có **khớp** lúc lưu không,
và ràng buộc `type` bất biến có còn hiệu lực với hình thức này không.

Cây nhóm hai cấp (`parentCategoryId` → `childCategoryId`) và `SKU-OTHER` nằm ngoài
cây đã có sẵn trong fixture, dựng riêng cho đúng loại kịch bản này.

## Implementation notes
- AC-06: CTKM đặt trên `parentCategoryId`, đánh giá giỏ chứa `SKU-685` (thuộc nhóm con) → được áp; giỏ chứa `SKU-OTHER` → không được áp
- AC-07: `POST` tạo → `GET /v2/promotions/:id` → so khớp từng trường đã gửi; đặc biệt kiểm `groups[0].lines` **không** nhân đôi
- AC-08: `PUT` với `type = 'INVOICE_DISCOUNT'` → 400 `PROMOTION_TYPE_IMMUTABLE`, rồi `SELECT` xác nhận `type` trong DB không đổi
- Cùng file với T-01-02 — hai ticket chia nhau `describe` khác nhau, không tranh nhau `beforeAll`

## Done when
- [x] AC-06 chứng minh cả chiều khớp (SKU-685 ở nhóm con → giảm 68.500) lẫn chiều không khớp (SKU-OTHER → 0)
- [x] AC-07 so khớp type/name/priority/applyTo/autoApply + nội dung dòng REWARD, và đếm `promotion_lines` = 1 để bắt lỗi nhân đôi
- [x] AC-08 khẳng định 400 rồi `SELECT type` xác nhận DB không đổi
- [x] `pnpm --filter @erp/api test:e2e -- promotion-item-discount` xanh — 7/7, 88,2s
