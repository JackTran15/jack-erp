---
id: T-01-03
uow: UOW-01
title: PaymentSummaryBlock — Tổng tiền sau CTKM hàng hóa, dòng Khuyến mại chỉ hóa đơn, ✕ chỉ hóa đơn
layer: web
type: feature
estimate: 3h
status: done
depends_on: [T-01-01]
blocks: [T-01-04]
verifies: [AC-07, AC-08, AC-09, AC-10]
tests: []
touches:
  - apps/pos-web/src/components/page-components/Checkout/CheckoutRightPane/PaymentSummaryPanel/Sections/PaymentSection/PaymentSummaryBlock/PaymentSummaryBlock.tsx
  - apps/pos-web/src/lib/page-libs/checkout/promotionPresentation.ts
assumptions: [A-01, A-04]
---

# T-01-03 — Panel phải: Khuyến mại = CTKM hóa đơn

## Context
ADR-02/ADR-05. `total` hiển thị = `useCheckoutGrandTotal() − totals.item`; dòng
*Khuyến mại* = `totals.invoice`, ẩn khi 0; ✕ chỉ loại program invoice-bucket.
`deriveSettlement` và `selectPromotionDiscountAmount` **không** đổi — Còn phải thu
giữ nguyên từng đồng.

## Implementation notes
- `promotionPresentation.ts`: `buildPromotionRowLabel` và `shouldShowPromotionRow` nhận thêm `invoicePrograms: AppliedProgram[]` / `invoiceTotal` thay vì cả response — "(X%)" khi đúng 1 program invoice kiểu PERCENT; hiện dòng khi `invoiceTotal > 0`. Cập nhật `promotionPresentation.test.ts` **không** — file test phía web không chạy (A-10), nhưng vẫn phải compile: sửa chữ ký gọi trong test nếu `tsc` bắt
- `PaymentSummaryBlock.tsx`: đọc `selectPromotionBucketTotals`; `handleClickExcludeAll` → chỉ `programIds` của invoice-bucket; text hộp xác nhận (`PROMOTION_EXCLUDE_CONFIRM.messageAll`) nhận danh sách đó
- Ô xám `loading` và "Chưa tính được khuyến mại" giữ như hôm nay

## Done when
- [x] Giỏ SKU-685 đơn: Tổng tiền 616.500, không có dòng Khuyến mại, Còn phải thu 616.500 — AC-07
- [x] Giỏ 2 dòng: Tổng tiền 716.500, "Khuyến mại (10%)" −10.000, Còn phải thu 706.500 — AC-08
- [x] Σ Thành tiền = Tổng tiền ở cả hai giỏ và giỏ có giảm tay (566.500 + 100.000 = 666.500) — AC-09
- [x] ✕ → hộp xác nhận chỉ nêu CTKM-B; sau đó Còn phải thu = Tổng tiền (666.500 trong kịch bản có giảm tay), nhãn CTKM-A còn — AC-10
- [x] `tsc --noEmit` xanh

Chạy `capture-pos-evidence.py --lines` 2026-09-18 trên `:3001`, đọc từ DOM:
```
SKU-685            panel {'tong': '616.500', 'km': None,      'due': '616.500'}   AC-07
+ SKU-100          panel {'tong': '716.500', 'km': '-10.000', 'due': '706.500'}   AC-08, nhãn "Khuyến mại (10%)"
+ giảm tay 50.000  panel {'tong': '666.500', 'km': '-10.000', 'due': '656.500'}   AC-09
✕ Khuyến mại       confirm: Bỏ áp dụng chương trình "CTKM-B hoa don 10% NON_PROMO_ONLY - claude"? Còn phải thu sẽ đổi từ 656.500 thành 666.500.
                   panel {'tong': '666.500', 'km': None,      'due': '666.500'}   AC-10, dòng SKU-685 vẫn có nhãn CTKM-A
RESULT: PASS
```
`promotionPresentation.test.ts` không sửa (không chạy — A-10); hai case
`shouldShowPromotionRow` trong đó giờ mô tả hành vi cũ — ghi vào nợ kỹ thuật ở PR.
