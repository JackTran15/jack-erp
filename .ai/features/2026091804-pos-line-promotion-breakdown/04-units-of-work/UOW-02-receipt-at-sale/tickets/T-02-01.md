---
id: T-02-01
uow: UOW-02
title: Factory + renderer hóa đơn in — promotionLabels và lineTotal sau CTKM hàng hóa
layer: web
type: feature
estimate: 3h
status: done
depends_on: [T-01-01]
blocks: [T-02-02, T-03-03]
verifies: [AC-11, AC-12, AC-13]
tests: []
touches:
  - apps/pos-web/src/interfaces/invoice-printing.interface.ts
  - apps/pos-web/src/lib/page-libs/checkout/checkoutReceiptFactory.ts
  - apps/pos-web/src/lib/page-libs/checkout/printing/renderInvoiceHtml.ts
assumptions: [A-05, A-12]
---

# T-02-01 — Dòng in mang nhãn CTKM

## Context
`buildCheckoutInvoicePayload` đã nhận `promotionEngineDiscounts` (appliedPrograms)
cho hai bucket tổng. Dùng cùng dữ liệu đó dựng index theo `CartLine.lineId` và
gắn vào từng dòng in.

## Implementation notes
- `InvoiceLineData` thêm `promotionLabels?: string[]`
- Factory (`lines: cart.map(...)`): `promotionLabels = index.byLine.get(l.lineId).map(formatPromotionLabel)`; `lineTotal = signedTotal − itemBucketOfLine` cho dòng bán (dòng trả giữ nguyên); `discountLabel` (giảm tay) giữ nguyên
- Renderer: sau `discountLabel`, in mỗi nhãn một `<div class="line-sub">` (escapeHtml)
- Khối tổng: **không** sửa (A-05)
- `checkoutReceiptFactory.test.ts` không chạy (A-10) nhưng phải compile

## Done when
- [x] HTML in tạm tính giỏ 2 dòng: dòng 1 `line-sub` "CTKM-A … (68.500)", TT 616.500; dòng 2 "CTKM-B … (10.000)", TT 100.000 — AC-11
- [x] Giảm tay + CTKM-A: 2 `line-sub`, TT 566.500 — AC-13
- [x] Khối tổng in y hệt trước — `div.row` Tiền hàng 785.000 / Khuyến mãi 78.500 / KM theo mặt hàng 68.500 / KM theo hóa đơn 10.000 / Tổng thanh toán 706.500, trùng POS-05 của 2026091803
- [x] `tsc --noEmit` xanh

`capture-pos-evidence.py --receipt` 2026-09-18: hai dòng in đúng nhãn/TT, khối
tổng không đổi; kịch bản giảm tay thêm dòng *Giảm giá 50.000*, Tổng thanh toán
656.500. `--checkout` (In hóa đơn bật) ghi hóa đơn `2609180004`, HTML in giống
tạm tính từng nhãn từng số (AC-12). `promotionEngineDiscounts` nhận
`PromotionAmount & Partial<AppliedProgramLike>` nên `checkoutReceiptFactory.test.ts`
vẫn compile.
