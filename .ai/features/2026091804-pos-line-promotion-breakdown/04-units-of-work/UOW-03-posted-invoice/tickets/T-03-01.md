---
id: T-03-01
uow: UOW-03
title: GET /invoices/:id trả tên + lineDiscounts của snapshot CTKM (+ e2e)
layer: api
type: feature
estimate: 3h
status: done
depends_on: []
blocks: [T-03-02]
verifies: [AC-14]
tests:
  - apps/api/test/e2e/invoice-applied-promotions.e2e-spec.ts   # new
  - apps/api/src/modules/pos/services/invoice.service.spec.ts   # thêm lúc làm: case cũ khẳng định shape {type, discountAmount} — sửa có chủ ý
touches:
  - apps/api/src/modules/pos/dto/draft-invoice.response.dto.ts
  - apps/api/src/modules/pos/services/invoice.service.ts
  - apps/api/src/modules/pos/services/invoice.service.spec.ts
  - apps/api/test/e2e/invoice-applied-promotions.e2e-spec.ts   # new
assumptions: [A-06, A-11]
---

# T-03-01 — DTO snapshot đủ để vẽ từng dòng

## Context
ADR-04. `invoice.service.ts:312-319` đã đọc `InvoiceCheckoutPromotionEntity`, chỉ
map `type`/`discountAmount`. Thêm `programId`, `code`, `name`, `priority`,
`lineDiscounts` (jsonb → `[]` khi null).

## Implementation notes
- DTO: thêm 4 field với `@ApiProperty`; `lineDiscounts` là class con `AppliedInvoicePromotionLineDto { lineId; discountAmount; unitPriceAfter }` để Swagger sinh schema
- Service: sắp theo `priority`, `createdAt` như engine
- Trước khi sửa: `grep -rn appliedPromotions apps/backoffice-web apps/api/src/modules/mobile packages --include=*.ts --include=*.tsx` — ghi kết quả vào ticket (A-11)
- e2e (fixture `buildCheckoutSagaFixture` + `seedPromotionFixtures` như `promotion-invoice-scope-checkout.e2e-spec.ts`): checkout thật giỏ 685+100 với CTKM-A/CTKM-B rồi `GET /invoices/:id` → assert 2 phần tử, `lineId` ∈ `items[].id`, số 68.500/616.500 và 10.000/90.000; case thứ hai: hóa đơn không CTKM → `[]`

## Done when
- [x] e2e mới xanh trên `erp_test` (2 case) — `invoice-applied-promotions.e2e-spec.ts` 2/2 ✓ 2026-09-18
- [x] `grep appliedPromotions` ngoài pos-web: kết quả dán vào ticket, không client nào vỡ
- [x] Swagger `/docs-json` có `AppliedInvoicePromotionLineDto` — API code mới chạy tạm trên `:4001` (`ts-node-transpile-only src/main.ts`, `DB_NAME=erp_dev_3008` vì `apps/api/.env` có DB_NAME hai lần và ConfigModule lấy dòng cuối `erp_clone_prod`): `AppliedInvoicePromotionDto` = programId, code, name, type, priority, discountAmount, lineDiscounts; `AppliedInvoicePromotionLineDto` = lineId, discountAmount, unitPriceAfter

`grep -rln appliedPromotions` ngoài pos-web:
`apps/api/src/modules/mobile/services/mobile-manager-invoice.service.ts` (chỉ
`.map(p => p.type)` — thêm field không ảnh hưởng, spec của nó vẫn xanh) và
`packages/api-client/src/generated/schema.ts` (regen ở T-03-02). backoffice-web
không dùng.

`invoice.service.spec.ts` có sẵn case khẳng định shape cũ `{type, discountAmount}`
— sửa có chủ ý sang shape mới (thêm `order` và `lineDiscounts: null → []`), khai
thêm vào `tests:`/`touches:`. Unit `jest invoice.service.spec`: 177/177.
