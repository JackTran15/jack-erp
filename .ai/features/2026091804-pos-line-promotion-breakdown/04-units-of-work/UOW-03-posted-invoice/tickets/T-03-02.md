---
id: T-03-02
uow: UOW-03
title: Regen api-client + InvoiceRow.appliedPromotions phía pos-web
layer: shared
type: chore
estimate: 1h
status: done
depends_on: [T-03-01]
blocks: [T-03-03]
verifies: [AC-17]
tests: []
touches:
  - packages/api-client/src/generated/schema.ts
  - packages/api-client/openapi.snapshot.json
  - apps/pos-web/src/interfaces/invoice.interface.ts
assumptions: [A-06]
---

# T-03-02 — Đồng bộ kiểu

## Implementation notes
- API code mới phải đang chạy trên :4000 khi `pnpm openapi:generate` — **không** `nest build` đè lên dev watcher (memory `nest-build-kills-dev-watcher`); nếu :4000 đang là `dist/main` cũ, restart có chủ ý và ghi lại
- `InvoiceRow.appliedPromotions` → `{ programId; code; name; type; discountAmount; lineDiscounts: { lineId; discountAmount; unitPriceAfter }[] }[]`; `groupPromotionsForPrint` vẫn nhận được (structural)

## Done when
- [x] `git diff packages/api-client` chỉ **thêm** — không xóa/đổi schema khác
- [x] `tsc --noEmit` pos-web xanh

Đối chiếu snapshot cũ/mới bằng script: schema xóa `[]`, path xóa `[]`; thêm
`AppliedInvoicePromotionLineDto`; `AppliedInvoicePromotionDto` +5 props
(`programId, code, name, priority, lineDiscounts`); kèm hai field
`MobileRevenueCategoryDto.quantity` / `MobileRevenueCategoryListDto.totalQuantity`
của PR #281 chưa từng được regen — cũng chỉ thêm.

Phát hiện lúc làm: tiến trình `:4000` **là** child của `nest start --watch`
(PID cha `nest.js start --watch`), tự rebuild + restart khi sửa `src/` — nên
`/docs-json` trên `:4000` đã có DTO mới ngay, không cần dựng instance riêng.
Bản side-build `dist-4001` thử trước đó (437 schema thay vì 602) đã xóa.
