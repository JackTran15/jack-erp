---
id: T-03-03
uow: UOW-03
title: InvoiceReceiptDialog + in lại — nhãn CTKM và Thành tiền từng dòng từ snapshot
layer: web
type: feature
estimate: 3h
status: todo
depends_on: [T-03-02, T-01-01, T-02-01]
blocks: [T-03-04]
verifies: [AC-15, AC-16]
tests: []
touches:
  - apps/pos-web/src/components/page-components/Checkout/CheckoutDialogs/CustomerDetailDialog/PurchaseHistoryTab/InvoiceReceiptDialog/InvoiceReceiptDialog.tsx
  - apps/pos-web/src/lib/page-libs/invoice-list/invoiceRowPrintPayload.ts
assumptions: [A-06, A-07]
---

# T-03-03 — Hóa đơn đã lưu vẽ như lúc bán

## Implementation notes
- Cả hai nơi: `buildLinePromotionIndex(invoice.appliedPromotions ?? [], new Set(items.map(i => i.id)))` — `lineId` là `invoice_items.id`
- Dialog: dưới tên mỗi dòng bán, span đỏ nghiêng cho từng `formatPromotionLabel`; Thành tiền hiển thị = `lineTotal − manual − itemBucket` (dialog hiện đang trừ `promotionDiscount` ở `goodsSubtotal` — đọc kỹ để không trừ hai lần)
- `invoiceRowPrintPayload.ts`: `promotionLabels` + `lineTotal` net item-bucket cho dòng bán; khối tổng (`groupPromotionsForPrint`) giữ nguyên
- Không gọi `/v2/promotions/evaluate` (assert bằng network log trong T-03-04)

## Done when
- [ ] Dialog `2609180003`: SKU-685 nhãn + 616.500; SKU-100 nhãn + 100.000 — AC-15
- [ ] In từ dialog: HTML từng dòng giống hóa đơn lúc bán — AC-16
- [ ] Hóa đơn không snapshot: dialog/in như hôm nay
- [ ] `tsc --noEmit` xanh
