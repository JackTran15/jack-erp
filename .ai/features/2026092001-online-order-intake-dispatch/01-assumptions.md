# Assumption register

Bốn câu đã được Akenzy chốt ngày 2026-09-20 (không CTKM đợt này · trả đơn về pool
· 1 đơn 1 chi nhánh · hoá đơn phát hành lúc giao shipper) nằm ở `00-intent.md`,
không phải giả định. Dưới đây là những gì chưa ai trả lời và tôi đang giả định.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Đơn web luôn có tên + số điện thoại khách, và số điện thoại là khoá khớp/tạo `customers`. | high | yes | `invoice_debts.customer_id` là NOT NULL — không có khách thì không mở được công nợ COD, toàn bộ luồng thanh toán phải làm lại. | resolved | Akenzy chốt 2026-09-20: đúng — SĐT là khoá khớp/tạo `customers`. |
| A-02 | Nguồn đơn lưu thành **kênh đăng ký được** (bảng `sales_channels` hoặc tương đương) + `external_order_id` duy nhất theo kênh, thay cho `sales_orders.sales_channel` varchar tự do. | medium | yes | Nếu giữ varchar tự do: mở Shopee/TikTok phải migration, và đối tác retry sẽ tạo đơn trùng vì không có khoá chống trùng cấp đơn. | resolved | Akenzy chốt 2026-09-20: bảng kênh đăng ký được + `external_order_id` unique theo kênh. Thêm sàn về sau = thêm một dòng dữ liệu + một API key. |
| A-03 | Pool "chưa phân" = `sales_orders.branch_id IS NULL` trên chính bảng cũ, không dựng bảng hàng chờ riêng. | high | yes | Sai thì phải đổi shape dữ liệu sau khi đã có UoW — kéo theo mọi query của Admin và chi nhánh. | resolved | Akenzy chốt 2026-09-20: đúng — pool là `branch_id IS NULL` trên chính `sales_orders`, không bảng hàng chờ riêng. |
| A-04 | `sales_orders.salesperson_id` nới thành nullable; đơn web không có tư vấn viên. | high | yes | Nếu buộc phải có: cần một "nhân viên hệ thống" giả, làm bẩn báo cáo hoa hồng và `invoices.salesperson_id`. | resolved | Akenzy chốt 2026-09-20: đúng — nới `salesperson_id` thành nullable. |
| A-05 | "Trả đơn về pool" = set `branch_id = NULL` + ghi lý do vào lịch sử điều phối, giữ nguyên status `SENT` — **không** thêm status mới vào `sales_order_status_enum`. | medium | no | Thêm status là migration enum + sửa `VALID_TRANSITIONS`; nếu thiếu, Admin không phân biệt được đơn mới với đơn đã bị từ chối một lần. | pending | |
| A-06 | Lịch sử điều phối (ai phân, phân cho ai, lúc nào, ai trả về, lý do) cần lưu thành **bảng sự kiện riêng**, không nhồi thêm cột vào `sales_orders`. | medium | no | Nhồi cột thì chỉ giữ được lần phân gần nhất; một đơn bị trả về hai lần sẽ mất vết lần đầu. | pending | |
| A-07 | Địa chỉ giao là **snapshot trên đơn**: `province_code`, `ward_code`, tên tỉnh/phường chốt lúc đặt, địa chỉ chi tiết, người nhận, SĐT nhận. Không FK cứng sang `geo_*`. | high | no | FK cứng sẽ vỡ khi dataset địa giới đổi (bảng đã mang sẵn `merged_from` cho đợt sáp nhập 2026); tên không snapshot thì chứng từ cũ đổi nội dung theo dataset mới. | pending | |
| A-08 | Quyền điều phối là **permission mới cấp tổ chức** (vd `sales-order.dispatch`), tách khỏi `SALES_ORDER_PERMISSIONS.approve` hiện có. | medium | yes | Dùng lại `approve` nghĩa là mọi thu ngân chi nhánh đều phân được đơn cho chi nhánh khác. | resolved | Akenzy chốt 2026-09-20: đúng — permission điều phối mới, cấp tổ chức, tách khỏi `approve`. |
| A-09 | Màn hình Admin đọc qua **đường cấp tổ chức mới**, không nới `BranchScopeGuard` trên `/mobile/sales-orders` hiện có. | high | no | Nới guard cũ là mở phạm vi cho cả app mobile của tư vấn viên — rò đơn giữa các chi nhánh. | pending | |
| A-10 | API key là danh tính của **website**, không phải của khách. ERP không quản lý tài khoản khách web; web tự xác thực khách rồi gọi ERP. | high | no | Nếu ERP phải quản lý tài khoản khách web thì phát sinh cả một miền auth mới, ngoài mọi ước lượng của đợt này. | pending | |
| A-11 | v1 **không có** endpoint để web huỷ đơn. Khách muốn huỷ thì gọi tổng đài, nhân viên huỷ trong ERP. | medium | no | Nếu web cần tự huỷ: thêm endpoint partner + ràng buộc "chỉ huỷ khi chưa phát hành hoá đơn"; không phá thiết kế, chỉ thêm việc. | pending | |
| A-12 | `/orders` backoffice được nối dữ liệu thật **chỉ ở các cột thuộc đợt này**; cột vận chuyển/đối soát trong mock giữ nguyên hoặc ẩn. | medium | no | Nối hết mọi cột mock là kéo cả miền vận chuyển vào, gấp nhiều lần phạm vi. | pending | |
| A-13 | Số chứng từ đơn web dùng `DocumentNumberingService` với một `DocumentType` riêng (không dùng chung dải `SALES_ORDER` của mobile). | low | no | Dùng chung dải thì không phân biệt được đơn web với đơn tư vấn viên trên chứng từ; đổi về sau là đổi số chứng từ đã phát hành. | pending | |
| A-14 | "User tự vào đơn hàng huỷ" = **người dùng ERP** (Admin, hoặc chi nhánh đang giữ đơn), không phải khách trên web. A-11 giữ nguyên: web không có endpoint huỷ. | medium | no | Nếu web phải huỷ được: thêm endpoint partner + quy tắc "chỉ huỷ khi chưa phát hành hoá đơn"; không phá thiết kế, chỉ thêm việc. | pending | |
| A-15 | Huỷ đơn đã phát hành hoá đơn chỉ được phép trong đúng giới hạn `CancelInvoiceService` hiện có: hoá đơn thuộc `CANCELLABLE_STATUSES` (paid/debt/partial-debt) và chưa có trả hàng đã tất toán (`assertNoSettledReturns`). Không nới các chặn đó. | high | no | Nới ra là trả tiền hai lần cho một đơn đã hoàn một phần — đúng cái `assertNoSettledReturns` sinh ra để chặn. | pending | |
| A-16 | **Không có cột nào trên `invoices` để chứa phí giao hàng thu khách.** EPIC-14062026 đã xác thực: "Tiền phí" là PLACEHOLDER 0, không có cột phí trong schema. Giả định: phí GH thu khách lưu **trên đơn**, không vào hoá đơn, và không tính vào doanh thu hàng hoá. | low | yes | Nếu phí phải nằm trên hoá đơn thì phát sinh migration `invoices` + sửa mọi công thức tổng tiền, công nợ và báo cáo doanh thu — đổi hẳn kích thước đợt này. | resolved | **BÁC BỎ.** Akenzy chốt 2026-09-20: phí GH thu khách **nằm trên `invoices`**, không phải trên đơn — ngược với khuyến nghị của tôi, chi phí đã được nêu trước khi chốt. Xem ADR dự kiến + A-21. |
| A-17 | "Thu hộ" (`cod`) là **cột hiển thị dẫn xuất**, không phải cột nhập: = `invoices.amount_due` (đã gồm phí GH sau khi A-16 bị bác bỏ). | medium | no | Nếu là cột nhập tay thì phải có đối soát lệch giữa số shipper thu và số đơn ghi — thêm một miền đối soát đã bị loại khỏi phạm vi. | pending | |
| A-18 | Đơn web mang `Loại đơn` = "Đặt hàng" (mock `/orders` đã khai đúng ba giá trị Đặt hàng / Bán lẻ / Trả hàng). | high | no | Sai thì chỉ là đổi nhãn một cột lọc. | pending | |
| A-19 | Hai màn Admin là **route mới** trên backoffice (điều phối + tất cả đơn), không phải tab bên trong `/orders`. `/orders` giữ nguyên vai trò lưới của chi nhánh. | medium | no | Nếu là tab: ít route hơn nhưng phải trộn ba phạm vi quyền trong một trang — dễ rò đơn của chi nhánh khác khi phân quyền sai. | pending | |
| A-20 | Tám cột vận chuyển/đối soát/sàn bị **ẩn khỏi dialog cột**, không xoá khỏi `ORDER_COLUMNS`. | high | no | Xoá hẳn thì lúc làm vận chuyển phải dựng lại cả định nghĩa cột, bề rộng và bộ lọc đã có. | pending | |

| A-21 | Phí GH thu khách hạch toán vào **tài khoản doanh thu dịch vụ / thu nhập khác**, không phải doanh thu hàng hoá, và không đi qua `resolve-accounts.step` như một dòng hàng. | low | no | Sai tài khoản là sai sổ cái, phải đảo bút toán hàng loạt. **Phải hỏi kế toán trước UoW hạch toán phí** — không chặn thiết kế, chặn đúng một ticket. | pending | |
| A-22 | Phí GH thu khách **không bị chiết khấu và không bị điểm tích trừ vào**: trong `computeAmountDue` nó cộng vào SAU mọi khoản giảm, và clamp 0 chỉ áp cho phần tiền hàng. | medium | no | Nếu để chiết khấu ăn vào phí thì khách được giảm cả phí giao — sai nghiệp vụ và lệch số shipper phải thu. | pending | |
| A-23 | Đơn/hoá đơn `RETURN` và `EXCHANGE` **không mang phí giao hàng** (phí đã thu không hoàn). `invoiceSignedTotalSql` giữ nguyên nhánh `netAmount` cho hai loại đó. | medium | no | Nếu phí phải hoàn thì đụng thêm miền trả hàng và công thức `net_amount` — ngoài phạm vi đang mô tả. | pending | |
| A-24 | Cơ sở TÍCH điểm là phần TIỀN HÀNG, **không** gồm phí giao — khớp với cơ sở ĐẢO điểm. | high | yes | Phát hiện lúc làm T-04-04, xác nhận rộng hơn lúc làm T-04-03: `pointsEarned = floor(amountDue / RATE)` nay gồm phí, trong khi `computeReversePoints` chạy trên `returnedNet` (qua `RefundableInvoiceHeader`, không có trường phí). Cap `Math.min(derived, pointsEarned)` là chặn TRÊN nên không bù được, nên TRẢ TOÀN BỘ vẫn đảo ít điểm hơn số đã phát. | resolved | Akenzy chốt 2026-09-21: tích điểm trên **tiền hàng**, trừ phí giao ra. Sửa `checkout-invoice.service.ts:185` và `compute-totals.step.ts` thành `floor((amountDue - shippingFeeAmount) / RATE)`; `LoyaltyPointsPublisher` dùng cùng cơ sở. Cơ sở đảo điểm giữ nguyên — sau khi sửa hai bên bằng nhau, trả toàn bộ thì điểm về 0. Xem T-04-06. |
| A-25 | `partner.order.create` được cấp qua một VAI TRÒ RIÊNG, tách khỏi vai trò `Đối tác` hiện có. | high | yes | Phát hiện lúc verify T-01-03: `permissions.seed.ts` có khai quyền nhưng `org-role-permissions.ts` không cấp cho vai trò nào (`PARTNER_PERMISSION_KEYS = ['partner.catalog.read']`), nên `POST /v2/partner/orders` trả 403 cho MỌI key — cả UOW-01 không chạy được. | resolved | Akenzy chốt 2026-09-21: thêm vai trò mới `Đối tác đặt hàng` mang ĐÚNG `['partner.order.create']`. Giữ `Đối tác` nguyên trạng một quyền. Lý do: key cấp để ĐẶT ĐƠN không nên đọc được bảng giá — đúng ý đã ghi trong `sales-order.constants.ts`. Một key có thể mang một, hoặc cả hai vai trò. Xem T-01-07. |
| A-26 | Bộ e2e KHÔNG chạy migration — `resetDatabase()` dựng schema bằng `synchronize(true)`, tức chỉ từ decorator của entity. Mọi ràng buộc viết bằng SQL thô trong migration **không tồn tại** trong DB e2e. | high | no | Phát hiện lúc làm T-01-05: ca replay và ca song song đỏ vì `UQ_sales_orders_org_channel_external` (partial unique index, chỉ có trong migration) không được tạo. Cùng lỗ hổng áp cho `CHK_sales_order_dispatch_events_shape` (repo không dùng `@Check` ở đâu cả), `FK_sales_orders_sales_channel`, `FK_api_keys_sales_channel`. Hệ quả: mọi ô done-when kiểu 'e2e chứng minh DB từ chối X' thực ra KHÔNG chứng minh gì — T-02-05, T-05-06, T-07-03 đều dính. Cách chữa tại chỗ (T-01-05 đang dùng, cùng kiểu với `CREATE EXTENSION unaccent` trong `geo.e2e-spec.ts`): tự tạo ràng buộc trong `beforeAll`. Cách chữa gốc: cho e2e chạy migration thay vì `synchronize`. | pending | |
| A-27 | Quyền mới CHỈ vào DB khi ai đó chạy seed — không có đồng bộ lúc boot, và feature này không có migration nào cấp quyền. | high | no | Đo được lúc đóng T-02-02: `pos.sales-order.dispatch` và `pos.sales-order.read-all` nằm trong `permissions` với **0 dòng role_permissions** cho tới khi chạy `pnpm seed:sync-admin-permissions`; trước đó `/admin/sales-orders` trả 403 cho mọi người — đúng triệu chứng T-01-03, vào bằng cửa khác. `PERMISSION_SEEDS` chỉ được đọc bởi ba seed script, không có đường nào khác. Hệ quả: Demo script UOW-02 bước 2 hỏng trên bất kỳ DB nào chưa chạy seed, và bản triển khai production cũng vậy. Chọn một: (a) chạy `seed:sync-admin-permissions` như một bước bắt buộc của mọi lần deploy, hoặc (b) thêm ticket migration cấp quyền để nó đi cùng schema. | pending | |
| A-28 | KM của đơn tư vấn viên đi vào hoá đơn dưới dạng MỘT khoản giảm dòng (`line_discount` = tay + KM), không ghi `invoice_items.promotion_discount` / `promotion_program_id`. | medium | no | Đo bằng `run_sales_flow.py` B1 (2026-09-22): `promotion_discount = 0` dù đơn có KM 20.000; approve() gộp `manualDiscount + promotionDiscount` (T-05-08 giữ nguyên cách gộp, chỉ đổi dạng ghi). Hệ quả: báo cáo theo CTKM không thấy KM đến từ đơn tư vấn viên; tổng tiền vẫn đúng. Đơn tư vấn viên chỉ có `promotionName` tự do, không có `promotion_program_id` → không tách được nếu không thêm cột lên đơn. Chọn: (a) chấp nhận, ghi rõ trong tài liệu báo cáo; (b) ticket thêm `promotion_program_id` lên `sales_order_lines` và ghi tách khi approve. | pending | |
| A-29 | Điểm khách DỰ KIẾN dùng KHÔNG bị khoá lúc thu ngân duyệt — duyệt chỉ kiểm tra số dư và ghi lên nháp; checkout kiểm tra lại và từ chối nếu không đủ. | medium | no | Đo bằng `run_sales_flow.py` B3 (2026-09-22): hai đơn cùng khách cùng đòi hết 139 điểm → cả hai PROCESSED; checkout #1 OK, checkout #2 → 400 `Insufficient points`. Không mất điểm, nhưng thu ngân ôm một đơn PROCESSED có nháp không thanh toán được cho tới khi sửa điểm trên hoá đơn (`PATCH /invoices/:id/points`). Chọn: (a) giữ, thêm hướng dẫn trên POS/mobile; (b) ticket giữ chỗ điểm (cột reserved trên thẻ, trừ lúc duyệt, trả lúc huỷ/từ chối). | pending | |

### Đợt 2 — yêu cầu khách 2026-09-24 (duyệt đơn · nhãn thiếu hàng · chọn chi nhánh từng đơn)

Bốn câu Akenzy chốt 2026-09-24: "Duyệt đơn" là **trạng thái mới Đã duyệt** · đủ/thiếu so với **tồn thực tế** (không trừ đơn đang chờ) · chọn chi nhánh bằng **cột inline trên lưới**, bỏ dialog phân chung · Validate báo cáo **theo chi nhánh đã chọn**. Cách biểu diễn "Đã duyệt" trong DB (giá trị enum mới hay cột `approved_at`) là quyết định thiết kế, để ở `03-logical-design.md`.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-30 | Duyệt là bước **bắt buộc trước khi phân**: pool Điều phối chứa cả đơn Chờ duyệt lẫn Đã duyệt, và chỉ đơn Đã duyệt mới phân (Lưu) được. | medium | yes | Đổi tiền điều kiện của AC-10 và `dispatch()` (`sales-order.service.ts:935`), e2e `admin-dispatch` phải sửa. Nếu duyệt chỉ là nhãn tham khảo thì guard này là thừa và chặn nhầm người điều phối. | resolved | **Thay bởi A-41** (Akenzy 2026-09-24, lần 2): điều phối phân đơn KHÔNG cần duyệt; duyệt chuyển sang chi nhánh. |
| A-31 | Duyệt chỉ áp cho đơn **trong pool** (`branch_id IS NULL`, tức đơn đối tác). Đơn tư vấn viên gửi từ mobile đã có chi nhánh, đi thẳng tới thu ngân như hiện nay. | high | yes | Nếu đơn mobile cũng phải duyệt thì đụng màn chi nhánh, app mobile và luồng thu ngân (`SENT → PROCESSED`) — gấp đôi phạm vi. | resolved | **Thay bởi A-43**: vẫn chỉ đơn từ điều phối, nhưng duyệt khi đơn đã ở chi nhánh (không còn "trong pool"). |
| A-32 | Cảnh báo lúc **duyệt** tính trên tồn **toàn chuỗi** (đơn chưa có chi nhánh), cùng phép tính với nhãn thiếu hàng của Task 2; Validate ở Task 3 mới tính theo chi nhánh đã chọn. | high | no | Nếu duyệt phải tính theo một chi nhánh thì màn duyệt cần chọn chi nhánh trước — trùng Task 3. | resolved | **Thay bởi A-44**: cảnh báo lúc duyệt tính theo tồn của chi nhánh đang giữ đơn. |
| A-33 | Đủ/thiếu so với **tồn thực tế** `SUM(stock_balances.quantity)`, không trừ SL của các đơn đang chờ. Hai đơn có thể cùng "đủ" trên cùng một món. | high | no | Nếu phải trừ đơn chờ thì cần truy vấn cam kết theo từng món, ảnh hưởng hiệu năng màn Điều phối. | resolved | Akenzy chốt 2026-09-24: tồn thực tế, không trừ đơn đang chờ. |
| A-34 | "Toàn chuỗi" = tổng `stock_balances.quantity` trên **mọi vị trí của mọi chi nhánh** trong tổ chức, theo `item_id` giải từ `itemCode` của dòng đơn (T-01-08). Gồm cả vị trí `is_tracked = false`; số âm cộng nguyên. | medium | no | Nếu phải loại vị trí ngừng theo dõi / chi nhánh ngừng hoạt động thì chỉ đổi một mệnh đề WHERE, nhưng số trên nhãn lệch với màn tồn kho. | pending | |
| A-35 | Nhãn "Thiếu hàng" là **snapshot lúc nhận đơn** (lưu trên đơn + từng dòng thiếu), không tự cập nhật khi tồn đổi. Màn duyệt và Validate luôn tính lại số sống. | medium | no | Nếu nhãn phải sống theo tồn thì thành cột tính lúc đọc (subquery mỗi dòng lưới) — đổi thiết kế dữ liệu, không đổi UI. | pending | |
| A-36 | Đơn thiếu hàng **vẫn được nhận** (201, vào pool). Nhãn là nội bộ: response của `POST /v2/partner/orders` không đổi, đối tác không biết đơn thiếu. | high | no | Nếu đối tác cần biết thì thêm một trường vào response + tài liệu API đối tác. | pending | |
| A-37 | Chọn chi nhánh ở **header cột** điền cho **mọi dòng trên trang đang xem**, ghi đè lựa chọn trước; sau đó sửa riêng từng dòng được. | medium | no | Nếu chỉ điền cho dòng đã tick thì đổi một điều kiện ở UI. | resolved | **Thay bởi A-48** (Akenzy 2026-09-24, lần 3): ô đầu cột nằm trong dialog Điều phối, điền cho mọi đơn đã tick. |
| A-38 | **Lưu** phân mọi dòng đã có chi nhánh (không cần tick); dòng chưa chọn bỏ qua. Không bắt buộc bấm Validate trước; thiếu hàng không chặn Lưu (cùng tinh thần "đồng ý vẫn được tạo đơn" của Task 1). | medium | no | Nếu Lưu phải chặn khi thiếu hàng hoặc bắt buộc Validate thì thêm một dialog xác nhận như lúc duyệt. | resolved | Giữ nguyên ý (không bắt Validate, thiếu không chặn), nhưng "Lưu" nằm trong dialog Điều phối và chỉ gửi các đơn đã tick có chi nhánh (A-48). |
| A-39 | Báo cáo Validate: mỗi đơn × mỗi dòng hàng, tồn tại chi nhánh đã chọn, dòng thiếu tô vàng; **đơn đủ hết đứng trước, đơn thiếu sau** (thiếu nhiều dòng hơn xuống cuối); trong một đơn dòng đủ trước dòng thiếu. Đơn chưa chọn chi nhánh không vào báo cáo. | medium | no | Chỉ đổi thứ tự/khung hiển thị trong dialog. | pending | |
| A-40 | Quyền duyệt dùng lại `pos.sales-order.dispatch` — không thêm permission mới. | medium | no | Nếu phải tách người duyệt khỏi người phân thì thêm permission + seed (và dính A-27: quyền chỉ vào DB khi chạy seed). | resolved | **Thay bởi A-46**: duyệt là việc của chi nhánh, không dùng quyền điều phối. |

### Đợt 2, lần 2 — Akenzy 2026-09-24: "điều phối thì không có duyệt đơn, duyệt đơn phải bên đơn hàng"

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-41 | Duyệt đơn nằm ở **màn đơn hàng của chi nhánh** (`/orders`), **sau** khi điều phối đã phân. Màn Điều phối chỉ phân (chọn chi nhánh, Validate, Lưu), không có nút duyệt, không đòi đơn đã duyệt. | high | yes | Đảo guard `ORDER_NOT_CONFIRMED` trên `dispatch()`, chuyển UI và endpoint duyệt sang phạm vi chi nhánh. | resolved | Akenzy chốt 2026-09-24 (câu hỏi "Duyệt ở đâu" → "Danh sách đơn chi nhánh"). |
| A-42 | Duyệt **bắt buộc trước khi thu ngân xử lý**: `approve()` (thu ngân, POS) từ chối đơn web chưa duyệt bằng 409 `ORDER_NOT_CONFIRMED`, không tạo hoá đơn nháp. | high | yes | Nếu duyệt chỉ là đánh dấu thì guard này chặn nhầm thu ngân. | resolved | Akenzy chốt 2026-09-24: "Bắt buộc trước xử lý". |
| A-43 | Chỉ **đơn từ điều phối** (đơn web/đối tác, `salesperson_id IS NULL`) phải duyệt. Đơn tư vấn viên gửi từ mobile giữ luồng cũ: không có trạng thái duyệt, thu ngân xử lý như trước. | high | yes | Nếu mọi đơn phải duyệt thì đụng app mobile và luồng tư vấn viên đang chạy. | resolved | Akenzy chốt 2026-09-24: "Chỉ đơn từ điều phối". |
| A-44 | Cảnh báo thiếu hàng lúc duyệt tính theo **tồn của chi nhánh đang giữ đơn** (dùng lại phép tính `forItems(org, items, branchId)`), không theo toàn chuỗi. | high | no | Chỉ đổi tham số `branchId` của phép kiểm tồn. | resolved | Akenzy chốt 2026-09-24: "Tồn của chi nhánh đó". |
| A-45 | **Trả đơn về pool xoá duyệt** (`confirmed_at/by := NULL`); chi nhánh nhận sau phải tự duyệt lại. Lịch sử giữ nguyên dòng `CONFIRM` cũ. | high | no | Nếu giữ duyệt thì chi nhánh mới nhận đơn "đã duyệt" bởi người không thuộc chi nhánh mình. | resolved | Akenzy chốt 2026-09-24: "Xoá duyệt". |
| A-46 | Quyền duyệt ở chi nhánh dùng lại **`pos.sales-order.approve`** (quyền xử lý đơn của chi nhánh) — không thêm permission mới, tránh A-27 (quyền mới chỉ vào DB khi chạy seed). Chi nhánh chỉ duyệt được đơn **của chính mình** (`BranchScopeGuard` + `branch_id = actor.branchId`). | medium | no | Nếu phải tách người duyệt khỏi thu ngân thì thêm permission + seed + cấp cho vai trò quản lý chi nhánh. | pending | |
| A-47 | Thu ngân (app mobile, ngoài repo này) lấy việc qua `GET /mobile/sales-orders?awaitingCashier=true`. Đơn web **chưa duyệt bị loại khỏi danh sách đó ở server**, nên thu ngân không thấy đơn chưa duyệt mà không phải sửa app; `approve()` vẫn chặn 409 `ORDER_NOT_CONFIRMED` nếu ai gọi thẳng. Lưới `/orders` của backoffice (không dùng `awaitingCashier`) vẫn thấy đủ để quản lý duyệt. | medium | no | Nếu thu ngân cần THẤY đơn chờ duyệt thì bỏ bộ lọc và app mobile phải hiện nhãn — việc của repo app. | pending | |

### Đợt 2, lần 3 — Akenzy 2026-09-24: "tick đơn xong rồi mới chọn chi nhánh từng đơn, bấm Điều phối mới phân"

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-48 | Lưới Điều phối **không** có cột chọn chi nhánh — chỉ ô tick. Tick đơn → nút **"Điều phối (n)"** → dialog liệt kê đúng các đơn đã tick, mỗi dòng một ô chọn chi nhánh, ô ở đầu cột chi nhánh điền cho cả danh sách; footer **[Validate] [Lưu]**. Validate mở báo cáo đủ/thiếu như hiện tại. Lưu phân từng đơn về chi nhánh của nó; đơn chưa chọn chi nhánh bị bỏ qua; dialog giữ mở khi có đơn lỗi, lỗi hiện đúng dòng. | high | yes | Đổi toàn bộ phần UI của US-11; API không đổi. | resolved | Akenzy chốt 2026-09-24 (chọn "Dialog từng đơn", có bản vẽ). |

### US-12 — Lịch sử điều phối (Akenzy 2026-09-24)

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-49 | Lịch sử mở bằng **modal qua nút** — không đổi hành vi click dòng (click dòng vẫn chọn dòng + panel chi tiết). | high | yes | Nếu click dòng phải mở modal thì đổi hành vi toàn lưới. | resolved | Akenzy chốt 2026-09-24: "Modal qua nút/icon". |
| A-50 | Hiện **đủ vòng đời**: nhận đơn → phân → duyệt → trả về → phân lại → thu ngân xử lý → huỷ / từ chối; mỗi mốc có thời gian, người làm, chi nhánh, lý do, trạng thái sau mốc. Trên **cả ba màn**: Điều phối, Tất cả đơn, Đơn hàng chi nhánh. | high | yes | Thiếu mốc thì phải thêm nguồn dữ liệu. | resolved | Akenzy chốt 2026-09-24: "Đủ vòng đời" + cả ba màn. |
| A-51 | **Không ghi thêm sự kiện mới**: vòng đời ghép lúc đọc từ `sales_order_dispatch_events` (DISPATCH / RETURN / CONFIRM — lặp được) và các cột của đơn cho mốc chỉ xảy ra một lần (`created_at/by`, `approved_at/by` + `invoice_id`, `rejected_at/by` + `reject_reason`, `cancelled_at/by` + `cancel_reason`). Nhờ vậy đơn cũ cũng có đủ mốc. | high | no | Nếu cần mốc lặp khác (sửa đơn, đổi phí) thì phải thêm sự kiện — ngoài phạm vi. | pending | |
| A-52 | Nút là **"Lịch sử"** trên toolbar của mỗi màn, áp cho **đơn đang chọn** (dòng vừa click); khoá khi chưa chọn dòng nào. | medium | no | Nếu muốn icon trên từng dòng thì thêm một cột — đổi UI, không đổi API. | pending | |
| A-53 | Chi nhánh chỉ xem được lịch sử của đơn **mình đang giữ**, nhưng thấy **toàn bộ** lịch sử đơn đó (kể cả "Cà Mau trả về: hết hàng"). Admin (`dispatch` hoặc `read-all`) xem mọi đơn trong tổ chức. | medium | no | Nếu chi nhánh không được thấy tên chi nhánh khác thì che trường chi nhánh ở mốc không thuộc mình. | pending | |
| A-54 | Người tạo đơn web là shadow user của API key — mốc "Nhận đơn" hiện **tên kênh** ("Website công ty"), không hiện tên shadow user. Mốc CONFIRM không lưu chi nhánh (CHECK đòi NULL) — chi nhánh duyệt suy từ DISPATCH gần nhất trước nó. | high | no | Sai tên hiển thị, không sai dữ liệu. | pending | |
