---
id: T-02-01
uow: UOW-02
title: Bảng sales_order_dispatch_events
layer: migration
type: feature
estimate: 2h
status: done
depends_on: []
blocks: [T-02-02]
verifies: [AC-12]
tests: []
touches:
  - apps/api/src/modules/sales-order/entities/sales-order-dispatch-event.entity.ts   # new
  - apps/api/src/database/migrations/1790100200000-CreateSalesOrderDispatchEvents.ts   # new
assumptions: [A-06]
---

# T-02-01 — Lịch sử điều phối

## Context
A-06: nhồi `dispatched_by` / `dispatched_at` vào `sales_orders` chỉ giữ được lần
phân gần nhất. Một đơn bị trả về hai lần sẽ mất vết lần đầu.

## Implementation notes
- `action` enum `DISPATCH | RETURN`; `from_branch_id` / `to_branch_id` nullable
  (phân lần đầu thì `from` NULL; trả về thì `to` NULL).
- `actor_user_id`, `reason` (nullable — chỉ `RETURN` bắt buộc có), `created_at`.
- Index `(organization_id, sales_order_id, created_at)`.
- Append-only: không `@UpdateDateColumn`, không soft-delete. Đây là vết, không phải bản ghi nghiệp vụ.

## Done when
- [x] `migration:run` + `revert` sạch
- [x] Ghi hai dòng cho cùng một đơn rồi đọc ra đúng thứ tự thời gian
