---
id: T-05-06
uow: UOW-05
title: e2e đơn web đi trọn đường tới công nợ COD
layer: test
type: test
estimate: 4h
status: done
depends_on: [T-05-02, T-05-04, T-05-05]
blocks: []
verifies: [AC-16, AC-17, AC-19, AC-20]
tests: []
touches:
  - apps/api/test/e2e/online-order-fulfilment.e2e-spec.ts   # new
assumptions: []
---

# T-05-06 — e2e đơn web đi trọn đường tới công nợ COD

## Implementation notes
- `OUTBOX_RELAY_DISABLED=1`.
- Đường đi đầy đủ: partner tạo đơn → Admin phân → chi nhánh **chưa mở ca** bấm
  xử lý (phải 409 `NO_OPEN_SESSION`, đơn không đổi) → mở ca → xử lý → hoàn tất →
  kiểm tồn, `amount_due`, `shipping_fee_amount`, `sales_channel`,
  `sales_order_id`, `invoice_debts` → tất toán công nợ.
- Ca kiểm âm bắt buộc: đọc `/mobile/sales-orders` bằng `X-Branch-Id` của chi
  nhánh kia → 0 dòng.

## Done when
- [x] `pnpm --filter @erp/api test:e2e -- online-order-fulfilment` xanh, dán output
- [x] Ca chưa mở ca chạy TRƯỚC ca mở ca trong cùng spec, để chứng minh đơn không bị hỏng bởi lần thử hỏng
