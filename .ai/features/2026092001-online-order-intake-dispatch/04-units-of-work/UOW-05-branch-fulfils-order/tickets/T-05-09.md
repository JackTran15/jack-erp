---
id: T-05-09
uow: UOW-05
title: Lưới đơn trả invoiceCode — tra mã hoá đơn theo lô, không chỉ ở chi tiết
layer: service
type: feature
estimate: 1h
status: done
depends_on: [T-05-07, T-05-08]
blocks: []
verifies: [AC-16]
tests:
  - apps/api/src/modules/sales-order/sales-order.service.spec.ts
touches:
  - apps/api/src/modules/sales-order/sales-order.service.ts
  - apps/api/src/modules/sales-order/sales-order.service.spec.ts
assumptions: []
---

# T-05-09 — Cột "Hóa đơn" trên lưới không được trống

## Context
`SalesOrderView.invoiceCode` chỉ được điền ở `getById` ("chỉ ở đường CHI TIẾT
mới tra mã hoá đơn — danh sách không N+1"). Hai lưới (`GET /mobile/sales-orders`,
`GET /admin/sales-orders`) luôn trả `null`. Backoffice `OrdersAllPage` có cột
"Hóa đơn" (`order-columns.ts`, renderer `invoiceLink`, frozen mặc định) → luôn
trống. `run_sales_flow.py` bắt được 2026-09-22.

## Implementation notes
- `invoiceCodesOf(rows, organizationId)` — một câu `IN (ids)` cho cả trang, cùng
  hình với `branchNamesOf`; `organizationId` trong WHERE. Không N+1.
- Cả hai list gọi nó và truyền vào `toView(row, lines, code)`.

## Done when
- [x] Spec listForOrganization: dòng có `invoiceId` mang `invoiceCode`, dòng pool `null`; `invoices.find` gọi ĐÚNG 1 lần, `findOne` không gọi
- [x] `run_sales_flow.py`: lưới `?status=PROCESSED` trả `invoiceCode` thật của B1 (kiểm tra thay cho NOTE)
