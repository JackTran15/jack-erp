---
id: T-08-02
uow: UOW-08
title: StockAvailabilityService — tồn theo item, toàn chuỗi hoặc một chi nhánh
layer: service
type: feature
estimate: 2h
status: done
depends_on: []
blocks: [T-08-03, T-09-03]
verifies: [AC-36]
tests:
  - apps/api/src/modules/sales-order/stock-availability.service.spec.ts
touches:
  - apps/api/src/modules/sales-order/stock-availability.service.ts   # new
  - apps/api/src/modules/sales-order/stock-availability.service.spec.ts   # new
  - apps/api/src/modules/sales-order/sales-order.module.ts
assumptions: [A-33, A-34]
---

# T-08-02 — StockAvailabilityService — tồn theo item, toàn chuỗi hoặc một chi nhánh

## Context
ADR-09: một phép tính dùng cho nhận đơn, duyệt và Validate.

## Implementation notes
- `forItems(orgId, itemIds, branchId?, manager?) → Map<itemId, number>`; `SUM(quantity)` trên `stock_balances`, không lọc `is_tracked`, số âm cộng nguyên (A-34).
- Item không có dòng balance nào → 0, không vắng khỏi map.
- Nhận `EntityManager` tuỳ chọn để chạy trong transaction của `createFromPartner`.
- Trả `number` (parse numeric), không trả chuỗi.

## Done when
- [x] Unit test: tồn rải hai chi nhánh cộng đủ; lọc theo một chi nhánh; item không có balance = 0; số âm cộng nguyên
- [x] Không đọc `stock_balances` của tổ chức khác (test)
