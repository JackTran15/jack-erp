---
id: T-09-01
uow: UOW-09
title: Migration + entity: confirmed_at/by, action CONFIRM
layer: migration
type: feature
estimate: 2h
status: done
depends_on: [T-08-01]
blocks: [T-09-02]
verifies: [AC-30]
tests: []
touches:
  - apps/api/src/modules/sales-order/entities/sales-order.entity.ts
  - apps/api/src/modules/sales-order/entities/sales-order-dispatch-event.entity.ts
  - apps/api/src/database/migrations/1790100600000-AddSalesOrderConfirmation.ts   # new
assumptions: [A-30]
---

# T-09-01 — Migration + entity: confirmed_at/by, action CONFIRM

## Context
ADR-08: "Đã duyệt" là cột, không phải giá trị status.

## Implementation notes
- `confirmed_at timestamptz NULL`, `confirmed_by uuid NULL`.
- `ALTER TYPE sales_order_dispatch_action_enum ADD VALUE 'CONFIRM'` — không chạy được trong transaction trên PG cũ: kiểm tra `transaction = false` cho migration này nếu cần; `down()` phải dựng lại enum (PG không DROP VALUE).
- Sửa `CHK_sales_order_dispatch_events_shape`: nhánh `CONFIRM` ⇒ `from_branch_id IS NULL AND to_branch_id IS NULL`.

## Done when
- [x] `migration:run` + `migration:revert` + `run` lại sạch
- [x] Insert thử một dòng `CONFIRM` qua SQL thành công, một dòng `CONFIRM` có `to_branch_id` bị CHECK từ chối
