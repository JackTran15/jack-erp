---
id: T-09-03
uow: UOW-09
title: Endpoint confirm + stock-check (sắp xếp đủ → thiếu) + regen api-client
layer: api
type: feature
estimate: 3h
status: done
depends_on: [T-09-02, T-08-02]
blocks: [T-09-04, T-09-05]
verifies: [AC-31, AC-35]
tests:
  - apps/api/src/modules/sales-order/stock-availability.service.spec.ts   # new (T-08-02)
touches:
  - apps/api/src/modules/sales-order/controllers/admin-sales-order.controller.ts
  - apps/api/src/modules/sales-order/dto/stock-check.dto.ts   # new
  - apps/api/src/modules/sales-order/stock-availability.service.ts   # new (T-08-02)
  - apps/api/src/modules/sales-order/stock-availability.service.spec.ts   # new (T-08-02)
  - packages/api-client/openapi.snapshot.json
  - packages/api-client/src/generated/schema.ts
assumptions: [A-32, A-39, A-40]
---

# T-09-03 — Endpoint confirm + stock-check (sắp xếp đủ → thiếu) + regen api-client

## Context
ADR-09: một endpoint đọc sống cho cả dialog duyệt (toàn chuỗi) và Validate (theo chi nhánh của từng đơn).

## Implementation notes
- `POST /admin/sales-orders/:id/confirm` — `@RequirePermission('pos.sales-order.dispatch')` (A-40).
- `POST /admin/sales-orders/stock-check { orders: [{ orderId, branchId? }] }` (tối đa 100) — cùng quyền; đơn ngoài tổ chức bị bỏ qua, không lộ.
- Service `checkOrders()`: gộp SL theo item mỗi đơn, gọi `forItems` một lần cho toàn chuỗi + một lần cho mỗi chi nhánh khác nhau.
- Xếp: `shortLineCount` tăng dần, rồi mã đơn; trong đơn dòng đủ trước dòng thiếu (A-39).

## Done when
- [x] Unit: thứ tự X, Y, Z của AC-31; branchId theo đơn cho ra tồn đúng chi nhánh
- [x] `pnpm openapi:generate` chạy, file sinh có trong cây làm việc (chưa commit theo yêu cầu), không sửa tay
