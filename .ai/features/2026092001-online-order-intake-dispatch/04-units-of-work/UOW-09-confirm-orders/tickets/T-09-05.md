---
id: T-09-05
uow: UOW-09
title: Nút "Duyệt đơn" + dialog cảnh báo thiếu hàng + cột Chờ duyệt/Đã duyệt
layer: frontend
type: feature
estimate: 4h
status: done
depends_on: [T-09-03, T-08-05]
blocks: [T-10-01]
verifies: [AC-30, AC-31, AC-32, AC-33]
tests: []
touches:
  - apps/backoffice-web/src/pages/orders/OrdersDispatchPage/OrdersDispatchPage.tsx
  - apps/backoffice-web/src/pages/orders/OrdersDispatchPage/ConfirmOrdersDialog.tsx   # new
  - apps/backoffice-web/src/hooks/orders/use-admin-sales-orders.ts
  - apps/backoffice-web/src/pages/orders/_lib/order-mapper.ts
  - apps/backoffice-web/src/pages/orders/_lib/build-order-columns.tsx
assumptions: [A-32, A-39]
---

# T-09-05 — Nút "Duyệt đơn" + dialog cảnh báo thiếu hàng + cột Chờ duyệt/Đã duyệt

## Context
Task 1: duyệt nhiều đơn, cảnh báo nếu thiếu, xếp đủ → thiếu, đồng ý vẫn duyệt.

## Implementation notes
- Bấm "Duyệt đơn" → gọi `stock-check` (không branchId) cho các đơn đã tick. Tất cả đủ → confirm ngay; có đơn thiếu → mở `ConfirmOrdersDialog` (vẽ theo thứ tự server trả, dòng thiếu nền vàng, [Huỷ] [Vẫn duyệt]).
- `useConfirmSalesOrders`: lặp per-id như `useDispatchSalesOrders`, gom `confirmed[]` / `failed[]`; lỗi hiện cạnh mã đơn trong dialog.
- Cột trạng thái: "Chờ duyệt" / "Đã duyệt" từ `confirmedAt`, cạnh badge Thiếu hàng.

## Done when
- [x] `pnpm --filter @erp/backoffice-web build` xanh
