---
feature: untracked-location-summary-report
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | For the report location column, a warehouse `(item × location)` pair counts as the item's shelf when `stock_balances.is_tracked = true`, **regardless of quantity** — the `sb.quantity > 0` predicate at `item-warehouse-location.util.ts:211` is dropped. This applies to all four reports that share the resolver (Tổng hợp nhập xuất tồn kho, Chi tiết doanh thu theo HĐ & mặt hàng, Doanh thu theo mặt hàng, Lợi nhuận theo mặt hàng); in the first one it also means the showroom fallback fires less often, because a tracked-but-empty warehouse shelf now counts. Same principle Akenzy chose on 2026-09-04 for "Đã xếp" (A-03 of `2026090401`). | high | yes | The whole of UOW-02 is reverted; the alternative — keep the quantity rule and instead add a preferred-shelf mapping whenever a balance row is created — is a write-path change in every movement flow, a different feature | **resolved** | Akenzy, 2026-09-21, asked directly: tracked at any quantity, across all four reports. ADR-02 accepted. |
| A-02 | (a) is fixed in the dialog only. The parent "Tổng hợp tồn kho" grid keeps summing `Tồn đầu kỳ` / `SL nhập` / `SL xuất` over every ledger row of the (item × storage) (`stock-summary.service.ts:409-427`), including rows at pairs that were later stopped. Consequence: when a stopped pair had movements inside the selected period, the dialog's footer for those three columns will be smaller than the parent cell (its `SL tồn` already matches, since both sides filter balances by `is_tracked`). | medium | yes | If the grid must agree, UOW-01 grows a second ticket that adds the same `NOT EXISTS (… is_tracked = false)` predicate to the grid's `periodQuery` and to its footer/export paths — and changes numbers users already see on the grid, which is why it is asked rather than assumed | **resolved** | Akenzy, 2026-09-21: dialog only. The grid's period columns stay as they are; the footer/parent drift on those three columns when a stopped pair moved in-period is accepted and recorded in ADR-01. |
| A-03 | The revenue and profit reports keep printing an empty "Mã vị trí" for an item whose only shelves are in the showroom (no `showroomFallback`), exactly as AC-08 of `2026091002` decided. The QA line "Chi tiết vị trí rỗng" is read as the tracked-warehouse-pair case in the screenshot (`TRUC79903-K-35` on `K09.01`), not as a request to fall back to the showroom. | medium | no | A one-line follow-up — pass `{ showroomFallback: true }` at `invoice-item-revenue-detail.report.ts:535` — plus a new AC; nothing in this feature has to be undone | **resolved** | Akenzy, 2026-09-21: keep the cell empty for showroom-only items; AC-08 of `2026091002` stands. Other blank cells in the screenshot are not part of this bug. |
| A-04 | In the dialog, "stopped" means a `stock_balances` row with `is_tracked = false` for that exact `(organization, item, location)`. A pair whose balance row was **deleted** ("Bỏ hàng hóa khỏi vị trí" at quantity 0, `inventory-location-stock.service.ts`) but which still has ledger history keeps appearing through the ledger arm — that arm exists for exactly those rows, and the bug report does not mention them. | high | no | If those rows should also disappear, the ledger arm would need a second predicate (`EXISTS` a tracked balance row) — a design change to the period view, not a filter tweak | pending | — |
| A-05 | No frontend change is needed for either symptom: `SkuBreakdownDialog.tsx` renders the API's `data` / `totals` / `itemCount` as-is, and the report column `locationCode` is a plain backed STRING column (`invoice-item-revenue.columns.ts:94`). Verified by reading both files; the API row set is the only thing that changes. | high | no | A stray client-side cache (`queryKey: ["sku-breakdown", body]`) would show stale rows until refetch; TanStack Query refetches on mount with the default `staleTime: 0`, so at worst one reopen | pending | — |
