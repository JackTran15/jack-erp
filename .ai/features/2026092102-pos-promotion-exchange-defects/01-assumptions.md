---
feature: pos-promotion-exchange-defects
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | CTKM áp cho dòng *mua thêm* ở **cả** đổi trả nhanh (`QUICK_EXCHANGE`) lẫn đổi trả theo hóa đơn (`INVOICE_RETURN`) — cùng một nhánh evaluate trên dòng OUT trong `CheckoutReturnService`, không tách theo `originalInvoiceId` | high | yes | UOW-03 phải thêm điều kiện variant ở cả FE lẫn BE; AC-13 bỏ | confirmed | Akenzy chốt 2026-09-21 qua vòng hỏi /plan ("Cả hai luồng") |
| A-02 | Tích điểm phiếu đổi tính trên tiền mua thêm **sau** CTKM: `pointsEarned = floor((newSubtotal − promotionDiscount) / POINT_EARN_VND_PER_POINT)` — cùng cơ sở `amountDue` của hóa đơn bán | high | yes | Một dòng trong `CheckoutReturnService`; AC-15 đổi số | confirmed | Akenzy chốt 2026-09-21 qua vòng hỏi /plan ("Trừ CTKM rồi mới tích") |
| A-03 | Dòng trả vẽ CTKM **cùng kiểu dòng bán**: nhãn đỏ nghiêng `Tên CTKM (số tiền)` dưới tên, *Thành tiền* gạch `−(unitPrice × SL)` và ghi `−(refundableUnitPrice × SL)`; số tiền nhãn = phân bổ/đơn vị × SL đang trả | high | no | Chỉ `InvoiceLineItemRow` + `formatPromotionLabel` | confirmed | Akenzy chốt 2026-09-21 qua vòng hỏi /plan ("Cùng kiểu dòng bán") |
| A-04 | Bản in phiếu đổi/trả: dòng mua thêm mang nhãn CTKM (đã đi qua `promotionEngineDiscounts` của `buildCheckoutInvoicePayload`), dòng trả **không** | high | no | Thêm ~1 ticket lớp in nếu đảo | confirmed | Akenzy chốt 2026-09-21 qua vòng hỏi /plan ("Dòng mua thêm có, dòng trả không") |
| A-05 | Cột *Mô tả* của modal = `promotion_programs.description` (ô "Mô tả" trên form backoffice), rỗng → "—"; không tự sinh câu tóm tắt từ cấu hình chương trình | medium | no | Một mapper (`promotionPresentation.ts`) + 1 field trên 3 interface | confirmed | Đúng — `PromotionResolver` chép `program.description` nguyên văn lên cả ba nhóm (e2e AC-01), mapper POS `programDescription()` đổi rỗng → undefined và `PromotionRow` in "—" (ảnh M-03, CTKM-B không mô tả). Không sinh câu tóm tắt nào |
| A-06 | Quà tặng (`gifts[]`) engine trả cho dòng mua thêm bị **bỏ qua** trong phiếu đổi — không sinh dòng `is_gift`, không trừ tồn quà. `GIFT_ITEM` có `discountAmount = 0` nên tiền không lệch | high | no | Thêm bước sinh dòng quà vào `CheckoutReturnService` (copy từ `persist-invoice.step.ts`) | confirmed | Đúng — `CheckoutReturnService` chỉ truyền `appliedPrograms` vào `persistAppliedPromotions` (ghi `promotion_discount` + snapshot), không sinh dòng `is_gift`; comment tại chỗ gọi (T-03-03). `GIFT_ITEM` có `discountAmount = 0` nên `newPromotionDiscount` không lệch |
| A-07 | `invoices.discount_amount` của phiếu đổi = Σ `promotionDiscount` dòng OUT (hôm nay `create-exchange-invoice.service.ts:131` ghi cứng 0) — cùng nghĩa với hóa đơn bán (`manual + promotion`, giảm tay đã nằm trong `lineTotal`). `refundableFactor` kẹp phần dư về 0 nên tiền hoàn về sau không đổi | medium | no | Báo cáo bán hàng (cột KM) bắt đầu cộng CTKM của phiếu đổi; nếu không muốn thì giữ 0 và chỉ đọc snapshot | confirmed | Đúng — `invoice.discountAmount = totals.newPromotionDiscount` ở checkout-return; e2e AC-16: 60.000 khi có CTKM, 0 khi không; smoke DB dev `discount_amount 68500` (T-03-03) |
| A-08 | FE quyết định chiều tiền y hệt BE: `net = (newSubtotal − promotionDiscount) − returnedNet`, trong đó `returnedNet = Σ refundableUnitPrice × SL` cho dòng trả có HĐ gốc. **Phát hiện khi đọc mã:** hôm nay `use-checkout-actions.ts` dùng `payloadLineSubtotal` (giá niêm yết) cho vế trả, trong khi BE `computeTotals` dùng `returnedNet` — với HĐ gốc có KM và món mua thêm nằm giữa hai số đó, FE gửi hoàn tiền còn BE đòi thu → 400 hoặc ghi nợ nhầm. Sửa vế trả là một phần của feature này | high | yes | Nếu chỉ được sửa vế mua thêm: AC-19 thu hẹp, lỗi chiều tiền với HĐ gốc có KM vẫn còn | confirmed | Akenzy chốt 2026-09-21 ("Sửa cả vế trả"). Thực hiện bằng cách FE lấy `netAmount`/`refundedAmount` từ `POST /invoices/:id/checkout-return/preview` (ADR-03 sửa) — cả hai vế do BE tính, FE không còn công thức riêng |
| A-09 | Ở tab đổi trả có dòng OUT, khi preview CTKM đang `loading` hoặc `unavailable` thì **chặn** *Thanh toán* bằng toast tiếng Việt ("Chưa tải được chương trình khuyến mãi — thử lại"). Khác luồng bán (dùng `amountDue` của draft làm trần), phiếu đổi không có draft mang số BE trước `checkout-return`, và BE trả 400 khi `Σpayments > netAmount` | medium | yes | Nếu muốn "vẫn cho thanh toán": phải thêm `netAmount` vào response `POST /invoices/exchanges` (evaluate 2 lần) — thêm 1 ticket BE + đổi contract | rejected | Akenzy chốt 2026-09-21 ("Vẫn cho thanh toán") — xem bảng Rejected |
| A-10 | `buildEvaluateCartLines` gửi `manualLineDiscount = lineDiscountAmount(line)` cho **cả** giảm tay `percent` lẫn `amount` (hôm nay chỉ `amount`, từ #144). Draft/exchange gửi `{type, value}` và BE tự quy ra tiền rồi bơm vào engine, nên preview phải gửi đúng số tiền đó để khớp. Hệ quả phụ: preview luồng **bán** cũng đúng hơn khi có giảm tay % + CTKM hóa đơn `NON_PROMO_ONLY` | medium | no | Số preview luồng bán đổi trong đúng trường hợp giảm tay %; BE không đổi | confirmed | Đúng — `buildEvaluateCartLines` gửi `manualLineDiscount = lineDiscountAmount(line)`; X-00 (`capture-uow04.py`): giảm tay 10% trên 685.000 → `manualLineDiscount: 68500`. Giỏ L-01/L-02 của 2026091804 (không giảm tay %) cho *Còn phải thu* không đổi (10/10 assert cũ vẫn ok) |
| A-11 | Snapshot CTKM của phiếu đổi ghi vào **cùng bảng** `invoice_checkout_promotions` với `invoice_id` = phiếu đổi, `line_discounts[].lineId` = `invoice_items.id` dòng OUT; `GET /invoices/:id` trên phiếu đổi tự trả `appliedPromotions` không cần sửa (đọc theo `invoice_id`) | high | no | Chi tiết/in lại phiếu đổi thiếu nhãn — chỉ hiển thị | confirmed | Đúng — snapshot ghi cùng bảng với `invoice_id` = phiếu đổi, `lineDiscounts[].lineId` = `invoice_items.id` dòng OUT (e2e AC-10); `GET /invoices/:id` trên phiếu đổi trả `appliedPromotions` không cần sửa (e2e AC-18) |
| A-12 | `eligible-returns` mở rộng mỗi dòng bằng `promotions[]` đọc từ snapshot HĐ gốc — phần phân bổ cho **mọi** loại CTKM (kể cả `INVOICE_DISCOUNT`) đều được liệt kê, vì `refundableUnitPrice` đã trừ cả hai nhóm; không tách `item`/`invoice` như ADR-02 của 2026091804 ở dòng trả | medium | no | Nhãn dòng trả liệt kê thừa/thiếu một loại; tiền không đổi | confirmed | Đúng — `loadLinePromotions` không lọc theo `type`; trên DB dev HĐ 2609180004 dòng SKU-100 liệt kê CTKM-B (`INVOICE_DISCOUNT`, unitDiscount 10.000) và `refundableUnitPrice 90.000` đã trừ đúng phần đó (T-02-01) |
| A-13 | Môi trường QA `jack-erp-pos.ducanhzed.com` dựng từ `main` (không có nhánh riêng) — ba ảnh QA phản ánh đúng mã đã đọc | low | no | Chỉ ảnh hưởng đối chiếu bằng chứng, không ảnh hưởng mã | confirmed | Không truy cập được máy chủ QA để xác minh trực tiếp; nhưng cả 3 lỗi tái hiện y hệt ảnh QA trên `main` local trước feature (chạy `capture-uow02.py` trên base commit qua `git stash`: dòng trả in 0, Tổng tiền 0; modal in "—"; tab đổi trả không có CTKM). Mã đã đọc là mã gây lỗi |
| A-14 | pos-web không có test runner (`"test": "echo test"`, không có `vitest` trong `node_modules/.bin`); bằng chứng POS = `tsc --noEmit` + ảnh headless `capture-pos-evidence.py` + assert DOM; file `.test.ts` viết kèm hàm thuần để giữ nếp nhưng không được chạy | high | no | Nếu có runner: thêm `tests:` vào ticket, không đổi mã | confirmed | Đúng — `apps/pos-web/package.json` `"test": "echo test"`, không có `vitest` trong `node_modules/.bin`, tsconfig loại `*.test.ts`. Các `.test.ts` mới/sửa được type-check qua tsconfig tạm + shim `vitest.d.ts` (T-01-02, T-04-02); bằng chứng POS = ảnh headless + assert DOM/network |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|-----------------|-----------------------|-------------|
| A-09 | Tab đổi trả có dòng mua thêm mà preview CTKM chưa sẵn sàng thì chặn *Thanh toán* | Akenzy muốn thu ngân vẫn thanh toán được; số phải là số BE chốt | ADR-05 viết lại: thêm `POST /invoices/:id/checkout-return/preview` (dry-run cùng code path load → evaluate → computeTotals, không ghi) — T-03-06 mới; AC-25 đổi nghĩa ("vẫn thanh toán, số theo BE"); AC-28 thêm; T-04-03 gọi preview sau khi tạo phiếu và dựng payload từ số BE; ADR-03 thu gọn (FE không còn công thức chiều tiền) |
