---
id: T-02-03
uow: UOW-02
title: Ảnh bằng chứng headless — dòng trả có nhãn CTKM, Thành tiền / Tổng tiền âm, trả nhanh không nhãn
layer: test
type: test
estimate: 2h
status: done
agent: aidlc-tester
depends_on: [T-02-02, T-01-02]
blocks: []
verifies: [AC-07, AC-08, AC-09]
tests: []
touches:
  - .ai/features/2026092102-pos-promotion-exchange-defects/evidence/capture-uow02.py   # new; import pos_evidence_lib, fork checkout() của 2026091804 để post HĐ gốc
assumptions: [A-13, A-14]
---

# T-02-03 — Bằng chứng UOW-02

## Context
POS không có test runner (A-14). Mẫu: `2026091804/capture-pos-evidence.py`
`--checkout` đã post một hóa đơn bán qua UI với `VITE_CHECKOUT_V2=true`; trang
`/return-goods` (`ReturnGoodsPage`, `ReturnInvoiceTable`, `ReturnItemsDialog`)
đã được lái bởi runner ở `exchange-invoice-returnable/07-verification.md`.

## Implementation notes
- `capture-uow02.py`:
  1. Đăng nhập (lib), tab bán quét SKU-685, *Thu tiền* → đọc mã HĐ S từ hóa đơn
     in / toast (fork `checkout()` của 2026091804).
  2. Điều hướng `/pos/return-goods`, tìm S (theo mã), mở hộp thoại trả, tick
     SKU-685 SL 1, *Đồng ý* → tab checkout mới.
  3. Đọc DOM dòng trả: nhãn `[data-promotion-label="return"]` = `CTKM-A … (68.500)`;
     ô *Thành tiền* có `line-through` = `-685.000` và số dưới = `-616.500`
     (số kỳ vọng tính từ fixture CTKM-A 10% — đọc lại `evaluate` để không hard-code sai).
  4. Panel: *Tổng tiền* = `-616.500`; *Trả lại khách* = `616.500`; ô Tiền mặt = `616.500`.
  5. Đóng tab, mở đổi trả nhanh với 1 × SKU-100 → không nhãn, *Thành tiền* `-100.000`.
  6. Ảnh: `R-01-return-line-ac07.png`, `R-02-panel-ac08.png`, `R-03-quick-return-ac09.png`.
- Selector của `/return-goods` và dialog: đọc từ component lúc làm; nếu thiếu
  `aria-label` ổn định thì dùng text tiếng Việt (`getByRole('button', { name: 'Đồng ý' })`).
- Ghi trong docstring: DB :4000 phải là DB có fixture CTKM-A (memory `api-db-binding-check`).

## Done when
- [x] `capture-uow02.py` exit 0, 3 ảnh (R-01/R-02/R-03) + 11 assert "ok" cho AC-07/08/09 — 2026-09-22, HĐ S = 2609220004 (SKU-685 bán 1 nên ý "SL 2" của AC-07 không tái hiện được trên S; nhãn = unitDiscount × qty đã assert ở qty 1)
- [x] *Trả lại khách* trong R-02 = 616.500; `POST /invoices/returns` (regular, 1 × SKU-685 theo S) rồi `POST checkout-return {refundMethod: CASH}` trên :4000 → `netAmount -616500, refundedAmount 616500` — trùng
