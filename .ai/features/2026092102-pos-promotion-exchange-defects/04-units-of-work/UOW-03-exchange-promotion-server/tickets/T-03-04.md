---
id: T-03-04
uow: UOW-03
title: e2e exchange-promotion — AC-10..AC-19 + AC-28 trên erp_test
layer: test
type: test
estimate: 4h
status: done
agent: aidlc-tester
depends_on: [T-03-03, T-03-06]
blocks: []
verifies: [AC-10, AC-11, AC-12, AC-13, AC-14, AC-15, AC-16, AC-17, AC-18, AC-19, AC-28]
tests:
  - apps/api/test/e2e/exchange-promotion.e2e-spec.ts
touches:
  - apps/api/test/e2e/exchange-promotion.e2e-spec.ts   # new; ghép mẫu quick-exchange-line-discount.e2e-spec.ts (exchanges + checkout-return) với checkout-saga-promotion.e2e-spec.ts (seed CTKM, post SALE v2)
assumptions: [A-01, A-02, A-07]
---

# T-03-04 — Hợp đồng số của phiếu đổi có CTKM

## Context
`quick-exchange-line-discount.e2e-spec.ts` đã có cách tạo phiếu đổi (`POST
/invoices/exchanges`) và post (`POST /invoices/:id/checkout-return`), kể cả
nhánh "theo hóa đơn gốc" (AC-05 của nó). `checkout-saga-promotion.e2e-spec.ts`
có cách seed CTKM (`seedPromotionFixtures`, `itemDiscountBody`) và post một SALE
có CTKM qua saga v2. `promotion-seed.ts`: SKU-685 = 685.000, SKU-100 = 100.000.

## Implementation notes
- Một `describe` mỗi AC, tên `it` bắt đầu bằng mã AC (`'AC-10: …'`), số kỳ vọng
  chép **nguyên** từ `02-requirements.md` — không tự tính lại trong test.
- Mỗi `it` tự tạo CTKM/phiếu riêng (id mới) để không phụ thuộc thứ tự; dọn CTKM
  ở `afterEach` bằng cách đổi status (mẫu file saga) để `findActive` không nhìn thấy.
- Assert bằng `dataSource.query` trên `invoice_items`, `invoice_checkout_promotions`,
  `invoices` — không chỉ response HTTP (success signal của intent).
- AC-13: hai lần post (không có / có `selectedProgramIds`) trên hai phiếu.
- AC-17/AC-18: sau AC-11 gọi `GET /invoices/Q/eligible-returns` và `GET /invoices/Q`.
- AC-28: preview draft E hai lần (không id / `excludedProgramIds`) → 7 số; `SELECT`
  chứng minh draft không đổi; rồi post với cùng `excludedProgramIds` → `netAmount`
  bằng preview.
- AC-19: post với `payments` khi net < 0 → expect 400 message
  `payments không được cung cấp khi netAmount < 0` (chuỗi có sẵn ở `validateRefundMatrix`).
- Chạy: `pnpm --filter @erp/api test:e2e -- exchange-promotion` (serial, DB
  `erp_test` — global-setup tự migrate). Không sửa file e2e nào khác.

## Done when
- [x] 11 `it` xanh (AC-10..AC-19, AC-28), mỗi AC đúng một `it`; 3 lần chạy, lần đầu AC-10 đỏ ở `createProgram` (201 mong đợi, lỗi tạm thời — không tái hiện ở 2 lần sau)
- [x] `test:e2e -- promotion quick-exchange` (12 suite, 89 test, 2026-09-22): 85 xanh; đỏ = `promotion-evaluate` AC-03 (đỏ sẵn trên main — memory `api-db-binding-check`) + 3 case `checkout-saga-promotion` AC-36 "socket hang up" trong lúc :4000/:3001 cùng chạy trên cùng Postgres — chạy lại riêng: 13/13 xanh
