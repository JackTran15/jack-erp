---
id: T-03-05
uow: UOW-03
title: Regenerate api-client cho CheckoutReturnDto (ids) và endpoint checkout-return/preview
layer: api
type: chore
estimate: 1h
status: todo
agent: aidlc-implementer
depends_on: [T-03-02, T-03-06]
blocks: []
verifies: []
tests: []
touches:
  - packages/api-client/src/generated/schema.ts
  - packages/api-client/openapi.snapshot.json
assumptions: []
---

# T-03-05 — `pnpm openapi:generate`

## Context
CLAUDE.md: sau khi đổi endpoint, chạy API rồi `pnpm openapi:generate`, commit cả
`schema.ts` lẫn `openapi.snapshot.json`; không sửa tay file generated.

## Implementation notes
- API trên :4000 phải là bản đã có T-03-02 (`nest --watch` tự rebuild — memory
  `nest-build-kills-dev-watcher`: **không** chạy `nest build` khi dev-api đang lên).
- `pnpm openapi:generate`; `git diff --stat packages/api-client` chỉ được 2 file;
  diff nội dung chỉ thêm `selectedProgramIds`/`excludedProgramIds` dưới
  `CheckoutReturnDto`, path `/invoices/{id}/checkout-return/preview` và hai schema
  `CheckoutReturnPreviewDto`/`CheckoutReturnPreviewResponseDto`. Bất kỳ diff nào khác (endpoint của người khác đang dở) →
  dừng, báo, không commit phần lạ.

## Done when
- [ ] Diff generated chỉ chứa 2 field mới + endpoint preview và 2 schema của nó
- [ ] `pnpm --filter @erp/api-client build` xanh; `tsc --noEmit` pos-web + backoffice-web xanh
