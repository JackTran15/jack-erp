---
id: T-04-04
uow: UOW-04
title: Ảnh bằng chứng headless — modal, nhãn, panel, payload và chặn preview ở tab đổi trả; bản in tạm tính
layer: test
type: test
estimate: 3h
status: done
agent: aidlc-tester
depends_on: [T-04-03, T-01-02]
blocks: []
verifies: [AC-21, AC-22, AC-23, AC-24, AC-25, AC-27]
tests: []
touches:
  - .ai/features/2026092102-pos-promotion-exchange-defects/evidence/capture-uow04.py   # new; import pos_evidence_lib; fork receipt_from_iframe() của 2026091804 cho AC-27
assumptions: [A-13, A-14]
---

# T-04-04 — Bằng chứng UOW-04

## Context
Cùng nền với `capture-uow02.py` (post S, mở đổi trả). Số kỳ vọng từ fixture
CTKM-A 10% trên SKU-685 (68.500; hoàn 616.500/đv) — đọc lại `POST
/v2/promotions/evaluate` lúc chạy để không hard-code sai. `page.on("request")`/`page.on("response")`
của Playwright để bắt body `POST /invoices/:id/checkout-return` và response của
`checkout-return/preview` (AC-24), và chứng minh luồng vẫn post khi evaluate POS
bị chặn (AC-25).

## Implementation notes
- Kịch bản (mỗi kịch bản một tab, chụp + assert):
  1. Trả 1 × SKU-685 theo S + mua 1 × SKU-685: modal có CTKM-A *Đã áp dụng*
     (`X-01-modal-applied-ac21.png`); dòng mua thêm nhãn `(68.500)`, *Thành tiền*
     ~~685.000~~ 616.500 (`X-02-line-ac22.png`); panel *Tổng tiền* 0, không
     *Còn phải thu*/*Trả lại khách* dương (`X-03-panel-zero-ac23.png`); *In tạm tính*
     → iframe: dòng mua thêm có nhãn + 616.500, khối trả không nhãn (`X-04-receipt-ac27.png`);
     *Thanh toán* → bắt response preview (`netAmount 0`) và body checkout-return:
     `refundMethod OFFSET`, không `payments`, không key ids (`X-05-payload-offset-ac24.json`).
  2. Như 1 nhưng bỏ CTKM-A trong modal: *Còn phải thu* 68.500; body `payments`
     tổng 68.500 + `excludedProgramIds: [CTKM-A]`; response `netAmount 68500`
     (`X-06-panel-excluded-ac23.png`, `X-07-payload-excluded-ac24.json`).
  3. Trả 1 × SKU-685 (616.500) + mua 1 × SKU-100 (100.000): *Trả lại khách* 516.500;
     body refund; response `refundedAmount 516500` (`X-08-refund-net-ac24.json`).
  4. AC-25 (a): `page.route("**/v2/promotions/evaluate", abort)` rồi quét thêm
     SKU-685 (panel không nhãn, *Còn phải thu* 68.500 vì thiếu CTKM) → *Thanh toán*
     → vẫn có `exchanges` → `checkout-return/preview` (response `netAmount 0`) →
     `checkout-return` với `OFFSET`; `SELECT` phiếu post có `promotion_discount 68500`
     (`X-09-evaluate-down-still-posts-ac25.png`, `X-10-payload-ac25.json`).
     AC-25 (b): bỏ route trên, `page.route("**/checkout-return/preview", abort)` →
     *Thanh toán* → toast `[data-sonner-toast][data-type="error"]` chứa "Không lấy
     được số tiền đổi trả"; assert **không** có request `checkout-return`
     (`X-11-preview-down-blocked-ac25.png`).
- Mỗi kịch bản post thật một phiếu đổi → S bị trả dần; post S mới cho mỗi kịch
  bản (S bán 1 × SKU-685 là đủ vì mỗi kịch bản trả 1).

## Done when
- [x] `capture-uow04.py` exit 0 (28 assert "ok"), X-01…X-11 trong `evidence/`; AC-21/22/23/24/25 đúng kịch bản. **AC-27 — ghi chú (Akenzy chốt 2026-09-22):** tab đổi trả không có nút *In tạm tính* (PaymentCTAButtons ẩn, hotkey Alt+P tắt cho return/exchange — có sẵn trước feature), nên `X-04-receipt-ac27.png` là **bản in hóa đơn ngay sau Thanh toán** (bật *In hóa đơn*; cùng `buildCheckoutInvoicePayload`/`promotionEngineDiscounts`): dòng mua thêm `CTKM-A … (68.500)` + 616.500, khối trả `-616.500` không nhãn — chưa phải bản in tạm tính
- [x] Ba cặp số ghi trong log/json: (1) panel Tổng tiền 0 / Còn phải thu 0 ↔ preview `netAmount 0`, BE 201 `netAmount 0` (X-05); (2) panel Còn phải thu 68.500 ↔ preview `netAmount 68500` ↔ BE `netAmount 68500` (X-07 — bỏ cả CTKM-A lẫn CTKM-B vì B tự áp thay A); (3) panel Trả lại khách 526.500 ↔ preview `refundedAmount 526500` ↔ BE `refundedAmount 526500` (X-08 — CTKM-B 10% áp cho SKU-100 nên không phải 516.500 của 02-requirements; số kỳ vọng đọc từ preview lúc chạy)
