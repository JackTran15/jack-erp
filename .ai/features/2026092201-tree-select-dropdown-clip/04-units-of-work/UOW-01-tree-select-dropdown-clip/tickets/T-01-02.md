---
id: T-01-02
uow: UOW-01
title: CrudFormDialog bỏ vá overflow-visible để body modal entity cây cuộn lại; chạy 07-verification
layer: web
type: feature
estimate: 1h
status: blocked
agent: aidlc-implementer
depends_on: [T-01-01]
blocks: []
verifies: [AC-05]
tests: []
touches:
  - apps/backoffice-web/src/components/crud/CrudFormDialog.tsx
assumptions: [A-04, A-06]
---

# T-01-02 — Bỏ `overflow-visible`, chạy kịch bản verify

## Context

`CrudFormDialog.tsx:423-443`: `isTreeEntity` ⇒ modal 720×560 **và** `bodyClassName="overflow-visible"` (#282). Vá
này không thoát được `overflow-hidden` của lớp bọc body trong `AppModal` (`app-modal.tsx:607`) nhưng bỏ mất
`overflow-auto` của body (`:619`) ⇒ form entity cây không cuộn được ở màn hình thấp. Sau T-01-01 dropdown không còn
phụ thuộc body nên vá này chỉ còn tác hại (ADR-02).

Phụ thuộc T-01-01: bỏ vá trước khi portal thì dropdown vẫn cắt như cũ, demo không cho thấy gì.

## Implementation notes

- Xoá dòng `bodyClassName={isTreeEntity ? "overflow-visible" : undefined}`.
- Sửa comment `:423-424` thành lý do còn đúng: entity cây có picker Mục cha nên modal rộng/cao hơn; dropdown tự
  portal (T-01-01). Giữ `defaultWidth`/`defaultHeight` 720×560.
- Không đụng `AppModal`, không đụng `StockSummaryFilterPopover` (A-08).
- Chạy `07-verification.md` ở `local-backoffice` với viewport desktop + laptop; ảnh vào `evidence/` (git-ignored — tham
  chiếu tên file trong ticket).

## Done when

- [x] `pnpm --filter @erp/backoffice-web exec tsc --noEmit` sạch — exit 0, 22/09/2026
- [x] 1440×720, Sửa `THU_KHAC`, danh sách đóng: lăn chuột trong vùng nội dung tới được ô "Thứ tự hiển thị" — body `overflow-y: auto` (trước: `visible`); ở 720 form 443px vừa body 441px nên không cần cuộn, ở **1440×560** body 409px cuộn 34px, ô 430→470 nằm trên footer 491 — `evidence/AC-05-h720-body-scrolled.png`, `AC-05-h560-body-scrolled.png` (AC-05)
- [x] Dialog Sửa Nhóm hàng hoá (entity cây còn lại) cũng cuộn được, dropdown vẫn không bị cắt — body `overflow-y: auto`, 14 mục trong dialog và viewport, chọn `03 · QUÀ TẶNG` dialog còn mở — `evidence/AC-06-item-category-dialog.png` (không hồi quy T-01-01)
- [ ] `07-verification.md` chạy xanh ở cả hai viewport; tên ảnh ghi vào ticket

## Kết quả 22/09/2026

`CrudFormDialog.tsx`: xoá `bodyClassName={isTreeEntity ? "overflow-visible" : undefined}`, comment viết lại; giữ
720×560. Bằng chứng bằng `capture-evidence.py` trên Vite `:3005` (org My Company). Trước sửa cùng script ghi
`overflowY: "visible"` cho body; sau sửa `"auto"` và cuộn được khi form tràn (viewport 560px).

Ô `07-verification.md` để trống: môi trường `local-backoffice` trong `.ai/aidlc.yaml` ghim `:3000`, cổng đó đang
phục vụ checkout `erp2`; runner không có tuỳ chọn đổi URL. Chạy được khi `:3000` trỏ về checkout này hoặc Akenzy
đồng ý thay bằng `capture-evidence.py` (cùng bước, thêm hình học popover/dialog).
