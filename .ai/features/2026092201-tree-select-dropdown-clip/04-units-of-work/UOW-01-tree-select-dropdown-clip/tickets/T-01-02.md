---
id: T-01-02
uow: UOW-01
title: CrudFormDialog bỏ vá overflow-visible để body modal entity cây cuộn lại; chạy 07-verification
layer: web
type: feature
estimate: 1h
status: todo
agent: aidlc-implementer
depends_on: [T-01-01]
blocks: []
verifies: [AC-05]
tests: []
touches:
  - apps/backoffice-web/src/components/crud/CrudFormDialog.tsx
assumptions: [A-04, A-06]
---

# T-01-02 — Bỏ `overflow-visible`, chạy kịch bản verify

## Context

`CrudFormDialog.tsx:423-443`: `isTreeEntity` ⇒ modal 720×560 **và** `bodyClassName="overflow-visible"` (#282). Vá
này không thoát được `overflow-hidden` của lớp bọc body trong `AppModal` (`app-modal.tsx:607`) nhưng bỏ mất
`overflow-auto` của body (`:619`) ⇒ form entity cây không cuộn được ở màn hình thấp. Sau T-01-01 dropdown không còn
phụ thuộc body nên vá này chỉ còn tác hại (ADR-02).

Phụ thuộc T-01-01: bỏ vá trước khi portal thì dropdown vẫn cắt như cũ, demo không cho thấy gì.

## Implementation notes

- Xoá dòng `bodyClassName={isTreeEntity ? "overflow-visible" : undefined}`.
- Sửa comment `:423-424` thành lý do còn đúng: entity cây có picker Mục cha nên modal rộng/cao hơn; dropdown tự
  portal (T-01-01). Giữ `defaultWidth`/`defaultHeight` 720×560.
- Không đụng `AppModal`, không đụng `StockSummaryFilterPopover` (A-08).
- Chạy `07-verification.md` ở `local-backoffice` với viewport desktop + laptop; ảnh vào `evidence/` (git-ignored — tham
  chiếu tên file trong ticket).

## Done when

- [ ] `pnpm --filter @erp/backoffice-web exec tsc --noEmit` sạch
- [ ] 1440×720, Sửa `THU_KHAC`, danh sách đóng: lăn chuột trong vùng nội dung tới được ô "Thứ tự hiển thị" — ảnh chụp (AC-05)
- [ ] Dialog Sửa Nhóm hàng hoá (entity cây còn lại) cũng cuộn được, dropdown vẫn không bị cắt (không hồi quy T-01-01)
- [ ] `07-verification.md` chạy xanh ở cả hai viewport; tên ảnh ghi vào ticket
