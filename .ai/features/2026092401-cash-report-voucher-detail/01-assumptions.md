---
feature: cash-report-voucher-detail
blocking_open: 0
---

# Assumption register

Bốn câu hỏi hỏi một lượt qua AskUserQuestion 2026-09-24 (A-01..A-04); phần còn lại trả lời
bằng đọc code và truy vấn erp_dev_3008.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Ô bấm được: **Số chứng từ** (#3, #5) → dialog phiếu; **Số hóa đơn** (#3, #5) và **Tham chiếu** (#3 — #5 không có cột này) → dialog hóa đơn, chỉ khi dòng gắn hóa đơn. Tham chiếu loại khác (GOODS_RECEIPT, FUND_SWAP, TRANSFER, PAYABLE, MANUAL, REVERSAL) là text | high | yes | Thêm dialog cho phiếu nhập / chuyển quỹ = thêm UoW mới, thêm khoá ẩn `referenceType/referenceId` | resolved | Akenzy chọn "Số chứng từ + Số hóa đơn + Tham chiếu" 2026-09-24 qua AskUserQuestion |
| A-02 | Dialog phiếu **dùng lại 4 dialog của Sổ quỹ** ở chế độ VIEW (`ReceiptVoucherDialog`, `PaymentVoucherDialog`, `DepositReceiptVoucherDialog`, `DepositPaymentVoucherDialog`); không truyền `onRequestEdit`/`onSave` ⇒ chỉ có In / Xuất khẩu / Đóng (`ReceiptVoucherDialog.tsx:669-700`) | high | yes | Dựng dialog read-only riêng: +1 ticket FE lớn, lệch giao diện Sổ quỹ | resolved | Akenzy chọn "Dùng lại dialog của Sổ quỹ" 2026-09-24 qua AskUserQuestion |
| A-03 | Người có quyền báo cáo nhưng thiếu quyền đọc phiếu (`accounting.{cash_receipt,cash_payment,bank_receipt,bank_payment}.read`) hoặc quyền `reporting.invoice.branch.read`: **ẩn link** (ô là text), không gọi API. Không thêm endpoint | high | yes | Nếu muốn họ vẫn xem được: endpoint `/reports/cash-fund/voucher-detail` gated `reporting.cash.read`, + OpenAPI + e2e | resolved | Akenzy chọn "Ẩn link khi thiếu quyền" 2026-09-24 qua AskUserQuestion |
| A-04 | Thêm `REFUND` vào `INVOICE_REFERENCE_TYPES` của `cash-in-out-list.report.ts:60` và `expense-list-by-category.report.ts:62` (hằng số cục bộ, không dùng chung với `CashFundPeriodService`) ⇒ Số hóa đơn / Tham chiếu của phiếu hoàn tiền có mã hóa đơn; tổng tiền và phân loại "Chi khác" của #2 không đổi | high | yes | Nếu REFUND.reference_id không phải `invoices.id`, JOIN ra NULL — không sai số, chỉ không có link | resolved | Akenzy chọn "Có, thêm REFUND" 2026-09-24 qua AskUserQuestion; erp_dev_3008: 44/44 `cash_payments` REFUND khớp `invoices.id`; consumer `invoice-cancel-refund-cash.consumer.ts` link `(REFUND, invoiceId)` |
| A-05 | GET chi tiết phiếu và hóa đơn chỉ lọc theo tổ chức, không theo chi nhánh ⇒ ở chế độ Chuỗi, dòng của chi nhánh khác chi nhánh header vẫn mở được; `BranchScopeGuard` chỉ kiểm `X-Branch-Id` của header thuộc user | high | no | Dòng khác chi nhánh 404 → cần gửi `X-Branch-Id` theo chi nhánh của dòng | confirmed | `where: { id, organizationId }` ở `cash-receipts.service.ts:1067`, `cash-payments.service.ts:1056`, `bank-receipts.service.ts:1108`, `bank-payments.service.ts:1212`; `GetInvoiceDetailHandler` nhánh `id` chỉ lọc `organizationId` |
| A-06 | Id hóa đơn đi trong khoá ẩn **`REPORT_ROW_INVOICE_ID` (`_invoiceId`)** sẵn có thay vì khoá mới trong `CASH_FUND_ROW_KEYS`, để resolver `invoiceDetail` hiện có dùng lại nguyên vẹn. Khoá ẩn không phải cột catalog ⇒ không vào export, không đổi OpenAPI | high | no | Đặt khoá mới + resolver riêng — vài dòng | confirmed | `report-drilldown.ts:66-73`; `ReportRow` là record động |
| A-07 | Loại phiếu → dialog theo `voucherKind` của dòng: `CASH_RECEIPT`→Receipt, `CASH_PAYMENT`→Payment, `BANK_RECEIPT`→DepositReceipt, `BANK_PAYMENT`→DepositPayment. Dòng không phải chi tiết (`rowKind` ∈ opening / grandTotal / group) không có `voucherId` ⇒ không bấm được | high | no | Sai map = mở nhầm dialog; lộ ngay ở demo | confirmed | `CashFundDocumentKind` (`column.ts:31`), `toRow` ở hai report gán `VOUCHER_ID/KIND` chỉ cho dòng detail |
| A-08 | Nút "Hóa đơn" trong `ReceiptVoucherDialog` (`onOpenInvoice(code)`) được nối vào `setDetailInvoice({ code, id: null })` của store báo cáo — tra theo mã, ưu tiên chi nhánh header rồi toàn tổ chức (`GetInvoiceDetailHandler`) | medium | no | Mã trùng giữa hai chi nhánh ở chế độ Chuỗi có thể ra hóa đơn chi nhánh header; chấp nhận vì Sổ quỹ cũng tra theo mã | confirmed | Cùng hành vi `LedgerCashPage.openInvoiceByCode`; `invoice-report.controller.ts:122-128` |
| A-09 | `usePermissionCheck` đọc danh sách quyền từ localStorage, không phản ứng; đủ cho gating link vì đổi chi nhánh tải lại trang | high | no | Link không cập nhật khi quyền đổi giữa phiên — chấp nhận, như mọi màn khác | confirmed | `hooks/usePermissionCheck.ts:17-30` |
