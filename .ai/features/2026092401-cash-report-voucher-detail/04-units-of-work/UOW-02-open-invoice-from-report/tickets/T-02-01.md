---
id: T-02-01
uow: UOW-02
title: API — _invoiceId trên dòng #3/#5 và REFUND tính là tham chiếu hóa đơn
layer: api
type: feature
estimate: 2h
status: done
depends_on: []
blocks: [T-02-02, T-02-03]
verifies: [AC-08]
tests:
  - apps/api/src/modules/reporting/cash-fund-report/reports/cash-in-out-list.report.spec.ts
  - apps/api/src/modules/reporting/cash-fund-report/reports/expense-list-by-category.report.spec.ts
touches:
  - apps/api/src/modules/reporting/cash-fund-report/reports/cash-in-out-list.report.ts
  - apps/api/src/modules/reporting/cash-fund-report/reports/cash-in-out-list.report.spec.ts
  - apps/api/src/modules/reporting/cash-fund-report/reports/expense-list-by-category.report.ts
  - apps/api/src/modules/reporting/cash-fund-report/reports/expense-list-by-category.report.spec.ts
assumptions: [A-04, A-06]
---

# T-02-01 — Khoá ẩn id hóa đơn + REFUND

## Implementation notes

- Cả hai file: `INVOICE_REFERENCE_TYPES` thêm `'REFUND'` (comment A-20 → nêu thêm A-04 của feature này).
- SELECT thêm `inv.id AS invoice_id` cạnh `inv.code AS invoice_number`; `RawRow.invoice_id: string | null`.
- `toRow`: `[REPORT_ROW_INVOICE_ID]: r.invoice_id ?? null` (import từ `@erp/shared-interfaces`).
- Không thêm cột catalog, không đổi `FILTERABLE`, không đổi `exportSource` ngoài việc SQL chung có thêm cột.
- Spec: dòng INVOICE và REFUND có `_invoiceId`; dòng MANUAL có `null`; `columns` không chứa `_invoiceId`.

## Done when

- [x] `pnpm --filter @erp/api test -- cash-in-out-list.report.spec.ts expense-list-by-category.report.spec.ts` xanh — 2 suites, 50/50 (thêm 2 case `_invoiceId`/REFUND + assert SQL `'REFUND'` và `inv.id AS invoice_id`)
- [x] `pnpm --filter @erp/api exec tsc --noEmit` xanh (không `nest build` khi :4000 đang chạy — memory) — `npx tsc --noEmit -p apps/api/tsconfig.json`: no errors
- [x] POST search trên erp_dev_3008 trả `_invoiceId` cho dòng REFUND, tổng không đổi so với trước — My Company, 2026, tất cả cửa hàng: #3 134 dòng, 13 REFUND (`REFUND RTN-…`) + 103 INVOICE đều có `_invoiceId` trỏ đúng hóa đơn cùng mã; count/Σ thu/Σ chi = 134 / 191.309.500 / 236.782.000 bằng SUM trực tiếp trên 4 bảng chứng từ (JOIN không nhân dòng); #5 14/20 dòng có hóa đơn mang `_invoiceId`; `/columns` không có `_invoiceId`
