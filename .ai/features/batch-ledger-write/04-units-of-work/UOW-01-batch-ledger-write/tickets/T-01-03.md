---
id: T-01-03
uow: UOW-01
title: E2E verification — real 1.686-line import, correctness + timing
layer: test
type: test
estimate: 2h
status: in_progress
depends_on: [T-01-02]           # transitively covers T-01-01, which T-01-02 already depends on
blocks: []
verifies: [AC-01]
touches:
  - apps/api/test/e2e/goods-receipt-batch-import.e2e-spec.ts   # new; follows goods-receipt-from-transfer.e2e-spec.ts conventions
assumptions: [A-04]
---

# T-01-03 — E2E verification: real 1.686-line import, correctness + timing

## Context

Closes the loop back to the actual production failure (log requests `daf45c08…`,
`dca74236…`) and to `00-intent.md`'s Success signal. Depends on both T-01-01 and T-01-02
being merged.

## Implementation notes

- New e2e spec, same seeding/auth pattern as `goods-receipt-from-transfer.e2e-spec.ts`.
- Build the 1.686-line `lines[]` payload from the real
  `NhapkhauHangHoaNhapKho SHOWROOM.xls` shape: same SKU list, same quantities, same
  warehouse/location (`Showroom BMT` / `DEFAULT`), including the 6 SKUs that appear twice.
- Assert:
  - `POST /goods-receipts` returns 200/201 with `status: "POSTED"`.
  - Wall-clock elapsed time is single-digit seconds (assert `< 15_000` ms with margin for
    CI variance — the point is "not 111-139s", not a tight perf budget).
  - `stock_balances` for the duplicated SKU (e.g. `TX3150-D`) shows quantity `+2` at
    `Showroom BMT / DEFAULT`.
  - `stock_ledger_entries` count for the receipt equals 1.686.
- Then run the full existing suite to catch regressions:
  `pnpm --filter @erp/api test -- stock-ledger`,
  `pnpm --filter @erp/api test -- item-storage-location`,
  `pnpm --filter @erp/api test:e2e`.

## Done when

- [ ] New e2e spec passes locally against the docker-compose Postgres
- [ ] Elapsed time assertion passes and is not flaky-tight (documented margin)
- [ ] `pnpm --filter @erp/api test` and `pnpm --filter @erp/api test:e2e` both green
- [ ] Actual measured elapsed time recorded back into `00-intent.md`'s Success signal
      (replace the "single-digit seconds" estimate with the real number)
