---
id: T-06-03
uow: UOW-06
title: Vitest cho gate admin và luồng hủy trên dialog
layer: test
type: test
estimate: 2h
status: blocked
depends_on: [T-06-02]
blocks: []
verifies: [AC-17, AC-18]
touches:
  - apps/pos-web/src/components/page-components/Checkout/CheckoutDialogs/CustomerDetailDialog/PurchaseHistoryTab/InvoiceReceiptDialog/InvoiceReceiptDialog.test.tsx   # new; theo hình dạng các file .test.ts sẵn có trong pos-web
assumptions: [A-09]
---

# T-06-03 — Test giao diện hủy hóa đơn

## BLOCKED — pos-web chưa có test runner

Kiểm bằng hai cách độc lập:

1. `apps/pos-web/package.json` → `"test": "echo test"`.
2. Đọc `dependencies` + `devDependencies` bằng script: **không** có `vitest`,
   `@testing-library/*`, hay `jsdom`. Không có `vitest.config.*`.

Ba file `*.test.ts` đang nằm trong `src/lib/page-libs/checkout/` **chưa từng được chạy**.
Kế hoạch G3 giả định sai rằng chúng có runner.

Muốn làm ticket này thì phải dựng hạ tầng test cho pos-web trước: thêm `vitest`,
`@testing-library/react`, `jsdom`, cấu hình, và sửa script `test`. Đó là quyết định thêm
dependency cho repo, không phải chi tiết trong phạm vi ticket → cần Akenzy quyết.

Phần đỡ được đã làm ở T-06-02: điều kiện hiện nút tách thành hàm thuần
`canCancelInvoice()` trong `constants/invoice-cancel.constant.ts`, nên khi có runner thì
test chỉ là gọi hàm, không cần render dialog.

## Context

Gate quyền nằm ở frontend (A-09), nghĩa là không có test backend nào bảo vệ nó. Nếu không
có test ở đây thì một lần refactor là nút hiện ra cho mọi người.

## Implementation notes

- Mock `useCurrentUserQuery` để đổi roles giữa các case.
- Mock mutation hủy; assert nó được gọi với `reason` đã nhập.
- Case lỗi: mutation reject với message → khẳng định message hiển thị nguyên văn.

## Done when

- [ ] Case admin → nút hiện (AC-17)
- [ ] Case không phải admin → nút không hiện (AC-18)
- [ ] Case xác nhận → mutation được gọi đúng `reason`
- [ ] Case lỗi → message từ API hiển thị nguyên văn
- [ ] `pnpm --filter @erp/pos-web test` xanh
