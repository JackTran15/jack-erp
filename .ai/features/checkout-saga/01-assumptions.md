---
feature: checkout-saga
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Mô hình nhất quán là **một transaction + outbox**: mọi ghi DB (kể cả trừ kho, bút toán GL, thu quỹ, quỹ tiền gửi) nằm trong một `dataSource.transaction`; Kafka/WS thành dòng outbox ghi cùng transaction | high | yes | Toàn bộ epic — quyết định hình dạng mọi UoW | confirmed | Akenzy chọn "1 transaction + outbox" trong phiên lập kế hoạch 2026-08-04 |
| A-02 | Phạm vi chỉ là **đơn bán** (`POST /invoices/:id/checkout`); đơn trả/đổi để epic sau | high | yes | Gấp đôi khối lượng, thêm ~936 dòng đối chiếu | confirmed | Akenzy chọn "Chỉ bán hàng" 2026-08-04 |
| A-03 | Cutover: endpoint mới `/v2/pos/checkout`, **pos-web đổi sau feature flag** (`VITE_CHECKOUT_V2`) | high | yes | UOW-05 có hay không có phần web | confirmed | Akenzy chọn "Endpoint mới + POS đổi sau feature flag" 2026-08-04 |
| A-04 | Khuyến mại: **server tự tính lại**, bỏ mọi con số client gửi; client chỉ gửi `selectedProgramIds[]` + `voucherCode?` | high | yes | Hợp đồng API của UOW-04, và mô hình tin cậy | confirmed | Akenzy chọn "Server tính lại, bỏ số của client" 2026-08-04 |
| A-05 | Cấp số hóa đơn là **step mới trong transaction** khóa `document_number_counters` bằng `setLock('pessimistic_write')`, không gọi `DocumentNumberingService` | high | yes | ADR-02; đổi thì đổi cả thứ tự step và lời hứa "không nhảy số" | confirmed | Akenzy chọn "Step cấp số riêng trong tx" 2026-08-04 |
| A-06 | `computeResetKey` + `formatDocumentNumber` + `formatDate` là **private** trong `DocumentNumberingService`, nên step 08 buộc phải chép lại (~40 dòng thuần, không I/O) | high | no | Thêm ~40 dòng trùng lặp và nguy cơ trôi lệch định dạng giữa v1 và v2 | confirmed | Đọc `document-numbering.service.ts:189-215, 248-273` ngày 2026-08-04. Giảm rủi ro bằng test parity bắt buộc (AC-14): cùng một rule, `generate()` và step 08 phải cho **cùng định dạng** trên ma trận continuous/date/suffix × 4 `ResetPolicy` |
| A-07 | `preview()` là **public** và đã tự gọi `ensureDefaultActiveRule`, nên preflight gọi nó vừa validate cấu hình sớm vừa đảm bảo rule tồn tại; step 08 chỉ còn resolve + increment, **không tự tạo rule** | high | no | Nếu sai, step 08 phải xử lý ca thiếu rule bên trong transaction | confirmed | Đọc `document-numbering.service.ts:217-246` ngày 2026-08-04 |
| A-08 | `pos-web` chỉ có **một** chỗ gọi checkout: `use-query-invoice.ts:91` → `invoiceService.checkout` → `invoice.service.ts:77`. Không có path `/invoices/:id/checkout` hard-code ở nơi khác | high | no | UOW-05 phải sửa nhiều file hơn dự kiến | confirmed | `grep invoiceService.checkout` + `grep '/invoices'` toàn `apps/pos-web/src` ngày 2026-08-04 — đúng một call site |
| A-09 | Khoá dòng counter giữ từ step 08 tới COMMIT **không** thành thắt cổ chai ở tải thật (một chi nhánh vài quầy, checkout vốn tuần tự theo người) | medium | no | Phải chuyển `next-document-number` xuống áp chót + `UPDATE invoices SET code` — đổi thứ tự step trong UOW-02/03, không đổi hợp đồng | confirmed | Đo thật ở T-03-09 (2026-08-05), `checkout-saga-perf.e2e-spec.ts` (spike, `it.skip` mặc định): **tuần tự (N=50, cùng chi nhánh)** — v1 p50=29.7ms p95=62.6ms max=109.2ms; v2 p50=36.2ms p95=46.7ms max=54.0ms (v2 p95 = **0.75×** v1, tức là nhanh hơn, không chậm hơn). **Đồng thời (10 đơn cùng lúc, cùng chi nhánh)** — v2: 10/10 thành công, p50=174.0ms p95=281.0ms; v1: chỉ **5/10 thành công** (5 ca còn lại ném `QueryFailedError: could not serialize access due to concurrent update` — chính bug (a) của `00-intent.md`, `SERIALIZABLE` của v1 tự chối request chứ không xếp hàng), trong số 5 ca thành công p50=87.9ms p95=92.7ms. Tỷ lệ p95 thô (v2/v1) = 3.03× — vượt ngưỡng 50% đã đề ra bằng chữ, nhưng phép so sánh này khập khiễng: mẫu số của v1 chỉ tính trên nửa số request **sống sót**, còn v2 xếp hàng (qua `pessimistic_write`) nên 10/10 đều xong, đổi lại độ trễ cao hơn. Akenzy duyệt 2026-08-05: **chấp nhận nguyên trạng, không đổi thiết kế** — 281ms p95 cho ca cực đoan "10 đơn cùng một tích tắc, cùng chi nhánh" vẫn dưới 300ms tuyệt đối, và rủi ro thật ("một chi nhánh vài quầy") không đạt tới 10 giao dịch đồng thời; đổi lại, v2 không có ca nào **mất hẳn** như v1 (giá trị thật của việc sửa bug (a), không phải chi phí phụ). Đường lùi ADR-02 (dời `next-document-number` xuống áp chót) vẫn còn nguyên nếu tải thật sau này chứng minh khác |
| A-10 | `MembershipCardService.awardPointsForInvoice` **có thể không** nhận `manager` | medium | no | Không có: phương án mặc định (ADR-04) vốn đã là đẩy EARN qua outbox | confirmed | Xác minh ở T-03-05 (2026-08-05): đọc `membership-card.service.ts:124-127` — `awardPointsForInvoice(invoice, actor)` **không có tham số `manager`**, tự mở `dataSource.transaction(...)` riêng. Xác nhận thật sự không composable, không chỉ là giả định thận trọng — ADR-04 (giữ EARN qua outbox) là lựa chọn duy nhất khả thi, không phải lựa chọn an toàn hơn trong nhiều lựa chọn |
| A-11 | Đọc CTKM ở preflight (ngoài transaction) là chấp nhận được: nếu CTKM đổi giữa preflight và COMMIT thì đơn dùng bản đọc lúc preflight | medium | no | Một đơn hiếm hoi dùng CTKM vừa bị dừng vài trăm ms trước | pending | Chấp nhận có chủ ý; `EvaluateCartQuery` cũng đọc ngoài transaction. Ghi lại ở G5 nếu thực tế khác |
| A-12 | Thêm cột nullable vào `invoice_items` (`is_gift`, `promotion_program_id`) **không** đổi hành vi luồng v1 vì v1 không đọc và không ghi hai cột đó | high | no | Nếu sai, luồng v1 đang chạy thật bị ảnh hưởng — nghiêm trọng | confirmed | Xác minh ở T-04-01 ngày 2026-08-05, ba lớp: (1) 230 suite / 1967 test unit xanh sau migration; (2) không có `SELECT *` nào trên `invoice_items` trong repo, hai chỗ SQL thô đều liệt kê cột tường minh; (3) `InvoiceItemEntity` không khai báo hai cột mới nên TypeORM không select và không insert chúng. Ca "một đơn v1 chạy thật" hoãn tới T-02-08. **Đính chính:** lý do hoãn ghi ban đầu ("sổ sách `migrations` của `erp_test` hỏng") là chẩn đoán **sai** — e2e không dùng migration, xem A-17; lý do thật là hook timeout, xem A-18, nay đã gỡ |
| A-13 | `idempotency_key` mặc định lấy **`invoiceId`** khi client không gửi header `x-idempotency-key`; unique index là **partial** — `UNIQUE (organization_id, idempotency_key) WHERE status <> 'FAILED'` — để lần chạy hỏng vẫn retry được, còn lần đã `COMPLETED` thì replay | high | no | Nếu partial index không hợp lệ thì retry sau khi FAILED bị chặn; phải đổi sang xóa row FAILED trước khi chạy lại | confirmed | Xác minh trên Postgres thật ở T-01-01 ngày 2026-08-04: hai dòng `FAILED` cùng key được phép; chạy lại sau `FAILED` được phép; dòng live thứ hai cùng key bị chặn bằng `23505`; cùng key khác org được phép. Hành vi runtime còn phải kiểm ở T-02-02 (AC-11). **Bổ sung (T-02-09, 2026-08-05):** viết e2e thật cho AC-10 lộ ra `load-draft` (preflight) chặn `INVOICE_NOT_CHECKOUTABLE` **trước khi** `open-saga` (transactional) kịp thấy request là replay, vì sau lần chạy thành công hóa đơn không còn là draft — một resubmit hợp lệ (mất response, client tự thử lại) không bao giờ chạm được logic replay. Sửa: `load-draft` thêm một lượt đọc nhẹ không khoá — có `checkout_saga` `COMPLETED` khớp `(organizationId, idempotencyKey)` thì cho qua guard; `open-saga` vẫn là nguồn sự thật duy nhất quyết định replay. Xem T-01-05 (mục sửa đổi sau khi nộp) |
| A-14 | Chạy song song v1 (`SERIALIZABLE`) và v2 (`FOR UPDATE`) trên cùng dòng counter là an toàn: cả hai đều khoá dòng đó, v1 gặp `40001` thì đã có sẵn đường retry | medium | no | Nếu sinh deadlock thật, phải tuần tự hoá bằng advisory lock hoặc dừng chạy song song | pending | Xác minh ở AC-15 (test xen kẽ v1/v2, mỗi bên 10 đơn) — điều kiện bắt buộc của UOW-02 |
| A-17 | Bộ e2e dựng schema **từ entity** qua `resetDatabase` → `ds.synchronize(true)`, **không** từ migration. Migration chỉ dùng cho `erp_dev` và production; `erp_test` vẫn cần được pre-seed schema một lần để `app.init()` chạy được | high | no | Bảng/cột nào chỉ có trong migration mà không có entity sẽ **không tồn tại** trong DB e2e | confirmed | Đọc `test/e2e/setup/test-app.ts:59-62` và xác minh bằng thực nghiệm ngày 2026-08-05: drop sạch `erp_test` → `app.init()` chết vì `relation "permissions" does not exist`, tức app boot **trước** khi synchronize chạy. Đã phục hồi bằng `DB_NAME=erp_test pnpm migration:run` (146 migration, 140 bảng) |
| A-18 | `beforeAll` của bộ e2e mất ~130s (createTestApp + `synchronize(true)` trên ~140 entity + seed), vượt xa `jest.setTimeout(30_000)` chốt cứng trong `test/e2e/setup/jest-setup.ts` — nên **mọi** spec e2e của repo fail trước khi chạy test nào | high | no | Không có e2e nào chạy được, kể cả của epic khác | confirmed | Đo bằng một spec probe tạm ngày 2026-08-05: `duration 130523ms`. `--testTimeout` trên CLI vô hiệu vì `jest-setup.ts` gọi `jest.setTimeout` sau. Akenzy duyệt nâng lên 180s; sau khi nâng, `auth.e2e-spec` **15/15 pass** |
| A-19 | `OutboxRelayService` poll mỗi 2s và đua với `synchronize(true)` — trong lúc synchronize drop/tạo lại schema thì `outbox_messages` biến mất, poller ném và jest báo "Test suite failed to run" **giả** dù mọi test đã pass | high | no | Suite luôn đỏ dù test xanh; và T-03-08 cần relay bật nên phải xử lý riêng | confirmed | Xác minh 2026-08-05: chạy `auth.e2e-spec` → 15/15 test pass nhưng suite đỏ vì `QueryFailedError: relation "outbox_messages" does not exist` từ `outbox-relay.service.ts:77`. Thêm `OUTBOX_RELAY_DISABLED=1` → **suite xanh hoàn toàn**. Lệnh chạy e2e chuẩn của epic: `DB_NAME=erp_test OUTBOX_RELAY_DISABLED=1 pnpm --filter @erp/api test:e2e -- <spec>` |
| A-20 | Hai cột `invoice_items.is_gift` và `promotion_program_id` **bắt buộc** phải khai báo trên `InvoiceItemEntity`, không thể dùng raw query hay entity bóng | high | no | Nếu không khai báo thì cột không tồn tại trong DB e2e (A-17) và T-04-07 không thể pass; entity bóng trỏ cùng bảng thì xung đột với `synchronize` | confirmed | Hệ quả trực tiếp của A-17. Akenzy duyệt ngày 2026-08-05 thêm hai `@Column` vào `invoice-item.entity.ts`; quyết định này thay thế phương án cũ của T-04-02 |

| A-21 | `resetDatabase` → `ds.synchronize(true)` không chỉ dựng schema — nó **prune** mọi bảng không có entity nào trong `AppModule` đang chạy, kể cả bảng bookkeeping `migrations` của chính nó. Trước T-01-08 (nối `CheckoutSagaModule` vào `pos.module.ts`), ba bảng mới của epic **không có entity nào được app biết tới** nên chạy e2e sẽ xoá sạch chúng cộng cả bảng `migrations` | high | no | Chạy nhầm một e2e spec bất kỳ trước T-01-08 sẽ xoá `checkout_saga`/`checkout_saga_step`/`invoice_checkout_promotions` khỏi `erp_test`, và để lại `erp_test` ở đúng trạng thái "127 bảng / 0 dòng migrations" đã bị chẩn đoán sai ở A-17 — tức đây mới là cơ chế thật đằng sau triệu chứng ban đầu | confirmed | Tái hiện ngày 2026-08-05: chạy `auth.e2e-spec` + `deposit-fund.e2e-spec` sau khi migrate `erp_test` (146 migration, 140 bảng) → còn lại 127 bảng, `migrations` biến mất, `checkout_saga`/`checkout_saga_step`/`invoice_checkout_promotions` biến mất. Phục hồi bằng drop + recreate + `migration:run`. **Quy tắc thao tác cho phần còn lại của epic**: không chạy bất kỳ e2e spec nào cho tới khi T-01-08 xong; sau T-01-08, `synchronize(true)` tự dựng lại đúng schema từ entity ở mỗi `beforeAll` như mọi bảng khác, hết cảnh báo |

| A-22 | `AccountResolverService`, `CashFundResolverService` (đã có, không sửa) ném `BadRequestException` với message **chuỗi thô**, không phải object `{code}`; `DocumentNumberingService.preview` ném `NotFoundException` (404) chứ không phải 400 — cả ba lệch với bảng "Error taxonomy" đã hứa. Mọi step gọi ba service này phải **bắt và bọc lại** ở biên step, không sửa service gốc | high | no | Nếu không bọc, client v2 nhận lỗi không có `details.code`, phá hợp đồng API đã hứa ở 03-logical-design.md, và test theo `details.code` sẽ fail | confirmed | Đọc source ba service + xác minh bằng 5 test tạm ngày 2026-08-05 dùng đúng nguyên văn message các service thật ném, khẳng định `ResolveAccountsStep`/`ResolveFundsStep` bọc đúng thành `ACCOUNT_NOT_CONFIGURED` / `PAYMENT_ACCOUNT_INVALID` / `CASH_FUND_NOT_CONFIGURED` / `DOC_NUMBER_RULE_MISSING` (404→400). **T-03-03/T-03-04 gọi cùng nhóm service ở phase transactional và phải áp cùng cách bọc** — không phát hiện lại từ đầu |

| A-23 | `POST /auth/switch-branch` **thu hồi session của người gọi nó** (`sessionStore.revokeSession(current.jti)` trong `auth.service.ts`). Bất kỳ e2e nào cần đổi chi nhánh hoạt động phải làm trên một lượt đăng nhập rời, không bao giờ trên session dùng chung của fixture | medium | no | Nếu quên, mọi test chạy sau trong cùng file sẽ 401 hàng loạt vì token dùng chung đã bị thu hồi — dễ nhầm là lỗi saga | confirmed | Bắt được ở T-01-10 ngày 2026-08-05: dùng `fx.seed.accessToken` cho `switch-branch` làm 7/8 test còn lại 401. Sửa bằng đăng nhập rời trước khi switch-branch. Còn một bẫy liên quan đã biết từ trước: `actor.branchId` ưu tiên JWT hơn header (`X-Branch-Id`), nên đổi chi nhánh bắt buộc qua `switch-branch`, không phải đăng nhập lại |

| A-24 | Đối với **kỳ đánh số hoàn toàn mới** (không có dòng `document_number_counters` nào cho `(ruleId, resetKey)` đó), `pessimistic_write` không khoá được gì (chưa có dòng để khoá) — hai checkout đồng thời có thể cùng thấy "chưa có counter" và cùng insert, va `UQ_rule_reset_key`. Khác `open-saga`, ở đây **không thể** bắt lỗi rồi thử lại trong cùng transaction (Postgres đưa cả transaction vào trạng thái aborted sau một statement lỗi). Bên thua rollback sạch, trả `400 DOC_NUMBER_COUNTER_CONFLICT` — retryable, không phải mất dữ liệu | medium | no | Chỉ lộ ra đúng lúc hai đơn đầu tiên của một kỳ đánh số mới (vd đầu tháng) chạy đồng thời — hiếm nhưng có thật. Nếu có ai "dọn dẹp" thành auto-retry trong cùng transaction sẽ hỏng vì Postgres không cho phép | confirmed | Phát hiện khi viết `next-document-number.step.ts` (T-02-03) — service gốc né bằng `SERIALIZABLE` + tự mở transaction MỚI để thử lại, cách đó không dùng được khi saga sở hữu đúng một transaction cho cả checkout. Chứng minh 8 giao dịch đồng thời thật trên counter **đã tồn tại** vẫn ra đúng 8 số duy nhất — ca hiếm chỉ là dòng counter chưa tồn tại lần đầu |

| A-25 | `JournalService.post(dto, actor, manager)` — even when given `manager` (the documented "composable" path) — still (a) calls `DocumentNumberingService.generate()` unconditionally, a non-participating `SERIALIZABLE` transaction that mints a JOURNAL document number which does **not** roll back with the caller's transaction (exactly the bug ADR-02 closed for INVOICE numbers, present here for JOURNAL numbers), and (b) publishes `JOURNAL_POSTED` directly to Kafka right after its own write, with zero awareness of whether the caller's outer transaction has committed yet (bug (b)/(c) in 00-intent.md, reintroduced). Neither is documented in `post()`'s own "composable" doc comment. `post-journal.step.ts` (T-03-02) does not call `JournalService.post()` at all — it writes `JournalEntryEntity`/`JournalLineEntity` directly via `ctx.manager` and mints the number with `mintDocumentNumber` (generalized from T-02-03's `next-document-number.step.ts`, same ADR-02 fix, now shared by both DocumentTypes) | high | yes | If missed, a checkout that fails on a *later* step (post-cash, post-deposit, redeem-points…) after post-journal already ran would leave a JOURNAL number burned and a phantom `JOURNAL_POSTED` event on the wire for a journal entry that never actually committed — the exact class of bug this whole epic exists to close, reintroduced one layer down | confirmed | Read `journal.service.ts:57-97` on 2026-08-05 while implementing T-03-02: `generate()` call and `eventPublisher.publish(JOURNAL_POSTED, ...)` both happen unconditionally, regardless of whether `manager` was passed. Checked `CashService.recordMovement`/`DepositService.createAndPostInternal` (T-03-03/T-03-04's targets) for the same pattern — neither calls `documentNumberingService` or `eventPublisher` at all, so this is isolated to `JournalService`, not a repo-wide pattern |

| A-26 | v1's cash sale double-posts the cash GL account: `JournalSaleConsumer` posts a "sale" JE that already debits the cash GL account per CASH payment line, and *separately* `PosCashSaleConsumer` → `CashReceiptsService.createAndPostInternal` → `CashService.recordMovement` posts a *second*, independent JE that debits the same cash GL account again (`buildJournalLines` for `DEPOSIT`: DR cash, CR contra/revenue) — plus creates a `cash_receipts` (Phiếu thu) voucher v2 has no equivalent of. `post-cash.step.ts` (T-03-03) does not replicate this: it posts only the `cash_movements` row (the till ledger), not a second journal entry or voucher — the sale's own accounting entry is already complete from `post-journal.step.ts` | high | no | If ever "fixed" by making post-cash also post a JE, it would double-post the cash/revenue GL lines within *one* transaction — worse than v1's version, which is at least split across two independently-triggered async consumers. Anyone reopening this must not add a JE or voucher write to post-cash | confirmed | Traced the full v1 chain on 2026-08-05 while implementing T-03-03: `pos-cash-sale.consumer.ts` → `cash-receipts.service.ts:499-524` (calls `docNumbering.generate(CASH_RECEIPT, ...)`, then `cashService.recordMovement`) → `cash.service.ts:193-260` (`buildJournalLines` DEPOSIT case: DR `cashAccountId`, CR `contraAccountId` — identical pair to what `journal-sale.consumer.ts` already posts for the same invoice) → `journalService.post(..., manager)`. Pre-existing, undocumented double-post in v1 itself; out of scope for this epic (00-intent.md's bug list doesn't include it) — flagged here only so v2 doesn't accidentally reintroduce it while chasing "parity". **Extended to deposit/bank movements (T-03-04, 2026-08-05)**: `DepositService.recordSingleAccountMovementInTx` has the identical shape — it also calls `journalService.post(...)` internally (`JournalSource.BANK_MOVEMENT`, DR the deposit account's GL / CR contra) for the exact pair `post-journal.step.ts` already posts for a bank_transfer/card line. `post-deposit.step.ts` applies the same fix: writes only the `deposit_movements` row, no second JE |
| A-27 | `post-deposit.step.ts` does not implement fee computation (`DepositFeeService.computeFee`/`postFee`) or settlement-delay value-dating (`docDate` + `settlementDays` → `valueDate`, R2/TKT-DFR-04) that `pos-deposit-sale.consumer.ts` does for non-cash POS payments. `feeAmount` is always 0, `netAmount` always equals the gross `amount`, `valueDate` always equals `docDate` | medium | no | A branch with a non-zero card/bank fee rate configured would see v2's deposit fund balance and `net_amount` differ from what v1 would have recorded (v1 nets the fee out; v2 doesn't), and `valueDate` differs whenever `settlementDays > 0` (affects available-vs-book balance checks on later withdrawals) | confirmed | Traced `pos-deposit-sale.consumer.ts:96-105` on 2026-08-05 while implementing T-03-04 — the ticket's own scope (Done-when criteria) never mentions fees or settlement dates, only movement/parity and the period-lock. Left as an explicit, documented gap rather than silently dropped; a fee-bearing branch's true parity would need a dedicated follow-up ticket, not a silent surprise |

| A-28 | The `POS_CHECKOUT_ACKNOWLEDGED` WS notification is emitted directly (synchronously, in-process via `WebSocketEmitterService.emitToBranch`) right after the transaction commits — not enqueued as a fourth outbox row alongside `SALE_POSTED`/`TEMP_WAREHOUSE_INVOICE_FULFILL`/`LOYALTY_POINTS_AWARD` | medium | no | If someone later "fixes" this into a fourth outbox enqueue expecting the same durability guarantee as the other three, it will silently do nothing — there is no consumer that turns a delivered Kafka message back into a `emitToBranch` call, so the cashier's screen would just never get the notification | confirmed | The ticket explicitly allowed either choice ("nếu độ trễ ~2s không chấp nhận được thì emit thẳng sau COMMIT cũng được"). Decided while implementing T-03-05: `OutboxRelayService` only knows how to republish outbox rows to Kafka topics — routing a WS event through it would need a brand-new consumer subscribing to that topic and calling `emitToBranch`, which does not exist and is unplanned scope. `emitToBranch` itself is synchronous, in-process, and has no durability semantics to begin with (a reconnected client wouldn't want a stale replay anyway), so outbox's guarantees don't even apply to it. Implemented as: `enqueue-outbox.step.ts` sets `ctx.wsNotification` (content only, inside the transaction); `CheckoutSagaOrchestrator.runTransactional` emits it via the new `CHECKOUT_WS_EMITTER` port only *after* `dataSource.transaction(...)` resolves, wrapped in try/catch (same best-effort posture as `recordFailure`/`dispatchNow`) |

| A-29 | `CheckoutSagaOrchestrator.runTransactional`'s automatic `outboxRelay.dispatchNow()` (added T-02-01) now skips the call when `process.env.OUTBOX_RELAY_DISABLED === '1'` — a check `OutboxRelayService.dispatchNow()`/`pollOnce()` themselves deliberately do NOT have (by design, so crash-recovery tests can simulate "relay down" then trigger a dispatch manually) | high | no | Without this, every real v2 commit during e2e (with `OUTBOX_RELAY_DISABLED=1` already set, per A-19's established convention) still triggers a real Kafka publish + real consumer processing per commit — slow (measured 327s+ vs ~90s for the same file), and caused `afterAll`'s `app.close()` to time out at 180s from live Kafka activity. It also broke a same-session e2e assertion that assumed loyalty EARN never actually applies within this slice (true only because enqueue-outbox didn't exist yet when that test was written) | confirmed | Discovered T-03-06, 2026-08-05, wiring `enqueue-outbox` (T-03-05) into the pipeline for the first time and running `checkout-saga.e2e-spec.ts` for real: run 1 showed `card.points` off by the exact EARN amount from a *prior* test (deterministic, not racy) and the afterAll timeout; run 2 (after the fix) dropped runtime to ~135-151s and `card.points` behaved as documented again. Fixed in the orchestrator (checkout-saga's own code), not `OutboxRelayService` (shared infra, unrelated features already depend on its current crash-recovery-friendly behavior) — and this is exactly the mechanism T-03-08 (Kafka-down e2e, AC-18) already planned to rely on, so the fix enables that ticket rather than conflicting with it |
| A-32 | `post-cash.step.ts` has no realistically externally-triggerable failure mode. `funds.cashAccountId` is re-resolved fresh by `resolve-funds` in the very same request's preflight phase, so anything that would break it (fund deleted, a second fund appearing) makes preflight reject the request before any transactional step runs; the step mints no document number and has no unique constraint of its own to violate | medium | no | T-03-07's own text asked for a failure forced at post-journal → post-cash → post-deposit (steps 15/16/17); without this finding, the ticket's middle case would either be skipped silently or "solved" by mocking application code, breaking this epic's established all-real e2e philosophy | confirmed | Traced directly from `post-cash.step.ts`'s source while implementing T-03-07 (2026-08-05). Substituted with `post-deposit` (17) as the third real position instead — still "after cash was collected" (post-cash succeeds first on a mixed CASH + bank_transfer invoice), just one step later, via a genuinely lockable resource (`deposit_period_lock`) that step actually depends on |
| A-33 | A **transactional**-phase checkout failure (any step 06–19) is not "0 rows changed" — `checkout_saga` gains exactly one row (the FAILED trail, written by a second transaction per ADR-01) even though the other 7 business tables are untouched. Only a **preflight** failure (steps 01–05, before any transaction opens) leaves `checkout_saga` itself untouched too, because `open-saga` never ran | high | no | A test asserting bare `countBusinessRows` equality across a transactional-phase failure fails deterministically (not a flake) — exactly what happened drafting T-03-07's three new cases before this was accounted for | confirmed | Reproduced directly: T-03-07's three new cases (each a genuine transactional-phase failure) all failed with `checkout_saga` off by `+1` until the assertion was corrected to `{...before, checkout_saga: before.checkout_saga + 1}`. AC-07's existing two preflight-failure cases (`compute-totals` throwing) correctly keep asserting bare equality — unaffected, because they never reach `open-saga` at all |
| A-34 | Bug (h) from `00-intent.md`, as literally stated ("hai draft cùng áp một voucher đều checkout được" — two drafts that both applied the same voucher can both check out), does **not** reproduce on v1 for a genuinely simultaneous race. `checkout-invoice.service.ts:308` calls `PromotionApplyService.commitPromotions` **inside v1's own checkout transaction**, which calls the exact same `VoucherService.markUsed` conditional UPDATE v2 uses — so a real `Promise.all` race of two v1 checkouts (both having already `apply()`-ed the same voucher earlier, a separate transaction each) still produces exactly one winner and one clean rollback, same as v2 | medium | no | If T-05-02's own demo script had simply *assumed* v1 would show the double-redemption and reported that without checking, it would have been a fabricated finding — the "chạy tay, ghi lại" instruction on this ticket exists precisely to catch this kind of gap between an epic's original bug catalog and what the code actually does today | confirmed | Reproduced directly (2026-08-05): a temporary e2e script (`voucher.service.ts`'s real `apply()`/`checkout` endpoints, deleted after running) — voucher applied to both draft A and B, then `POST /invoices/:id/checkout` raced via `Promise.all`. Result: A `201` (paid), B `409` (still draft), voucher `is_used=true` with `redeemed_invoice_id` = A's id. T-05-01/T-05-02's own value is unaffected — v2 still needed to implement the same protection inside its *own* transaction (there was no guarantee it would exist until T-05-01 wrote it), and v2 additionally fixes the true timing gap bug (h) implies (a `commitPromotions`-equivalent running minutes after `apply()`, by which point a *different*, non-simultaneous voucher-consuming action could have happened) — that slower-paced scenario was not re-tested here, only the literal "both racing at checkout" case the demo script asks for |
| A-30 | `persist-invoice.step.ts`'s gift-line writes (T-04-04) deviate from the ticket's own text in two ways. (1) The "gift item has no sellable location" check runs in the **transactional** phase (this step), not truly in preflight as the implementation notes first asked — because the gift set is only known once `evaluate-promotion` (a preflight step) has already run, and re-resolving it a second time in a dedicated preflight step would mean calling `resolveBranchItemLocations` twice for the same invoice. (2) The gift invoice_item's `costPrice` is populated from `ItemEntity.purchasePrice` (one extra batched query), even though neither the ticket's implementation notes nor its Done-when checklist mention `costPrice` at all | medium | no | (1) is a documented, ticket-sanctioned fallback, not a silent deviation — the ticket text itself says "nếu phải phát hiện muộn thì để rollback và ghi lý do", and throwing mid-transaction still rolls back the whole saga (no invoice, no line, no lock outlives the failure), so nothing is left half-done. (2) if skipped, `costPrice` defaults to 0 for every gift line, and `gross-profit-by-invoice.report.ts:156-161` reads `invoice_items.costPrice` directly for its "cost of goods" column — an invoice with a gift line would silently overstate gross profit by the gift's true cost. Both risks are worth carrying as a register row rather than a surprise | confirmed | (1): moving the check into a dedicated preflight step would need `evaluate-promotion` itself to also resolve locations (duplicating a DB round trip already paid for) or a brand-new preflight step reading `ctx.promotion` (touches list for T-04-04 names only `persist-invoice.step.ts`). Kept in this step, documented, per the ticket's own contingency clause. (2): `InvoiceItemEntity.costPrice`'s own column comment says "server-populated, not from client" and normal draft lines already populate it the same way (`catalog?.purchasePrice ?? 0` in `invoice.service.ts`) — treated as fulfilling that column's existing contract, not as unrequested scope creep |
| A-35 | `checkout-saga.e2e-spec.ts`'s AC-03 and AC-07 ("leaves every table unchanged" row-count snapshots) occasionally see +1 `cash_movements`/+1 `journal_entries` that shouldn't be there | AC-02 (runs immediately before AC-03 in file order) calls a **real v1 checkout** (`POST /invoices/:id/checkout`) as its parity baseline — v1 publishes Kafka topics consumed **asynchronously** by real, unrelated consumers (`journal-sale.consumer.ts`, `pos-cash-sale.consumer.ts`). `OUTBOX_RELAY_DISABLED=1` (A-19) does not gate this — that flag only stops v2's own outbox relay. When those consumers land late, their rows fall inside the *next* test's before/after window | Test-only flakiness, not a functional regression in v2: across 4 isolated runs of `checkout-saga.e2e-spec.ts` in this session (`DB_NAME=erp_test OUTBOX_RELAY_DISABLED=1`), only AC-03/AC-07 ever flipped (19/19, 17/19, 16/19, 18/19) — every parity, idempotency (AC-10), concurrency (AC-11/15), failure-injection (AC-13/16/17), promotion and voucher test passed every time. `checkout-saga-concurrency.e2e-spec.ts` showed the same pattern once (AC-15, run alongside 4 other files) and passed clean in isolation | confirmed | Diagnosed 2026-08-06 during T-05-04's final e2e pass. Also confirmed, separately: a truly empty `erp_test` (dropped+recreated) breaks `PermissionSyncService.onApplicationBootstrap()`, which queries `permissions` during `app.init()` **before** `resetDatabase()`'s `synchronize(true)` creates the schema — every prior "green" run relied on `erp_test` already having schema from an earlier run. Fixed for this session by running `migration:run` against `erp_test` once. Left as a register row, not a code fix — stabilizing AC-02/AC-03 (e.g. wait for the v1 side-effects to land, or stop using a real v1 checkout as the baseline) touches test code outside T-05-04's verification-only scope; Akenzy to decide |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-15 | Ban đầu định **chấp nhận nhảy số hóa đơn**, gọi `DocumentNumberingService.generate` ở preflight (ADR-02 bản nháp đầu) | Có thể khoá thẳng `document_number_counters` bằng `manager` của saga mà **vẫn không sửa** `DocumentNumberingService` — số nằm trong transaction, rollback là nhả số | ADR-02 viết lại: thêm step `next-document-number` vào phase transactional; step preflight `reserve-document-number` bị bỏ; sinh thêm A-06, A-09, A-14 và AC-14, AC-15, AC-16 |
| A-16 | Ban đầu định dựa vào `IdempotencyInterceptor` (Redis) để chống double-checkout | Interceptor chỉ lưu **sau khi** handler resolve nên không có khoá in-flight; hai request giống hệt vẫn chạy cả hai. Bốn saga sẵn có đều tự bảo vệ bằng unique index ở tầng DB | Chống trùng chuyển xuống DB: A-13 (`idempotency_key` mặc định `invoiceId` + partial unique index), thêm AC-10 và AC-11 |
