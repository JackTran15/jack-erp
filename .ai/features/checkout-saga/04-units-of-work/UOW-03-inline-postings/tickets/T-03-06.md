---
id: T-03-06
uow: UOW-03
title: Nối 5 step vào pipeline + dispatchNow sau commit
layer: application
type: feature
estimate: 2h
status: done
depends_on: [T-03-01, T-03-02, T-03-03, T-03-04, T-03-05, T-02-09]
blocks: [T-03-07, T-03-08, T-03-09, T-04-03]
verifies: []
touches:
  - apps/api/src/modules/pos/checkout-saga/checkout-saga.module.ts                          # amended
  - apps/api/src/modules/pos/checkout-saga/interface/checkout-saga.controller.ts             # amended
  - apps/api/src/modules/pos/checkout-saga/interface/checkout-saga.controller.spec.ts        # amended
  - apps/api/src/modules/pos/checkout-saga/application/checkout-saga.orchestrator.ts         # amended
  - apps/api/src/modules/pos/checkout-saga/application/checkout-saga.orchestrator.spec.ts    # amended
  - apps/api/test/e2e/checkout-saga.e2e-spec.ts                                              # amended
assumptions: [A-29]
---

# T-03-06 — Ráp phần ghi sổ

## Implementation notes
- Đăng ký step 14–18 vào đúng vị trí giữa `redeem-points` (13) và `close-saga` (19).
- Gọi `outboxRelay.dispatchNow()` ngay sau khi transaction commit — hook đã đặt sẵn ở T-02-01.
  Bọc `try/catch`: relay lỗi thì chỉ log, tuyệt đối không làm hỏng response, vì dữ liệu đã an toàn
  trong `outbox_messages` rồi và poller sẽ gửi sau.
- Cập nhật `totalSteps`.

## Done when
- [x] Một đơn thành công đi qua đủ 18 step của lát này (14–18 nối thêm; step 09
      `redeem-voucher` của UOW-05 vẫn chưa có nên chưa phải 19), trail `seq` liên tục
- [x] `dispatchNow` chạy sau COMMIT, và lỗi của nó không đổi mã trả về cho client
- [x] `dryRun` vẫn dừng sau preflight

## Kết quả kiểm chứng (2026-08-05)

`tsc --noEmit` sạch. Subset `checkout-saga`: 20 suite / 211 test xanh (không đổi số test — thuần nối
dây, +2 test mới ở phần `OUTBOX_RELAY_DISABLED`, xem dưới). Toàn bộ repo: 250/250 suite, 2180/2181
test xanh. Xác minh boot thật: `[InstanceLoader] CheckoutSagaModule dependencies initialized`, không
lỗi DI cho `ItemCostSnapshotModule` mới.

**Phát hiện thật khi chạy e2e lần đầu với đủ 18 step (không phải đoán):** `dispatchNow()` của
orchestrator (đã có từ T-02-01) kích hoạt publish Kafka + consumer xử lý **thật**, mỗi lần một
checkout thật commit — trước T-03-06 vô hại vì `outbox_messages` luôn rỗng (chưa có step nào ghi vào).
Sau khi nối `enqueue-outbox` (T-03-05) thật, chạy `checkout-saga.e2e-spec.ts`:
- File từ ~90s tăng lên **327.9s**, và `afterAll` (`app.close()`) **timeout ở 180s**.
- `card.points` của ca "điểm không đủ" sai lệch đúng +5 — **tất định**, không phải flaky: điểm tích
  của ca THÀNH CÔNG trước đó áp dụng thật vào thẻ đúng lúc ca sau chạy, vì `OUTBOX_RELAY_DISABLED=1`
  (đã export theo quy ước A-19) chỉ tắt TIMER định kỳ của `OutboxRelayService`, không tắt `dispatchNow()`
  — cờ này cố tình được thiết kế để test crash-recovery vẫn gọi tay `dispatchNow()`/`pollOnce()` sau khi
  giả lập "relay chết".

Sửa: `runTransactional` chỉ gọi `dispatchNow()` khi `OUTBOX_RELAY_DISABLED !== '1'` — sửa ở orchestrator
(code riêng của saga), không đụng `OutboxRelayService` (hạ tầng dùng chung, tính năng khác đã phụ thuộc
đúng hành vi hiện tại của nó). Chạy lại sau sửa: **135–151s** (2 lần), `afterAll` không còn timeout,
`card.points` đúng lại như tài liệu ban đầu. Thêm 2 test vào `checkout-saga.orchestrator.spec.ts`
(16→18 test) khẳng định cả hai nhánh (`OUTBOX_RELAY_DISABLED=1` → không gọi; vắng cờ → vẫn gọi như cũ).
Chi tiết đầy đủ ở A-29 (`01-assumptions.md`), cross-reference ở T-02-01 và T-02-08.

**Xác minh bằng e2e thật (không phải suy đoán), chạy 4 lần:**
- `checkout-saga.e2e-spec.ts`: 16 test — 2 lần đầu (trước sửa) có lỗi tất định (card.points, afterAll
  timeout) + 1 lỗi cơ học (13→18, đã sửa); 2 lần sau sửa: **15/16 mỗi lần**, chỉ còn đúng 1 flake đã
  biết từ trước (AC-03 hoặc AC-07, khác nhau mỗi lần — xác nhận đây là race bất đồng bộ thật của
  consumer v1, không phải lỗi tất định mới, và không liên quan tới ticket này).
- `checkout-saga-concurrency.e2e-spec.ts`: **4/5 xanh** (1 `it.skip` chủ đích), 63.7s, không hồi quy.
