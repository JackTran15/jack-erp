---
feature: checkout-voucher-party
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Checkout v2 **sinh thêm** phiếu thu cho đơn tiền mặt, kiểu voucher-only: chỉ ghi `cash_receipts` + dòng chi tiết, link vào `cash_movement` và `journal_entry` mà saga đã ghi inline, **không** post thêm bút toán nào | high | yes | Chọn sai thì hoặc Sổ quỹ tiếp tục mất phiếu khi bật v2, hoặc tái tạo đúng cái double-post GL mà A-26 đã cảnh báo | confirmed | Akenzy chọn "v2 sinh Phiếu thu (voucher-only)" 2026-08-14, sau khi được nêu rõ ba phương án và hệ quả GL của từng cái |
| A-02 | Phạm vi gồm cả bốn nhóm: phiếu thu bán hàng tiền mặt, phiếu thu tiền thừa, phiếu chi hoàn tiền đổi trả, và **phiếu thu/chi tiền gửi** | high | yes | Nhóm tiền gửi gần gấp đôi khối lượng và kéo theo A-08 (phải **tạo mới** phiếu thu tiền gửi chứ không chỉ điền ô) | confirmed | Akenzy chọn cả bốn nhóm 2026-08-14 |
| A-03 | Ô Địa chỉ = địa chỉ khách hàng, khách không có địa chỉ thì lấy `branches.address` của chi nhánh bán | high | yes | Sai thì lệch ngữ nghĩa với phiếu tạo tay (ở đó Địa chỉ luôn là của đối tượng nộp) và kế toán đọc hai loại phiếu theo hai nghĩa | confirmed | Akenzy chọn "Địa chỉ khách hàng, thiếu thì lấy chi nhánh" 2026-08-14, không chọn phương án "luôn là chi nhánh" trong mô tả ban đầu |
| A-04 | Không backfill phiếu cũ; chỉ đúng từ phiếu mới trở đi | high | yes | `PT000152` và toàn bộ phiếu POS đã post vẫn trống bốn ô, không có đường đối soát tự động | confirmed | Akenzy chọn "Không, chỉ áp dụng phiếu mới" 2026-08-14 |
| A-05 | `staff_id` (cash) / `collected_by`·`paid_by` (bank) nhận **`employee_profiles.user_id`** của nhân viên bán hàng, không phải `invoices.salesperson_id`; không có nhân viên bán hàng thì lấy `invoices.staff_id` (người lập hoá đơn) | high | no | Ghi nhầm id thì ô "Nhân viên thu" vẫn trống y như bây giờ và ta tưởng đã sửa xong. Dialog tra bằng `GET /admin/users/:id`, còn `salesperson_id` là khoá của `employee_profiles` | confirmed | Đúng, và đã nhìn thấy tận mắt: `PT000045`/`PT000044` có `staff_id` trỏ `staff-hcm@erp.local`, dialog hiện `NV000001 · Nhân viên HCM`. Nhánh thoái lui cũng được dùng thật — e2e chốt đơn không có nhân viên bán hàng ra đúng `invoices.staff_id` |
| A-06 | Một hoá đơn POS chỉ có tối đa **một** dòng thanh toán CASH | medium | no | v1 hiện publish một event cho **mỗi** dòng tiền mặt, còn `PosCashSaleConsumer` dedupe theo `(INVOICE, invoiceId)` → hai dòng tiền mặt chỉ sinh một phiếu thu mang số tiền của **dòng đầu**. Nếu giả định này sai thì đó là một lỗi tiền có sẵn của v1, nằm ngoài phạm vi nhưng phải ghi lại. v2 gộp tổng các dòng CASH nên không mắc | open | **Không kiểm được** — mọi đơn dựng khi verify đều một dòng tiền mặt, POS UI không được dùng để tách hai dòng. Hệ quả bị khoanh lại: v2 gộp tổng nên miễn nhiễm (test hai dòng CASH xanh); rủi ro chỉ còn ở v1 và vẫn là lỗi tiền có sẵn, không do feature này gây ra |
| A-07 | `CashVoucherPartnerType.CUSTOMER` và `BankVoucherPartnerType.CUSTOMER` là hai enum khác nhau nhưng cùng giá trị chuỗi `'CUSTOMER'`, nên một snapshot dùng chung cho cả hai họ chứng từ chỉ cần ép kiểu ở biên | high | no | Nếu sai thì phải tách hai đường resolver, thêm một lớp map enum | confirmed | Đã đọc `cash-vouchers/enums.ts:99` và `deposit-vouchers/enums.ts:71` — bốn nhánh trùng từng chữ. Ép kiểu một chỗ ở `refund-bank.consumer.ts` và một chỗ ở mỗi đường tiền gửi, đều có comment trỏ về đây |
| A-08 | Phiếu thu tiền gửi cho thanh toán không tiền mặt **chưa từng tồn tại**: `PosDepositSaleConsumer` chỉ gọi `DepositService.createAndPostInternal` (movement + bút toán), không tạo `bank_receipts`; `post-deposit.step.ts` của v2 cũng chỉ ghi movement | high | no | UOW-04 không phải "điền ô" mà là "tạo loại chứng từ mới cho một luồng đang chạy". Sổ tiền gửi sẽ xuất hiện phiếu thu cho mọi đơn thẻ/chuyển khoản — cần chủ sở hữu biết trước, và là lý do UOW-04 xếp cuối, cắt được mà không gãy ba UoW kia | confirmed | Đúng, và còn nặng hơn dự đoán: `BankReceiptReferenceType` **không có** cả member `INVOICE` — chưa ai từng cần tham chiếu hoá đơn bán cho phiếu thu tiền gửi. Phải thêm enum + migration `1788200000000` |
| A-09 | Hoá đơn trả hàng / đổi hàng cũng mang `customer_id` và `salesperson_id` như hoá đơn bán, nên cùng một resolver dùng được cho phiếu chi hoàn tiền | medium | no | Nếu phiếu trả nhanh (QUICK, không hoá đơn gốc) không có khách thì phiếu chi để trống đối tượng — đúng hành vi mong muốn, chỉ cần test bao | confirmed | Hoá đơn trả mang `customer_id`/`salesperson_id` như hoá đơn bán, cùng một resolver dùng được. Lưu ý: **chỉ xác nhận bằng đọc code và unit test** — luồng đổi trả chưa chạy thật lần nào (xem UOW-02) |
| A-10 | `resolvePartySnapshot` (`cash-vouchers/shared/voucher-party.ts:49`) đã có sẵn đúng thứ tự ưu tiên "địa chỉ của partner ?? địa chỉ caller truyền vào", nên A-03 chỉ là chuyện truyền `branches.address` vào `input.address` chứ không phải viết logic mới | high | no | Nếu tự viết lại thứ tự này ở chỗ khác thì có hai định nghĩa Địa chỉ trong cùng codebase | confirmed | `resolvePartySnapshot` giữ nguyên, không sửa một dòng. `buildPosInvoiceParty` đặt cùng file và dùng lại `blankToUndefined`, nên quy tắc địa chỉ vẫn chỉ có một chỗ để đọc |
| A-11 | `post-journal` chạy **trước** `post-cash` trong danh sách bước của saga (`checkout-saga.controller.ts:183-185`), nên bước ghi phiếu đọc được `journalEntryId` và id movement qua `CheckoutContext` mà không phải đảo thứ tự bước nào | high | no | Đảo thứ tự bước là thay đổi có rủi ro cao hơn hẳn phần còn lại của feature | confirmed | `post-journal` chạy trước `post-cash`/`post-deposit`, không phải đảo bước nào. Chỉ thêm hai slot vào `CheckoutContext` (`journalEntryId`, `cashReceiptId`) — bỏ `cashMovementId` vì không ai đọc |
| A-12 | Chỉ cần sửa backend. Dialog `ReceiptVoucherDialog` / `PaymentVoucherDialog` / hai dialog tiền gửi đã đọc `partnerNameSnapshot`, `partnerAddressSnapshot`, `payerName`/`payeeName`, `staffId` và tự tra mã+tên qua `usePartnerLookup` | high | no | Nếu sai thì làm xong backend mà ô vẫn trống, và ta đi tìm lỗi nhầm phía | confirmed | Đúng: không sửa một dòng FE nào mà dialog vẫn hiện đủ bốn ô (ảnh S3–S5). Có một hệ quả phát sinh: ô nạp bất đồng bộ (`fetchStaffById`) không assert được bằng `count input[value=…]` vì React không cập nhật thuộc tính sau lần render đầu |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-R1 | Checkout v2 sinh phiếu thu giống v1, chỉ thiếu dữ liệu trong bốn ô | v2 **không sinh phiếu thu nào**. `post-cash.step.ts:31-33` ghi rõ: chỉ `cash_movements`, "No `cash_receipts` (Phiếu thu) voucher either" | "Áp dụng cho checkout 2" là **thêm chứng từ**, không phải điền ô. Đây là phần nặng nhất của feature và là lý do phải hỏi lại chủ sở hữu trước khi lên plan |
| A-R2 | Đơn bán ở checkout cũng sinh phiếu chi | Đơn bán không bao giờ sinh phiếu chi. Phiếu chi duy nhất trong họ checkout đến từ **đổi trả hoàn tiền**: `RefundCashConsumer` (tiền mặt) và `RefundBankConsumer` (tiền gửi) | Nếu không kiểm thì sẽ đi tìm chỗ điền ô trong luồng bán và không thấy gì, rồi kết luận sai |
| A-R3 | Ghi `invoices.salesperson_id` vào `cash_receipts.staff_id` là xong ô "Nhân viên thu" | `salesperson_id` là `employee_profiles.id`, còn dialog tra `staffId` bằng `GET /admin/users/:id`. Hai không gian id khác nhau | Phải đi thêm một chặng `employee_profiles.user_id`. Không có chặng này thì ô vẫn trống mà test đơn vị vẫn xanh — đúng loại lỗi chỉ lộ khi click thật |
| A-R4 | Cứ gọi `PartnerResolverService.resolve` như đường tạo phiếu tay là được | Hàm đó **throw `BadRequestException`** khi id không tra được (`partner-resolver.service.ts:59-63`). Trên đường v2 nó nằm trong transaction checkout | Một khách bị xoá sẽ **làm hỏng cả đơn hàng tại quầy**. Đường POS phải có resolver riêng, không throw |
