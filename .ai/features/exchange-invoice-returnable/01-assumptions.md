---
feature: exchange-invoice-returnable
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | "Chưa hiện hoá đơn mới" = mở khoá trả trên chính hoá đơn `EXCHANGE`, KHÔNG tách thành phiếu trả + hoá đơn bán mới | high | yes | Toàn bộ quy mô: hướng tách 2 chứng từ là epic riêng (đánh số, hạch toán, báo cáo) | confirmed | Akenzy chọn "Cho trả trực tiếp trên HĐ đổi", 2026-08-22 |
| A-02 | Lưới `Đổi trả hàng` cần một cột **Loại** ("Bán hàng" / "Đổi trả") để thu ngân phân biệt | high | no | Thu ngân chọn nhầm chứng từ; chỉ ảnh hưởng UOW-02 | confirmed | Akenzy chọn "Thêm cột Loại", 2026-08-22 |
| A-03 | Cột "Tổng thanh toán" giữ nguyên `getInvoiceSignedTotal` (EXCHANGE → `netAmount`, có thể âm) | high | no | Một công thức tiền thứ tư trong POS; footer tổng phải tính lại | confirmed | Akenzy chọn "Giữ nguyên net có dấu", 2026-08-22 |
| A-04 | Chỉ dòng `direction = OUT` ("Mua thêm") của hoá đơn đổi là trả được; dòng `IN` (hàng khách đã trả) không bao giờ được chào trả lại | high | no | Trả trùng hàng khách không cầm ⇒ xuất kho khống + chi tiền khống | confirmed | Đúng, kiểm chạy thật: `eligible-returns` trên `RTN-202607-00001` trả đúng 1 dòng OUT, dòng IN `ABA2777-D-38` không lọt; gửi thẳng dòng IN lên `POST /invoices/exchanges` → 400 "is an inbound (returned) line and cannot be returned again" (chốt ở `assertLineEligible`, ADR-03) |
| A-05 | Giá trị hoàn cho một dòng "Mua thêm" = `lineTotal / quantity` của chính dòng đó, không prorate thêm | high | no | Hoàn sai tiền; lệch giữa số POS xem trước và số checkout tính | confirmed | Đúng trên dữ liệu thật: dòng OUT `SETVOANM-D` `lineTotal` 170.000 / `quantity` 2 → `refundableUnitPrice` = 85.000, và 85.000 × 2 = 170.000 khít `lineTotal`. `refundableFactor` = 1 đúng như dự đoán vì `headerResidual` = 0. Cơ sở ban đầu: Cơ sở: `create-exchange-invoice.service.ts:119` đặt cứng `discountAmount: 0` / `depositAmount: 0`, `checkout-return.service.ts` không ghi `pointsDiscountAmount`, và `promotionDiscount` chỉ được gán cho dòng IN (`checkout-return.service.ts:388-405`) ⇒ `refundableFactor` = 1 trên mọi hoá đơn `EXCHANGE` |
| A-06 | `getEligibleLines` phải truyền **toàn bộ** dòng (IN + OUT) vào `refundableUnitValues` rồi mới lọc kết quả, để bản xem trước khớp với `computeReturnedNet` lúc checkout (hàm này đọc toàn bộ `originalItems`) | high | no | Xem trước và số thực thu lệch nhau ⇒ nợ ảo, đúng lớp lỗi đã gặp ở `[[project_pos_promotion_apply_construction]]` | confirmed | Đã cài đúng thứ tự ở `getEligibleLines` (tính trên toàn bộ dòng, lọc kết quả). Khoá hai lớp: test "divides the header residual over every line, not just the OUT ones" (dựng `pointsDiscountAmount` > 0 để hai cách ra hai số khác nhau) và test xuyên service ở `checkout-return.service.spec.ts` so `refundedAmount` với chính số `getEligibleLines` chào |
| A-07 | Hoá đơn đổi ghi nợ (`DEBT` / `PARTIAL_DEBT`) cũng trả được, giống hoá đơn bán ghi nợ; cấn trừ công nợ chạy đúng vì `lockOriginalDebt` tra `invoice_debts` theo `invoiceId` + `documentType = CREDIT_INVOICE`, không quan tâm `invoices.type` | high | no | Hoá đơn đổi ghi nợ bị loại khỏi lưới, hoặc hoàn tiền mặt trong khi khách còn nợ | confirmed | Đúng ở đường đọc, kiểm chạy thật: 3 hoá đơn đổi `debt` (`RTN-202607-00012/00010/00009`) có trong kết quả tìm kiếm. Phần cấn trừ công nợ chỉ có bằng chứng test ("settles the exchange own debt first, cash only for the remainder") — post thật chưa chạy |
| A-08 | Chuỗi đổi không giới hạn độ sâu — đổi của đổi của đổi vẫn hợp lệ | medium | no | Cần thêm ràng buộc độ sâu + thông báo UI | confirmed | Không có ràng buộc độ sâu ở bất kỳ đâu trong chuỗi (`create-exchange` / `assertLineEligible` / `checkout-return` đều không đọc độ sâu). Đã chạy thật tới độ sâu 2 (tạo nháp phiếu trả trỏ vào dòng OUT của một hoá đơn EXCHANGE → 201). Độ sâu ≥3 không dựng; nếu sau này cần chặn thì là tính năng mới |
| A-09 | Không cần migration: `invoice_items.returned_quantity` và `direction` đã tồn tại trên mọi dòng bất kể `invoices.type`, và `direction` mặc định `OUT` ở cấp DB nên dòng SALE cũ vẫn lọc đúng | high | no | Nếu sai thì phải backfill dữ liệu lịch sử trước khi mở khoá | confirmed | Đúng, không migration nào được tạo (`git status` trên `src/database/migrations` sạch) và cả tìm kiếm lẫn `eligible-returns` chạy đúng trên dữ liệu lịch sử có sẵn của `erp_dev`. Cơ sở ban đầu: Cơ sở: `invoice-item.entity.ts:74-78`; dòng SALE ghi `ItemDirection.OUT` tường minh ở `persist-invoice.step.ts:226` |
| A-10 | Backoffice không có màn hình đổi-trả-theo-hoá-đơn ⇒ thay đổi này chỉ chạm pos-web | high | no | Thiếu một mặt UI phải sửa | confirmed | Đúng — diff cuối cùng không chạm file nào trong `apps/backoffice-web`. Cơ sở ban đầu: Cơ sở: không file nào trong `apps/backoffice-web/src` nhắc `returnable` hay `eligible-returns` |
| A-11 | pos-web gọi API bằng `http` thô + interface `InvoiceRow` cục bộ (không dùng `@erp/api-client`) ⇒ đổi BE kiểu additive không bắt buộc `pnpm openapi:generate` để pos-web chạy được | high | no | FE build đỏ, hoặc regen kéo theo diff của công việc chưa commit khác | confirmed | Đúng: `apps/pos-web/src/lib/common/http.ts` là wrapper `fetch` viết tay, không axios, không `@erp/api-client`; kiểu body khai tại chỗ ở `dtos/invoice.dto.ts`. pos-web typecheck + 100/100 vitest + chạy thật ở `:3002` đều xanh mà chưa hề `openapi:generate`. Việc regen vẫn còn nợ cho `packages/api-client` (backoffice mới là bên dùng). Cơ sở ban đầu: Ghi nhận ở `[[project_return_goods_four_fixes]]` |
| A-12 | Giá vốn khi trả một dòng "Mua thêm" lấy `costPrice` đã chốt trên chính dòng OUT đó (qua `costPriceByOriginalItemId`), đúng bằng giá vốn đã ghi sổ lúc đổi | high | no | Đảo giá vốn theo giá mua hiện tại ⇒ lãi gộp lệch | confirmed | Đúng, và không nằm ở `checkout-return.service.ts` như ticket T-01-04 tưởng: giá vốn chốt lúc **tạo nháp**, `create-exchange-invoice.service.ts:82-91` lấy `costPrice` từ entity mà `assertLineEligible` trả về. Đã có test sẵn `create-exchange-invoice.service.spec.ts:184` |
| A-13 | Điểm tích luỹ đảo đúng mà không phải sửa gì: hoá đơn đổi ăn điểm trên `newSubtotal`, trả dòng OUT claw back theo `returnedNet` qua `computeReverseBase` — hai vế khớp khi A-05 đúng | medium | no | Khách bị trừ thừa/thiếu điểm sau lần đổi thứ hai | confirmed | Không sửa gì cho điểm tích luỹ và không test nào đang có bị đỏ (2917 test API xanh). Bằng chứng là cấu trúc + test: `computeReverseBase` claw back theo `returnedNet`, mà A-05 đã xác nhận đúng. Chưa chạy thật vì phải post phiếu trả |
| A-14 | Kiểm thử ở môi trường dev chạy từ **checkout gốc**, không phải worktree này; `apps/api/.env` không có trong worktree (gitignored) | high | no | Sửa ở worktree nhưng kiểm ở bản khác ⇒ kết luận sai | rejected | Hoá ra không cần kiểm từ checkout gốc. Đã dựng môi trường riêng cho worktree: chép `.env` từ checkout gốc, API `:4000`, vite `:3002` (`./node_modules/.bin/vite --port 3002 --strictPort`), thêm env `local-pos-worktree` vào `.ai/aidlc.yaml`. `:3001` để yên cho checkout gốc. Rủi ro mà giả định này cảnh báo — kiểm nhầm bản — vẫn thật, chỉ là né bằng cách khác. Cơ sở ban đầu: `[[reference_worktree_dev_server_parent_checkout]]` |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-15 | `refundable-value.util.ts` phải sửa để loại dòng IN khỏi mẫu số | `refundableFactor` = `max(0, 1 − headerResidual / sumNetLine)`, và `headerResidual` = 0 trên mọi hoá đơn `EXCHANGE` (A-05) ⇒ hệ số bằng 1 dù mẫu số có gồm dòng IN hay không | Util giữ nguyên, không sửa. Việc lọc dòng nằm ở `getEligibleLines`, không ở util. Bỏ 1 ticket khỏi kế hoạch |
| A-16 | `checkout-return.service.ts` phải sửa để nhận hoá đơn gốc kiểu `EXCHANGE` | Đã tổng quát sẵn: cập nhật `returned_quantity` theo `originalInvoiceItemId` (không đọc `type`), `lockOriginalDebt` theo `invoiceId` + `CREDIT_INVOICE`, fan-out sự kiện phân nhánh theo `invoice.type` của **chứng từ mới**, không phải của gốc | Không có ticket nào cho service này; chỉ thêm test hồi quy chứng minh nó đã đúng |
