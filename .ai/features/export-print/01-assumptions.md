---
feature: export-print
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Gọi `buildData` một lần với `limit` lớn trả về **toàn bộ** dòng và totals đã đúng cho cả tập, ở cả 4 miền báo cáo | high | yes | UOW-01: exporter phải lặp trang và tự cộng totals; đổi hợp đồng `ReportExportService` | confirmed | Đọc code 2026-07-27: cả 4 miền đều materialize → filter → totals-over-all → slice cuối (`report-data.util.ts:55,76`; `customer-debts.report.ts:280`; `profit-by-item.report.ts:217`; `revenue-by-item.report.ts:254`) |
| A-02 | `MAX_REPORT_ROWS = 50_000` là trần đủ cho export đồng bộ; không cần job bất đồng bộ ở bản đầu | medium | no | Nếu org lớn chạm trần thường xuyên → thêm UoW job + WS notify | pending | Thu hẹp sau A-18 (2026-07-27): trần chỉ còn áp cho đường **single-shot** (report tổng hợp). Report có `exportSource` stream theo trang nên không cần trần. Log số dòng theo `reportType` là dữ liệu quyết định report nào cần `exportSource` tiếp theo |
| A-03 | Export dùng lại đúng permission đọc sẵn có của từng miền (`inventory.reports.read`, …); không thêm permission key mới | high | no | Phải seed permission mới + cập nhật RBAC | confirmed | Là convention có sẵn: `cash-voucher-v2.controller.ts:34` ghi rõ "Reuses the receipt read permission rather than introducing a new key, which would 403 every existing role until RBAC granted it" (đọc code 2026-07-27) |
| A-04 | "In" ở màn báo cáo = in **bảng đang xem** (A4 ngang/dọc), không phải một mẫu chứng từ riêng | high | no | Đổi nội dung UOW-02 | pending | — |
| A-05 | Payload in chứng từ dựng được từ endpoint `GET /:id` sẵn có, không cần query mới | high | no | Thiếu field lẻ (địa chỉ, người ký) → thêm 1 ticket bổ sung field, không phải rework UoW | pending | Hạ blocking sau discovery 2026-07-27: cả 7 loại đều đã có `GET /:id` — `goods-receipts/:id`, `inventory/goods-issues/:id` (`goods-issue.controller.ts:157`), `inventory/transfer-orders/:id` (`:392`), `cash-receipts/:id`, `cash-payments/:id`, `bank-receipts/:id`, `bank-payments/:id` (mỗi cái ở `:47`). `GoodsReceiptService.getById:591` đã attach counterparty + purchasing employee. **Còn cần người xác nhận:** mẫu in cần thêm field nào ngoài dữ liệu sẵn có (chữ ký, địa chỉ kho, người giao/nhận) |
| A-06 | Nhãn cột tiếng Việt lấy từ `ReportColumnHeader.name` (hoặc `displayName` người dùng đặt lại); không cần nguồn nhãn mới | high | no | Thêm bảng nhãn riêng cho file xuất | pending | — |
| A-07 | Chi nhánh in trên đầu file lấy từ `actor.branchId` như `StockSummaryExportService` đang làm; báo cáo chuỗi (nhiều chi nhánh) để trống dòng đó | medium | no | Khối header workbook cần nhận danh sách chi nhánh | pending | — |
| A-08 | Sổ chi tiết tiền mặt (`LedgerCashPage`) không thuộc registry báo cáo v2 nên cần builder riêng, dùng lại `ReportWorkbookBuilder` ở mức thấp | high | no | UOW-05 to hơn dự kiến | confirmed | Tiền đề đúng (sổ quỹ vẫn ngoài registry) nhưng hết là rủi ro sau ADR-06: sổ quỹ cấp `ExportFetcher` của nó và dùng trọn pipeline thay vì mượn mỗi builder. UOW-05 nay `depends_on: [UOW-06]` |
| A-09 | Không có deadline / release train ràng buộc (người dùng chưa nêu) | low | no | Thứ tự UoW phải xếp lại theo mốc phát hành | pending | — |
| A-10 | Người hưởng lợi chính là kế toán + thủ kho ở backoffice; POS không nằm trong phạm vi | medium | no | Nếu POS cũng cần → thêm UoW cho pos-web | pending | — |
| A-11 | 5 nút stub báo cáo/chứng từ thuộc phạm vi feature này (2 ở `StorageReportShell`, 1 `LedgerCashPage`, 2 `InvoiceDetailDialog`) | medium | no | Stub còn lại vẫn bắn toast → success signal không đạt | **rejected** | Sai một phần. 2 stub ở `StorageReportShell` nằm trên UI **không vào được**: mọi mục nav `/reports/storage/*` trong `navConfig.ts` đang bị comment (phát hiện 2026-07-27 khi làm T-01-06). Bề mặt báo cáo đang chạy là `chain-store/reports/ReportPage`, vốn **chưa có** nút Xuất khẩu/In nào. Hệ quả: G3 reopen, T-01-06/T-01-07 đổi mục tiêu sang shell đang chạy; UOW-02 đổi mục tiêu tương tự |
| A-12 | `crud-entity-toolbar:69` (export cho generic CRUD platform) **ngoài** phạm vi — nó xuất danh sách bản ghi CRUD, không phải báo cáo hay chứng từ | medium | no | Thêm 1 UoW cho generic CRUD export | pending | — |
| A-19 | 8 file trong `examples/ERP` (4 `.xlsx` + 4 `.pdf` xuất từ MISA eShop) là **chuẩn trình bày** người dùng muốn, không phải ví dụ tham khảo | high | yes | UOW-09..11 sai mục tiêu; house-style phải lấy từ nguồn khác | confirmed | Người dùng xác nhận 2026-07-30: "báo cáo bên backoffice đang khác với những báo cáo trong files", và chốt phạm vi "4 tài liệu + lan house-style ra toàn bộ" |
| A-20 | Các cột trong mẫu mà repo không có dữ liệu (Serials, Giá bán, Thành tiền giá bán, Vị trí nhập, Đơn giá/Thành tiền của chuyển kho, Người vận chuyển) được **bỏ hẳn**, không dựng cột rỗng | high | yes | Phải thêm cột + migration cho `goods_receipt_lines` / `transfer_order_lines` — đổi hẳn kích thước feature | **đã sửa một phần** | Chốt ban đầu 2026-07-30: "Chỉ cột có dữ liệu thật". **Người dùng đảo lại 2026-07-30 (cùng ngày)** cho đúng 2 cột: `Giá bán` và `Thành tiền giá bán` phải **có mặt nhưng bị ẩn**, y như mẫu. Xem A-24. Các cột còn lại (Serials, Vị trí nhập, Đơn giá/Thành tiền của chuyển kho, Người vận chuyển) **vẫn bỏ** — chúng không có nguồn dữ liệu nào |
| A-24 | `Giá bán` lấy từ `ItemEntity.sellingPrice` (giá bán mặc định của mặt hàng), `Thành tiền giá bán` = `SL × sellingPrice` | medium | no | Nếu nghiệp vụ muốn giá bán **tại thời điểm lập phiếu** thì phải thêm cột vào `*_lines` + migration; hôm nay chứng từ kho không lưu giá bán theo dòng | pending | Đọc entity 2026-07-30: `ItemEntity.selling_price` có thật (decimal 18,2, default 0). Mẫu MISA để hai cột này bằng **0** ở mọi dòng — nghĩa là MISA cũng không có dữ liệu; ta điền được số thật nên điền số thật. **Cần người xác nhận:** giá bán mặc định của mặt hàng có đúng là thứ cần in không, hay phải là giá tại thời điểm lập phiếu |
| A-25 | "Chuyển kho" và "Lệnh điều chuyển" là **một** chứng từ (`VoucherKind.TRANSFER_ORDER`) | medium | no | Nếu là hai chứng từ khác nhau thì thiếu hẳn một mapper + một route export | pending | Repo chỉ có một route export chuyển kho (`/inventory/transfer-orders/:id/export`); UI gọi nó là "Lệnh điều chuyển" (`inventoryNavigation.ts:42`, `TransferOrdersPage.tsx:462`), mẫu MISA gọi là "PHIẾU CHUYỂN KHO". `StockTransferPage` là màn khác và hai nút In/Xuất khẩu ở đó đang `disabled` từ trước — ngoài phạm vi |
| A-21 | `Kho nhập` của phiếu chuyển kho lấy ở **cấp phiếu** (`TransferOrderEntity.destinationStorageId`) là đủ — mọi dòng của một phiếu về cùng một kho đích | medium | no | Nếu sau này cho phép mỗi dòng về một kho khác → cột này sai; phải thêm `destination_storage_id` cấp dòng | pending | Đọc entity 2026-07-30: `TransferOrderLineEntity` không có cột đích nào, nên hôm nay giả định này **là** mô hình dữ liệu, không phải lựa chọn |
| A-22 | Header báo cáo một dòng có `wrapText` là chấp nhận được thay cho header merge 2 dòng của mẫu (`A8:A9`) | medium | no | Nếu người dùng đòi đúng merge 2 dòng → `XlsxStreamWriter` phải giữ 2 dòng chưa commit trước khi merge, ngược với lối stream của ADR-08 | confirmed | Đối chiếu byte-level ở T-09-04 (2026-07-30): 43 thuộc tính so, **42 khớp**, đúng 1 lệch và chính là cái này — `header.border4` mẫu `false` vì ô trên của cặp merge không có viền dưới; ta `true` vì header một dòng. In ra giống nhau. Người dùng duyệt plan có nêu rõ điểm lệch này |
| A-23 | ~15 exporter buffered cũ (`StockSummaryExportService`, `DepositLedgerService`, `PosDailySummaryExportService`, `csv/*`, `customer/csv/*`, `stock-take`) **không** đổi theo house-style ở feature này | high | no | Người dùng thấy 2 phong cách file song song trong cùng hệ thống → phải mở thêm một UoW dọn dẹp | confirmed | Người dùng chọn phạm vi "4 tài liệu + lan ra toàn bộ **đường ExportPipeline**", loại phương án "toàn bộ, kể cả 7 exporter cũ" (2026-07-30). Ghi nhận là nợ kỹ thuật đã biết, không phải sơ suất |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-13 | jack-erp chưa có chức năng xuất Excel nào (theo khảo sát MISA §9.1) | Đã có 7 service dựng workbook bằng `exceljs` và ~14 route export đang chạy; `exceljs` là dependency sẵn có của `@erp/api` | Feature đổi từ "xây mới" thành "gom về builder dùng chung + nối vào nền tảng báo cáo v2". Bỏ mọi ticket cài đặt thư viện Excel |
| A-14 | Cần thêm puppeteer/wkhtmltopdf để render PDF (khảo sát §9.3) | Repo đã có 2 đường in chạy production: `renderInvoiceHtml`→iframe→`window.print()` (pos-web) và `jspdf`→Blob→tab mới (backoffice); `jspdf` đã là dependency của backoffice-web | ADR-02 chọn HTML→iframe→print. Bỏ ticket thêm dependency + ticket cài font tiếng Việt vào Docker image. Xác nhận bởi Akenzy, 2026-07-27 |
| A-15 | Phải chọn giữa localStorage và `report_templates` làm nguồn cấu hình cột trước khi export được | Client gửi kèm `columns[]` khi export → server không cần biết config lưu ở đâu | Bỏ ticket migrate localStorage → `report_templates`. Hai shell FE vẫn tồn tại song song, export đúng ở cả hai. Xác nhận bởi Akenzy, 2026-07-27 |
| A-17 | Nút In/Xuất khẩu ở `StorageReportShell` là bề mặt người dùng đang dùng | 8 trang `/reports/storage/*` không có mục nav nào đang bật; bề mặt sống là `ReportPage` (4 miền, dùng `report-data-source.ts`) | T-01-06/T-01-07 và UOW-02 chuyển sang `ReportPageToolbar`. Success signal trong `00-intent.md` sửa lại: đếm stub trên UI vào được, không đếm stub trên trang đã bị gỡ khỏi nav |
| A-16 | Exporter phải tự phân trang vì `limit` bị chặn `@Max(500)` | `@Max(500)` chỉ nằm trên DTO HTTP (`inventory-report-search.dto.ts:48`); `buildData` nhận limit tuỳ ý và đã cộng totals trên toàn tập | `ReportExportService` gọi `buildData` đúng một lần. Bỏ vòng lặp phân trang và phần tự cộng totals |
| A-18 | Gọi `buildData` một lần với `limit` lớn là chiến lược fetch đủ an toàn cho export (hệ quả ngầm của A-01/A-16) | Đúng về totals, sai về tải. `buildData` `getMany()` toàn bộ tập lọc vào RAM rồi `rows.slice` (`invoice-order-listing.report.ts:171,255`; `report-data.util.ts:74`) — nghĩa là export không thêm tải DB nào so với xem trang 1, nhưng cũng không có cách nào giảm. Trần 50k chỉ tồn tại ở inventory-reports và `assertUnderRowCap(result.total)` chạy **sau** khi đã materialize (`stock-summary.report.ts:127`); invoice/profit/debt không có trần nào. Cộng thêm `In(invoiceIds)` với tới 50.000 id và `writeBuffer()` giữ trọn workbook trong RAM | ADR-03 superseded. Thêm ADR-06 (pipeline 3 mảnh), ADR-07 (keyset+partition đặt trong report definition), ADR-08 (WorkbookWriter stream + COUNT chặn trước materialize). G2 reopen 2026-07-27 theo yêu cầu của Akenzy; scope B: chỉ report kiểu liệt kê có `exportSource`. Sinh UOW-06; UOW-05 chuyển sang phụ thuộc UOW-06 |
