---
id: T-05-03
uow: UOW-05
title: Đóng feature — regen api-client, release note, build sạch
layer: test
type: chore
estimate: 2h
status: done
depends_on: [T-05-02, T-03-03]
blocks: []
verifies: [AC-14, AC-16]
touches:
  - packages/api-client/src/generated/schema.ts
  - packages/api-client/openapi.snapshot.json
  - apps/pos-web/src/dtos/invoice.dto.ts
assumptions: []
---

# T-05-03 — Đóng feature

## Context
Ba endpoint POS đổi response và một DTO đổi tên field ⇒ phải sinh lại client và ghi lại thay đổi
hành vi trước khi coi là xong.

## Implementation notes
- `SearchPurchaseHistoryBody`: `totalPaid` → `totalAmount` (khớp DTO ở T-05-01).
- Chạy API rồi `pnpm openapi:generate`; **kiểm diff** — đợt 1 không có diff vì các endpoint chưa khai
  `@ApiOkResponse`, nhưng đừng giả định lần này giống vậy.
- Release note: ô lọc "Tổng thanh toán" ở tab Lịch sử mua hàng nay lọc theo **con số đang hiển thị**
  chứ không theo số tiền đã thu. Thói quen cũ "lọc ≤ 0 để xem đơn chưa thu" sẽ không còn đúng.
- `pnpm build` + `pnpm --filter @erp/api test` toàn bộ.

## Done when
- [x] api-client sinh lại, diff đã kiểm — `totalPaid` → `totalAmount` ở
      `PurchaseHistorySearchV2Dto`, cộng phần đợt 1 (`columnFilters`, `pageSize`) và DTO đổi trả
      nhanh mà snapshot cũ chưa có
- [x] Release note: `.ai/features/footer-grand-totals-standard/09-release-note.md` (repo không có
      CHANGELOG hay thư mục release note dùng chung; không ghi vào `.ai/architecture.md`)
- [x] `pnpm build` sạch; `npx jest` 1.940 pass / 1 skip. Một suite
      (`deposit-period-lock.service.spec.ts`) đỏ do jest worker SIGSEGV — chạy riêng 8/8 xanh, không
      liên quan thay đổi này.
