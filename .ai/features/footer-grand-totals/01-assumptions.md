---
feature: footer-grand-totals
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID   | Assumption                                                                                                                                                                                 | Confidence | Blocking | Blast radius if wrong                                                                 | Status  | Resolution |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------- | -------------------------------------------------------------------------------------- | ------- | ---------- |
| A-01 | Trên 8 báo cáo kho, lọc-theo-cột phía server phải phủ **mọi cột đang có ô lọc**, kể cả các cột ghép bằng JS (Đang chuyển đi, Sắp nhận về, cột chi nhánh động của pivot)                     | low        | yes      | Phạm vi UoW báo cáo — cột ghép JS cần semi-join riêng ⇒ +1 UoW                           | resolved | Chủ sở hữu chốt 2026-08-13: giữ lọc đủ mọi cột; sinh UOW-06 riêng cho các cột ghép JS |
| A-02 | Cột số trên báo cáo chuyển từ khớp-chuỗi-trên-text-đã-định-dạng sang **toán tử số** (`=`, `≤`, `≥`, khoảng) — hành vi lọc thay đổi thấy được với người dùng                                 | medium     | yes      | UX bộ lọc của 8 báo cáo; nếu sai phải mô phỏng khớp chuỗi trên số đã format ở server     | resolved | Chủ sở hữu chốt 2026-08-13: dùng toán tử số, thống nhất với `filterKind: number-range` của 3 trang phiếu kho |
| A-03 | Người đứng tên phê duyệt gate trong audit trail là chủ sở hữu các feature trước trong repo                                                                                                  | medium     | yes      | Chỉ là audit trail, nhưng `pass` bị từ chối nếu không có tên người thật                  | resolved | Chủ sở hữu chốt 2026-08-13: ký `Loc Tran` (không phải `Akenzy` như các feature trước) |
| A-04 | Footer phải **tái hiện đúng** các quirk sẵn có (dòng pending-only không chịu filter lưới; khử trùng `incomingAssigned`) thay vì sửa cho "đúng nghiệp vụ"                                    | high       | no       | Nếu sai: footer khớp nghiệp vụ nhưng lệch tổng các cột đang hiển thị — mâu thuẫn thấy được | confirmed | Chủ sở hữu chốt 2026-08-13: "chỉ sửa footer tính sai"; sửa bản chất tách việc riêng |
| A-05 | Giữ `totalQuantity` trong `StockSummaryResponse` song song với `totals` mới là đủ để không phá consumer hiện có (export, báo cáo chuỗi)                                                    | high       | no       | Một consumer vỡ; phát hiện ngay bằng `pnpm build` + spec                                 | pending | —          |
| A-06 | CTE totals mới của `stock-summary` chạy trong ngân sách chấp nhận được ở quy mô ~8k cặp (item, storage)                                                                                     | medium     | no       | Trang Tổng hợp tồn kho chậm đi; van xả là cờ `includeTotals`                              | pending | —          |
| A-07 | Response của 8 báo cáo được cache Redis theo `CACHE_NAMESPACE`; bump namespace là cách hợp lệ để tránh entry cũ thiếu `totals`                                                              | high       | no       | Cache lạnh trong thời gian ngắn sau deploy                                                | pending | —          |
| A-08 | Không consumer nào ngoài `StorageReportShell` phụ thuộc vào việc endpoint báo cáo bị chặn ở `pageSize ≤ 200` (tầng báo cáo chuỗi gọi thẳng service với `MAX_REPORT_ROWS`)                   | medium     | no       | Nới `@Max` làm lộ đường ép server tải nặng qua HTTP; giảm bằng cách giữ trần rõ ràng      | pending | —          |
| A-09 | Không cần migration hay đổi schema — mọi tổng lấy được từ cột sẵn có                                                                                                                        | high       | no       | Nếu sai, phạm vi đổi hẳn: có migration thì phải xét lại toàn bộ kế hoạch                  | pending | —          |

## Rejected assumptions

| ID   | What we assumed                                                                                          | What is actually true                                                                                     | Consequence                                                                     |
| ---- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| A-R1 | Có thể sửa footer 8 báo cáo bằng cách nạp đủ tập kết quả rồi giữ lọc/phân trang phía client (rẻ hơn nhiều) | Chủ sở hữu yêu cầu **tất cả phân trang server**, footer lấy total từ API                                    | Lọc-theo-cột buộc phải xuống server ⇒ A-01/A-02 sinh ra, phần báo cáo phình thêm |
| A-R2 | `BaseDataTable` có thể tự tính footer một chỗ cho cả app                                                  | `TableColumn.footer` chỉ là `ReactNode`; bảng không biết gì về dữ liệu nguồn (`BaseDataTable.tsx:60-65`)    | Không có bản sửa tập trung; phải sửa từng trang, nên mới cần cắt UoW theo trang  |
