---
id: T-01-02
uow: UOW-01
title: search-goods-issues-v2 trả totalAmount toàn tập
layer: application
type: feature
estimate: 2h
status: done
depends_on: [T-01-01]
blocks: [T-01-04]
verifies: [AC-05]
touches:
  - apps/api/src/modules/inventory/goods-issue/queries/search-goods-issues-v2.handler.ts
  - apps/api/src/modules/inventory/goods-issue/dto/goods-issue-search-v2.dto.ts
  - apps/api/src/modules/inventory/goods-issue/queries/search-goods-issues-v2.handler.spec.ts   # new nếu chưa có
assumptions: []
---

# T-01-02 — search-goods-issues-v2 trả totalAmount toàn tập

## Context
Giống hệt T-01-01, đổi bảng nguồn. `TOTAL_AMOUNT_SUBQUERY` ở `:18-19`
(`SUM(l.quantity * l.unit_price)` trên `goods_issue_lines`), đang dùng cho filter ở `:67`.
Scope gốc gồm cả điều kiện `status != CANCELLED` (`:51-54`) — phải nằm trong `buildQuery`, nếu
không footer sẽ cộng cả phiếu đã huỷ.

Phụ thuộc T-01-01 vì pattern chốt ở đó; đây là bản áp lại.

## Implementation notes
- Chép nguyên cấu trúc `buildQuery` của T-01-01.
- Giữ `status != CANCELLED` và scope chi nhánh trong `buildQuery`, không để ngoài.
- Query totals không join `lines`.

## Done when
- [x] Response có `totalAmount`
- [x] Spec bất biến `limit` (1 vs 100) pass
- [x] Spec: phiếu `CANCELLED` không được tính vào `totalAmount`
