---
id: T-06-05
uow: UOW-06
title: Xoá cờ chuyển tiếp của shell và chốt lại toàn bộ
layer: web
type: chore
estimate: 3h
status: done
depends_on: [T-06-02, T-06-04, T-02-04, T-03-04, T-04-03]
blocks: []
verifies: [AC-17, AC-22]
touches:
  - apps/backoffice-web/src/pages/reports/storage/_shared/StorageReportShell.tsx
  - apps/backoffice-web/src/pages/reports/storage/StockSummaryReportPage.tsx
  - apps/backoffice-web/src/pages/reports/storage/StockQuantityDetailsReportPage.tsx
  - packages/api-client/src/generated/schema.ts
  - packages/api-client/openapi.snapshot.json
assumptions: []
---

# T-06-05 — Xoá cờ chuyển tiếp của shell và chốt lại toàn bộ

## Context
T-03-04 thêm cờ `serverPaged` để 7 trang chưa chuyển vẫn chạy đường cũ. Tới đây cả 8 trang đã
chuyển, cờ trở thành nợ.

Hai cột điều chuyển ở báo cáo 1 và 3 được để lại TODO ở T-04-03 — T-06-02 đã làm xong phần server,
ticket này bù nốt phần FE.

## Implementation notes
- Xoá cờ và mọi nhánh chạy đường cũ trong shell; xoá luôn `applyColumnFilter`/`toComparableText`
  nếu không còn ai dùng.
- Bổ sung ô lọc + footer cho hai cột "Đang chuyển đi" / "Sắp nhận về" ở báo cáo 1 và 3; xoá TODO.
- Chạy API, `pnpm openapi:generate`, commit file sinh ra.
- Chạy `pnpm build` và `pnpm --filter @erp/api test` toàn bộ.

## Done when
- [x] Không còn nhánh phân trang client nào trong `pages/reports/storage/`
- [x] Hai cột điều chuyển có ô lọc và footer ở báo cáo 1 và 3 (AC-17)
- [x] `pnpm build` sạch; không test nào đang xanh chuyển đỏ; api-client đã commit (AC-22)

## Ghi chú khi thi công

Xoá luôn `REPORT_CLIENT_PAGE_SIZE` (chính là trần 200 cũ) khỏi `apiFilters.ts`, cùng
`applyColumnFilter`/`toComparableText` đã thành mồ côi trong shell. Shell nay **không có** đường
lùi về cộng-theo-trang: nếu `totals` vắng thì footer trống, chứ không âm thầm cộng `rows` — cách
cộng đó chính là lỗi vừa sửa và nó trông đúng trên mọi tập dữ liệu vừa một trang.
