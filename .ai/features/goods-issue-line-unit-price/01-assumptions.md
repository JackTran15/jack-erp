---
feature: goods-issue-line-unit-price
blocking_open: 0
---

# Assumption register — goods-issue-line-unit-price

Sổ này chỉ ghi những chỗ **phán đoán**, không ghi sự thật đã đọc được từ code (những thứ đó
thuộc `03-logical-design.md`).

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Xuất kho theo giá nhập tay làm giá vốn bình quân của tồn còn lại trôi khỏi giá vốn thật, và feature **chấp nhận** trôi — không treo phần chênh vào TK 632 | high | yes | Thêm hẳn một UoW ghi bút toán chênh lệch giá vốn + sửa INV-3 | confirmed | Akenzy chọn "Có — ghi sổ theo giá nhập tay" 2026-08-22 sau khi được cảnh báo rủi ro, và duyệt mục Out of scope tương ứng khi pass G0 |
| A-02 | `unitPrice = 0` (hoặc thiếu) nghĩa là **"chưa nhập"**, không phải "giá bằng 0" → server tự lấy bình quân tức thời | high | yes | Ngưỡng phân biệt sai làm mọi phiếu tự sinh ghi sổ giá trị 0 và rút `inventory_value` về 0 | confirmed | Akenzy chọn "Fallback về giá vốn bình quân" 2026-08-22 |
| A-03 | Chân xuất **tự sinh** của lệnh điều chuyển (`deriveExportLines`, không có form nhập giá) ghi sổ theo **`items.purchase_price`** như dòng code hiện có, chứ không đổi về bình quân tức thời | medium | yes | Điều chuyển liên chi nhánh định giá theo catalog thay vì giá vốn thật; sai lệch lan ra báo cáo kho | confirmed | Akenzy chọn "Giá mua trên danh mục (purchase_price)" 2026-08-22, **ngược khuyến nghị đã nêu**. Lý do phương án này vẫn vững: chân nhập `TRANSFER_IN` vốn đã ghi sổ theo `purchase_price`, nên giữ nguyên cả hai đầu là cách rẻ nhất để chúng cân bằng. Hệ quả phải nhớ: chân xuất **đổi hành vi** từ bình quân sang catalog |
| A-04 | Khi sửa phiếu xuất đã ghi sổ và **tăng** số lượng một dòng, phần tăng định giá theo **đơn giá mới của dòng đó**, không theo bình quân hôm nay như `goods-issue.service.ts:472-482` đang làm | medium | yes | INV-2 vỡ ngay lần sửa đầu tiên: `SUM(line_value)` không còn bằng giá trị phiếu | confirmed | Akenzy chọn "Đơn giá mới của chính dòng đó" 2026-08-22. Thay thế luật A-05 của feature `warehouse-voucher-edit-delete` cho riêng phiếu xuất — ghi thành ADR ở G2 |
| A-05 | Chân nhập `TRANSFER_IN` phải nhận **đúng đơn giá từng dòng của chân xuất**, giữ tách các dòng trùng SKU, để giá trị hai đầu cân bằng tuyệt đối | medium | yes | Chi nhánh nhận định giá tồn khác chi nhánh gửi; mục tiêu "hai đầu cân bằng" trong Success signal không đạt | confirmed | Akenzy chọn "Đổi — hai đầu cân bằng" 2026-08-22. Với đường tự sinh, A-03 làm hai đầu vốn đã trùng `purchase_price` nên không phải sửa; thay đổi thực tế chỉ nằm ở đường có form (`buildImportLinesFromInput`) |
| A-06 | Form **"Xuất kho theo lệnh điều chuyển"** (`buildExportLinesFromInput:979`) có cho người dùng nhập đơn giá, và giá đó phải được tôn trọng y như phiếu xuất thường | medium | no | Một màn hình vẫn ghi đè giá, người dùng báo lại đúng lỗi cũ ở đường khác | confirmed | **Đúng.** `buildExportLinesFromInput:979` truyền thẳng `l.unitPrice ?? 0`, và sau ADR-01 `post()` tôn trọng nó. Khoá lại bằng test `lets the export form override that price, line by line` (T-04-01) |
| A-07 | Báo cáo kho (`transfer-report`, `stock-period`, `document-detail`) **không cần sửa**: ý nghĩa cột `goods_issue_lines.unit_price` vẫn là "giá vốn đã ghi sổ", chỉ nguồn của nó đổi | high | no | Thêm một UoW rà lại 3 service báo cáo | confirmed | **Đúng, không phải sửa gì.** Ý nghĩa cột `unit_price` không đổi (vẫn là giá vốn đã ghi sổ), chỉ nguồn của nó đổi. Toàn bộ 291 suite / 3162 test xanh, trong đó có `inventory-reports`; không service báo cáo nào bị chạm |
| A-08 | Không luồng nào ngoài `transfer-order` gọi `GoodsIssueService.create/createAndPost/update`; `stock-take` dựng `GoodsIssueLineEntity` trực tiếp với giá vốn nên không đi qua `post()` | high | no | Một luồng ẩn (POS / kho tạm / xếp kệ) ghi sổ giá 0 hoặc giá catalog | confirmed | **Đúng.** Lượt rà bắt buộc ở T-04-01 (`grep` cả lời gọi service lẫn `new GoodsIssueLineEntity`) chỉ tìm thấy `transfer-order` và handler v2 — handler v2 là đường người dùng đi, không phải luồng ẩn. `stock-take` dựng entity trực tiếp nên không qua `post()` |
| A-09 | Vòng re-price `goods-issue.service.ts:524-540` trở nên **thừa** dưới mô hình giá-người-dùng: nếu sổ luôn ghi theo giá của chính phiếu thì `Σ line_value = Σ (qty × giá dòng)` là hiển nhiên, INV-2 tự đúng | medium | no | Phải giữ lại vòng re-price và tìm cách khác để hai dòng trùng SKU không đè nhau | confirmed | **Đúng, và mạnh hơn dự đoán.** Xoá cả hai vòng lặp mà INV-2 vẫn đúng — kiểm trên Postgres thật: `−30.900.000` lúc ghi sổ, `−27.500.000` sau khi sửa, và mọi dòng sổ cũ giữ nguyên `posted_at` (T-01-03, T-02-04) |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-10 | `342.941` là bình quân gia quyền của hai dòng người dùng nhập | Là bình quân giá vốn của DD780 trên **toàn chi nhánh** (`Σ line_value / Σ quantity`); bình quân hai dòng 30/60 phải là 343.333 | Bài toán không phải "sửa công thức phân bổ" mà là "bỏ ghi đè" — cắt bỏ hẳn hướng sửa vòng re-price |
| A-11 | Tầng tính chênh lệch sổ kho gộp nhầm hai dòng trùng SKU | `voucher-delta.util.ts:62-88` cộng gộp **đúng** cả hai phía trước khi so sánh, có comment nói rõ phiếu được phép mang hai dòng trùng | Không có UoW nào cho `voucher-delta.util.ts`; lỗi nằm ở vòng re-price phía sau và ở `applyDeltaToLines` |
| A-13 | Phiếu xuất chênh lệch kiểm kê định giá theo **bình quân tức thời** | Nó định giá theo `ItemCostSnapshotService.snapshotCosts` — ảnh giá mua chốt một lần cho cả phiếu (`stock-take.service.ts:682-685`), không đụng `getInstantAverageCost` | Chỉ sai ở phần mô tả cơ chế; điều AC-11 thực sự khoá ("không đổi") vẫn đúng vì `stock-take` dựng `GoodsIssueLineEntity` trực tiếp và không đi qua `post()`. Đã sửa câu chữ AC-11, ticket T-04-01 và kịch bản demo UOW-04 |
| A-12 | Đây là thiết kế mới cần dựng từ đầu | `stock-transfer.service.ts:375-376` và `goods-receipt.service.ts:814,1275` đã làm đúng mô hình cần có | Feature thu về "kéo `goods-issue` về đúng chuẩn hai module anh em", không phát minh cơ chế mới |
