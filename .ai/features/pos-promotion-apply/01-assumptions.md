---
feature: pos-promotion-apply
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID   | Assumption                                                                                                                                                     | Confidence | Blocking | Blast radius if wrong                                                        | Status    | Resolution                                                                                                                       |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ----------------------------------------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------- |
| A-01 | Thu ngân được phép chọn quà khi CTKM ở `mode: ONE_OF`, và lựa chọn đó phải tới được server — tức `CheckoutV2Dto` cần thêm trường mới                          | high       | yes      | UOW-02 đổi contract BE + regen OpenAPI; nếu sai thì cả UoW biến mất           | confirmed | Akenzy chọn "Thêm field BE + dialog chọn quà" (06/08/2026). Luôn mở dialog, kể cả khi chỉ có 1 ứng viên                          |
| A-02 | Nguồn dữ liệu xem trước là `POST /v2/promotions/evaluate`, **không** phải `dryRun` trên checkout                                                               | high       | yes      | Toàn bộ UOW-03 đổi endpoint, đổi shape store, đổi cách validate voucher       | confirmed | Akenzy chọn "POST /v2/promotions/evaluate" (06/08/2026)                                                                          |
| A-03 | `evaluate` không validate voucher, nên POS phải có đường riêng để biết mã voucher hợp lệ trước khi checkout                                                    | high       | yes      | Thu ngân nhập mã sai vẫn thấy tổng tiền "đẹp", tới lúc chốt mới lỗi          | confirmed | Hệ quả trực tiếp của A-02 + Akenzy chọn thêm `GET /v2/vouchers/lookup?code=` (06/08/2026)                                        |
| A-04 | Ba hành vi MISA vào phạm vi: hiện lý do CTKM bị bỏ qua, cho thu ngân đổi CTKM thắng khi tranh chấp dòng, giảm giá tay mức hóa đơn có bắt buộc lý do            | high       | yes      | Thừa/thiếu 2–3 UoW                                                            | confirmed | Akenzy chọn 3/4 mục (06/08/2026); **loại** "tính lại lười" — POS ta tính lại ngay                                                |
| A-05 | Role `STAFF` không có `promotion.read` lẫn `pos.promotion.read` nên gọi `evaluate` sẽ 403                                                                      | high       | yes      | Mọi UoW xem trước chết ngay ở môi trường thật dù code đúng                    | confirmed | Đọc trực tiếp `database/seeds/org-role-permissions.ts` — `STAFF_PERMISSION_KEYS` không chứa key nào thuộc module `promotion`     |
| A-06 | `apps/pos-web` không có test runner: `"test": "echo test"`, vitest không nằm trong dependency nào, 4 file `*.test.ts` dưới `lib/page-libs/checkout/` chưa từng chạy | high       | yes      | Mọi ticket ghi "unit test xanh" là lời hứa không kiểm chứng được              | confirmed | Đọc `apps/pos-web/package.json` + grep `vitest` toàn repo → 0 kết quả. UOW-01 phải dựng runner trước, nếu không thì bỏ hẳn cột test |
| A-07 | Server luôn tự tính lại tiền giảm; FE chỉ gửi lựa chọn (id chương trình, mã voucher, id quà), không gửi số tiền                                                | high       | yes      | Nếu sai, FE và BE có thể lệch số và không ai là nguồn sự thật                 | confirmed | ADR-06 của `checkout-saga` + AC-19; ghi lại trong gap doc §5                                                                     |
| A-08 | Cho thu ngân đổi CTKM thắng ⇒ `selectedProgramIds` phải được mở rộng ngữ nghĩa: không chỉ chọn CTKM `auto_apply=false`, mà còn **đè lên `priority`** khi tranh chấp | medium     | yes      | `PromotionResolver` phải sửa (không chỉ mở rộng đầu vào) — tăng rủi ro hồi quy cho engine đã xanh 31/31 ticket | confirmed | Suy ra từ A-04: không có cách nào khác để lựa chọn của thu ngân thắng được `priority`. Đánh dấu rủi ro cao ở UOW-04             |
| A-09 | Giảm giá tay mức hóa đơn cần trường BE mới (số tiền/%, lý do bắt buộc, phạm vi `ALL` \| `NOT_DISCOUNTED`) — hiện `CheckoutV2Dto` chỉ có `manualLineDiscount` ở mức **dòng** trên `EvaluateCartDto` | medium     | yes      | UOW-05 phình từ "FE nối dây" thành "đổi contract + migration cột hóa đơn"      | confirmed | Đọc `evaluate-cart.dto.ts` (chỉ có `manualLineDiscount` per line) và `checkout-v2.dto.ts` (không có trường nào cho mức hóa đơn) |
| A-10 | Tính lại preview **ngay** khi giỏ hàng/voucher/CTKM đổi, có debounce; không bắt chước cơ chế hỏi-rồi-mới-cập-nhật của MISA                                     | high       | no       | UX khác MISA; thu ngân quen MISA có thể thấy lạ                               | confirmed | Akenzy **không** chọn "tính lại lười" ở câu 3 (06/08/2026)                                                                       |
| A-11 | Mã voucher là duy nhất trong phạm vi tổ chức nên `lookup?code=` trả tối đa 1 kết quả                                                                            | medium     | no       | Endpoint lookup phải trả mảng và POS phải cho chọn                            | pending   | —                                                                                                                                |
| A-12 | Một hóa đơn chỉ áp được **một** voucher (`CheckoutV2Dto.voucherCode` là `string`, không phải mảng)                                                             | high       | no       | UI voucher phải thành danh sách nhiều mã                                      | pending   | —                                                                                                                                |
| A-13 | Thu ngân được phép **bỏ** một CTKM `auto_apply=true` mà server đã tự áp                                                                                        | low        | no       | Thiếu một nút "bỏ áp dụng"; contract chưa có trường loại trừ                  | confirmed | Akenzy chọn lên UOW-09 để đóng gap này (12/08/2026), sau khi live-test lộ 2 triệu chứng cùng gốc: nút X ở dòng tổng tự áp lại CTKM vừa bỏ, checkbox "Đã áp dụng" trong dialog không untick được. Quyết định: field mới `excludedProgramIds` (ADR-07); nút X ở dòng tổng bỏ **hết** mọi CTKM đang áp cùng lúc, không chỉ một dòng; mọi hành động loại trừ đều qua hộp xác nhận (đồng bộ pattern UOW-04/AC-12) |
| A-14 | Đổi `CheckoutV2Dto`/`EvaluateCartDto` là **thêm trường optional**, không phá client cũ, nên không cần version `/v3`                                            | high       | no       | Phải thêm version mới thay vì sửa tại chỗ                                     | pending   | —                                                                                                                                |
| A-15 | Badge "(X%)" trên dòng Khuyến mại chỉ hiện khi đúng 1 chương trình `INVOICE_DISCOUNT` kiểu `PERCENT` đang áp một mình — nhiều CTKM cùng lúc thì ẩn %, vì % lẻ không mô tả đúng tổng đã cộng dồn | high       | yes      | T-07-02 sai quy tắc hiển thị ⇒ số % gây hiểu lầm cho thu ngân                 | confirmed | Akenzy chọn "omit khi stacked" trong phiên review QA bug list (10/08/2026)                                                       |
| A-16 | Nút X trên dòng Khuyến mại chỉ bỏ tick CTKM **tùy chọn** (`selectedProgramIds → []`); không override CTKM `auto_apply=true` đang thắng — việc đó thuộc `UOW-04-cashier-overrides-winner` (còn `todo`) | high       | no       | Thiếu phạm vi rõ ràng cho T-07-03 nếu không ghi lại                          | confirmed | Suy ra trực tiếp từ phạm vi đã chốt của UOW-04; xác nhận cùng phiên QA (10/08/2026)                                              |
| A-17 | "KM theo mặt hàng" trên hóa đơn in **không** gộp giảm giá tay của thu ngân (`CartLine.lineDiscount`, nhãn "Giảm giá" có sẵn) — hai nguồn tiền giữ tách dòng riêng | high       | yes      | T-08-02/T-08-03/T-08-04 sai nếu cộng chung; hóa đơn in sai ý QA               | confirmed | Akenzy chọn "Tách thành dòng riêng" thay vì cộng dồn, phiên review QA (10/08/2026)                                               |
| A-18 | In lại hóa đơn (Danh sách hóa đơn / Lịch sử mua hàng) cần một API đọc mới vì bảng `invoice_checkout_promotions` hiện chỉ được ghi (`persist-invoice.step.ts`), chưa từng có chỗ đọc ra | high       | yes      | T-08-04 không có nguồn dữ liệu nếu thiếu T-08-01                              | confirmed | Xác nhận bằng cách grep toàn backend cho `InvoiceCheckoutPromotionEntity` — không có query/controller nào đọc bảng này            |

## Rejected assumptions

| ID   | What we assumed                                                     | What is actually true                                                                                                            | Consequence                                                                                                              |
| ---- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| A-90 | `apps/pos-web` có vitest thật, khác `backoffice-web`                | `"test": "echo test"`, không có vitest ở bất kỳ `package.json` nào; 4 file `*.test.ts` tồn tại nhưng không có runner nào chạy   | Sửa mục Constraints của `00-intent.md`; thêm A-06; UOW-01 gánh việc dựng runner trước khi bất kỳ ticket nào hứa "test xanh" |
| A-91 | Thu ngân đã có sẵn quyền gọi `/v2/promotions/evaluate`              | `STAFF_PERMISSION_KEYS` không chứa key nào thuộc module `promotion`; `evaluate` đòi `promotion.read` → 403                       | Thêm A-05; RBAC lên thành ticket chặn trong UOW-01 chứ không phải việc dọn dẹp cuối                                        |
| A-92 | Nối POS chỉ là việc frontend, như gap doc PV-1/PV-2 gợi ý           | 3 lựa chọn của user (A-01, A-04, A-08, A-09) đều đòi trường mới hoặc đổi logic `PromotionResolver`                              | Phạm vi BE mở rộng: 1 endpoint mới + 3 thay đổi contract + 1 sửa resolver; không còn là "chỉ nối dây"                     |
