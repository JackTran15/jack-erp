---
id: T-01-06
uow: UOW-01
title: E2E — evaluate bằng token STAFF trả đúng số trên giỏ 1 dòng
layer: test
type: test
estimate: 2h
status: todo
depends_on: [T-01-05]
blocks: []
verifies: [AC-01, AC-22]
touches:
  - apps/api/test/e2e/promotion-evaluate-pos.e2e-spec.ts   # new; cùng khuôn với checkout-saga-promotion.e2e-spec.ts
  - apps/api/test/e2e/setup/checkout-saga-fixture.ts
assumptions: [A-05]
---

# T-01-06 — E2E quyền và số tiền của evaluate

## Context
Phần FE không có runner (ADR-06), nên lưới an toàn tự động của UoW này nằm ở backend. Bài test
phải chạy bằng **token role STAFF** chứ không phải admin — chạy bằng admin sẽ xanh ngay cả khi
A-05 chưa được sửa, tức là một bài test không phát hiện được đúng lỗi mà nó tồn tại để bắt.

## Implementation notes
- Tái dùng fixture `checkout-saga-fixture.ts`; thêm helper tạo user role `STAFF` nếu chưa có
- Case 1: token STAFF + 1 dòng giá 1.495.000 có CTKM giảm 30% ⇒ 200, `promotionDiscount` = 448.500,
  `amountAfterPromotion` = 1.046.500
- Case 2: token thiếu cả hai key ⇒ 403 (khẳng định guard vẫn chặn đúng, không mở toang)
- Case 3: token backoffice có `promotion.read` ⇒ 200 (đường cũ không vỡ)
- E2E chạy tuần tự `maxWorkers: 1`, `forceExit: true`; teardown kafkajs treo có thể giả dạng
  "suite failed" — đọc output thật, đừng tin mỗi exit code

## Done when
- [ ] 3 case trên xanh trong `pnpm --filter @erp/api test:e2e`
- [ ] Case 2 thực sự đỏ nếu tạm bỏ guard (kiểm bằng cách sửa tạm rồi hoàn lại)
