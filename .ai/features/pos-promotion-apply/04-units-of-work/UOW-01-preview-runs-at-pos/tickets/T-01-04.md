---
id: T-01-04
uow: UOW-01
title: promotionService.evaluate + hook preview có debounce và abort
layer: web
type: feature
estimate: 4h
status: done
depends_on: [T-01-02, T-01-03]
blocks: [T-01-05]
verifies: [AC-01, AC-02, AC-04]
touches:
  - apps/pos-web/src/services/promotion.service.ts        # new; cùng khuôn services/invoice.service.ts
  - apps/pos-web/src/dtos/promotion.dto.ts                # new
  - apps/pos-web/src/hooks/page-hooks/checkout/use-checkout-promotion-preview.ts   # new
assumptions: [A-02, A-07, A-10]
---

# T-01-04 — Service + hook gọi evaluate

## Context
Đây là lời gọi `evaluate` **đầu tiên** từ `pos-web` — hiện `grep "/promotions/evaluate"` trong
`apps/pos-web/src` trả 0 kết quả.

Hook chạy mỗi khi giỏ hàng đổi, mà thu ngân quét mã liên tục, nên debounce và huỷ lời gọi cũ
không phải tối ưu hoá sớm: thiếu nó thì kết quả về trễ của giỏ hàng cũ sẽ ghi đè kết quả mới
và hiện sai tiền (AC-02).

## Implementation notes
- `promotionService.evaluate(body): Promise<EvaluateCartResponse>` → `POST /v2/promotions/evaluate`,
  dùng `http` client sẵn có trong `services/`
- `lineId` gửi lên là id dòng giỏ hàng phía client; server echo lại để map kết quả về đúng dòng
  mà không phải đoán theo thứ tự
- Debounce **300ms**; mỗi lần gọi mới `abort()` lời gọi đang bay (AbortController)
- Không gọi khi giỏ rỗng — trả về sớm, đặt slice về `idle` (AC-04)
- Đầu vào lấy từ store: dòng giỏ hàng, `customerId`, `selectedProgramIds`. Ba trường của UoW
  sau (`selectedGifts`, `manualDiscount`, voucher) **chưa** truyền ở ticket này
- Lỗi bất kỳ ⇒ slice `unavailable`, **không** throw ra ngoài, **không** chặn luồng bán hàng
- NFR Performance của feature này được verify tại đây

## Kết quả (07/08/2026)

- `promotion.service.ts` + `promotion.dto.ts` + `use-checkout-promotion-preview.ts` đã viết.
- **Phải sửa thêm `lib/common/http.ts`** ngoài danh sách `touches` ban đầu: `http.post` không
  nhận `AbortSignal`, mà không có nó thì yêu cầu abort của ticket này không thực hiện được.
  Thêm tham số `opts?: { signal }` — tương thích ngược, mọi call site cũ không đổi.
- **Chưa truyền `selectedProgramIds`**: slice đó do T-02-03 tạo cùng dialog chọn CTKM. Mô tả
  ticket ban đầu ghi là truyền ở đây, nhưng làm vậy sẽ tham chiếu một trường chưa tồn tại.
- **Bug phát hiện lúc click-through (07/08/2026): hook chưa được gọi ở đâu cả.** `grep
  useCheckoutPromotionPreview` chỉ trả về đúng dòng định nghĩa — không nơi nào import/gọi nó,
  nên tính năng không chạy gì trên app thật dù `tsc` sạch. Đã thêm `useCheckoutPromotionPreview()`
  vào `CheckoutPage.tsx`, cùng chỗ và cùng khuôn với `useSyncCartOnHand()` (hook side-effect
  void khác, gọi 1 lần ở page level). `CheckoutPage.tsx` không có trong `touches` ban đầu.

## Done when
- [x] Giỏ rỗng không phát lời gọi nào — `lines.length === 0` trả về sớm trước khi hẹn giờ
- [x] `tsc --noEmit` của `pos-web` sạch
- [x] Quét 5 mã trong 1 giây phát ra ≤ 2 lời gọi, kết quả ứng với giỏ hàng cuối cùng
- [x] Chặn endpoint trong DevTools ⇒ slice về `unavailable`, không exception nào lọt ra console

## Click-through (07/08/2026, tài khoản `staff-hn@erp.local`, role STAFF)

- 5 lần sửa SL liên tiếp (2→3→4→5→6) trong < 1s batch ⇒ **đúng 1** `POST /v2/promotions/evaluate`
  (Network tab), kết quả hiển thị khớp SL cuối (6, không phải giá trị giữa chừng) — AC-02.
- Monkey-patch `window.fetch` chặn riêng `/promotions/evaluate` (không có API "block URL" trong
  bộ công cụ trình duyệt đang dùng, patch fetch cho cùng hiệu ứng) ⇒ panel hiện "Chưa tính được
  khuyến mại", nút Thu tiền vẫn `disabled: false` (kiểm bằng JS), bấm được, hoá đơn chốt thành
  công (`INV-202608-00003`, `POST /invoices/:id/checkout` trả 201) — AC-03, đúng NFR Chịu lỗi.
  Không exception nào của app lọt console (1 exception thấy được là của extension Chrome khác,
  không liên quan `pos-web`).
- Giỏ rỗng ⇒ không dòng "Khuyến mại", không request nào — xác nhận lại AC-04 bằng mắt.
