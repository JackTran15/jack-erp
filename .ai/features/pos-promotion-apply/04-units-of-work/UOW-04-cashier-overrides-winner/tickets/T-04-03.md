---
id: T-04-03
uow: UOW-04
title: Hộp xác nhận trước khi hoán đổi CTKM tranh chấp
layer: web
type: feature
estimate: 3h
status: done
depends_on: [T-04-01]
blocks: []
verifies: [AC-11, AC-12]
touches:
  - apps/pos-web/src/components/page-components/Checkout/CheckoutDialogs/PromotionSelectionModal/PromotionSelectionModal.tsx
  - apps/pos-web/src/hooks/page-hooks/checkout/use-checkout-promotion.ts
  - apps/pos-web/src/constants/checkout-messages.constant.ts
assumptions: [A-08]
---

# T-04-03 — Xác nhận trước khi hoán đổi

## Context
Hoán đổi CTKM đổi số tiền khách phải trả, nên không được xảy ra do một cú tick nhầm.
Hệ tham chiếu MISA hỏi *"CTKM A và B cùng áp dụng cho 1 hàng hóa. Bạn có muốn đổi thành áp
dụng chương trình A không?"* — ta giữ đúng nội dung đó vì thu ngân đã quen.

Sau UOW-02, CTKM bị bỏ qua đang hiện checkbox **disabled**. Ticket này mở lại checkbox cho
riêng nhóm `RESOURCE_TAKEN` — các lý do khác (hết hạn, sai chi nhánh, chưa đủ điều kiện) vẫn
disabled vì tick vào cũng không giải quyết được gì.

## Implementation notes
- Chỉ dòng có `reason === 'RESOURCE_TAKEN'` mới tick được; các reason khác giữ disabled
- Tick vào ⇒ hộp xác nhận nêu **tên cả hai** chương trình, không dùng id
- Huỷ ⇒ checkbox trả về trạng thái cũ, không gọi API, không đổi store
- Xác nhận ⇒ thêm id vào `selectedProgramIds`, preview chạy lại, panel cập nhật theo số server
- Câu chữ vào `checkout-messages.constant.ts` cùng chỗ với các announcement khác, không hardcode
  trong component
- Tick nhiều dòng `RESOURCE_TAKEN` trong một lần mở dialog: hỏi từng cái, không gộp — mỗi lần
  hoán đổi là một quyết định về tiền

## Done when
- [x] Dòng `RESOURCE_TAKEN` tick được; dòng lý do khác vẫn disabled
- [x] Hộp xác nhận nêu đúng tên hai chương trình
- [x] Huỷ không để lại thay đổi nào trong store hay Network
- [x] Xác nhận ⇒ panel đổi sang số của chương trình mới, lấy từ server

Live-verified qua Chrome (backoffice tạo 2 CTKM INVOICE_DISCOUNT trỏ cùng khách hàng/toàn
đơn — "UOW04 Demo A" priority 10/10%, "UOW04 Demo B" priority 20/50% — rồi POS thêm 1 dòng
750.000):
- Dialog: A "Đã áp dụng", B "Đã bị chương trình UOW04 Demo A giành mất" (dòng B tick được,
  các dòng khác — KM tang hang hoa, TEST LIVE CLICKTHROUGH — vẫn khoá)
- Tick B → Đồng ý → hộp "Đổi chương trình khuyến mãi": *Chương trình "UOW04 Demo A" và
  "UOW04 Demo B" cùng áp dụng cho 1 hàng hóa. Bạn có muốn đổi thành áp dụng chương trình
  "UOW04 Demo B" không?* → Đổi chương trình → panel: Khuyến mại -375.000, Còn phải thu
  375.000 (= 50% của 750.000, đúng số của B, lấy từ server)
  — không phải -75.000 (10% của A)
- Mở lại dialog: đối xứng đúng — B "Đã áp dụng", A "Đã bị chương trình UOW04 Demo B giành mất"
- Tick A → Đồng ý → hộp xác nhận hiện lên → Huỷ: `read_network_requests` trong suốt chuỗi
  tick→Đồng ý→Huỷ trả về rỗng (0 request); dialog mở lại đúng trạng thái cũ (B vẫn "Đã áp
  dụng", A vẫn "Đã bị chương trình UOW04 Demo B giành mất"); panel phải bên phải không đổi
  (-375.000 / 375.000)
- Dọn dẹp: xóa 2 CTKM demo sau khi verify xong

**Bổ sung sau demo ban đầu**: theo yêu cầu người dùng, đổi phần "tick được" từ click-cả-dòng
(chỉ có highlight nền) sang checkbox vuông thật bên trái "Tên chương trình" — khớp UI tham
chiếu MISA. Dùng `PosCheckbox` có sẵn của pos-web (không phải tạo mới, `@erp/ui` không có
Checkbox). Root của dòng đổi từ `<button role="row">` sang `<div role="row">` (nút không được
lồng input/label hợp lệ) — bù lại bằng `onClick` trên div + checkbox tự có bàn phím riêng
(native input, `onKeyDown` cho Enter).

Bắt được 1 lỗi hiển thị thật khi verify sống: `PosCheckbox`'s `disabled` prop quyết định luôn
nền (`bg-gray-200`), đè lên nền xanh của `checked`. Ban đầu truyền `disabled={rowDisabled}`
(gồm cả `locked`) khiến dòng "Đã áp dụng" (đang thắng, auto-apply) hiện checkbox tích nhưng nền
xám — trông như CHƯA tích. Sửa: chỉ truyền `disabled={promotion.disabled}` (không gồm
`locked`) cho checkbox và cho phần mờ cả dòng — `locked` vẫn chặn click (qua handler, không
qua `disabled`) nhưng không còn làm dòng "Đã áp dụng" trông như bị vô hiệu hoá.
