---
id: T-04-04
uow: UOW-04
title: E2E — hoán đổi CTKM ghi đúng chương trình lên hoá đơn
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-04-02]
blocks: []
verifies: [AC-11]
touches:
  - apps/api/test/e2e/checkout-saga-promotion.e2e-spec.ts
assumptions: [A-08]
---

# T-04-04 — E2E hoán đổi tới tận hoá đơn

## Context
T-04-02 chứng minh resolver đúng ở mức domain. Bài này chứng minh lựa chọn đó đi xuyên qua
saga và ghi đúng vào `invoice_checkout_promotions` — nơi kế toán sẽ đọc.

## Implementation notes
- Fixture: 2 CTKM cùng trỏ 1 SKU, A giảm 30% priority 10, B giảm 50% priority 20
- Case 1: checkout không gửi `selectedProgramIds` ⇒ hoá đơn ghi **A**, `discountAmount` = 448.500
- Case 2: checkout gửi `selectedProgramIds: [B]` ⇒ hoá đơn ghi **B**, `discountAmount` = 747.500,
  và **không** có dòng snapshot nào cho A
- ~~Case 2 khẳng định thêm: `invoice_items` của dòng đó có `promotion_program_id` trỏ về B~~ —
  sai so với code thật: cột đó chỉ được set cho dòng **gift** (`persist-invoice.step.ts`,
  `createGiftLines`), một ITEM_DISCOUNT không tạo dòng `invoice_items` riêng nên cột này luôn
  `null` cho trường hợp này. Thay bằng khẳng định ở `invoice_checkout_promotions.line_discounts`
  (đúng `discountAmount: 747500`, tức là số của B chứ không phải A) — verify bằng cách chạy thật,
  không chỉ tin vào mô tả ticket.
- Số 747.500 lấy từ thực nghiệm MISA 06/08/2026 (1.495.000 giảm 50%)

## Done when
- [x] 2 case xanh trong `pnpm --filter @erp/api test:e2e`
- [x] Case 2 khẳng định không còn dấu vết của A trên hoá đơn

Chạy `pnpm --filter @erp/api test:e2e -- checkout-saga-promotion`: 9/9 xanh (7 case cũ + 2 case
mới). Dòng "Test suite failed to run" / exit code khác 0 sau đó là do `OutboxRelayService`
polling sau khi app đã đóng ("relation outbox_messages does not exist") — nhiễu đã biết, không
liên quan (xem CLAUDE.md: "kafkajs consumers leave handles open... check actual test output, not
just the exit message").

Phát hiện thêm ngoài phạm vi ticket: `test/e2e/setup/global-setup.ts` có bug làm suite luôn từ
chối chạy — nó so `process.env.DB_NAME` (đã bị `.env.test` ghi đè thành `erp_test`) với chính nó
thay vì so với `apps/api/.env` (dev, `erp_dev`) thật. Sửa để đọc trực tiếp `.env` bằng
`dotenv.parse()` thay vì tin `process.env` sau khi `.env.test` đã load đè lên.
