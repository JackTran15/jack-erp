---
id: T-07-04
uow: UOW-07
title: Test hardening dòng Khuyến mại
layer: test
type: test
estimate: 1h
status: done
depends_on: [T-07-03]
blocks: []
verifies: [AC-25, AC-27]
touches:
  - apps/pos-web/src/lib/page-libs/checkout/promotionPresentation.test.ts   # new
assumptions: []
---

# T-07-04 — Test hardening

## Context
Gộp lại coverage của T-07-02 và thêm case hồi quy còn thiếu: "0 chương trình áp ⇒ dòng không
được render". Nếu T-07-02 chưa tách riêng một hàm thuần `shouldShowPromotionRow(data)` thì tách
ra ở đây để có chỗ test — đừng test qua component render nếu một hàm thuần đủ khẳng định.

## Implementation notes
- Case: 0 chương trình áp / `promotionDiscount = 0` ⇒ `shouldShowPromotionRow` trả `false`
- Case: `promotionDiscount > 0` với 1 CTKM PERCENT ⇒ `true` + label kèm %
- Case: 2 chương trình cùng áp ⇒ `true` + label phẳng
- Chạy `pnpm dlx vitest run apps/pos-web/src/lib/page-libs/checkout/promotionPresentation.test.ts`

## Done when
- [x] Toàn bộ case trên xanh (đã có sẵn phần lớn từ T-07-02; thêm 3 case gộp
      `shouldShowPromotionRow` + `buildPromotionRowLabel` cùng đọc chung 1
      `EvaluateCartResponse`, đúng cách `PaymentSummaryBlock.tsx` dùng — 49/49 `checkout/`)
- [x] `tsc --noEmit` của `pos-web` sạch
