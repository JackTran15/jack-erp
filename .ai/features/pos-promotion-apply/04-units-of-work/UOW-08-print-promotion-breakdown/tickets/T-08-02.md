---
id: T-08-02
uow: UOW-08
title: Helper gom khuyến mại theo hóa đơn/mặt hàng cho hóa đơn in
layer: web
type: feature
estimate: 2h
status: done
depends_on: []
blocks: [T-08-03, T-08-04]
verifies: [AC-29, AC-30, AC-32]
touches:
  - apps/pos-web/src/lib/page-libs/checkout/printing/promotionPrintBuckets.ts   # new
  - apps/pos-web/src/lib/page-libs/checkout/printing/promotionPrintBuckets.test.ts   # new
  - apps/pos-web/src/interfaces/invoice.interface.ts
assumptions: [A-17]
---

# T-08-02 — Helper gom nhóm khuyến mại cho in

## Context
Một hàm thuần dùng chung cho cả 2 payload builder (T-08-03 hóa đơn vừa thanh toán, T-08-04 in
lại) — cả hai nguồn dữ liệu (preview `AppliedProgram[]` và snapshot đã lưu từ T-08-01) đều chỉ
cần `type` + `discountAmount`.

## Implementation notes
```ts
export interface PromotionAmount { type: PromotionProgramType; discountAmount: number; }
export function groupPromotionsForPrint(programs: PromotionAmount[]): {
  itemDiscountTotal?: number;
  invoiceDiscountTotal?: number;
} { /* INVOICE_DISCOUNT → invoiceDiscountTotal; ITEM_DISCOUNT/TIERED_DISCOUNT/BUY_M_GET_N →
       itemDiscountTotal; GIFT_ITEM loại khỏi cả 2; tổng = 0 ⇒ field đó undefined, khớp đúng
       cách amountRow() trong renderInvoiceHtml.ts ẩn dòng khi amount == null */ }
```
- Thêm `appliedPromotions?: { type: PromotionProgramType; discountAmount: number }[]` vào
  `InvoiceRow` (`apps/pos-web/src/interfaces/invoice.interface.ts`), khớp field mới của T-08-01

## Done when
- [x] Test đủ 5 type: INVOICE_DISCOUNT, ITEM_DISCOUNT, TIERED_DISCOUNT, BUY_M_GET_N (đều cộng
      đúng bucket), GIFT_ITEM (bị loại, không cộng đâu cả)
- [x] Case toàn bộ = 0 ⇒ cả 2 field `undefined`, không phải `0`
- [x] `pnpm dlx vitest run apps/pos-web/src/lib/page-libs/checkout/printing/promotionPrintBuckets.test.ts`
      xanh (7/7; 56/56 toàn bộ thư mục `checkout/`)
- [x] `tsc --noEmit` của `pos-web` sạch
