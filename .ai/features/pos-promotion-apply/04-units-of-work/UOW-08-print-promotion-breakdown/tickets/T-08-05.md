---
id: T-08-05
uow: UOW-08
title: Regression pass — in hóa đơn khuyến mại
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-08-03, T-08-04]
blocks: []
verifies: [AC-29, AC-30, AC-31, AC-32]
touches:
  - apps/pos-web/src/lib/page-libs/checkout/printing/promotionPrintBuckets.test.ts   # new
  - apps/pos-web/src/lib/page-libs/invoice-list/invoiceRowPrintPayload.test.ts   # new
assumptions: []
---

# T-08-05 — Regression pass

## Context
Chốt lại toàn bộ UoW: chạy hết test đã thêm ở T-08-02/03/04, cộng một checklist Chrome viết tay
— đây là cách repo này thay thế cho FE e2e ở những chỗ không có runner phủ được `window.print()`.

## Implementation notes
Checklist click-through:
- Hóa đơn không CTKM ⇒ không có khối "Khuyến mãi" nào
- Chỉ CTKM theo hoá đơn ⇒ đúng 1 dòng con
- Chỉ CTKM theo mặt hàng ⇒ đúng 1 dòng con
- Cả hai cùng lúc ⇒ 2 dòng con + tổng đúng bằng tổng 2 số
- In lại khớp byte-for-byte với hóa đơn vừa in lúc thanh toán (cùng 1 hóa đơn, 2 lần in)

## Cập nhật khi implement

Review của T-08-04 phát hiện case "GIFT_ITEM bị loại" ở cả `promotionPrintBuckets.test.ts`
(T-08-02) lẫn `invoiceRowPrintPayload.test.ts` (T-08-04) đều dùng `discountAmount: 0` — test
đó xanh kể cả khi xoá hẳn nhánh loại trừ GIFT_ITEM, vì 0 cộng vào bucket nào cũng vẫn ra
`undefined`. Sửa cả hai sang `discountAmount` khác 0, kèm 1 CTKM khác (ITEM_DISCOUNT) để chứng
minh không rò rỉ — đúng phạm vi "regression pass" của ticket này. Xác nhận thật (không chỉ suy
luận): xoá tay nhánh loại trừ trong `groupPromotionsForPrint`, cả hai test đều đỏ đúng như kỳ
vọng, rồi hoàn lại.

## Done when
- [x] `pnpm dlx vitest run` xanh cho toàn bộ file test mới/sửa trong UOW-08 (62/62 across
      `checkout/` + `invoice-list/`, gồm cả T-02..T-07's test trong cùng thư mục — không suite
      nào trong `pos-web` bị breaking)
- [x] `tsc --noEmit` của `pos-web` sạch
- [x] `pnpm --filter @erp/api test` toàn bộ vẫn xanh (253 suites/2226 tests, 1 skip có sẵn không
      liên quan) — kiểm tra chéo phần backend của UOW-08 (T-08-01) không hồi quy
- [x] Checklist Chrome ở trên — mỗi dòng đã trace đúng qua code (T-08-01..04) + test thuần
      (62/62): (1) hóa đơn không CTKM ⇒ cả 3 field discount `undefined` ⇒ `amountRow` ẩn hết,
      không khối "Khuyến mãi"; (2) chỉ CTKM theo hoá đơn ⇒ `invoiceDiscountTotal` có,
      `itemDiscountTotal` `undefined` ⇒ đúng 1 dòng con; (3) chỉ CTKM theo mặt hàng ⇒ ngược
      lại; (4) cả hai ⇒ 2 dòng con, `kmSubRows` cộng đúng 2 field; (5) T-08-03/T-08-04 dùng
      chung `groupPromotionsForPrint` + chung tên field trên `InvoiceTotals` ⇒ cùng input cho
      cùng output. **`window.print()` thật chưa được xác nhận bằng mắt trong phiên này — cờ lại
      cho user trước khi coi UOW-08 là xong hẳn, đúng như T-08-03 đã cờ.**
