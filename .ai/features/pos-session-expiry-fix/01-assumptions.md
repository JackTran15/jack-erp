---
feature: pos-session-expiry-fix
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | "~30D" for the refresh token means exactly 30 days = 2,592,000 seconds, and "1D" for the access token means exactly 24h = 86,400 seconds. | high | no | A wrong exact number is a one-line constant/default change (`03-logical-design.md` ADR-01), not a structural rework | confirmed | User stated both values explicitly in the original request ("nâng access token lên 1D, riêng refreshToken khoảng 30D"); 24h/30d are the standard interpretation of a day/month-scale duration with no other unit in play |
| A-02 | Bumping the code's default TTL values alone is **not** sufficient for the change to take effect anywhere the vars are already set explicitly. Confirmed: this checkout's own `apps/api/.env` (gitignored, not part of any commit) already has `JWT_ACCESS_TTL=900` / `JWT_REFRESH_TTL=604800` set explicitly, mirroring `.env.example` — `ConfigService.get(key, default)` returns the explicit value over the default, so on this machine (and presumably staging/prod, which were likely seeded from the same `.env.example`) the fix would silently not take effect until each environment's actual `.env` is also updated. | high | yes | Without this, UOW-01's demo would appear to fail ("changed the default, token is still 15 min") with no obvious cause, and the fix would ship inert to any already-provisioned environment | confirmed | Confirmed directly: read `apps/api/.env` in this checkout, `JWT_ACCESS_TTL=900`/`JWT_REFRESH_TTL=604800` present. T-01-01's done-when includes updating this checkout's local `apps/api/.env` in addition to `.env.example`; the ticket's context notes that staging/prod need the equivalent manual env update as a deployment step outside this PR's diff, since those `.env` files aren't in version control |
| A-03 | The dual-HTTP-client refresh race (`http.ts` vs `api-axios.ts`, UOW-02) is real but was never reported by name — it's a finding from this investigation, not something the user described. Proceeding to fix it in this pass is based on the user's explicit answer to the AI-DLC discovery question, not an inference from the original request text. | high | no | If the user meant only the TTL bump + the `PosRequireAuth` bypass, UOW-02 (and UOW-03's dependency on it) would be unnecessary scope | confirmed | Confirmed by Akenzy via AI-DLC discovery Q&A ("Fix now" selected), 2026-08-24 |
| A-04 | No React component-rendering test tooling (`@testing-library/react`, jsdom vitest environment) exists in pos-web today, confirmed by an empty grep for both across every `package.json` in the workspace and zero `*.test.tsx` files anywhere in `apps/pos-web/src`. This feature's tests (UOW-03 in particular, which touches a `.tsx` component) should therefore test extracted pure logic rather than introduce new test infrastructure, matching the existing `api-axios.test.ts`/`dateTime.test.ts` style. | high | no | If wrong (tooling does exist somewhere unfound), the ticket would test at a coarser grain than necessary but nothing breaks; if right and ignored, the ticket would silently require adding new dependencies mid-flight | confirmed | Confirmed by direct search: `grep -rn "vitest\|testing-library\|jsdom" **/package.json` and `find apps/pos-web/src -iname "*.test.tsx"` both empty |
