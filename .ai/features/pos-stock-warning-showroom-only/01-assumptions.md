---
feature: pos-stock-warning-showroom-only
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Ngưỡng cảnh báo **và** con số `Tồn:` hiển thị cùng đổi sang tồn showroom — không tách hai cơ sở | high | yes | Cảnh báo đỏ bên cạnh dòng chữ `Tồn: 12` là tự mâu thuẫn; thu ngân sẽ coi cảnh báo là lỗi hệ thống và bỏ qua | confirmed | Akenzy chọn "Đổi luôn — hiện Tồn: 4" 2026-08-22 |
| A-02 | Dialog chọn biến thể (`ProductVariantSelectionModal`) cũng đổi sang tồn showroom | high | yes | Dialog ghi `Tồn: 12`, chọn xong vào giỏ thành `Tồn: 4` — cùng một mặt hàng, hai con số, trong cùng một thao tác | confirmed | Akenzy chọn "Đổi theo showroom" 2026-08-22 |
| A-03 | Mặt hàng chưa xếp kệ showroom (tồn showroom = 0, kho lưu trữ còn hàng) **cảnh báo mọi lần bán** — đó là hành vi đúng, không phải nhiễu | high | yes | Nếu tỉ lệ hàng chưa ra quầy cao thì quầy thấy cảnh báo liên tục và học cách bấm bỏ qua, làm hỏng giá trị của chính cảnh báo này | confirmed | Akenzy chọn "Chấp nhận — đúng nghiệp vụ" 2026-08-22, sau khi đã nêu rõ rủi ro nhờn cảnh báo |
| A-04 | Không được lọc catalog bằng `direction=showroom`: `aggregateStockRows` lọc **hàng** (row) trước khi gom, nên mặt hàng chỉ có `stock_balances` ở kho lưu trữ sẽ biến mất khỏi kết quả, không tìm được và không bán khống được nữa | high | yes | Mất hẳn khả năng bán mặt hàng chưa chuyển ra quầy — một cách vá làm hỏng nhiều hơn sửa | confirmed | Đọc code: `getCatalog` chạy `FROM stock_balances` nên dòng bị lọc là item bị mất; nhánh `searchCatalogByTerm` chỉ giữ lại item khi `locationId IS NULL`. Cách đúng là **thêm trường** tồn showroom, giữ nguyên tập kết quả |
| A-05 | "Showroom" trong feature này định nghĩa **giống hệt** đường trừ kho: `storages.is_main_storage = true` trong chi nhánh, đúng bộ lọc của `resolveBranchItemLocations(..., { showroomOnly: true })` | high | yes | Cảnh báo tính trên một tập kho, kho lại bị trừ ở tập khác — đúng bằng loại lỗi đang chữa, chỉ đổi mặt | confirmed | Akenzy chọn "`storages.is_main_storage` — giống đường trừ kho" 2026-08-22, sau khi đã nêu đánh đổi: `PosCatalogService` sẽ mang hai định nghĩa showroom (tham số `direction` vẫn dùng bảng `showrooms`). Chốt thành ADR-01 |
| A-06 | Tập storage có bản ghi `showrooms` **trùng** tập `storages.is_main_storage = true` của cùng chi nhánh. `BranchService.create` (:111-136) tạo cả hai cùng lúc trên một storage; chi nhánh cũ / seed thì chưa đối chiếu được | medium | no | Sau ADR-01 đây **không còn** ảnh hưởng tính đúng của cảnh báo (cảnh báo và trừ kho cùng đọc `is_main_storage`, khớp theo cấu tạo). Nhưng nếu lệch thì kho mà UI gọi là "Showroom MT46" không phải kho POS trừ hàng — một lỗi dữ liệu cần biết, và tham số `direction` của Chuyển kho nhanh sẽ nói khác cảnh báo bán hàng | pending | DB dev (`localhost:5433/erp_dev`) không chạy lúc lập kế hoạch. Truy vấn đối chiếu vẫn là một mục bắt buộc trong Demo script của UOW-01 — để **phát hiện lỗi dữ liệu**, không còn để chốt thiết kế |
| A-07 | `quantityOnHand` (tổng toàn chi nhánh) **giữ nguyên** ý nghĩa và giá trị; feature này chỉ *thêm* một trường bên cạnh | high | no | Chuyển kho nhanh (`FastStockTransferPage`) và mọi consumer khác của catalog đang đọc tổng chi nhánh sẽ đổi hành vi âm thầm | confirmed | Khoá bằng test: `keeps quantityOnHand as the branch-wide total` (cả hai service) và `leaves the direction filter on its own showrooms-table classification` — case sau dùng fixture cố ý cho hai cách phân loại bất đồng nhau nên đỏ ngay nếu ai repoint `direction` |
| A-08 | Chú thích ở `use-checkout-session-cart.ts:168-171` và `checkout-session.store.ts:352-354` ("chỉ SUM toàn chi nhánh là chuẩn") phải được **viết lại**, không để lại | high | no | Nhận định sai nằm ngay cạnh code đúng là cái bẫy cho lần sửa sau | confirmed | Đã viết lại cả hai ở T-01-03; grep `SUM toàn chi nhánh` nay chỉ còn khớp câu **phủ định** mới ("KHÔNG phải `quantityOnHand` (SUM toàn chi nhánh)") |
| A-09 | FE tuyệt đối không tự cộng/lọc `locations[]` để ra tồn showroom: breakdown per-location có thể chứa cặp +/− bù trừ, và FE không biết location nào thuộc showroom | high | no | Con số cảnh báo sai theo kiểu khó truy, mỗi mặt hàng một kiểu | confirmed | Phân loại nằm trọn ở BE; FE chỉ đọc `showroomQuantity` qua một hàm thuần `readShowroomOnHand`. Không có chỗ nào ở FE đụng `locations[]` để tính tồn |
| A-10 | `lookupByCode` (quét mã vạch/SKU) phải trả cùng trường tồn showroom như `getCatalog`; hiện query của nó **không** select `isShowroom` | high | no | Thêm hàng bằng quét mã cho `maxQty` khác với thêm hàng bằng tìm kiếm — cùng mặt hàng, hai ngưỡng cảnh báo | confirmed | Đã thêm cờ vào cả ba query ở T-01-02, và T-01-01 có case riêng `reports the same showroom-only basis as the search path (A-10)` |
| A-11 | Đường trừ kho có một nhánh thoái lui hiếm: `invoice.service.ts:194,393` dùng `itemLocationMap.get(itemId) ?? item.locationId`, tức khi chi nhánh không có main storage / không có location `Mặc định` thì `locationId` client gửi lên (`defaultLocationId` = vị trí nhiều hàng nhất, có thể là kho lưu trữ) mới được dùng | medium | no | Trong đúng nhánh đó cảnh báo tính trên showroom nhưng kho bị trừ ở kho lưu trữ. Là cấu hình chi nhánh hỏng sẵn, không phải hệ quả của feature này | pending | Không xử lý ở đây; ghi lại để không kết luận nhầm khi gặp |
| A-12 | Không đổi `defaultLocationId` (đang là vị trí nhiều hàng nhất) sang ưu tiên showroom | medium | no | Chỉ có tác dụng trong nhánh thoái lui A-11; đổi nó là mở rộng phạm vi sang đường trừ kho | confirmed | Không đụng. Case `keeps quantityOnHand as the branch-wide total` khẳng định `defaultLocationId === 'L-WH'` (vị trí nhiều hàng nhất, thuộc kho lưu trữ) — hành vi cũ giữ nguyên |
| A-13 | `apps/pos-web` chạy được test bằng `npx vitest run` dù `package.json` khai `"test": "echo test"` | high | no | Nếu sai thì phần FE chỉ verify được bằng click-through, không có test tự động khoá hành vi | confirmed | Đúng: 103/103 xanh ở T-01-04, và zustand store nạp thẳng được trong vitest (persist không có `localStorage` ở môi trường node nhưng không chặn) — đây là file test đầu tiên cho một store trong repo |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-R1 | Chỉ cần truyền `direction=showroom` ở lời gọi catalog của màn bán hàng là xong | `aggregateStockRows` lọc theo **dòng stock_balances**, nên mặt hàng không có dòng nào ở showroom bị loại khỏi kết quả — mất khỏi ô tìm kiếm, mất khỏi lưới, không bán khống được nữa | Phải **thêm trường** `showroomQuantity` cạnh `quantityOnHand`, giữ nguyên tập kết quả trả về (A-04) |
| A-R2 | FE tự cộng các phần tử `locations[]` thuộc showroom là đủ, không cần đụng BE | FE không có thông tin location nào thuộc showroom (`locations[]` chỉ có `locationId`, `name`, `quantity`), và breakdown có thể chứa cặp +/− bù trừ. Suy ra showroom từ chuỗi `name` là đoán mò | Phân loại phải nằm ở BE, nơi đã có `showrooms` / `storages` (A-09) |
| A-R3 | Cảnh báo hiện tại "bật muộn" là do lỗi làm tròn hoặc do snapshot cũ | Không phải: `use-checkout-session-cart.ts:172` gán thẳng `maxQty = product.quantityOnHand` và `quantityOnHand` là SUM mọi vị trí trong chi nhánh — bật muộn là do **cơ sở tính**, đúng bằng lượng hàng nằm trong kho lưu trữ | Không đi tìm bug đồng bộ; sửa cơ sở số liệu |
| A-R4 | Đây là bug FE thuần, sửa trong `pos-web` là đủ | Con số FE cần không tồn tại trong payload hiện tại. Không có BE thì không có gì để đọc | Feature bắt buộc có phần BE (DTO + service + test), FE chỉ đổi nguồn đọc |
