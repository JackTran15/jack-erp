---
id: T-02-06
uow: UOW-02
title: Regenerate OpenAPI sau khi thêm storages
layer: api
type: chore
estimate: 1h
status: done
depends_on: [T-02-02, T-01-07]
blocks: []
verifies: [AC-12]
touches:
  - packages/api-client/openapi.snapshot.json
  - packages/api-client/src/generated/schema.ts
assumptions: []
---

# T-02-06 — Regenerate api-client

## Context

Lần regenerate thứ hai, sau khi `storages` và `PosVariantStorageDto` vào DTO. Cùng quy
trình như T-01-07.

## Implementation notes

- API chạy ở `:4000`, kiểm đúng bản: `curl -s localhost:4000/docs-json | grep PosVariantStorageDto`
- `pnpm openapi:generate`, không sửa tay `schema.ts`

## Done when

- [x] `docs-json` có `PosVariantStorageDto` và trường `storages`
- [x] Diff `schema.ts` chỉ chứa phần bổ sung của UOW-02 — **không áp dụng nguyên văn**, cùng
      lý do ghi ở T-01-07: implementation của UOW-01 và UOW-02 đã done trước khi ticket
      regenerate nào chạy, nên một lượt `openapi:generate` phủ cả hai. Xem ghi chú ở T-01-07.
