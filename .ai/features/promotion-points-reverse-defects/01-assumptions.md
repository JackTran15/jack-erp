---
feature: promotion-points-reverse-defects
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | The reverse event should carry an explicit point count rather than money, so the consumer stops re-deriving points from `subtotalDelta` | high | yes | Payload contract, both producers, the consumer, 4 spec files | confirmed | Confirmed by Akenzy, 2026-08-19 — chose "Carry points in the payload" over the money-shaped workaround |
| A-02 | On the return path the correct rule is **cap the existing money-derived reverse at the original invoice's recorded `pointsEarned`**, not re-derive it. A blocked original caps to 0; a full return caps to exactly what was earned; a partial return stays below the cap and keeps today's prorated number. **Revised 2026-08-19: valid only once A-10 is repaired** | high | yes | UOW-03's whole shape; `checkout-return.service.spec.ts` pins `subtotalDelta` at 464.000 / 800 / 190 and those numbers must not move | confirmed | Confirmed by Akenzy, 2026-08-19 — the cap fixes the blocked case while leaving every pinned partial-return number untouched. Re-confirmed after G2 was reopened: keep the cap, and make the column it reads trustworthy via the UOW-05 backfill |
| A-03 | Sending `invoice.pointsEarned` from cancel is behaviour-preserving for accruing invoices, because both sides compute `floor(amountDue / POINT_EARN_VND_PER_POINT)` — verified at `compute-totals.step.ts:83` (v2), `checkout-invoice.service.ts` (v1) and the entity comment on `invoice.entity.ts:85` | high | no | If they can diverge, cancel would reverse a different number than today for normal invoices | confirmed | Verified in code during discovery, 2026-08-19; no human input needed |
| A-04 | When the reverse resolves to 0 points, keeping the consumer's existing delta-0 `point_history` NO-OP row is correct — it is today's convention and doubles as the dedup marker (`loyalty-points-reverse.consumer.ts:35-47` keys on `type = ADJUST AND delta <= 0`) | medium | no | A 0-delta row appears in the customer's point history for every blocked cancel; cosmetic, but visible to staff | confirmed | Confirmed by Akenzy, 2026-08-19 — keep the row; it is the dedup marker the consumer's replay guard keys on, and QA's complaint was the −80 row, not a 0 row |
| A-05 | The consumer must tolerate in-flight money-only events during deploy, so the new point field is optional and the money path stays as the fallback | medium | no | A redeploy that drains topics first would allow a required field and a simpler consumer | pending | — |
| A-06 | The impact assessment (UOW-04) is read-only and runs against the `erp_clone_prod` clone on localhost:5433, not against production | medium | no | If the affected rows are dev-test only, the remediation decision is trivially "none" | confirmed | Resolved by T-04-02, 2026-08-19 — the clone predates `accrue_points`, so it holds zero #15/#16 rows. It does hold real production data for two unrelated findings; there is one organization, so no dev/real split exists. See `08-impact-assessment.md` |
| A-10 | `invoices.points_earned` faithfully records what an invoice earned, so reading it is equivalent to asking the ledger | high | yes | ADR-02's cap, and anything else that trusts the column | **rejected** | Disproved by T-04-02, 2026-08-19 — see the rejected table below |
| A-11 | Backfilling `points_earned` from `point_history` reconstructs the true earn for the affected rows, because the ledger's `earn` entries are the record the async award consumer actually wrote | high | yes | UOW-05; if the ledger is also incomplete the backfill writes wrong numbers | confirmed | Verified against data, 2026-08-19 — for all 24 rows the backfill would touch, the ledger earn equals `floor(amount_due / 10000)` exactly, so two independent sources agree on every one. The ledger is NOT complete in general (3 invoices have the inverse gap, see A-12), but it is corroborated for precisely the rows being written |
| A-12 | Neither `points_earned` nor `point_history` is complete on its own: 24 invoices have the column empty and the ledger right, 3 have the column right and the ledger empty | high | no | Only the backfill's blast radius — it writes solely where `points_earned = 0 AND ledger earn > 0`, so the 3 inverse rows are never touched | confirmed | Verified against data, 2026-08-19 — `HD-202607-00038/00040/00042`, whose cards were never credited. Recorded as Finding C rather than fixed here |
| A-07 | No schema migration is needed: `points_earned`, `points_reversed` and `points_balance_after` already exist (`invoice.entity.ts:85-98`) and no new column is introduced | high | no | A migration ticket would have to be added to every UoW that writes points | confirmed | Verified in code during discovery, 2026-08-19 |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-08 | #15 is a v2-saga-only defect, so v1 `checkout-invoice.service.ts:285` needs the same one-word fix | v1 has no `pointsBlocked` concept at all, and `cardBalance != null` already implies `customerId`, so v1's `pointsEarned` and `invoice.pointsEarned` are identical there — the line is benign as written | No v1 change in UOW-01. The real v1 gap is that `accrue_points = false` is ignored end to end, which is out of scope per 00-intent and needs its own ticket |
| A-10 | `invoices.points_earned` records what the invoice earned, so `points_earned = 0` means "earned nothing" | For invoices issued before 2026-07-20 it frequently means "the column was never populated". 24 of 90 posted customer sales in `erp_clone_prod` read 0 while the ledger shows a real earn, 3.224 points in total — `INV-202607-00001` reads 0 against a ledger earn of 1.050. The column was added by `c9488fab` on 2026-07-16 and rows around that deploy were never filled in | ADR-02's cap would have refused to reverse 3.224 points across 24 still-returnable invoices — the mirror image of #16. G2 reopened; the cap is kept but now depends on UOW-05 backfilling the column first. Two new ACs (AC-13, AC-14) and a fifth UoW |
| A-09 | QA is testing the default POS build | `VITE_CHECKOUT_V2` defaults to off in code (`invoice.service.ts:96`, `vite-env.d.ts:11`), and #15 cites a v2-saga line while reporting a correct `points_earned = 0` — only v2 produces that. Akenzy confirmed 2026-08-19 that all checkout in fact runs v2, so the code default and the deployed reality disagree | The fix is verified against v2, which is the only path in use. The v1 follow-up changes shape: not "teach v1 about `pointsBlocked`" but "flip the default or delete v1", since the repo pins the flag nowhere and a build that misses it falls back to a path that ignores `accrue_points` |
