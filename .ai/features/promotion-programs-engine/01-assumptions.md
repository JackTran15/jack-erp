---
feature: promotion-programs-engine
blocking_open: 0
---

# Assumption register

Mỗi dòng là một chỗ trống mà kế hoạch phải lấp. Dòng `confirmed` mang nguồn chốt; dòng
`pending` chỉ được phép khi sai cũng không buộc làm lại (`Blocking = no`).

Nguồn chốt chính: `docs/26-promotion-design.md` §7 (bảng truy vết FR/BR) và §10 (lệch pha
FE↔BE), viết ở TKT-KM-01 và đã duyệt cùng epic 22/07/2026.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Khi nhiều CTKM cùng khớp một tài nguyên, chọn **first-match-wins theo `priority ASC, createdAt ASC`**, không cộng dồn, không tự chọn "có lợi nhất cho khách" | high | yes | Toàn bộ `PromotionResolver` + `CartState`; đổi luật = viết lại engine và mọi AC tính tiền | confirmed | BR-001, chốt ở epic 22/07/2026 và `docs/26-promotion-design.md` §7; ADR-03 |
| A-02 | Tài nguyên tranh chấp gồm **3 loại**: mỗi dòng hàng, slot giảm giá cấp hóa đơn, slot quà tặng | high | yes | `CartState` 3 nhóm; nếu quà và giảm dòng dùng chung slot thì thứ tự pha đổi | confirmed | BR-001 diễn giải ở `docs/26-promotion-design.md` §6; hiện thực `domain/engine/cart-state.ts` |
| A-03 | Giảm cấp dòng chạy **trước** giảm cấp hóa đơn; `NON_PROMO_ONLY` = chỉ các dòng chưa bị chiếm | high | yes | Thứ tự 3 pha của resolver; số tiền giảm cuối cùng | confirmed | BR-002, `docs/26-promotion-design.md` §7 — đúng ngữ nghĩa MISA |
| A-04 | `end_date` được phép null (vô thời hạn); FE cảnh báo khi lưu nhưng **không chặn** | high | yes | `DateWindow`, invariant BR-004, migration nullable | confirmed | BR-003, `docs/26-promotion-design.md` §7 |
| A-05 | CTKM thuộc **tổ chức**; phạm vi chi nhánh nằm ở bảng nối `promotion_branches`, rỗng = toàn chuỗi | high | yes | Schema, `findActive`, search branch scope; nhầm sang `promotion_programs.branch_id` (chi nhánh người tạo) là lỗi im lặng | confirmed | BR-005, khớp `StoreScopePromotionSection` của FE; `docs/26-promotion-design.md` §7 |
| A-06 | Bậc thang chồng lấn hoặc `from >= to` → **chặn khi lưu (400)**, không chỉ cảnh báo | high | yes | Invariant BR-004 trong `PromotionProgram.create()` | confirmed | FR-032, `docs/26-promotion-design.md` §7 |
| A-07 | `end_time < start_time` là **ca qua đêm** và phải hỗ trợ, không phải lỗi nhập | high | yes | `TimeWindow.contains()` dùng OR thay vì AND | confirmed | FR-022, `docs/26-promotion-design.md` §7 |
| A-08 | Cấp số nhân quà = `floor(tổng tiền / min_amount)`, **không có trần** số lượng quà | medium | yes | `GiftItemStrategy`; thiếu trần có thể sinh số quà lớn bất thường trên hóa đơn to | confirmed | FR-041, `docs/26-promotion-design.md` §7 — chấp nhận không thêm cột trần |
| A-09 | "Rẻ nhất" của `BUY_M_GET_N` + `CHEAPEST` tính theo `unitPrice` **trong giỏ** (đã trừ giảm giá tay), không phải giá niêm yết | medium | yes | `BuyMGetNStrategy`; sai thì số tiền miễn phí lệch mỗi khi thu ngân sửa giá | confirmed | FR-034, `docs/26-promotion-design.md` §7 |
| A-10 | Làm tròn về **đồng** bằng `Math.round`, một helper `roundVnd` duy nhất dùng chung | high | yes | Mọi số tiền; FE dùng công thức khác sẽ hiện số lệch với BE | confirmed | Quy tắc "Làm tròn" của epic; `domain/model/value-objects/money.ts` |
| A-11 | `type` **immutable** sau khi tạo; đổi hình thức = nhân bản thành CTKM mới | high | yes | `UpdatePromotionHandler`, UI disable control ở chế độ Sửa | confirmed | FR-006, `docs/26-promotion-design.md` §7 |
| A-12 | `promotion_lines.target_id` polymorphic trên 3 bảng nên **không đặt FK**; target không resolve được thì engine bỏ qua im lặng | high | yes | Schema + engine; đặt FK sẽ chặn xóa item và làm vỡ dữ liệu cũ | confirmed | ERD của epic; ADR-05 |
| A-13 | Trạng thái CTKM chỉ có **2** giá trị `TRACKING`/`STOPPED`; `ENDED` của FE là badge tính ở client (`status=TRACKING` và `end_date < hôm nay`), không phải cột lưu | high | yes | pg enum, radio FR-010, filter trạng thái, badge `ProgramsTable` | confirmed | `docs/26-promotion-design.md` §10.1 — quyết định đã ghi, **code FE chưa theo** (`programs.constants.ts:45-49` còn 3 giá trị); áp ở T-04-03 |
| A-14 | Sinh nhật có **4** chế độ, gồm `RANGE` (trước/sau N ngày) với quấn vòng năm | high | yes | pg enum + 2 cột `birthday_before_days`/`birthday_after_days`; `CustomerScope.matches()` | confirmed | `docs/26-promotion-design.md` §10.2 — phát hiện khi đối chiếu `GeneralInfoPromotionSection.tsx:77-120` đã dựng sẵn UI |
| A-15 | 3 cột tổng hợp của voucher là **dữ liệu suy ra** tại query time, không phải field nhập, không lưu cột riêng | high | yes | `SearchVouchersV2Handler`; form voucher không có ô nhập tương ứng | confirmed | `docs/26-promotion-design.md` §7 (dòng `[?] REQ:272`); khớp khảo sát MISA mục 5.2 |
| A-16 | 1 dòng `vouchers` = 1 voucher = **1 lần dùng**; mã nhập tay, không sinh hàng loạt | medium | yes | `totalQuantity` luôn = 1; phát hành theo lô sau này sẽ cần cột lô | confirmed | FR-050/051, `docs/26-promotion-design.md` §7 |
| A-17 | Không cần khóa sửa CTKM "đã phát sinh giao dịch" trong epic này | medium | yes | `UpdatePromotionHandler` không có guard; `assertNotReferenced()` chỉ đặt sẵn chỗ | confirmed | FR-007, `docs/26-promotion-design.md` §7 — `invoice_promotions` chưa liên kết `promotion_programs` nên khái niệm chưa tồn tại; chốt ở epic POS |
| A-18 | Dùng `CommandBus` cho write dù skill `cqrs-search-endpoint` ghi "CQRS commands are not used in this repo" | medium | yes | Cấu trúc tầng application; đảo lại = gộp 5 handler thành một service phình to | confirmed | Sai lệch chủ ý #1 của epic; ADR-02 |
| A-19 | `modules/promotion/` là module đầu tiên phân lớp clean architecture; module khác **không** phải theo | medium | yes | Vị trí file, quy tắc import; hiểu nhầm sẽ kéo theo refactor toàn repo | confirmed | Sai lệch chủ ý #2 của epic; ADR-01; đã ghi vào `.ai/architecture.md` |
| A-20 | Tồn kho quà tặng ngoài phạm vi — engine chỉ trả `gifts[]` đề xuất, chưa trừ kho, chưa ghi giá vốn | high | no | Nếu nghiệp vụ đòi trừ kho ngay thì cần thêm một pha ghi sổ, nhưng nằm ở epic POS chứ không sửa engine | pending | BR-006 — hoãn có chủ ý sang epic POS checkout; theo dõi ở đó |
| A-21 | Không cần cache cho `evaluate`: vài chục CTKM/org thì ~9 truy vấn có index là đủ nhanh | medium | no | Nếu POS đo thấy chậm thì thêm cache Redis keyed `org:branch:date` + invalidate ở command — thêm được sau, không phải viết lại | pending | Chưa có số đo; chốt sau khi epic POS benchmark |
| A-22 | UI bậc thang chỉ hỗ trợ `tier_basis = QUANTITY`; `ITEM_VALUE`/`INVOICE_VALUE` có ở schema + engine nhưng **chưa có UI** | high | no | Người dùng không tạo được 2 chế độ kia từ backoffice (vẫn tạo được qua API). Bổ sung UI là ticket FE độc lập | pending | `docs/26-promotion-design.md` §10.4 — giới hạn phạm vi được chấp nhận, không thu hẹp schema/engine theo FE |
| A-23 | `tierDiscountUnit`/`goodsDiscountMethod` là **một** giá trị form-wide, trong khi API cho phép `discountMode` khác nhau theo từng tier | high | no | CTKM tạo qua Swagger với tier trộn `PERCENT`/`AMOUNT`, mở form Sửa rồi lưu sẽ bị ghi đè về một mode. Là giới hạn UI, không phải bug mapper | pending | `docs/26-promotion-design.md` §10.6 — ghi nhận để UOW-04 không coi là round-trip hoàn hảo |
| A-24 | Lưới an toàn của FE là type-check + unit test mapper (`pnpm dlx vitest run`) + click-through tay | high | no | Lỗi FE chỉ lộ khi click tay; giảm rủi ro bằng cách dồn logic dễ mất dữ liệu vào mapper đã có test | pending | Quan sát trực tiếp: `"test": "echo test"` trong `apps/backoffice-web/package.json`. Gắn vitest thật không thuộc phạm vi epic này |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-25 | Toàn bộ quan hệ con của `findActive` gộp được vào **1** câu SQL | Con trải trên 6 bảng; gộp một câu tạo cross-join `lines × tiers` theo từng group | Bất biến đổi thành "hằng số theo số bảng, không phụ thuộc số program": `IN (programIds)` mỗi bảng con, chạy song song `Promise.all` → 6 câu. Ghi ở amdt KM-06; kiểm ở AC-22 |
| A-26 | `catalog.loadItems()` chỉ cần nạp item **có trong giỏ** | `GIFT_ITEM` và `BUY_M_GET_N` (`SPECIFIC`) trỏ tới item **không nằm trong giỏ**; thiếu chúng thì quà biến mất im lặng | `EvaluateCartHandler` phải `findActive` **trước** rồi gộp `targetItemIds` vào danh sách nạp; `UNKNOWN_ITEM` chỉ kiểm dòng giỏ, không kiểm target quà. Ghi ở amdt KM-05 |
| A-27 | `BUY_M_GET_N` + `CHEAPEST` cũng cần lưới `REWARD` như `SPECIFIC` | `CHEAPEST` tặng chính SP rẻ nhất trong lưới `CONDITION`, không có lưới `REWARD` riêng | Invariant "lưới `REWARD` rỗng → lỗi" phải trừ thêm `BUY_M_GET_N` + `CHEAPEST`. Ghi ở amdt KM-04 |
| A-28 | 4 lưới chọn hàng của form đã có chỗ chứa id thật để gửi làm `targetId` | `TierProduct`/`GiftProduct`/`BuyGetRow`/`GoodsDiscountRow` chỉ có `code`/`sku` text tự do; `ProgramFormState` còn thiếu hẳn `priority`, `buyQuantity`, `giftQuantity` | Thêm `targetId`/`itemId` vào 4 type và 3 field số vào `ProgramFormState` ngay ở KM-12 — không có thì round-trip 5 hình thức không thể đúng. Ghi ở amdt KM-12 và `docs/26-promotion-design.md` §10.5 |
| A-31 | Thêm cột `status` cho voucher (T-06-01) là đủ để màn hình phản ánh đúng trạng thái | Voucher có **hai** khái niệm "còn hiệu lực" chạy song song: `is_active` (legacy — `create`=true, `deactivate`=false, được `validate()`/`markUsed()` đọc để chặn ở quầy) và `status` (mới — bộ lọc, badge, toàn bộ UI đọc). `VoucherService.deactivate` chỉ lật `isActive`, **không** chạm `status`. Hệ quả đo được 2026-08-04: bấm "Ngừng theo dõi" → toast thành công, voucher hết dùng được ở POS, nhưng danh sách vẫn hiện "Đang theo dõi" và vẫn nằm trong bộ lọc `TRACKING` | Cùng loại A-29/A-30: T-06-01 thêm cột đúng spec, không ai nói `deactivate` phải làm gì với nó. Sinh ticket **T-06-05** cho `deactivate` set **cả hai**, giữ cổng chặn legacy và làm trạng thái hiển thị nói thật. Việc hợp nhất hai cờ thành một là nợ của epic POS, không giải trong epic này |
| A-30 | 10 cột hiển thị của FR-050 là đủ cho dòng danh sách voucher | Thiếu **`id`**. `VoucherSummaryRow` trả đúng 10 cột FR-050 liệt kê, nhưng thanh công cụ cần id để gọi `PUT /v2/vouchers/:id`, `/duplicate`, `DELETE` — không có id thì Sửa/Nhân bản/Xóa không bấm được. Phát hiện 2026-08-04 khi nối `VouchersPage` | Cùng loại với A-29: T-06-01 làm đúng spec, spec quên định danh dòng. Sinh ticket **T-06-04** thêm `id` vào `VoucherSummaryRow` (API + FE). Lưu ý mô hình gộp theo `code`: hôm nay 1 dòng = 1 voucher (A-16) nên `id` của nhóm là không nhập nhằng; nếu sau này phát hành theo lô thì phải chốt lại "sửa/xóa một nhóm code" nghĩa là gì |
| A-29 | Bộ lọc kỳ (FR-002) map thẳng được vào `DateRangeFilterDto` trên `start_date` | `FilterBuilder.applyDateRange` sinh `col >= :from`, mà `NULL >= x` là `NULL` → **loại sạch CTKM vô thời hạn**. BR-003/A-04 cho phép `start_date`/`end_date` null, nên CTKM "vô thời hạn" là ca hợp lệ chứ không phải dữ liệu bẩn. Phát hiện 2026-08-03 khi nối `ProgramsPage` vào API thật: `KM000001` (`start_date IS NULL`, `TRACKING`) không hiện với bộ lọc mặc định `Năm nay` | Cả hai ticket đều làm đúng spec của mình — spec thiếu, không phải code sai. Sinh ticket mới **T-03-05** lọc kỳ null-tolerant ngay trong `SearchPromotionsV2Handler`, **không** đụng `FilterBuilder` dùng chung (cột date không-null ở module khác vẫn cần ngữ nghĩa hiện tại) |
| A-33 | 9 giá trị `SkippedProgramReason` đều tới được client qua `POST /v2/promotions/evaluate` | Chỉ **6** tới được. `findActive` lọc `status = TRACKING`, `start_date`/`end_date` và phạm vi chi nhánh **ngay trong SQL**, nên `STOPPED`, `DATE_WINDOW`, `BRANCH_SCOPE` không bao giờ thành ứng viên và ba reason đó chỉ chạm được từ unit test của domain. Phát hiện 2026-08-04 khi AC-03 chạy qua HTTP thật trả `skippedPrograms: []` | AC-03 viết sai chứ không phải code sai: `skippedPrograms` là danh sách **suýt đạt** cho thu ngân ("thêm 50k nữa được quà"), còn CTKM chủ cửa hàng đã tắt thì không phải suýt đạt — trả về mọi CTKM đã tắt/hết hạn của tổ chức trên mỗi lần bấm phím là chi phí tăng theo lịch sử khuyến mại mà không có user story nào đỡ. Sửa AC-03 thành hợp đồng thật; giữ kiểm tra `STOPPED` trong `eligibility.ts` làm lớp chặn thứ hai cho caller đưa program từ nguồn khác |
| A-32 | Thứ trong tuần và khung giờ của CTKM so theo giờ **nghiệp vụ** | So theo giờ **tiến trình**: `isoDayOfWeek` dùng `Date#getDay()`, `TimeWindow.timeOfDayFromDate` dùng `Date#getHours()`. Đúng tình cờ khi server chạy giờ Việt Nam, **sai** khi container chạy UTC — CTKM "18:00–21:00" sẽ nổ lúc 01:00–04:00 giờ VN. Phát hiện 2026-08-04: ca qua đêm 22:00–02:00 với `at` cố định `01:00Z` bị bỏ qua vì máy chạy UTC+7 đọc ra 08:00 | Không sửa trong epic này: sửa đúng cần chốt nguồn múi giờ (hằng số `Asia/Ho_Chi_Minh` hay cột cấu hình theo tổ chức) và ảnh hưởng mọi so sánh ngày/giờ của engine, vượt phạm vi UOW-07. E2E tạm dựng `at` từ thành phần giờ địa phương để không phụ thuộc nơi chạy. Trước khi triển khai lên hạ tầng không chạy giờ VN thì **phải** giải quyết |
| A-34 | Đổi chi nhánh đang làm việc = gửi header `X-Branch-Id` khác, như `CLAUDE.md` mô tả | Header **không** đổi được `actor.branchId`. `AuthService` luôn nhét `branchId = branchIds[0]` vào JWT, còn `actor-context.decorator.ts` giải theo thứ tự `jwt > header > branchIds[0]` — nhánh `jwt` luôn có giá trị nên nhánh `header` là code chết với mọi token phát qua `/auth/login`. Cơ chế thật là `POST /auth/switch-branch`: kiểm chi nhánh có được gán không, thu hồi session cũ, phát token mới mang `branchId` mới. Phát hiện 2026-08-04 khi ca branch-scope thứ ba của AC-21 không thấy CTKM ghim chi nhánh dù đã gửi đúng header | Không phải bug của epic này và không sửa ở đây — nhưng mọi test/tính năng cần "nhìn từ chi nhánh khác" phải đi qua `switch-branch`, không phải header. `X-Branch-Id` chỉ còn tác dụng với token không mang claim `branchId`. Câu mô tả trong `CLAUDE.md` ("The active branch is passed per-request as `X-Branch-Id` header") gây hiểu nhầm — đáng sửa ở lần đụng `CLAUDE.md` tới |
