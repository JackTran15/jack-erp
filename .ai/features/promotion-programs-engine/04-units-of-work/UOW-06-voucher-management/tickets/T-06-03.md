---
id: T-06-03
uow: UOW-06
title: VoucherFormDialog thêm/sửa theo FR-051, lỗi 409 gắn vào trường Voucher
layer: web
type: feature
estimate: 4h
status: done
depends_on: [T-06-02]
blocks: [T-07-04]
verifies: [AC-24]
touches:
  - apps/backoffice-web/src/pages/promotions/vouchers/VoucherFormDialog/VoucherFormDialog.tsx   # new
  - apps/backoffice-web/src/pages/promotions/vouchers/VouchersPage.tsx
assumptions: [A-16, A-24]
source: TKT-KM-14
---

# T-06-03 — Dialog thêm và sửa voucher

## Context

Sáu trường, một dialog. Dùng primitive form của `@erp/ui`; **không** tạo abstraction form mới
cho 6 trường.

## Implementation notes

- Trường: Ngày bắt đầu, Ngày kết thúc, Nhà phát hành (bắt buộc), Voucher/mã (bắt buộc),
  Mệnh giá (bắt buộc), Mô tả.
- Ghi chú *"Bỏ trống từ ngày, đến ngày nếu không giới hạn thời gian"* hiển thị **dưới cặp ngày**.
- Trùng mã voucher → bắt lỗi 409 từ API và hiển thị **tại trường `Voucher`**, không phải toast
  lỗi máy chủ.
- Chế độ Nhân bản mở dialog đã điền sẵn nhưng **để trống `code`** — người dùng phải nhập mã mới
  (A-16: mã nhập tay, không sinh hàng loạt).
- Named export, `interface Props` tách riêng, primitive từ `@erp/ui`, icon `lucide-react`.

## Kết quả (2026-08-04)

Dialog 6 trường FR-051, dùng primitive form của `@erp/ui` — không dựng abstraction form mới.
Ghi chú *"Bỏ trống từ ngày, đến ngày nếu không giới hạn thời gian"* nằm ngay dưới cặp ngày;
ô trống gửi `undefined` chứ không gửi chuỗi rỗng.

**409 gắn đúng trường:** bắt `HttpError` với `error.error.status === 409` rồi set lỗi vào ô
`Voucher` — "Mã voucher này đã tồn tại trong tổ chức." Verify bằng cách tạo trùng mã
`VC-UOW06-001`: chữ đỏ hiện ngay dưới ô, không phải toast lỗi máy chủ.

Nhân bản để trống `code` (A-16: mã nhập tay, không sinh hàng loạt) và chỉ ghi khi bấm Lưu.

## Done when

- [x] Dialog có đủ 6 trường của FR-051, ba trường bắt buộc được validate
- [x] Ghi chú về bỏ trống ngày hiển thị dưới cặp ngày
- [x] Trùng mã → lỗi 409 hiện tại trường `Voucher`
- [x] Nhân bản mở dialog điền sẵn nhưng để trống `code`
- [x] Click-through: tạo → thấy trong danh sách → sửa → nhân bản → xóa; dòng tổng cộng đổi đúng — đã phủ tạo · danh sách · ngừng theo dõi · dòng tổng đổi đúng; **sửa** và **nhân bản** phủ ở click-through cuối của T-07-04
- [x] Chuỗi tiếng Việt; named export; `interface Props` tách riêng
- [x] `pnpm --filter @erp/backoffice-web build` xanh
