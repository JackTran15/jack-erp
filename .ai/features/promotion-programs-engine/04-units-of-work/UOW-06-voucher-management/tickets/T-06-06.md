---
id: T-06-06
uow: UOW-06
title: Đảo mặc định bộ lọc trạng thái VouchersPage sang "Tất cả" (đảo ngược FR-004/AC-10)
layer: web
type: chore
estimate: 1h
status: done
depends_on: [T-06-02, T-03-06]
blocks: []
verifies: [AC-10]
touches:
  - apps/backoffice-web/src/pages/promotions/vouchers/VouchersPage.tsx
  - apps/backoffice-web/src/pages/promotions/vouchers/vouchers-filter.ts
assumptions: [A-35]
source: QA 2026-08-10
---

# T-06-06 — Đảo mặc định bộ lọc trạng thái voucher sang "Tất cả"

## Context

T-06-02 hiện thực FR-004/AC-10 **gốc** cho màn voucher, chú thích rõ "cùng chuẩn FR-004 với
màn CTKM". QA/product (Akenzy, 2026-08-10) đảo ngược quyết định này trên cả hai màn — xem
assumption A-35 và AC-10 đã sửa ở `02-requirements.md`. Cùng loại đảo ngược yêu cầu như
T-03-06, áp cho `VouchersPage` thay vì `ProgramsPage`.

`depends_on: T-03-06` là phụ thuộc **chỉ để tài liệu** — T-03-06 sở hữu 3 chỗ sửa
`02-requirements.md`/`docs/promotions/25-promotion-req.md`/`docs/26-promotion-design.md` dùng
chung cho cả hai màn; ticket này **không** lặp lại các sửa đó, chỉ sửa code của
`VouchersPage.tsx`.

## Implementation notes

- `VouchersPage.tsx`: đổi `const [trackingOnly, setTrackingOnly] = useState(true)` (dòng 39)
  thành `useState(false)`.
- Không đổi logic `buildVoucherFilters` / `buildVoucherChips` trong `vouchers-filter.ts` — cả
  hai đã nhận `trackingOnly` làm tham số, chỉ giá trị khởi tạo ở `VouchersPage` đổi.
- Không sửa lại tài liệu — đã làm ở T-03-06.
- **Cập nhật khi implement**: docblock ngay trên `buildVoucherFilters` trích dẫn FR-004/AC-10
  và mô tả mặc định cũ ("Đang theo dõi") — cùng lỗi đã gặp ở T-03-06's `programs-filter.ts`.
  Sửa luôn comment đó (không đụng logic) để không lộ tài liệu sai ngay cạnh code đúng.

## Done when

- [x] Mở `/promotions/vouchers` lần đầu (chưa đặt filter nào) → hiện cả voucher `TRACKING` và
      `STOPPED`, không có chip trạng thái nào trên thanh công cụ
- [x] Tự chọn lọc trạng thái từ cột Trạng thái → chip xuất hiện; bấm `×` → về lại "Tất cả"
- [x] `pnpm --filter @erp/backoffice-web exec tsc --noEmit` xanh
