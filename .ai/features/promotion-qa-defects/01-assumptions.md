# Assumption register — promotion-qa-defects

Mọi ô "Resolution" phải ghi **ai** chốt và **khi nào**. Giả định chưa chốt mà `Blocking = yes`
sẽ chặn G1 — đó là điều kiện cố ý.

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | `invoice_checkout_promotions.line_discounts` phủ đủ toàn bộ `discount_amount` của từng CTKM, và `lineId` trong jsonb trỏ đúng `invoice_items.id` — tức backfill cột `promotion_discount` từ blob này là chính xác, không mất phần nào. | high | yes | Nếu sai, phân bổ theo dòng thiếu hụt và công thức hoàn tiền D2 trả về số nhỏ hơn thực tế → vẫn chi sai, tức UOW-01 phải cắt lại. | **resolved** | Truy vấn `erp_dev` 13/08/2026 (Claude, xác minh trước khi viết G1): 7/7 dòng có `discount_amount = Σ line_discounts` (INVOICE_DISCOUNT 1.638.000 = 7×234.000; GIFT_ITEM 0=0), số dòng lệch = **0**; 9/9 `lineId` khớp `invoice_items.id`. Backfill an toàn. T-01-01 vẫn chạy lại đúng 2 truy vấn này như bước tiền điều kiện. |
| A-02 | ~~Hoàn tiền không bao giờ vượt số khách **đã thực trả**: `refund ≤ share × O.totalPaid`.~~ **BÁC BỎ khi làm T-01-03.** Không kẹp theo `totalPaid`. | high | no | Hệ quả của việc bác: HĐ bán chịu **chưa** trả đồng nào mà trả hàng thì vẫn có thể chi ra tiền mặt. Đây là lỗi có sẵn từ trước, không phải do feature này sinh ra, và thu ngân có lựa chọn OFFSET cho ca đó. | **resolved — đảo chiều** | **Bằng chứng (Claude, 13/08/2026, khi implement T-01-03):** `invoice-debt.service.ts` **không bao giờ** ghi lại `invoices.total_paid` — tiền khách trả nợ về sau nằm ở `debt_payments`. Nên một HĐ bán chịu đã trả **hết** nợ vẫn đọc `totalPaid = 0`. Kẹp theo `totalPaid` sẽ hoàn cho khách đó **0đ** — một lỗi nặng hơn lỗi nó định chặn. Đã gỡ mệnh đề `min(...)`. 4 test `debt offset routing` đỏ vì đúng lý do này chính là thứ phát hiện ra vấn đề; **không** sửa test để lách. Muốn kẹp đúng thì phải cộng cả `debt_payments`, việc đó nằm ngoài phạm vi lỗi #1. |
| A-03 | `promoCodeCount` giữ nguyên 0 và giữ ghi chú hoãn; không nằm trong đợt sửa này. | high | no | Không có — chỉ là một ô báo cáo vẫn bằng 0 như hiện tại, không tệ đi. | **resolved** | Akenzy duyệt plan 13/08/2026, mục "Ngoài phạm vi". Chẩn đoán "lệch bảng" của QA đã được bác bằng code: `get-pos-daily-summary.handler.ts:132` là hằng số, không phải truy vấn. |
| A-04 | Không backfill lại `refunded_amount` / `cash_movements` / Phiếu chi đã POSTED của các hoá đơn đã sai. | high | no | Nếu cần chỉnh số đã post thì phải làm bằng phiếu điều chỉnh riêng, không sửa bản ghi — thêm việc, nhưng không đụng thiết kế feature này. | **resolved** | Akenzy duyệt plan 13/08/2026. Nhất quán với luật repo: giao dịch đã post là bất biến, sửa bằng bút toán đảo. Dữ liệu hiện tại là dev-test. |
| A-05 | Bỏ 3 bộ lọc `status` / khoảng ngày / branch khỏi SQL `findActive` (D4) không gây vấn đề hiệu năng: số CTKM mỗi tổ chức ở quy mô hàng chục, và bộ lọc đã chuyển xuống engine chạy trên RAM. | medium | no | Nếu một tổ chức tích luỹ hàng nghìn CTKM thì `POST /v2/promotions/evaluate` chậm dần — mọi lần tính tiền đều gọi. Giảm nhẹ: giữ biên ngày rộng trong SQL. | **resolved** | Chốt (Claude, 13/08/2026): T-05-01 vẫn nạp có biên — chỉ CTKM chưa xoá mềm của tổ chức, và thêm biên ngày rộng ±1 năm quanh `at`. CTKM ngừng theo dõi / ngoài khoảng ngày hẹp vẫn tới được tầng lý do, nhưng lịch sử xa không bị kéo về. Ticket phải ghi rõ biên này. |
| A-06 | Ca 500 INTERNAL_ERROR của lỗi #8 tái hiện được. Đường đã tìm thấy (`groups[0]` không guard khi `groups: []`) là đường **qua API**; form backoffice luôn gửi 1 group với `lines` rỗng nên **không** crash. | low | no | Nếu QA gặp một đường khác thì bản vá guard `groups[0]` không sửa được lỗi họ báo — CTKM hỏng vẫn làm chết quầy. | **resolved** | Chốt cách xử lý (Claude, 13/08/2026): `T-06-01` **bắt buộc tái hiện trước khi sửa** và ghi lại payload gây lỗi. Guard `groups[0]` ở cả 5 strategy vẫn làm bất kể kết quả (đó là lỗi thật, và `BUY_M_GET_N + CHEAPEST` dính y hệt). Nếu tái hiện ra nguyên nhân khác thì mở thêm ticket trong UOW-06, không âm thầm coi là đã xong. |
| A-07 | Sửa `TimeWindow.contains` thành nửa khoảng là **đổi hành vi có chủ ý**, và `time-window.spec.ts` hiện đang khoá hành vi cũ ("thiếu một đầu = cả ngày") nên phải sửa spec cùng lúc. | high | no | Không có — chỉ là sửa spec sẽ bị coi nhầm là "sửa test cho qua" nếu không ghi lý do. | **resolved** | Akenzy chốt 13/08/2026 qua ví dụ: "đặt 18:00, bán lúc 09:00 vẫn được giảm" là lỗi; ca qua đêm 22:00–02:00 đang đúng, giữ nguyên. Ticket T-05-02 phải dẫn dòng này khi sửa spec. |
| A-08 | Hai field `startTime` / `endTime` vẫn để optional, **không** thêm validate "both-or-neither". | medium | no | Nếu người dùng muốn bắt buộc nhập cả hai thì thêm một luật vào `validate()` — một ticket nhỏ, không đụng D5. | **resolved** | Chốt (Claude, 13/08/2026, đã nêu rõ với Akenzy): một khi nửa khoảng đã có nghĩa ("18:00 → hết ngày"), bắt buộc cả hai sẽ **mâu thuẫn** với chính bản sửa và cắt mất một cấu hình hợp lệ. Akenzy trả lời #7 bằng mô tả hành vi mong muốn, không chọn phương án validate. |
