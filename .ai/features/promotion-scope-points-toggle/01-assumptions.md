---
feature: promotion-scope-points-toggle
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | "Phạm vi áp dụng" is locked to `ALL_ITEMS` for the invoice-discount promotion variant — the radio picker is disabled/removed entirely, no longer user-editable | high | yes | `02-requirements.md` US-01, `ApplyScopePromotionSection.tsx` removal scope | confirmed | Confirmed by Akenzy via AI-DLC discovery Q&A, 2026-08-17 |
| A-02 | "Tích điểm cho khách hàng" is per-PROMOTION-PROGRAM (not global), default OFF (unchecked = do not accumulate points); if an invoice has multiple applied promotion programs and ANY has the checkbox unchecked, the ENTIRE invoice earns ZERO points (not partial/prorated) | high | yes | `02-requirements.md` US-02/US-03/US-05, checkout points-calc logic | confirmed | Confirmed by Akenzy via AI-DLC discovery Q&A, 2026-08-17 |
| A-03 | Existing saved invoice-discount programs currently stored with `invoiceScope = NON_PROMO_ONLY` are left as-is, not retroactively migrated to `ALL_ITEMS` — only new/edited programs are forced to `ALL_ITEMS` going forward | medium | no | Backward-compat scope only: if wrong, a follow-up backfill migration is needed for already-saved programs; does not change the shape of `02-requirements.md` | pending | — |
| A-04 | The "this invoice earns zero points" decision is persisted as a new flag written once at checkout time (single source of truth the async consumer reads), rather than independently re-derived by `MembershipCardService.awardPointsForInvoice` re-querying which promotion programs applied — not decided here; this is the central technical risk of the feature (migration shape + both the sync `compute-totals.step.ts` path and the async `loyalty-points.consumer.ts` → `membership-card.service.ts` path must agree, with real drift risk if the two paths independently reimplement the same rule). Not marked blocking at G1: nothing in `02-requirements.md`'s ACs depends on which mechanism is chosen, since the ACs are written at the observable-outcome level (`invoice.pointsEarned === 0`, sync/async agree). This MUST become an ADR at G2 before logical design can pass — G2 requires ≥1 ADR and this is the natural (and only obvious) candidate | low | no | Data model shape of the points-eligibility flag, both checkout-saga steps, both loyalty-consumer files — largest blast radius in this feature; deferred to a G2 ADR, not a G1 blocker | pending | — |
| A-05 | The "Tích điểm cho khách hàng" checkbox is scoped to the invoice-discount promotion variant only (same form that carries the scope lock), matching how `02-requirements.md` US-02 is written — not decided here. The human specified the checkbox in the context of "Khuyến mại" broadly, not explicitly scoped to invoice-discount only, so this is a genuine open question, not a safe default. If the checkbox should instead appear on all five promotion-type variants (`PromotionProductDiscount`, `PromotionTieredDiscount`, `PromotionBuyGet`, `PromotionGift` too), it also raises what the persisted flag defaults to for programs of those types created before this ships (backend column defaults to `false` per the "default OFF" rule, but those forms would have no UI to ever turn it on unless also updated) — worth flagging loudly since a wrong default could silently zero out points on every non-invoice-discount promotion type. Not marked blocking at G1: it does not invalidate US-01..US-06 as written, only whether they later need siblings for the other four variant forms | medium | no | FE: which of the 5 promotion-variant form components render the checkbox. Backend: whether the points-eligibility column needs an explicit backfill/default strategy for program types that currently have no UI path to set it. Escalate to an ADR at G2 alongside A-04 | pending | — |

## Rejected assumptions

None yet — no assumption in this register has been rejected.
