---
feature: quick-exchange-line-discount
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID   | Assumption                                                                                                                                                                                                                                      | Confidence | Blocking | Blast radius if wrong                                                                                                           | Status    | Resolution                                                             |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------- |
| A-01 | KM theo dòng phải chảy đúng ở **cả hai chiều** — dòng trả (IN) lẫn dòng mua thêm (OUT)                                                                                                                                                          | high       | yes      | `ReturnInvoiceLineDto` + `CreateReturnInvoiceService` nằm ngoài phạm vi nếu chỉ làm chiều OUT ⇒ UOW-01 cắt lại                  | confirmed | Akenzy chọn "Cả dòng trả và dòng mua thêm", 2026-08-26                 |
| A-02 | Khi `checkout-return` lỗi, phiếu nháp EXCHANGE được **giữ lại** và phải khôi phục đúng thành tab đổi trả (không xoá tự động, không ẩn khỏi danh sách)                                                                                           | high       | yes      | UOW-02 đổi hoàn toàn: xoá draft là 1 ticket FE, khôi phục đúng chiều là 3 ticket                                                | confirmed | Akenzy chọn "Giữ + khôi phục đúng thành tab đổi trả", 2026-08-26       |
| A-03 | Backend phải chặn cứng: `/invoices/:id/checkout` từ chối draft `type != SALE`                                                                                                                                                                   | high       | yes      | Không có UOW-03; mọi đường vòng (tab cũ trong localStorage, client khác) vẫn tạo được hoá đơn rác                               | confirmed | Akenzy chọn "Có — /checkout từ chối draft không phải SALE", 2026-08-26 |
| A-04 | Không sửa dữ liệu hoá đơn đã hỏng trong prod                                                                                                                                                                                                    | high       | yes      | Thêm 1 UoW script rà soát + báo cáo                                                                                             | confirmed | Akenzy chọn "Không đụng dữ liệu cũ", 2026-08-26                        |
| A-05 | Chỉ `pos-web` gọi `POST /invoices/exchanges` và `POST /invoices/returns` — không có client ngoài (mobile, tích hợp) phụ thuộc shape hiện tại                                                                                                    | medium     | no       | Thêm field `@IsOptional()` vẫn tương thích ngược, nên rủi ro chỉ là client cũ tiếp tục gửi thiếu KM ⇒ vẫn ra số gộp như hôm nay | confirmed | Akenzy xác nhận không có client ngoài, 2026-08-26. Trong repo: mọi lời gọi đều thuộc `apps/pos-web`, đường ra API duy nhất là `services/invoice.service.ts` |
| A-06 | Ở mode **regular** (có hoá đơn gốc), KM dòng trên dòng trả chỉ ảnh hưởng `returnSubtotal` (giá trị hiển thị + cổng kiểm tra), không ảnh hưởng số tiền hoàn — số hoàn vẫn prorate từ hoá đơn gốc qua `refundableUnitValues`/`computeReturnedNet` | high       | no       | Nếu sai, số hoàn của đơn đổi trả theo hoá đơn gốc bị đổi ngoài ý muốn ⇒ AC-05 (regression guard) bắt được trước khi phát hành   | confirmed | Akenzy đóng 2026-08-26 dựa trên AC-05 e2e: bán 685.000 −30%, trả dòng đó theo hoá đơn gốc → hoàn đúng 479.500, không phải 685.000 |
| A-07 | Thu ngân chấp nhận việc phiếu nháp đổi trả cũ **biến mất khỏi "HĐ lưu tạm"** sau khi tất toán lại từ tab khôi phục (FE xoá phiếu nguồn)                                                                                                         | medium     | no       | Nếu sai: phiếu nháp gốc nằm lại danh sách sau khi đã bán xong, thu ngân phải tự bấm ×                                           | confirmed | Akenzy xác nhận 2026-08-26 — cùng hành vi với luồng bán thường: phiếu nháp rời danh sách sau khi đã bán xong |

## Rejected assumptions

| ID   | What we assumed                                                 | What is actually true                                                                                                                                                                       | Consequence                                                                |
| ---- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| A-08 | `create-exchange-invoice.service.ts` bỏ KM vì DTO chưa có field | `CreateInvoiceItemDto` **đã có sẵn** `lineDiscountType/Value/Reason` (dùng cho luồng SALE); chỗ hỏng là `buildNewLineEntities` chỉ đọc `line.lineDiscount` và FE hardcode `lineDiscount: 0` | T-01-02 chỉ còn phải mở rộng `ReturnInvoiceLineDto`, không phải cả hai DTO |
