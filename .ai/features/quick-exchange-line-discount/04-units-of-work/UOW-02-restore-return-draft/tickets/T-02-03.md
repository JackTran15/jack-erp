---
id: T-02-03
uow: UOW-02
title: Vitest cho khôi phục phiếu nháp đổi/trả
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-02-02]
blocks: []
verifies: [AC-06, AC-07]
touches:
  - apps/pos-web/src/lib/page-libs/checkout/invoicePayloadMapper.test.ts
  - apps/pos-web/src/stores/common/checkout-session.store.test.ts
assumptions: []
---

# T-02-03 — Vitest cho khôi phục phiếu nháp đổi/trả

## Context

`apps/pos-web` chạy được `npx vitest run` dù `package.json` khai `"test": "echo test"` —
đã có sẵn các file `*.test.ts` cạnh mã nguồn (`checkoutReceiptFactory.test.ts`,
`invoiceRowPrintPayload.test.ts`), viết theo đúng khuôn đó.

## Implementation notes

- Fixture: một `InvoiceRow` type EXCHANGE với 1 dòng IN (460.000) + 1 dòng OUT
  (685.000, `lineDiscountType: percent`, `value: 30`, `reason: sale30`)
- Khẳng định trọn chuỗi: mapper → `openDraftInNewSession` → hai giỏ đúng chiều, KM khôi phục
- Case RETURN và case SALE làm regression guard
- Không dựng test cho dialog/JSX — chỉ mapper + store, nơi có logic thật

## Done when

- [x] `npx vitest run` trong `apps/pos-web` xanh, gồm cả các test cũ
- [x] AC-06, AC-07 có test khẳng định trực tiếp — xuyên suốt từ hàng API tới tab đang mở

## Kết quả

- `checkout-session.store.test.ts` (+4 test, nối vào file sẵn có): chạy trọn chuỗi
  `InvoiceRow → mapInvoiceRowToDraftInvoice → openDraftInNewSession → session`.
  Test mapper đơn lẻ không đủ: mapper đúng mà store bỏ qua `checkoutVariant` thì phiếu đổi
  vẫn mở lại thành đơn bán — đúng lỗi đã xảy ra
- Zustand store nạp được dưới vitest thuần (không cần jsdom) — đã probe trước khi viết
- `invoicePayloadMapper.test.ts`: 15/15; toàn bộ `npx vitest run`: 135 pass / 3 fail, cả 3 là
  `api-axios.test.ts` (401 refresh) đỏ sẵn từ trước

**AC-08 không có test tự động.** Nó nằm trong `useCheckoutActions` (React hook) và pos-web
không có RTL/jsdom — xem ghi chú ở T-02-02. AC-08 chỉ được kiểm bằng bước 4-5 của demo
script UOW-02.
