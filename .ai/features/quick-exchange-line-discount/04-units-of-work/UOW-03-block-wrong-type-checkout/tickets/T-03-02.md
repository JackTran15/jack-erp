---
id: T-03-02
uow: UOW-03
title: Test guard checkout sai loại chứng từ
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-03-01]
blocks: []
verifies: [AC-09, AC-10]
touches:
  - apps/api/src/modules/pos/checkout-saga/application/steps/preflight-steps.spec.ts
  - apps/api/src/modules/pos/services/checkout-invoice.service.spec.ts
assumptions: []
---

# T-03-02 — Test guard checkout sai loại chứng từ

## Context

Guard này là lưới an toàn cuối cùng của cả feature; nếu nó âm thầm hỏng thì không có
triệu chứng nào ở UI cho tới khi báo cáo lệch. Test phải bám cả hai luồng.

## Implementation notes

- Unit test cho `LoadDraftStep`: draft EXCHANGE → ném `INVOICE_NOT_CHECKOUTABLE`;
  draft SALE → đi tiếp; replay đã hoàn tất → vẫn qua được
- Unit test cho `CheckoutInvoiceService` (v1) với cùng ba case
- Viết cạnh các spec sẵn có của saga (`*.step.spec.ts`) theo đúng khuôn hiện hành

## Done when

- [x] `pnpm --filter @erp/api test` xanh
- [x] Có test khẳng định phiếu **không** bị đổi trạng thái khi guard chặn

## Kết quả

- 4 test mới: EXCHANGE và RETURN bị chặn ở cả `LoadDraftStep` (saga v2) lẫn
  `CheckoutInvoiceService` (v1)
- Test saga khẳng định phiếu **không** bị đụng tới: `itemRepo.find` chưa từng được gọi,
  `ctx.invoice` vẫn undefined. Test v1 khẳng định `isDraft`/`status` giữ nguyên
- `preflight-steps.spec.ts` + `checkout-invoice.service.spec.ts`: 96/96 xanh
- `pnpm --filter @erp/api test`: 3386 pass, vẫn chỉ 2 test đỏ sẵn có ở `auth.service.spec.ts`
