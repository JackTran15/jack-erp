---
feature: quick-exchange-single-invoice
blocking_open: 0         # count of blocking + pending; must be 0 to pass G1
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| A-01 | Đổi trả nhanh không kiểm `returned_quantity` (không có hoá đơn gốc để kiểm) là chấp nhận được — giữ nguyên hành vi hôm nay, `checkout-return.service.ts:350` đã `continue` khi dòng thiếu `originalInvoiceItemId` | high | no | Không có — hành vi không đổi | confirmed | Akenzy 2026-08-14: xác nhận qua câu hỏi "dữ liệu cũ / phạm vi", chốt không mở rộng kiểm tra |
| A-02 | Chuyển doanh thu hàng mua thêm từ chứng từ `SALE` sang `EXCHANGE` là chấp nhận được cho báo cáo: nhóm theo `direction` (`revenue-by-item`, `invoice-item-revenue`, `profit-by-item`, `gross-profit-by-invoice`, `business-results`) **không đổi số**; nhóm theo `type` (`pos-daily-report` đếm `saleInvoices`/`returnInvoices`/`exchangeInvoices`, `invoice-order-listing` một dòng một hoá đơn) **sẽ đổi** số đếm và số dòng | medium | yes | Sai ⇒ phải giữ hai chứng từ ⇒ huỷ UOW-01 và UOW-02 | confirmed | Loc Tran 2026-08-14: được trình bảng hệ quả cụ thể (nhóm theo `direction` không đổi số; `pos-daily-report` đổi số đếm theo loại; `invoice-order-listing` giảm số dòng và chuyển gross→net, footer không đổi) và chốt *"Xác nhận, chấp nhận đổi số đếm"* |
| A-03 | `apps/pos-web` không có test runner (`"test": "echo test"`, không vitest/jest trong package.json của app lẫn root) ⇒ file `*.test.ts` hiện có không chạy | high | no | Verification FE phải qua demo script chạy tay, không qua CI. Nếu sai (có runner ẩn) thì chỉ là được thêm mạng lưới an toàn | confirmed | Đọc `apps/pos-web/package.json` + grep `vitest\|jest` toàn repo, 2026-08-14 — không có |
| A-04 | Trên `main`, POS không áp khuyến mại/voucher lên hoá đơn: `apps/pos-web/src/services/` không có lời gọi promotion/voucher nào, `buildCreateInvoicePayload` không gửi voucher, `selectEffectivePointsRedeemed` ép 0 cho non-SALE ⇒ gộp chứng từ không làm mất khuyến mại nào | high | no | Sai ⇒ cần cổng promotion trên EXCHANGE, thêm ít nhất một UoW backend | confirmed | Grep `apps/pos-web/src/services/` cho `promotion\|voucher\|applyPromotion`, 2026-08-14 — không kết quả |
| A-05 | Kế hoạch cố ý bỏ qua checkout saga v2. Khi `feat/promotions` merge, bốn file sẽ conflict: `use-checkout-actions.ts`, `dtos/invoice.dto.ts`, `stores/common/checkout-session.store.ts`, `lib/page-libs/invoice-list/invoiceRowPrintPayload.ts` | high | no | Người merge `feat/promotions` phải gỡ conflict thủ công; không ảnh hưởng đúng/sai của epic này | confirmed | Akenzy 2026-08-14: *"hiện tại trên main không có v2, cứ checkout từ main, và bỏ qua v2"* |
| A-06 | Panel thanh toán không cần biết variant: `PaymentSection.tsx:103` quyết định theo dấu net (`isRefundFlow = settlementGrandTotal < 0`) và `PaymentSummaryBlock` chỉ hỏi `selectIsReturnExchangeInvoice` (đúng cho **cả hai** variant), không nhánh nào rẽ theo `QUICK_EXCHANGE` | high | no | Sai ⇒ UOW-02 phình từ 2 lên ~4 ticket | confirmed | Đọc `PaymentSection.tsx` + `PaymentSummaryBlock.tsx` + `checkout-session.store.ts:689-697`, 2026-08-14 — không có nhánh nào phân biệt hai variant trong panel |
| A-10 | "Phí đổi trả" (`returnFee`) là số **chỉ để hiển thị/quyết toán ở FE**: `buildCheckoutReturnPayload` không gửi nó lên BE, và chân SALE hôm nay cũng không tính nó ⇒ gộp chứng từ không làm đổi cách xử lý phí | medium | no | Sai ⇒ cần thêm field vào `CheckoutReturnDto` + hạch toán phí, một UoW riêng | pending | — |
| A-07 | `POST /invoices/exchanges` không có client nào ngoài pos-web (`createExchange` chỉ được gọi ở `use-query-invoice.ts`), nên nới `originalInvoiceId` thành optional không phá client nào | high | no | Không có — nới lỏng validation là backward-compatible kể cả khi có client khác | confirmed | Grep `invoices/exchanges` toàn repo, 2026-08-14 — chỉ `invoice.service.ts:createExchange` và `invoice.controller.ts:238` |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
| --- | --- | --- | --- |
| A-08 | Phải viết một service/endpoint mới cho "quick exchange" vì `CheckoutReturnService` chỉ hiểu đơn có hoá đơn gốc | `CheckoutReturnService` đã hoàn toàn null-safe với `originalInvoice`: bỏ qua `returned_quantity` khi thiếu `originalInvoiceItemId` (L350), `computeReverseBase` có nhánh fallback (L518-528), `computeRedeemedCreditBack` trả 0 (L537-554), OFFSET tự hạ cấp về CASH khi không tìm thấy công nợ (L170-191) | Epic co từ "một endpoint + service mới" xuống "một nhánh `if` trong `create-exchange-invoice.service.ts`". UOW backend giảm từ ~3 xuống 1. |
| A-09 | Gộp chứng từ đòi migration (thêm cột liên kết, hoặc `originalInvoiceId` đang `NOT NULL`) | `invoices.original_invoice_id` đã `nullable` với comment *"null for quick-return"*; `invoice_items.original_invoice_item_id` cũng nullable | Không có ticket migration nào trong epic. Bỏ hẳn một UoW dự kiến. |
