---
feature: report-row-cap-pushdown
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Lưới chỉ gửi **một** toán tử cho mỗi cột, nên `ColumnFilterDto[]` → `ReportColumnFilterDto` là phép chuyển không mất mát cho lưu lượng thật | high | no | Một request tự dựng gửi `{col, gt, lt}` sẽ bị adapter bỏ bớt một vế → lưới lọc thiếu mà không báo | confirmed | Đọc `invoice-report.api.ts:186-203`: `out.push({ col, [op]: value })` — mỗi cột đúng một khoá. Giảm rủi ro bằng thiết kế: adapter **ném 400** khi gặp nhiều toán tử thay vì im lặng bỏ (ADR-02) |
| A-02 | Đổi `equals` / `startsWith` / `endsWith` từ phân biệt hoa thường (JS) sang không phân biệt (SQL `LOWER`/`ILIKE`) là chấp nhận được | high | no | Người dùng lọc `equals "ABA"` sẽ thấy thêm dòng `aba` | confirmed | Đường GET cũ của **chính 7 báo cáo này** đã dùng `buildReportColumnFilter`, tức đã không phân biệt hoa thường từ trước; sau khi đẩy xuống thì hai đường hết mâu thuẫn với nhau |
| A-03 | Ô số NULL không tồn tại ở các cột đẩy xuống SQL, nên khác biệt "JS coi NULL là 0 / SQL loại dòng NULL" không phát sinh | high | no | Lọc `openingQty = 0` mất dòng mà đường JS vẫn trả | confirmed | Đúng cho `StockPeriodService`: CTE `combined` bọc `COALESCE(..., 0)` quanh **mọi** cột số (opening/in/out + 7 cột phân rã), nên không có ô NULL nào. KHÔNG đúng ở hai chỗ khác, đã ghi vào ticket tương ứng: `inSalePrice`/`outSalePrice` của document-detail null ở nguồn không mang giá bán (T-10-02), và ô pivot của mặt hàng vắng mặt ở một chi nhánh (T-08-03) |
| A-04 | Join `item_providers` (nhà cung cấp chính) vào SQL của engine không làm nở số dòng | high | no | `total` và footer của "Tổng hợp NXT tồn kho" sai theo hệ số nhân | confirmed | `UQ_item_providers_primary ON item_providers(item_id) WHERE is_primary = true` (migration `1779700000000-ItemManagementPhase1.ts:54`) — tối đa một nhà cung cấp chính mỗi mặt hàng |
| A-05 | Sáu cột `inWh` / `inOther` / `outPurchaseReturn` / `outWh` / `outVoid` / `outOther` của "Chi tiết số lượng tồn" là chỗ trống chưa có dữ liệu, không cần biểu thức SQL | high | no | Lọc trên chúng trả 400 thay vì trả rỗng như hôm nay | confirmed | `stock-quantity-detail.report.ts:156-166` gán thẳng `null` cho cả sáu |
| A-06 | Trần `MAX_REPORT_ROWS` gỡ khỏi `buildData` sẽ để đường `/export` và `/print-payload` không còn ai canh | high | yes | Xuất khẩu một tổ chức 74515 dòng nạp hết vào RAM, không còn 400 chặn trước → nguy cơ sập tiến trình API | confirmed | `report-export.service.ts:183-186` chỉ gọi `assertUnderRowCap` khi định nghĩa có `countRows()`; **không định nghĩa nào trong 6 cái không-cursor có `countRows()`**. Hệ quả: mỗi UoW phải bổ sung `countRows()` **trong cùng lượt** với việc gỡ assert (ADR-01) |
| A-07 | Cột động `branch.qty.<branchId>` của bảng pivot cần spec SQL sinh theo từng chi nhánh, không phải map tĩnh | high | yes | UOW-07 ước lượng thiếu; lọc theo cột chi nhánh im lặng không tác dụng sau khi đẩy xuống | confirmed | Đúng, và nặng hơn dự tính. `stock-balance-pivot.service.ts:141-165` chọn mặt hàng của trang bằng `itemPageSql` chỉ join `items`; ô theo chi nhánh mới fan-out ở `cellSql` bước sau. Lọc theo `branch.qty.<id>` phải thành truy vấn con tương quan trên `stock_balances` ghép vào `itemPageSql` **và** `itemCountSql`, sinh theo đúng các khoá chi nhánh có trong request → specs phải dựng động mỗi lần gọi. `stock-by-store-pivot.report.ts:94` đã nhận khoá động vào catalog nên hợp đồng phía ngoài không đổi |
| A-08 | Câu count của `StockPeriodService` (`buildItemSqls`) hiện **không** join `locations` / `products` / `inventory_item_categories`, nên mọi spec văn bản mới phải kèm join tương ứng vào cả câu count | high | yes | Câu count 42P01 (relation không tồn tại) ngay khi người dùng lọc theo Nhóm hàng hoặc Vị trí | confirmed | Đọc `stock-period.service.ts:499-556`: `dataSql` có `joinLoc`/`pr`/`ic`, `countSql` chỉ join `items` |
| A-09 | `StockBalancePivotService` hiện **không** áp `columnFilters` vào SQL (chỉ băm vào khoá cache), nên bảng pivot cần dựng specs từ số 0 | high | no | UOW-07 dài gấp đôi dự tính | confirmed | Chú thích tại `stock-balance-pivot.service.ts:9`: "hiện chỉ vào khoá cache; áp vào SQL ở UOW-06 (T-06-03)" |
| A-10 | Đường GET cũ (`InventoryReportsController` → `inventory-reports.service.ts`) dùng chung các engine và phải giữ nguyên hành vi | high | no | Màn hình báo cáo kho cũ đổi số mà không ai yêu cầu | confirmed | `runStockPeriod` (`inventory-reports.service.ts:396-448`) gọi thẳng `stockPeriodService.aggregate`; specs chỉ **thêm** khoá mới nên request cũ không đổi nghĩa |
| A-11 | `filters.unit` và `filters.brand` (thanh lọc, không phải lưới) phải đẩy xuống SQL **trong cùng UoW** với báo cáo tương ứng | high | yes | Để lại dạng JS thì sau khi phân trang xuống SQL chúng chỉ lọc trang hiện tại — sai âm thầm, tệ hơn lỗi 400 hôm nay | confirmed | `stock-summary.report.ts:133-134` và 4 báo cáo khác lọc sau `paginateRows`; `periodColumnSpecs` đã có sẵn `unit` và `brand` nên chỉ là gộp vào `columnFilters` |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-12 | Bỏ `assertUnderRowCap` khỏi `buildData` là đủ để hết lỗi 400 | Bỏ trần mà không đẩy phân trang xuống SQL thì `aggregate({pageSize: 50000})` vẫn nạp 74515 dòng vào RAM — hết 400 nhưng đổi thành chậm và tốn RAM | Không có UoW nào chỉ gỡ trần; mỗi UoW gỡ trần đều phải kèm pushdown và `countRows()` |
| A-14 | Bốn cột điều chuyển chỉ cần port nguyên nghĩa quy tắc JS sang SQL | Không port được. Quy tắc "chỉ lượt pending đầu tiên" đếm thiếu **và** không xác định — `loadPendingTransfers` không có `ORDER BY`, nên lượt nào thắng là do Postgres. Không có nghĩa nào để port | G2 mở lại 2026-08-22; ADR-07 chốt sửa đúng (cộng đủ mọi nguồn) thay vì port. T-03-03/04/05 viết lại. Cột "Sắp nhận về" sẽ tăng — thay đổi số nhìn thấy được |
| A-13 | Thu hẹp kỳ báo cáo sẽ giảm số dòng xuống dưới trần | Tồn đầu kỳ cần toàn bộ lịch sử trước `startDate`; số dòng là số cặp (mặt hàng × vị trí) có phát sinh, gần như không phụ thuộc độ dài kỳ | Lời khuyên "narrow the period" trong thông báo lỗi là sai; AC-12 yêu cầu sửa lại thông báo còn sót ở đường export |
