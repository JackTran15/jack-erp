---
id: T-02-04
uow: UOW-02
title: countRows() cho stock-summary và gỡ trần khỏi buildData
layer: application
type: feature
estimate: 2h
status: done
depends_on: []
blocks: [T-02-05]
verifies: [AC-12]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/stock-summary.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/stock-summary.report.spec.ts
---

# T-02-04 — countRows() cho stock-summary và gỡ trần khỏi buildData

## Context

Cùng khuôn ADR-01 như T-01-02, cho báo cáo trong ảnh chụp lỗi. Làm riêng và làm trước T-02-05 để lúc pushdown vào thì đường export đã có người canh sẵn.

## Implementation notes

- `countRows` gọi `stockPeriod.aggregate` với `pageSize: 1` và cùng scope `buildData` dựng, chỉ đọc `total`
- Dùng lại đúng hàm dựng scope mà `buildData` dùng — tách ra một private method để hai đường không thể lệch nhau
- Gỡ `assertUnderRowCap(result.total)` khỏi `buildData` trong cùng ticket

## Done when

- [x] `prepareExport` ném 400 trên tổ chức 74515 dòng, kèm đúng con số
- [x] `print-payload` cũng ném 400
- [x] `countRows` và `buildData` dựng cùng một scope — test khẳng định bằng cách so hai `total`
