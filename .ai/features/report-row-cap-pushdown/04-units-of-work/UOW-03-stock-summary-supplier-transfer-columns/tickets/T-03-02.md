---
id: T-03-02
uow: UOW-03
title: Bỏ primarySuppliers() khỏi stock-summary, đọc thẳng cột SQL
layer: application
type: refactor
estimate: 2h
status: done
depends_on: [T-02-05, T-03-01]
blocks: [T-03-04]
verifies: [AC-07]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/stock-summary.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/stock-summary.report.spec.ts
---

# T-03-02 — Bỏ primarySuppliers() khỏi stock-summary, đọc thẳng cột SQL

## Context

Sau T-03-01 thì dòng đã mang sẵn `supplier`, nên truy vấn JS phụ và `Map<itemId, name>` thành thừa. Xoá nó cũng bỏ luôn một round trip DB mỗi request.

## Implementation notes

- Xoá `primarySuppliers()` và tham số `suppliers` của `toRow`
- `toRow` đọc `r.supplier` từ dòng engine
- Bỏ `ItemProviderEntity` khỏi constructor nếu không còn chỗ nào dùng — và bỏ luôn khai báo repository tương ứng trong module nếu nó chỉ phục vụ chỗ này
- Thêm `supplier` vào `KEY_MAP` nếu tên khoá lệch (ở đây trùng, ghi rõ trong test)

## Done when

- [x] Cột nhà cung cấp hiển thị đúng như trước khi sửa, trên cùng fixture
- [x] Không còn truy vấn `item_providers` nào phát ra từ định nghĩa báo cáo
- [x] Lọc `supplier` chạy dưới SQL, `total` phản ánh tập đã lọc
- [x] `tsc` sạch sau khi bỏ dependency khỏi constructor
