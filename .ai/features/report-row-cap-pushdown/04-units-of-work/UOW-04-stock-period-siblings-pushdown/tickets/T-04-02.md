---
id: T-04-02
uow: UOW-04
title: Chuyển stock-summary-by-store.report.ts sang pushdown + countRows
layer: application
type: feature
estimate: 3h
status: done
depends_on: [T-04-01]
blocks: []
verifies: [AC-12, AC-15, AC-22]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/stock-summary-by-store.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/stock-summary-by-store.report.spec.ts
---

# T-04-02 — Chuyển stock-summary-by-store.report.ts sang pushdown + countRows

## Context

Cùng khuôn T-02-05, nhưng nhẹ hơn: specs mức mặt hàng đã có từ UOW-02, chỉ thêm hai cột chi nhánh của T-04-01.

## Implementation notes

- `KEY_MAP`: `name`→`itemName`, `endingQty`→`closingQty`, `endingValue`→`closingValue`, `branch`→`branch`
- `countRows()` theo khuôn ADR-01, gỡ `assertUnderRowCap` cùng lượt
- Xoá `applyColumnFilters` / `paginateRows` / `buildTotalsRow` và hai dòng lọc `filters.unit`/`filters.brand`

## Done when

- [x] Trả 200 trên fixture 74515 dòng với đúng `limit` dòng
- [x] Lọc `branch` làm `total` và `totals` đổi theo
- [x] `prepareExport` vẫn ném 400 khi vượt trần
- [x] File không còn tham chiếu `assertUnderRowCap`
