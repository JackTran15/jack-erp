---
id: T-04-04
uow: UOW-04
title: Chuyển stock-quantity-detail.report.ts sang pushdown + countRows
layer: application
type: feature
estimate: 3h
status: done
depends_on: [T-04-03]
blocks: []
verifies: [AC-12, AC-16, AC-22]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/stock-quantity-detail.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/stock-quantity-detail.report.spec.ts
---

# T-04-04 — Chuyển stock-quantity-detail.report.ts sang pushdown + countRows

## Context

Ticket cuối của nhánh `StockPeriodService`. Sau ticket này cả ba báo cáo chạy trên engine đó đều đã ở dưới SQL.

## Implementation notes

- `buildData` truyền `page`/`pageSize`/`columnFilters` xuống `aggregate` với `includeBreakdown: true`
- `countRows()` theo khuôn ADR-01, gỡ `assertUnderRowCap` cùng lượt
- Xoá `applyColumnFilters` / `paginateRows` / `buildTotalsRow` và hai dòng lọc `filters.unit`/`filters.brand`

## Done when

- [x] Trả 200 trên fixture 74515 dòng
- [x] `totals` chứa đủ các cột phân rã và bằng tổng toàn tập
- [x] `prepareExport` vẫn ném 400 khi vượt trần
- [x] File không còn tham chiếu `assertUnderRowCap`
