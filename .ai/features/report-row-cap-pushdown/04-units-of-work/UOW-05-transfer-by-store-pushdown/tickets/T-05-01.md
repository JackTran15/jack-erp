---
id: T-05-01
uow: UOW-05
title: Dựng transfer-report.service.spec.ts và 4 spec mặt hàng
layer: service
type: feature
estimate: 4h
status: done
depends_on: []
blocks: [T-05-05]
verifies: [AC-05, AC-23]
touches:
  - apps/api/src/modules/inventory-reports/services/transfer-report.service.ts
  - apps/api/src/modules/inventory-reports/services/transfer-report.service.spec.ts # new — engine chưa có test nào
---

# T-05-01 — Dựng transfer-report.service.spec.ts và 4 spec mặt hàng

## Context

`transfer-report.service.ts` (29KB) hiện KHÔNG có file spec nào — kiểm bằng `ls services/*.spec.ts`. Nên ticket này gánh cả việc dựng khung test đầu tiên cho engine, không chỉ thêm spec cột. Đó là lý do nó chỉ ôm 4 trong 7 cột mới; ba cột còn lại ở T-05-05.

## Implementation notes

- Dựng `transfer-report.service.spec.ts` với fixture tối thiểu chạy được `transferByBranch`
- Thêm `brand`→`i.brand`, `group`→`ic.name`, `parentSku`→`pr.code`, `parentName`→`pr.name`
- Bổ sung join `products` / `inventory_item_categories` vào câu count (ADR-04)
- Chỉ THÊM khoá — đường GET cũ đi qua cùng hàm này (A-10)

## Done when

- [x] File spec mới chạy được và có ít nhất một test cho đường không lọc
- [x] Lọc từng cột trong bốn cột mới chạy ở cả câu dữ liệu lẫn câu count, không 42P01
- [x] `total` không đổi khi bộ lọc rỗng
- [x] Test hồi quy đường GET cũ vẫn xanh
