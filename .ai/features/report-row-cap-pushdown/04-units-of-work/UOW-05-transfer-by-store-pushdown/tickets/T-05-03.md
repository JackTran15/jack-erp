---
id: T-05-03
uow: UOW-05
title: Chuyển transfer-by-store.report.ts sang pushdown + countRows
layer: application
type: feature
estimate: 3h
status: done
depends_on: [T-01-01, T-05-02]
blocks: [T-05-04]
verifies: [AC-12, AC-21, AC-22]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/transfer-by-store.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/transfer-by-store.report.spec.ts
---

# T-05-03 — Chuyển transfer-by-store.report.ts sang pushdown + countRows

## Context

Cùng khuôn T-02-05. Báo cáo này có `NON_ADDITIVE` nên `toTotalsRow` phải nhận tập đó.

## Implementation notes

- `KEY_MAP`: `name`→`itemName`; các khoá còn lại trùng tên sau T-05-01
- `toTotalsRow(dto.columns, result.totals, KEY_MAP, NON_ADDITIVE)`
- `countRows()` theo khuôn ADR-01, gỡ `assertUnderRowCap` cùng lượt
- Xoá `applyColumnFilters` / `paginateRows` / `buildTotalsRow` và hai dòng lọc `filters.unit`/`filters.brand`

## Done when

- [x] Trả 200 trên fixture vượt trần
- [x] `prepareExport` vẫn ném 400 khi vượt trần
- [x] Mọi khoá trong `COLUMNS` tra ra spec — test duyệt `COLUMNS` khẳng định
- [x] File không còn tham chiếu `assertUnderRowCap`
