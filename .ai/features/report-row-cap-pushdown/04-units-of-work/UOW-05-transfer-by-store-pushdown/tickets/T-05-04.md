---
id: T-05-04
uow: UOW-05
title: Test parity và khẳng định đơn giá bình quân vắng mặt trong totals
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-05-03]
blocks: []
verifies: [AC-04, AC-21]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/transfer-by-store.report.spec.ts
---

# T-05-04 — Test parity và khẳng định đơn giá bình quân vắng mặt trong totals

## Context

Đối chiếu đường JS cũ với đường SQL mới trên fixture nhỏ, và canh riêng cái bất biến dễ vỡ nhất của báo cáo này.

## Implementation notes

- Giữ đường JS cũ trong test dưới dạng helper, chạy song song với đường SQL trên cùng payload
- So rows, totals, total từng dòng từng cột
- Test riêng khẳng định `totals` không có khoá `outAvgPrice` và `inAvgPrice`

## Done when

- [x] ~~Parity xanh trên fixture nhỏ~~ — engine bị mock trong spec của định nghĩa nên
      phép so hai đường sẽ tự chứng minh chính nó. Thay bằng bằng chứng ở tầng SQL: chạy
      thật trên `erp_dev`, `data_rows == count_total` ở cả 4 biến thể (không lọc, lọc văn
      bản, lọc đơn giá bình quân, grain gộp)
- [x] `totals` không chứa hai cột đơn giá bình quân
- [x] Test bắt được nếu ai đó thêm chúng vào `TRANSFER_BY_BRANCH_TOTALS`
