---
id: T-05-05
uow: UOW-05
title: 3 spec còn lại: color, size, targetBranch
layer: service
type: feature
estimate: 3h
status: done
depends_on: [T-05-01]
blocks: [T-05-02]
verifies: [AC-21]
touches:
  - apps/api/src/modules/inventory-reports/services/transfer-report.service.ts
  - apps/api/src/modules/inventory-reports/services/transfer-report.service.spec.ts # new in T-05-01
---

# T-05-05 — 3 spec còn lại: color, size, targetBranch

## Context

Ba cột còn lại của catalog. `targetBranch` cần join `branches` vào câu count; `color`/`size` là truy vấn con tương quan giống hệt khuôn đã dùng ở T-02-01.

## Implementation notes

- `color`/`size` → lặp lại nguyên văn truy vấn con mà câu dữ liệu đang dùng, để giá trị lọc và giá trị hiển thị là một
- `targetBranch` → tên chi nhánh nhận; bổ sung join `branches` vào câu count
- Báo cáo hợp nhất hai nguồn chứng từ; spec phải trỏ vào alias của tầng ngoài cùng

## Done when

- [x] Lọc `targetBranch` trả đúng tập trên fixture nhiều chi nhánh nhận
- [x] Lọc `color` trả đúng những dòng mà cột `color` hiển thị giá trị đó
- [x] `total` không đổi khi bộ lọc rỗng
