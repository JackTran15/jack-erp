---
id: T-06-03
uow: UOW-06
title: Chuyển document-detail.report.ts sang pushdown + KEY_MAP
layer: application
type: feature
estimate: 3h
status: done
depends_on: [T-01-01, T-06-02]
blocks: [T-06-04, T-10-01]
verifies: [AC-11, AC-20, AC-22]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/document-detail.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/document-detail.report.spec.ts
---

# T-06-03 — Chuyển document-detail.report.ts sang pushdown + KEY_MAP

## Context

Không cần `countRows()`: báo cáo này khai báo `exportSource` nên export stream bằng con trỏ và CỐ Ý không áp trần (ngoại lệ hợp lệ của ADR-01, xem chú thích `report-export.service.ts:180-182`). Nhưng vẫn phải gỡ `assertUnderRowCap` khỏi `buildData`. Năm cột mặt hàng và hai cột giá bán chưa có spec nên lọc chúng trả 400 — trạng thái trung gian có chủ ý, đóng ở UOW-10.

## Implementation notes

- `KEY_MAP`: `name`→`itemName`, `reference`→`referenceNumber`; các khoá còn lại trùng tên sau T-06-01..02
- `toTotalsRow(dto.columns, result.totals, KEY_MAP, NON_ADDITIVE)`
- Gỡ `assertUnderRowCap` khỏi `buildData`; KHÔNG thêm `countRows()`
- Xoá `applyColumnFilters` / `paginateRows` / `buildTotalsRow`
- Ghi chú ngay tại chỗ gỡ assert rằng đường export của báo cáo này dùng keyset nên không cần trần

## Done when

- [x] Trả 200 trên fixture vượt trần
- [x] `exportSource` vẫn hoạt động, không bị 400 (đường keyset cố ý không có trần)
- [x] Lọc `group` hoặc `inSalePrice` ném 400 nêu đích danh tên cột
- [x] File không còn tham chiếu `assertUnderRowCap`
