---
id: T-07-01
uow: UOW-07
title: PIVOT_COLUMN_SPECS: 9 spec mặt hàng + join vào itemPageSql và itemCountSql
layer: service
type: feature
estimate: 4h
status: done
depends_on: []
blocks: [T-07-02]
verifies: [AC-05]
touches:
  - apps/api/src/modules/inventory-reports/services/stock-balance-pivot.service.ts
  - apps/api/src/modules/inventory-reports/services/stock-balance-pivot.service.spec.ts
---

# T-07-01 — PIVOT_COLUMN_SPECS: 9 spec mặt hàng + join vào itemPageSql và itemCountSql

## Context

A-09: engine này KHÔNG áp `columnFilters` vào SQL — chú thích tại `stock-balance-pivot.service.ts:9` nói rõ nó chỉ băm vào khoá cache. Nên phải dựng specs từ số 0. Cấu trúc hai bước là điểm cần hiểu trước: `itemPageSql` chọn mặt hàng CỦA TRANG (chỉ join `items`), rồi `cellSql` mới fan-out ô theo chi nhánh. Vị từ mức mặt hàng phải vào bước MỘT.

## Implementation notes

- Dựng `PIVOT_COLUMN_SPECS`: `sku`→`i.code`, `name`→`i.name`, `unit`→`i.unit`, `brand`→`i.brand`, `group`→`ic.name`, `parentSku`→`pr.code`, `parentName`→`pr.name`, `color`/`size`→ truy vấn con tương quan
- Bổ sung `LEFT JOIN products pr` và `LEFT JOIN inventory_item_categories ic` vào `itemPageSql` VÀ `itemCountSql`
- `cellSql` giữ nguyên — nó đã nhận `itemIds` của trang nên không cần lọc lại
- Chỉ dựng specs ở ticket này; ghép `buildReportColumnFilter` vào truy vấn là việc của T-07-03

## Done when

- [x] Specs dựng đủ 9 khoá, có test duyệt khẳng định mỗi khoá tra ra biểu thức
- [x] `itemPageSql` và `itemCountSql` chạy được với join mới, không 42P01
- [x] `total` không đổi so với trước khi thêm join, trên cùng fixture
