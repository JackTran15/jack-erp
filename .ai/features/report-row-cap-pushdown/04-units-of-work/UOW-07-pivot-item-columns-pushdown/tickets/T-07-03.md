---
id: T-07-03
uow: UOW-07
title: Ghép buildReportColumnFilter vào itemPageSql, itemCountSql và loadBranchTotals
layer: service
type: feature
estimate: 4h
status: done
depends_on: [T-07-02]
blocks: [T-07-04, T-08-01]
verifies: [AC-19]
touches:
  - apps/api/src/modules/inventory-reports/services/stock-balance-pivot.service.ts
  - apps/api/src/modules/inventory-reports/services/stock-balance-pivot.service.spec.ts
---

# T-07-03 — Ghép buildReportColumnFilter vào itemPageSql, itemCountSql và loadBranchTotals

## Context

Ghép specs vào ba câu. `loadBranchTotals` là cái dễ quên nhất — nó tính footer bằng một truy vấn RIÊNG (`stock-balance-pivot.service.ts:89-96`), nên bỏ sót nó là footer mô tả một tập khác với lưới, đúng cái bất biến ADR-04 tồn tại để giữ.

## Implementation notes

- `buildReportColumnFilter(specs, ...)` một lần, mảnh vị từ dùng cho cả ba câu
- Đánh số tham số vị trí cẩn thận: ba câu hiện có số tham số cơ sở khác nhau
- `loadBranchTotals` phải nhận cùng mảnh vị từ và cùng join
- Xoá chú thích cũ ở dòng 9 ("hiện chỉ vào khoá cache; áp vào SQL ở UOW-06") — nó đã thành sai

## Done when

- [x] Lọc một cột mặt hàng làm `total`, `data` và `totals` cùng đổi theo
- [x] Footer khớp tập đã lọc, không phải toàn tổ chức
- [x] Ba câu nhận cùng một mảnh vị từ — test khẳng định bằng cách so số dòng với tổng ở footer
- [x] Tham số vị trí đúng ở cả ba câu, không lệch chỉ số
