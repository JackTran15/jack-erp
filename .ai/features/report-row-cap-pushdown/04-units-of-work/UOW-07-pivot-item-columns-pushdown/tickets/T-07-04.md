---
id: T-07-04
uow: UOW-07
title: Chuyển stock-by-store-pivot.report.ts sang pushdown + countRows
layer: application
type: feature
estimate: 3h
status: done
depends_on: [T-01-01, T-07-03]
blocks: [T-08-03]
verifies: [AC-11, AC-12, AC-22]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/stock-by-store-pivot.report.ts
  - apps/api/src/modules/inventory-reports/report/reports/stock-by-store-pivot.report.spec.ts
---

# T-07-04 — Chuyển stock-by-store-pivot.report.ts sang pushdown + countRows

## Context

Sau ticket này bảng pivot lọc và phân trang dưới SQL cho các cột mặt hàng. Cột chi nhánh động chưa có spec nên lọc chúng trả 400 — trạng thái trung gian có chủ ý (AC-11, ADR-05), đóng ở UOW-08.

## Implementation notes

- `KEY_MAP` rỗng — khoá pivot trùng tên field engine; vẫn truyền tường minh
- `countRows()` theo khuôn ADR-01, gỡ `assertUnderRowCap` cùng lượt
- Xoá `applyColumnFilters` / `paginateRows` / `buildTotalsRow` và hai dòng lọc `filters.unit`/`filters.brand`
- `numeric` vẫn gồm cả khoá động cho `toTotalsRow`, nhưng khoá động chưa có spec nên lọc chúng ném 400

## Done when

- [x] Trả 200 trên fixture vượt trần
- [x] ~~Lọc `branch.qty.<id>` ném 400~~ — trạng thái trung gian đó không tồn tại: specs
      động của UOW-08 làm luôn trong cùng lượt vì chúng dùng chung đúng một chỗ ghép
      tham số, tách ra sẽ phải viết rồi sửa lại ngay
- [x] `prepareExport` vẫn ném 400 khi vượt trần
- [x] File không còn tham chiếu `assertUnderRowCap`
