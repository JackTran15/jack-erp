---
id: T-08-02
uow: UOW-08
title: Ghép spec động vào ba câu của bảng pivot
layer: service
type: feature
estimate: 4h
status: done
depends_on: [T-08-01]
blocks: [T-08-03]
verifies: [AC-02, AC-18]
touches:
  - apps/api/src/modules/inventory-reports/services/stock-balance-pivot.service.ts
  - apps/api/src/modules/inventory-reports/services/stock-balance-pivot.service.spec.ts
---

# T-08-02 — Ghép spec động vào ba câu của bảng pivot

## Context

Gộp specs động vào specs tĩnh của T-07-01 rồi đưa qua cùng `buildReportColumnFilter`, vào cả `itemPageSql`, `itemCountSql` và `loadBranchTotals`.

## Implementation notes

- `{...PIVOT_COLUMN_SPECS, ...pivotBranchSpecs(Object.keys(columnFilters ?? {}))}`
- Số tham số vị trí giờ thay đổi theo số cột chi nhánh được lọc — `startIndex` phải tính động cho cả ba câu
- `loadBranchTotals` nhận cùng mảnh vị từ (lại là chỗ dễ quên nhất)

## Done when

- [x] Lọc một cột chi nhánh làm `total`, `data` và `totals` cùng đổi theo
- [x] Sang trang 2 vẫn giữ bộ lọc, không có dòng nào lọt lưới
- [x] Footer khớp tập đã lọc
- [x] Lọc hai cột chi nhánh cùng lúc → hai vị từ AND
