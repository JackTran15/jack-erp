---
id: T-09-01
uow: UOW-09
title: Xoá tầng lọc JS khỏi report-data.util.ts
layer: shared
type: chore
estimate: 2h
status: done
depends_on: [T-01-04]
blocks: [T-09-02]
verifies: [AC-23]
touches:
  - apps/api/src/modules/inventory-reports/report/report-data.util.ts
  - apps/api/src/modules/inventory-reports/report/report-data.util.spec.ts # new in T-01-04
---

# T-09-01 — Xoá tầng lọc JS khỏi report-data.util.ts

## Context

Sau tám UoW, `applyColumnFilters` / `paginateRows` / `buildTotalsRow` không còn ai gọi trong miền kho. Xoá theo kết quả grep thật, không theo trí nhớ — một helper có thể còn được báo cáo ngoài miền kho dùng.

## Implementation notes

- Chạy grep trước khi xoá từng hàm; hàm nào còn người gọi thì giữ và ghi rõ ai đang gọi
- Gỡ re-export `assertUnderRowCap` / `MAX_REPORT_ROWS` khỏi `report-data.util.ts` — `row-cap.util.ts` vẫn là nhà của chúng, cho đường export
- Không đụng `row-cap.util.ts`

## Done when

- [x] `tsc` sạch sau khi xoá
- [x] `pnpm --filter @erp/api test` xanh
- [x] ~~Grep chứng minh không còn người gọi nào~~ — **có người gọi, nên GIỮ.**
      `transfer-summary.report.ts` vẫn dùng cả ba hàm, và đúng: engine của nó
      (`summarize`) trả một dòng mỗi chi nhánh, không nhận page/pageSize, nên không có
      gì để đẩy xuống và không có gì phình theo danh mục. Chỉ gỡ phần re-export
      `assertUnderRowCap` / `MAX_REPORT_ROWS`, thứ thật sự không còn ai import
- [x] `row-cap.util.ts` không đổi
