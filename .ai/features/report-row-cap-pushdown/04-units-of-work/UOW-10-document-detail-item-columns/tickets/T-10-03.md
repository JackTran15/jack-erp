---
id: T-10-03
uow: UOW-10
title: Test: mọi khoá COLUMNS của document-detail đều tra ra spec
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-06-04, T-10-02]
blocks: []
verifies: [AC-05]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/document-detail.report.spec.ts
---

# T-10-03 — Test: mọi khoá COLUMNS của document-detail đều tra ra spec

## Context

Báo cáo có catalog dài nhất trong bảy cái (27 cột). Một test duyệt `COLUMNS` bắt được cột bị bỏ sót rẻ hơn nhiều so với người dùng gõ vào ô lọc rồi nhận 400.

## Implementation notes

- Duyệt `COLUMNS` của định nghĩa, với mỗi khoá gọi `toEngineFilters` rồi `buildReportColumnFilter` và khẳng định không ném
- Cột nào cố ý không lọc được thì phải nằm trong một danh sách loại trừ khai báo tường minh, kèm lý do

## Done when

- [x] Toàn bộ 27 khoá tra ra spec, hoặc nằm trong danh sách loại trừ có lý do
- [x] Thêm một cột mới vào `COLUMNS` mà quên spec → test đỏ
- [x] ~~Danh sách loại trừ rỗng — mọi cột đều lọc được~~ — **không đạt, có chủ ý.**
      `branchCode` và `receiverBranchCode` vẫn nằm ngoài: `toRow` gán cứng cả hai bằng
      `null` vì bảng `branches` không có cột code. Lọc một cột chỉ có thể là null thì
      400 đúng hơn là một bộ lọc trông như đang chạy
