# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Với báo cáo không có group, `<tr>` tầng 2 rỗng vẫn cho `height = 0` nên phép đo tầng 1 không bị chia đôi | medium | yes | Sai offset của hàng ô lọc trên **mọi** báo cáo không group — tức đa số báo cáo | resolved | Né hẳn thay vì đặt cược: khi `!hasGroups` thì không render `<tr>` tầng 2 và bỏ `rowSpan={2}`, nên cách trình duyệt phân bổ chiều cao của ô spanning không còn ảnh hưởng tới phép đo |
| A-02 | `tr.getBoundingClientRect().height` không bị `position: sticky` của các ô con làm lệch | high | yes | Toàn bộ phép đo chiều cao header sai, hàng ô lọc hở hoặc chồng lên tiêu đề | resolved | `<tr>` không sticky (chỉ `<th>`/`<td>` sticky) nên rect của nó là rect layout. Được xác nhận trên trình duyệt thật ở bước verify S4: kéo cột cho label xuống dòng, hàng ô lọc phải tụt xuống theo đúng phần cao thêm |
| A-03 | Trong registry có ít nhất một báo cáo dùng group header, để kiểm được đủ 3 tầng offset chứ không chỉ 2 | medium | no | AC-04 chỉ kiểm được nhánh `!hasGroups`, nhánh group đi thẳng ra production không có bằng chứng | resolved | `report-invoice-and-order-list.registry.ts` có 3 group (`REVENUE`, `REVENUE_TMDT`, `CUSTOMER_PAYMENT`) → báo cáo `invoice_and_order_list`, xem được tại `/reports/sales#invoice_and_order_list` |
| A-04 | Có báo cáo cố tình không có hàng Tổng, nên nhánh "không render `<tfoot>`" là đường chạy thật chứ không phải phòng xa | high | no | Thêm code xử lý một trạng thái không tồn tại | resolved | `config.summaryLabel` rỗng ở "Kết quả kinh doanh" (`/reports/profit#business_results`) — comment tại `ReportPageTableView.tsx:491-494` nói rõ đây là cố ý |
| A-05 | Dữ liệu thật của chi nhánh Hồ Chí Minh đủ ≥ 50 dòng ở kỳ chọn để dựng được trạng thái "đang cuộn giữa bảng" | medium | no | Bước verify S2/S3 không dựng được cảnh cuộn, phải đổi kỳ hoặc đổi báo cáo | resolved | Ảnh gốc của yêu cầu cho thấy 1.510 kết quả / 31 trang cho một tháng; chốt kỳ cụ thể khi viết `07-verification.md` và ghi số dòng vào cột Assert |
| A-06 | Khai hai viewport trong `07-verification.md` thì cả hai cùng chạy được trong một lần gọi `verify.py` | low | no | Nửa số bước đỏ vì lý do không liên quan tới tính năng, và nếu tin vào màu đỏ đó thì đi sửa nhầm chỗ | resolved | Sai. `refresh()` thu hồi jti cũ (`auth.service.ts:164`) ⇒ refresh token dùng-một-lần; bộ chạy đăng nhập một lần rồi phát lại cùng một `storageState` cho mọi viewport (`runner/run.mjs:352-355`) ⇒ viewport thứ hai luôn bị đá về đăng nhập. Chốt `viewports: [desktop]` và chạy laptop riêng (xanh 5/5) — xem `07-verification.md` mục Not verified here |
| A-07 | `position: sticky; bottom: 0` là đủ để hàng Tổng luôn nằm đáy vùng cuộn | high | yes | Bảng rỗng và bảng ngắn hiển thị sai, mà đây là trạng thái người dùng gặp mỗi lần chọn nhầm kỳ | resolved | Sai. Sticky chỉ giữ phần tử trong containing block của nó (`<table>`); bảng lùn thì không có gì để bám. Đo tại 1440×900: vùng cuộn 674px, thead 111, tfoot 32, mỗi dòng 41 ⇒ **dưới 13 dòng** là hàng Tổng trôi lên. Khắc phục bằng `<tr>` đệm cao 100% (AC-07, T-01-04) |
