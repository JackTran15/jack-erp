---
feature: revenue-by-item-misa-parity
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | `revenue.total` (Σ `invoice_items.line_total`) đúng bằng `Doanh thu` của MISA, tức `Tiền hàng − Khuyến mại − Điểm KM`. Feature chỉ đổi nhãn `Tổng`→`Doanh thu`, KHÔNG sửa aggregator | high | no | Nếu sai thì đây không còn là feature nhãn/thứ tự mà là bug số liệu; UOW-01 phải mở rộng sang `revenue-by-item.aggregator.ts` | confirmed | Đúng về logic (`line_total` đã trừ `lineDiscount` ở tầng entity). Kiểm trên UI thật (T-05-02) dòng tổng `41.895.000 = 41.895.000 − 0 − 0` ✓, nhưng data seed có Khuyến mại/Điểm KM = 0 nên chưa kiểm thực nghiệm phân biệt được — xem `07-reconciliation-note.md` |
| A-02 | Cột `Doanh thu` mang ký hiệu ĐÚNG `(6)=(3)-(4)-(9)` (theo registry FE), không copy ký hiệu lỗi của MISA `(4)=(1)-(2)` — MISA dùng lại số `(4)` cho 2 cột và viết sai công thức | high | no | Kế toán quen đọc ký hiệu MISA sẽ thấy lệch một ô chữ; sửa 1 dòng trong map DESCS | confirmed | Đã implement (`REVENUE_BY_ITEM_COLUMN_DESCS`, T-01-01) và xác nhận trên UI thật + workbook thật (T-05-01, T-05-02): ô header hiện đúng `(6)=(3)-(4)-(9)` |
| A-03 | Dòng tham số in **đầy đủ theo MISA**, kể cả filter chưa đặt (`Nhóm hàng hóa: Tất cả nhóm`, `Thương hiệu: Tất cả`), và lặp lại tên cửa hàng ở `Xem theo cửa hàng:` dù khối chi nhánh phía trên đã in | high | yes | Đảo ngược quyết định đang được ghi rõ trong `filterSummarySubtitle` / `invoiceFilterSummary` ("nothing is invented for an absent filter"); ảnh hưởng cả 4 báo cáo hóa đơn nếu làm sai chỗ | confirmed | Người dùng chọn "Parity MISA đầy đủ", 2026-07-30. Giới hạn phạm vi: chỉ `revenue-by-item`, xem ADR-02 |
| A-04 | Không thêm cột `Nhà cung cấp`; catalog đúng 14 cột như ảnh #2, và entry chết `SUPPLIER_NAME` bị xóa khỏi registry FE | high | yes | Nếu sai: cần resolve primary provider qua `ItemProviderEntity` → thêm 1 query + nguy cơ N+1, phát sinh 1 ticket | confirmed | Người dùng chọn "Bỏ, không thêm", 2026-07-30 |
| A-05 | `Điểm KM` (`revenue.promoPoints`) giữ nguyên placeholder `0`; không có backing loyalty theo dòng hóa đơn | medium | no | Nếu sau này có backing thì `Tỷ lệ KM` và `Doanh thu` đổi số (cả hai đã cộng `(9)` trong công thức nên sẽ tự đúng); không phải rework cấu trúc | confirmed | Mọi ô cột `Điểm KM` trên ảnh #2 và trên UI thật (T-05-02) đều bằng 0 — parity hiện tại không phân biệt được, nhưng đúng như dự đoán, không phải bug |
| A-06 | ~~Bug `statBy: "productTemplate"` → 400 được sửa TRONG feature này~~ — **BỊ BÁC BỎ, xem Rejected assumptions A-20** | high | no | — | rejected | Xem A-20 |
| A-07 | Sửa catalog BE là đủ và đúng: bảng trên màn hình, trang in và file Excel đều theo (ADR-01 của `export-print`); không tạo lớp override riêng cho export | high | yes | Nếu sai: 2 nguồn sự thật về cột, đúng thứ ADR-01 tồn tại để chặn | confirmed | Người dùng chọn "Cả 3 (màn hình + in + Excel)", 2026-07-30 |
| A-08 | 2 cột vị trí LUÔN có trong catalog ở mọi grain và mọi phạm vi cửa hàng; giá trị `null` khi không resolve được — giống MISA (ảnh #2 ở grain Mẫu mã vẫn có cột D/E, để trống) | high | yes | Đảo quyết định đang có trong `buildColumns` + comment ở registry FE; bảng trên màn hình ở grain Mẫu mã sẽ hiện 2 cột rỗng | confirmed | Người dùng chọn "Luôn giữ 14 cột, để trống", 2026-07-30. Xem ADR-03 |
| A-09 | `Thống kê theo chi nhánh: Không` in như **hằng số**. ERP không có chức năng tách dòng theo chi nhánh, nên câu này luôn đúng — không phải filter bịa | medium | no | Nếu sau này ERP có tách theo chi nhánh thì dòng này thành sai; 1 dòng trong builder | confirmed | Không tìm thấy filter tương ứng trong `InvoiceReportFilterPayload`; giá trị MISA trên ảnh #2 cũng là "Không". Đã implement như hằng số trong `RevenueByItemParamsBuilder` (T-04-01) |
| A-10 | `Loại hàng hóa: Hàng hóa` in như mặc định, dù filter `productType` hiện **không có tác dụng** (comment `revenue-by-item.report.ts:276`) | medium | no | Dòng tham số hứa một phạm vi mà query không áp — nhưng ERP hiện chỉ bán hàng hóa nên phát biểu vẫn đúng trên dữ liệu thật | confirmed | Đã implement với default `'product'` (T-04-01); comment tại chỗ ghi rõ giới hạn để lần sau `productType` có backing thì kiểm lại |
| A-11 | `Nhóm hàng hóa` và `Xem theo cửa hàng` in **tên thật** (resolve `ItemCategoryEntity.name`, `BranchEntity.name`) thay cho marker `đã lọc` hiện tại | high | no | Thêm 2 lookup vào đường export/print; nếu id không resolve được thì fallback về `đã lọc` | confirmed | Đã implement (`RevenueByItemParamsBuilder.branchName`/`categoryLabel`, T-04-01), xác nhận qua 11 unit test + trên UI thật hiện tên "Chi nhánh 211 TP. Đà Nẵng" khi mở dòng tham số qua builder |
| A-12 | Thứ tự 2 cột vị trí là `Mã vị trí` (D) rồi `Tên vị trí` (E) theo ảnh #2 — ngược với registry FE hiện tại (`Vị trí` order 4, `Mã vị trí` order 5) | high | no | Lệch 1 cặp cột so với file MISA; 1 dòng trong catalog | confirmed | Đã implement (T-01-02 catalog, T-01-05 registry FE) và xác nhận trên UI thật (T-05-02): cột D=`Mã vị trí`, E=`Tên vị trí` |
| A-13 | Nhãn của 3 báo cáo hóa đơn còn lại KHÔNG đổi — override nhãn/ký hiệu theo từng báo cáo, không sửa `INVOICE_REPORT_COLUMN_LABELS_VI` dùng chung | high | yes | Sửa map dùng chung sẽ đổi nhãn `Số lượng`/`Đơn giá`/`Tổng` trong `daily-sales-summary`, `invoice-order-listing`, `invoice-item-revenue-detail` mà không ai yêu cầu | confirmed | Suy ra từ lựa chọn phạm vi của người dùng (2026-07-30): phạm vi là 3 **mặt** của báo cáo này, không phải 4 báo cáo. Cơ chế ở ADR-01 |
| A-14 | Thêm `desc?` vào `DocumentColumn` là mở rộng tương thích ngược; `voucher-xlsx.writer.ts` và các payload chứng từ bỏ qua field mới | high | no | Nếu writer chứng từ vô tình đọc `desc` thì header chứng từ mọc thêm dòng; test hiện có của `voucher-xlsx.writer.spec.ts` sẽ bắt | confirmed | `voucher-xlsx.writer.spec.ts` chạy 20/20 không đổi sau T-02-01 và T-02-02 — field mới không ảnh hưởng chứng từ |
| A-15 | Feature `export-print` (G3, 42/51 done) không đang sửa dở `xlsx-stream.writer.ts` / `xlsx-style.ts` theo cách xung đột với UOW-02 ở đây | medium | no | Conflict khi merge; phải rebase 1 file | confirmed | `git status` trước khi sửa (T-02-02) cho thấy 2 file đã staged sẵn từ `export-print` UOW-09 — đúng nội dung đã đọc/test trước đó trong phiên này, không phải edit chạy song song. Sửa chồng lên sạch, không conflict |

## Rejected assumptions

| ID | What we assumed | What is actually true | Consequence |
|----|----------------|----------------------|-------------|
| A-16 | Export sai số nên phải sửa cách tính | `line_total` đã trừ `line_discount`, nên `revenue.total` = `Doanh thu` MISA về mặt số học | Bỏ khỏi phạm vi mọi thay đổi trong `revenue-by-item.aggregator.ts`; UOW-01 chỉ còn catalog + nhãn |
| A-17 | Registry cột ở FE là nguồn sự thật nên sửa ở FE là xong | `mapHeadersToTableConfig` lấy catalog từ `GET /reports/invoices/columns`; registry FE chỉ là fallback không bao giờ thắng | Không sửa registry FE để "fix" layout; chỉ xóa entry chết `SUPPLIER_NAME` (A-04) và đồng bộ lại comment |
| A-18 | Chỉ cần thêm ký hiệu công thức vào catalog là file Excel có ngay | `DocumentColumn` không mang `desc`; `resolveColumns` bỏ field này, `writeHeaderRow` chỉ ghi `column.label` | Phát sinh UOW-02 (đường dẫn `desc` xuyên payload → Excel → trang in), là lý do feature này không phải 1 ticket |
| A-19 | Chuỗi `; : False` cuối dòng tham số MISA có nghĩa và phải copy | Là lỗi render của MISA — nhãn rỗng cộng một boolean; không mang thông tin | Không copy. 2 cờ riêng của ERP (`statisticByBrand`, `allocateComboRevenue`) chỉ in khi bật, đặt sau các phần parity |
| A-20 | Chọn "Mẫu mã" trên báo cáo `revenue-by-item` gửi `statBy: "productTemplate"` → 400, vì tìm thấy chuỗi `productTemplate`/`"Mẫu mã"` trong `ReportFilterLine.tsx` khi grep theo nhãn | `revenue-by-item` dùng `REPORT_FILTERS_LINE.STATISTIC_BY` → render `STAT_BY_OPTIONS` (`packages/shared-interfaces/src/invoice-report/options.ts:76`), giá trị đã là `item\|parent\|group` — khớp `ReportGroupBy` sẵn. Chuỗi `productTemplate` tìm thấy khi grep thuộc `REPORT_FILTERS_LINE.STATISTIC_GROUP_BY_ITEM_OR_TEMPLATE`, chỉ dùng bởi `report-supplier-debts-detail-by-document-and-product.registry.ts` — một báo cáo công nợ nhà cung cấp hoàn toàn khác, không liên quan `revenue-by-item` | T-03-01 (sửa `ReportFilterLine.tsx`) bị hủy — sửa đúng file đó sẽ đổi giá trị của báo cáo công nợ nhà cung cấp, phá vỡ report không nằm trong phạm vi. AC-17 giữ nguyên nội dung Gherkin (vẫn đúng: chọn Mẫu mã → 200) nhưng chuyển từ "cần sửa" sang "đã đúng sẵn, khóa lại bằng đối chiếu tay ở T-05-02". UOW-03 chỉ còn 1 ticket (T-03-02: label `GROUP_BY_LABELS_VI` vẫn đảo ngược thật, không đổi) |
