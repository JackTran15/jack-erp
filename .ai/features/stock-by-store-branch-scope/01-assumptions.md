# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | "Chi nhánh phụ trách" trong Phụ lục 3.2 giới hạn được cả báo cáo tồn kho theo cửa hàng | medium | yes | Sai ⇒ cắt mất công năng so sánh tồn giữa các cửa hàng, hoặc ngược lại là mở dữ liệu ngoài ý khách | resolved | **BÁC BỎ.** Chủ dự án chốt 03/09/2026: riêng báo cáo này mọi vai trò mở được đều xem toàn bộ chi nhánh — đây là báo cáo so sánh giữa các cửa hàng. Xem ADR-04 (thay thế ADR-02). Ngược với câu chữ PQ-02 nên đã ghi thành điểm cần khách xác nhận |
| A-02 | `reporting.dashboard.consolidated.read` là khoá đúng để phân biệt "được xem toàn chuỗi" | high | no | Sai ⇒ phân biệt nhầm vai trò | resolved | **KHÔNG CÒN ÁP DỤNG cho báo cáo này**: sau ADR-04 mọi vai trò đều xem toàn bộ chi nhánh nên không cần phân biệt. Khoá vẫn đúng nghĩa ở nơi khác (`ReportingService.resolveBranchScope`, FE `canViewChain()`) |
| A-03 | Không còn client nào ngoài backoffice trong monorepo gọi `GET /reports/inventory/*` | medium | yes | Sai ⇒ xoá endpoint làm hỏng tích hợp bên ngoài | resolved | `grep -rn "reports/inventory/"` chỉ khớp `apps/backoffice-web/src/api/inventory-reports.ts`; pos-web không gọi. `packages/api-client` sinh tự động nên regenerate là đủ. Người dùng chọn "xoá hẳn" khi được hỏi |
| A-04 | `actor.branchId` luôn phản ánh chi nhánh người dùng vừa chọn trên UI | high | no | Sai ⇒ báo cáo hiện nhầm chi nhánh (sai dữ liệu, không phải lộ dữ liệu) | resolved | `BranchSelector` gọi `POST /auth/switch-branch` phát lại token với `branchId` mới (auth.service.ts:204-220); `ActorContext` ưu tiên `fromJwt` (actor-context.decorator.ts:32-33) |
| A-05 | Tài khoản kiểm thử được gán ÍT hơn tổng số chi nhánh, đủ để chứng minh việc nới phạm vi | high | no | Sai ⇒ bằng chứng vô nghĩa: gán hết thì thấy hết dù có nới hay không | resolved | Org MT có 15 chi nhánh; `bm.verify@erp.local` và `wh.verify@erp.local` mỗi tài khoản chỉ gán 2 (MT46 + MT211 = 35.150). Vậy `text=` một chi nhánh KHÔNG được gán là phép thử thật, và tổng phải là 263.340 chứ không phải 35.150 |
| A-06 | `pnpm build` + `pnpm --filter @erp/api test` đủ để bắt import mồ côi sau khi xoá ~15 file FE/BE | medium | no | Sai ⇒ lỗi runtime lộ ra lúc chạy thật | resolved | Đủ: `pnpm build` (nest build có gồm file spec) bắt ngay lỗi arity của constructor sau T-01-02, và `pnpm --filter @erp/backoffice-web build` xanh sau khi xoá 7 file + 8 route. Đã thêm grep vào done-when T-02-02/T-02-03, cả hai trả rỗng |
