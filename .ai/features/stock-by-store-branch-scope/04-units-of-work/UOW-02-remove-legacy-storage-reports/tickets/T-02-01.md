---
id: T-02-01
uow: UOW-02
title: Tách PERIOD_PRESETS ra khỏi DTO sắp xoá
layer: api
type: refactor
estimate: 1h
status: done
depends_on: []
blocks: [T-02-02]
verifies: [AC-08]
touches:
  - apps/api/src/modules/inventory-reports/dto/period-preset.ts   # new
  - apps/api/src/modules/inventory-reports/dto/inventory-report-query.dto.ts   # xoá bởi ticket này; đường dẫn không còn (không phải new)
  - apps/api/src/modules/inventory-reports/dto/inventory-report-filter.dto.ts
assumptions: []
---

# T-02-01 — Tách PERIOD_PRESETS

## Context
`dto/inventory-report-query.dto.ts` sẽ bị xoá ở T-02-02, nhưng nó export `PERIOD_PRESETS` và
`PeriodPresetLiteral` mà **đường v2 vẫn dùng**: `dto/inventory-report-filter.dto.ts:18` import
từ đó. Xoá thẳng là gãy build v2 — đây là cái bẫy duy nhất của UoW này, nên tách trước.

## Implementation notes
- Tạo `dto/period-preset.ts`, chuyển nguyên văn `PERIOD_PRESETS` và `PeriodPresetLiteral` sang,
  giữ nguyên giá trị và thứ tự (chúng vào OpenAPI enum — đổi thứ tự là đổi snapshot vô cớ)
- `inventory-report-filter.dto.ts:18` import từ file mới
- `inventory-report-query.dto.ts` tạm import lại từ file mới để build vẫn xanh giữa hai ticket
- Kiểm mọi nơi khác còn import hai ký hiệu này:
  `grep -rn "PERIOD_PRESETS\|PeriodPresetLiteral" apps/ packages/`

## Done when
- [x] `grep -rn "from './inventory-report-query.dto'" apps/api/src` chỉ còn khớp trong file
      legacy sắp xoá
- [x] `pnpm --filter @erp/api build` xanh
- [x] Giá trị và thứ tự phần tử của `PERIOD_PRESETS` không đổi
