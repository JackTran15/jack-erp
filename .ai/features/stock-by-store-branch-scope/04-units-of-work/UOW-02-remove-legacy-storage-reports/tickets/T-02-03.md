---
id: T-02-03
uow: UOW-02
title: Xoá 8 trang báo cáo kho legacy ở backoffice
layer: web
type: chore
estimate: 3h
status: done
depends_on: []
blocks: []
verifies: [AC-07]
touches:
  - apps/backoffice-web/src/pages/reports/storage   # xoá bởi ticket này; đường dẫn không còn (không phải new)
  - apps/backoffice-web/src/pages/dynamic-store-view.tsx   # xoá bởi ticket này; đường dẫn không còn (không phải new)
  - apps/backoffice-web/src/constants/page-registry.constant.tsx   # xoá bởi ticket này; đường dẫn không còn (không phải new)
  - apps/backoffice-web/src/hooks/useResolvedComponent.ts   # xoá bởi ticket này; đường dẫn không còn (không phải new)
  - apps/backoffice-web/src/hooks/use-inventory-reports.ts   # xoá bởi ticket này; đường dẫn không còn (không phải new)
  - apps/backoffice-web/src/hooks/use-report-filter-options.ts   # xoá bởi ticket này; đường dẫn không còn (không phải new)
  - apps/backoffice-web/src/api/inventory-reports.ts   # xoá bởi ticket này; đường dẫn không còn (không phải new)
  - apps/backoffice-web/src/App.tsx
  - apps/backoffice-web/src/components/layout/navConfig.ts
  - apps/backoffice-web/src/constants/reports/report-category.constant.ts
assumptions: [A-06]
---

# T-02-03 — Xoá trang báo cáo kho legacy

## Context
`/reports/storage/*` vẫn vào được bằng URL trực tiếp: nav trỏ tới chúng đã bị comment out từ
trước, và `routeAccess.ts:26` trả `allow` cho route không khớp nav nào. Trang
`StockByBranchReportPage` gọi `GET /reports/inventory/stock-by-branch` — endpoint T-02-02 xoá.

Menu sống hiện đi qua `REPORT_CATEGORY_METADATA` → `/reports/inventory` → `ReportPage` (v2),
không chạm gì tới nhóm file này.

## Implementation notes
Xoá:
- cả thư mục `pages/reports/storage/` (8 trang + `_shared/`)
- `pages/dynamic-store-view.tsx`, `constants/page-registry.constant.tsx` (gồm
  `STORAGE_REPORT_PATHS`), `hooks/useResolvedComponent.ts` (không nơi nào import — đã grep),
  `hooks/use-inventory-reports.ts`, `hooks/use-report-filter-options.ts`,
  `api/inventory-reports.ts`

Sửa:
- `App.tsx` — bỏ 8 `<Route path="/reports/storage/*">` (khoảng dòng 296-327) và các import trở
  thành mồ côi (`DynamicStoreView`, và `REPORT_TYPE_INVENTORY` nếu không còn dùng)
- `navConfig.ts` — xoá khối nav đã comment out trỏ tới các route vừa xoá (khoảng dòng 337-365);
  đây là dọn rác do chính thay đổi này tạo ra, không phải refactor tiện tay
- `constants/reports/report-category.constant.ts:61` — sửa comment "trang legacy
  /reports/storage/* vẫn truy cập trực tiếp được"
- `pages/inventory/_components/StockSummaryFilterPopover/StockSummaryFilterPopover.tsx:48` —
  comment trỏ tới `pages/reports/storage/_shared/ReportFilterDialog.tsx` đã xoá

**Không** đụng `pages/chain-store/reports/**` — đó là đường v2 đang chạy.

## Done when
- [x] `grep -rn "reports/storage\|use-inventory-reports\|api/inventory-reports\|pageRegistry\|DynamicStoreView\|useResolvedComponent\|use-report-filter-options" apps/backoffice-web/src` rỗng
- [x] `pnpm --filter @erp/backoffice-web build` xanh
- [x] Báo cáo → Kho vẫn chạy (verify S1, S3)
- [x] `/reports/storage/stock-by-branch` rơi vào màn hình 404 (verify S2)
