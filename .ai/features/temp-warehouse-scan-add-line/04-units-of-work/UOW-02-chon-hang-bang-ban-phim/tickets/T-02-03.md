---
id: T-02-03
uow: UOW-02
title: Test nhánh suggest/empty + chứng minh không lây sang Checkout
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-02-02, T-01-06]
blocks: []
verifies: [AC-08, AC-09]
touches:
  - apps/pos-web/src/lib/page-libs/fast-stock-transfer/fast-stock-transfer-scan-resolve.test.ts   # new; tạo ở T-01-06, ticket này thêm ca test vào
assumptions: [A-04]
---

# T-02-03 — Test nhánh suggest/empty + chứng minh không lây sang Checkout

## Context

Bổ sung vào file test của T-01-06 hai nhánh còn lại của bảng quyết định, và chốt phần
hồi quy AC-09 — thứ không test tự động được (không có DOM, ADR-06) nên phải nghiệm bằng
**diff** và bằng bước 6 của Demo script.

## Implementation notes

- Thêm ca test cho `decideScanOutcome`:
  - nhiều ứng viên → `suggest`, mảng trả về **giữ nguyên thứ tự** (dòng 0 là dòng sẽ sáng)
  - có query, 0 ứng viên → `empty`
  - `highlighted` khác null + nhiều ứng viên → vẫn `add(highlighted)`, không phải `suggest`
- Phần AC-09 ghi vào ticket dưới dạng kiểm chứng bằng lệnh, không phải test file:
  ```bash
  git diff --name-only | grep -E 'Checkout/|PosSelect' && echo "VI PHAM AC-09" || echo "OK"
  ```
  Dán kết quả vào phần bàn giao của ticket.
- Không thêm dependency nào.

## Done when

- [x] 3 ca test mới xanh
- [x] Toàn bộ test `pos-web` xanh bằng `npx vitest run`
- [x] Lệnh kiểm diff ở trên in `OK`, kết quả được dán vào ticket
- [x] Bước 6 của Demo script UOW-02 (Checkout + `PosSelect`) chạy tay, ghi lại kết quả

## Ghi chú lúc làm

3 ca test mà ticket này yêu cầu (`suggest` giữ nguyên thứ tự, `empty`, `highlighted` thắng
danh sách nhiều dòng) **đã được viết luôn ở T-01-06** — file test bao trọn 6 nhánh của bảng
quyết định trong một lần. Không viết thêm test trùng.

Kiểm chứng AC-09 bằng diff, chạy 2026-08-19:

```
$ git diff --name-only -- apps packages | grep -E 'Checkout/|PosSelect'
(khong co ket qua)  →  OK
```

Toàn bộ file bị đổi trong feature (8 file, gồm 2 file mới) đều thuộc `PosSearchPopover`
hoặc `fast-stock-transfer`. Không file nào của Checkout, Đổi trả hàng, hay `PosSelect.tsx`.
Thay đổi duy nhất trên component dùng chung là 2 prop optional có mặc định giữ nguyên hành
vi cũ (`popoverRef`, `autoHighlightFirst`).

Bước 6 của Demo script (bấm tay trên Checkout + `PosSelect`) **chưa chạy** — nằm ở phần
nghiệm thật tại G4.
