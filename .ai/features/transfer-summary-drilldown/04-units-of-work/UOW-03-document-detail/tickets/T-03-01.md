---
id: T-03-01
uow: UOW-03
title: transferLeg trên DTO và payload, dòng filter TRANSFER_LEG, khoá L2
layer: api
type: feature
estimate: 2h
status: done
depends_on: [T-02-01]
blocks: [T-03-02, T-04-01]
verifies: []
touches:
  - apps/api/src/modules/inventory-reports/dto/inventory-report-filter.dto.ts
  - packages/shared-interfaces/src/inventory-report/column.ts
  - packages/shared-interfaces/src/inventory-report/search.ts
  - apps/backoffice-web/src/constants/reports/report-filters.constant.ts
assumptions: []
---

# T-03-01 — Hợp đồng `transferLeg`

## Context

`sourceStoreId` và `receivingStoreIds` **đã có sẵn** (`inventory-report/search.ts:57-60`,
`inventory-report-filter.dto.ts:83-92`) và ánh xạ đúng nơi xuất / nơi nhập. Chỉ thiếu một trường:
chân nào của cặp là chứng từ chính.

## Implementation notes

```ts
export const TRANSFER_LEGS = ['in','out','received','unmatched'] as const;
export type TransferLeg = (typeof TRANSFER_LEGS)[number];

@ApiPropertyOptional({ enum: TRANSFER_LEGS })
@IsOptional() @IsIn(TRANSFER_LEGS as unknown as string[])
transferLeg?: TransferLeg;
```

Khai cả `'unmatched'` ngay từ ticket này dù L3 thuộc UOW-04: enum nửa vời sẽ buộc chạy
`openapi:generate` lần thứ hai.

`ValidationPipe` toàn cục dùng `whitelist + forbidNonWhitelisted` ⇒ không khai DTO thì request
400, và FE cast body nên trình biên dịch **sẽ không** nhắc.

Khoá + nhãn `inventory-transfer-document-detail` → `Chi tiết phiếu nhập xuất điều chuyển theo cửa
hàng và chứng từ`. Cột: `date`, `documentNumber`, `referenceDate`, `reference`, `warehouse`, `sku`,
`name`, `unit`, `qty`, `unitPrice`, `value`, `parentSku`, `parentName`, `group`.

Dòng filter FE `TRANSFER_LEG = 'transfer_leg'` theo mẫu `SKU` (`report-filters.constant.ts:20-22`
— "Không phải một ô nhập liệu"): thêm vào `ReportFilterValues` và map trong
`buildInventorySearchFilters`.

**`pnpm openapi:generate` chạy một lần, sau khi UOW-02 của [[stock-by-store-branch-scope]] land.**
Snapshot hiện còn 8 path `GET /reports/inventory/*` mà nó đã xoá; chạy sớm là ôm diff đó vào commit
này rồi lại phải chạy lại.

## Done when

- [x] `transferLeg` khai trong DTO với `@IsIn(TRANSFER_LEGS)`; giá trị ngoài enum bị `ValidationPipe` chặn 400
- [x] `openapi.snapshot.json` và `schema.ts` đã regenerate (chạy API thật trên :4000) và commit
- [x] Diff **chỉ** có `transferLeg` — 18 dòng thêm, không path nào bị xoá/thêm ngoài dự kiến (#240 đã land trước nên snapshot đã đồng bộ)
- [x] `pnpm --filter @erp/shared-interfaces build` xanh; `tsc` sạch ở cả hai app

## Kết quả 2026-09-03

`TRANSFER_LEGS` khai ở `@erp/shared-interfaces` (một nguồn, dùng cả DTO backend lẫn resolver FE).
Khai đủ bốn giá trị ngay từ đầu — kể cả `'unmatched'` của L3 thuộc UOW-04 — để không phải chạy
`openapi:generate` lần thứ hai.

Dòng filter `TRANSFER_LEG` theo đúng mẫu `SKU`: không registry nào khai nên `getReportFormLines`
không bao giờ render nó; nó tồn tại để drill-down đi chung đường `buildInventorySearchFilters`,
nhờ đó xuất khẩu và in trong dialog cũng thu hẹp đúng chân đang xem.

Diff `schema.ts` đúng 18 dòng, toàn bộ là `transferLeg` và enum của nó.
