---
id: T-04-01
uow: UOW-04
title: Khoá và nhãn cho báo cáo chênh lệch
layer: shared
type: feature
estimate: 1h
status: done
depends_on: [T-03-01]
blocks: [T-04-02]
verifies: []
touches:
  - packages/shared-interfaces/src/inventory-report/column.ts
assumptions: []
---

# T-04-01 — Khoá `inventory-transfer-difference-detail`

## Context

L3 là **khoá riêng**, không phải `leg` thứ tư của L2 (ADR-04): nhãn báo cáo đi vào tên file xuất
khẩu/in (`get-inventory-report-document.handler.ts:81`), nên gộp thì file L3 mang tên L2.

Giá trị `'unmatched'` của enum `TRANSFER_LEGS` đã được T-03-01 khai — ticket này không đụng DTO,
nên **không** cần chạy lại `openapi:generate`.

## Implementation notes

Nhãn: `Chi tiết chênh lệch điều chuyển`. Cột = cột của L2 trừ `warehouse` (MISA không hiển thị Kho
ở dialog này, và một phiếu chưa ai nhận thì kho đích chưa có nghĩa).

`INVENTORY_REPORT_TYPE_LABELS_VI` / `INVENTORY_REPORT_COLUMN_LABELS_VI` là `Record<…>` nên
TypeScript bắt buộc điền.

## Done when

- [x] `pnpm --filter @erp/shared-interfaces build` xanh
- [x] `openapi.snapshot.json` không đổi — `'unmatched'` đã nằm trong `TRANSFER_LEGS` từ T-03-01, ticket này chỉ thêm khoá báo cáo
