---
id: T-04-02
uow: UOW-04
title: TransferDifferenceDetailReport + spec bất biến Σ = |diffQty|
layer: api
type: feature
estimate: 3h
status: done
depends_on: [T-04-01, T-03-02, T-03-03]
blocks: [T-04-03]
verifies: [AC-04, AC-05, AC-11]
touches:
  - apps/api/src/modules/inventory-reports/report/reports/transfer-difference-detail.report.ts   # new
  - apps/api/src/modules/inventory-reports/inventory-reports.module.ts
assumptions: [A-02, A-03, A-05]
---

# T-04-02 — Report definition cho L3

## Context

Đây là nơi ba quyết định của feature gặp nhau và phải nhất quán:

- ADR-01: `unmatched` là **phần bù** của `received` trong cùng tập `out` ⇒ Σ ≡ `|diffQty|` đúng
  theo cấu trúc
- ADR-02: phiếu lập tay `reference_id NULL` **có mặt** ở đây (AC-04)
- ADR-03: điều chuyển legacy **vắng mặt** ở đây (AC-05)

Nếu spec nào trong ba cái đó đỏ thì lỗi nằm ở T-01-03 hoặc T-03-02, không phải ở ticket này.

## Implementation notes

Definition mỏng trên `TransferDetailService` với `leg` ép cứng `'unmatched'` — **không** đọc
`transferLeg` từ DTO, để không ai mở nhầm L3 thành L2 bằng cách sửa filter.

Clamp quyền như T-03-03. Phân trang trong SQL. `countRows()` cho ngưỡng xuất khẩu.

## Done when

- [x] Σ "Số lượng" ≡ |diffQty|: SG→DN cho 93, bằng đúng ô chênh lệch −93 (đo ở T-03-02). Đúng theo cấu trúc vì `received`+`unmatched` phân hoạch `out`
- [x] Spec: mọi dòng trả về có `reference` là null (AC-11) — cột Tham chiếu rỗng chính là phát hiện, không phải lỗi render
- [x] AC-04: không có dữ liệu để kiểm (0 phiếu lập tay trên `erp_dev`); vị ngữ đã được assert ở `transfer-report.service.spec.ts`
- [x] AC-05: nhánh `stock_transfers` bị loại khỏi `unmatched` — có spec ở `transfer-detail.service.spec.ts`
- [x] Cặp có `diffQty = 0` trả 0 dòng (SG→BH, SG→LX… đều chênh lệch 0 và `unmatched` rỗng)
- [x] `report-definitions.guard.spec.ts` xanh; jest toàn bộ 3546/3549

## Kết quả 2026-09-03

`tsc` sạch, spec riêng 4/4, jest `@erp/api` **3546/3549**.

Definition mỏng trên `TransferDetailService`, `leg` **ép cứng** `'unmatched'` chứ không đọc DTO —
có spec chứng minh một filter `transferLeg: 'out'` gửi kèm cũng không đổi được. Lý do: report này
tồn tại để tên file xuất khẩu nói "chênh lệch"; cho filter chuyển hướng nó sẽ sinh ra file có tên
mâu thuẫn với nội dung, và sai lệch đó chỉ lộ khi ai đó mở file ra.

Cột = cột của L2 **trừ `warehouse`** — một phiếu chưa ai nhận thì kho đích chưa có nghĩa. Spec dựng
danh sách cột của cả hai report rồi so, nên hai bên không thể trôi khỏi nhau khi thêm cột mới.

Dùng lại `resolveTransferPair` và `toTransferDetailRow` export từ L2 thay vì chép — hai bản sao của
luật phân quyền là hai bản sẽ lệch nhau.
