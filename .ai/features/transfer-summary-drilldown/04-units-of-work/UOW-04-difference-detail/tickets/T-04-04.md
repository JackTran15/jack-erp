---
id: T-04-04
uow: UOW-04
title: Chạy ai-dlc-verify trên local-backoffice và sinh bằng chứng
layer: test
type: test
estimate: 3h
status: done
depends_on: [T-04-03]
blocks: []
verifies: [AC-15, AC-16]
touches:
  - .ai/features/transfer-summary-drilldown/07-verification.md   # new
assumptions: [A-12]
---

# T-04-04 — Bằng chứng

## Context

`verify.py --doctor` đã trả **capable**, nên khối "Verification evidence" trong cả bốn `uow.md`
thật sự chặn G4. Ticket này là nơi mở khoá nó.

Bài học từ hai lượt chạy trước, đừng lặp lại:

- Lượt chạy của [[barcode-picker-hide-unit-price]] đỏ **toàn bộ** với `ERR_CONNECTION_REFUSED` trên
  `:3000` — dev server chưa bật.
- Lượt chạy đầu của [[sales-report-km-and-drilldown]] đỏ vì số kỳ vọng được đọc từ màn hình thay vì
  đo bằng SQL.
- Bảng Coverage của [[phu-luc-01-audit]] rỗng dù 40/40 xanh, vì mã `AC-BCK-06` không khớp
  `r"\bAC-\d+\b"` (`verify.py:290`).

## Implementation notes

Chuẩn bị: `make dev-api`, `make dev-backoffice`, `pnpm migration:run`, `pnpm seed:transfer-demo`,
rồi `verify.py … --doctor` xác nhận `local-backoffice` còn `capable`.

Frontmatter:

```yaml
---
feature: transfer-summary-drilldown
environments: [local-backoffice]
viewports: [desktop]
---
```

Chỉ `local-backoffice`: feature nằm trọn trong `/reports/inventory`, POS không có màn hình nào.
Chỉ `desktop`: `BackofficeLayout` là vỏ desktop cố định `ml-60`, không `matchMedia` ở đâu, và không
bug nào ở đây phụ thuộc chiều cao khung nhìn.

**Lấy số kỳ vọng từ bảng "Kỳ vọng seed" của T-01-04**, đã đo bằng SQL — không đọc từ màn hình.

Mọi bước đặt kỳ tường minh `2026-09-01`–`2026-09-02` rồi bấm "Lấy dữ liệu": phiên đăng nhập mở lại
preset dùng lần trước, và một ảnh xanh chụp lưới rỗng còn tệ hơn không có bằng chứng.

Mọi `Path` mang `?v=` riêng: `page.goto` tới URL chỉ khác hash là điều hướng cùng tài liệu, SPA
không remount, bước sau sẽ chụp nhầm báo cáo của bước trước.

Các bước phủ AC-01, AC-02, AC-03, AC-06..AC-16 trên kỳ dữ liệu thật; thay số bằng số đo thật
trước khi chạy. AC-04 và AC-05 xuống mục "Not verified here".

Ghi một câu trong phần văn xuôi ánh xạ toàn bộ AC về `AC-BCK-06` của Phụ lục 01 (mã bằng chứng
`BO-S10` trong `docs/client/phu-luc-01-checklist.csv`), vì runner không tự làm được.

## Cập nhật 2026-09-03 — chạy hoàn toàn trên dữ liệu thật, không seed

Kỳ verify là **09/07/2026 – 30/08/2026**, dải dữ liệu vận hành thật trong `erp_dev` (org MT).
Không có seed nào: đo cho thấy dữ liệu thật đã có **300 cặp phiếu đã ghép** và **27 phiếu đang
vận chuyển** trên **15 chi nhánh**, dư sức phủ AC-01/02/03 và AC-06..AC-13 — mạnh hơn hẳn một bộ
fixture ba chi nhánh.

Điểm quý nhất: dòng **"Chi Nhánh cũ không dùng"** chính là ca khách báo — *xuất 22, nhập về 31* —
sau khi sửa đọc 22 / 22 / 0. Ảnh chụp dòng đó là bằng chứng thuyết phục nhất của cả feature.

Số kỳ vọng lấy từ `01-assumptions.md` §"Đo trên dữ liệu thật", **đo lại bằng SQL** ngay trước khi
chạy verify (dump có thể được làm mới).

**AC-04 và AC-05 không chụp ảnh được** — dữ liệu thật có 0 trường hợp. Khai vào mục
`## Not verified here` kèm lý do và cách chúng được phủ thay thế; đừng dựng fixture chỉ để có ảnh.

## Done when

- [x] `verify.py … --write` exit 0, **12/12 xanh** trên `local-backoffice` desktop
- [x] `evidence_check.py` PASS — 12/12 AC có bằng chứng, 3 AC khai ngoài phạm vi ảnh
- [x] Bảng Coverage **không rỗng**: AC-01…AC-16 đều có bước (mã AC dạng `AC-\d+` nên regex runner khớp)
- [x] Mở tay S6/S8/S10: L1 có 14 dòng đối ứng footer 423/10.334/9.801/−533, L2 có 273 dòng `XK000107→NK000334` Tổng 882, L3 có 41 dòng `XK000437` Tham chiếu rỗng Tổng 57
- [x] `08-evidence.md` sha `e889e937` khớp HEAD
- [x] S11/S12 xanh, `count [data-sonner-toast][data-type="error"] = 0` (AC-15)

## Kết quả 2026-09-03 — 12/12 xanh, và bốn lỗi mà chính việc soi ảnh mới bắt được

Lượt chạy cuối: **12/12 xanh**, `evidence_check.py` PASS, sha `e889e937` khớp HEAD.

Bốn thứ hỏng mà **không assert nào ban đầu bắt được**, chỉ lộ ra khi mở ảnh và probe API:

1. **Tiến trình API cũ.** `nest start` khởi động ở T-03-01, trước khi L2/L3 tồn tại — ba endpoint
   chưa đăng ký. Dialog rỗng, mà assert chỉ bám tiêu đề nên vẫn xanh.
2. **`toTotalsRow` truyền sai tham số.** Tham số thứ 4 là `nonAdditive` (cột **cần null**), tôi
   truyền tập cột **số** — null hoá đúng những cột đáng có tổng. Dòng Tổng của cả ba report mới
   trả `None`.
3. **`no-text` là công cụ sai để hỏi "bảng có rỗng không".** Nó dùng `.count()` nên đếm cả node
   **ẩn**, và dialog L1 vẫn mounted phía sau L2 — assert đỏ trên một dialog đang có 273 dòng.
4. **Thiếu `INVENTORY_REPORT_BAND_LABELS_VI` cho khoá mới.** `buildInventoryHeaders` fallback
   `bandLabels[d.band] ?? d.band`, nên dialog L1 hiện `in`/`out`/`received`/`diff`/`inOut` — id thô
   tiếng Anh giữa báo cáo tiếng Việt. Fallback im lặng nên không có gì đỏ.

Bài học chung: **assert bám vỏ dialog thì xanh vô nghĩa.** Bộ assert cuối bám số liệu thật đo bằng
API (`XK000107`, `NK000334`, `882`, `57`) cùng một nhãn tiếng Việt chỉ tồn tại nếu bảng nhãn đủ
entry. Với bộ đó, một bảng rỗng hoặc một tiêu đề sai ngôn ngữ không thể xanh.
