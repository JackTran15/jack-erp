---
id: T-04-04
uow: UOW-04
title: Chạy ai-dlc-verify trên local-backoffice và sinh bằng chứng
layer: test
type: test
estimate: 3h
status: todo
depends_on: [T-04-03]
blocks: []
verifies: [AC-15, AC-16]
touches:
  - .ai/features/transfer-summary-drilldown/07-verification.md   # new
assumptions: [A-12]
---

# T-04-04 — Bằng chứng

## Context

`verify.py --doctor` đã trả **capable**, nên khối "Verification evidence" trong cả bốn `uow.md`
thật sự chặn G4. Ticket này là nơi mở khoá nó.

Bài học từ hai lượt chạy trước, đừng lặp lại:

- Lượt chạy của [[barcode-picker-hide-unit-price]] đỏ **toàn bộ** với `ERR_CONNECTION_REFUSED` trên
  `:3000` — dev server chưa bật.
- Lượt chạy đầu của [[sales-report-km-and-drilldown]] đỏ vì số kỳ vọng được đọc từ màn hình thay vì
  đo bằng SQL.
- Bảng Coverage của [[phu-luc-01-audit]] rỗng dù 40/40 xanh, vì mã `AC-BCK-06` không khớp
  `r"\bAC-\d+\b"` (`verify.py:290`).

## Implementation notes

Chuẩn bị: `make dev-api`, `make dev-backoffice`, `pnpm migration:run`, `pnpm seed:transfer-demo`,
rồi `verify.py … --doctor` xác nhận `local-backoffice` còn `capable`.

Frontmatter:

```yaml
---
feature: transfer-summary-drilldown
environments: [local-backoffice]
viewports: [desktop]
---
```

Chỉ `local-backoffice`: feature nằm trọn trong `/reports/inventory`, POS không có màn hình nào.
Chỉ `desktop`: `BackofficeLayout` là vỏ desktop cố định `ml-60`, không `matchMedia` ở đâu, và không
bug nào ở đây phụ thuộc chiều cao khung nhìn.

**Lấy số kỳ vọng từ bảng "Kỳ vọng seed" của T-01-04**, đã đo bằng SQL — không đọc từ màn hình.

Mọi bước đặt kỳ tường minh `2026-09-01`–`2026-09-02` rồi bấm "Lấy dữ liệu": phiên đăng nhập mở lại
preset dùng lần trước, và một ảnh xanh chụp lưới rỗng còn tệ hơn không có bằng chứng.

Mọi `Path` mang `?v=` riêng: `page.goto` tới URL chỉ khác hash là điều hướng cùng tài liệu, SPA
không remount, bước sau sẽ chụp nhầm báo cáo của bước trước.

15 bước phủ AC-01..AC-16 — khung bảng ở phần "Verification (ai-dlc-verify)" của
`/Users/macbook/.claude/plans/v-ai-dlc-verify-get-velvet-flurry.md`; thay số bằng số đo thật trước
khi chạy.

Ghi một câu trong phần văn xuôi ánh xạ toàn bộ AC về `AC-BCK-06` của Phụ lục 01 (mã bằng chứng
`BO-S10` trong `docs/client/phu-luc-01-checklist.csv`), vì runner không tự làm được.

## Cập nhật 2026-09-03 — thêm bước trên dữ liệu thật

`erp_dev` giờ có dữ liệu vận hành kỳ **09/07/2026–30/08/2026**. Bằng chứng nên có **hai** cụm bước:

- **Cụm seed** (01–02/09/2026): phủ AC-04 và AC-05, hai thứ dữ liệu thật không có
  (0 phiếu lập tay, 0 `stock_transfers` liên chi nhánh).
- **Cụm dữ liệu thật** (09/07/2026–30/08/2026): phủ AC-01, AC-03, AC-06..AC-13 ở quy mô thật —
  18 chi nhánh, và dòng "Chi Nhánh cũ không dùng" đúng là ca người dùng báo (*xuất 22, nhập về 31*),
  sau khi sửa thành 22 / 22 / 0. Ảnh này là thứ đáng gửi khách, không phải ảnh của seed.

Số kỳ vọng cho cụm dữ liệu thật lấy từ bảng trong `01-assumptions.md` §"Đo trên dữ liệu thật", và
**đo lại** sau khi T-01-03 land.

## Done when

- [ ] `verify.py .ai/features/transfer-summary-drilldown --write` exit 0, mọi bước xanh
- [ ] `evidence_check.py .ai/features/transfer-summary-drilldown` exit 0
- [ ] Bảng Coverage trong `08-evidence.md` **không rỗng** — mọi AC có ít nhất một bước
- [ ] Mở tay từng ảnh của S6, S9, S12: lưới có dòng, không phải "Hiển thị 0 - 0 trên 0 kết quả"
- [ ] `08-evidence.md` có commit sha khớp HEAD
- [ ] Hai bước hồi quy (Báo cáo 2, Báo cáo 7) xanh và không có toast lỗi (AC-15)
