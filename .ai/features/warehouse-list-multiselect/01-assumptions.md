# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Chuyển kho và Lệnh điều chuyển chỉ cần multi-select, không cần nút "In tem mã" | high | yes | Nếu sai thì checkbox ở 2 trang này không dùng được vào việc gì, phải làm thêm 2 toolbar item + gom lines | resolved | Đã hỏi người dùng 2026-08-27, chốt "Chỉ thêm checkbox, không thêm nút" |
| A-02 | Chưa tick phiếu nào thì "In tem mã" in theo dòng đang xem (hành vi hiện tại) | high | yes | Nếu sai thì nút phải disabled, đổi hành vi người dùng đang quen | resolved | Đã hỏi người dùng 2026-08-27, chốt "In theo dòng đang xem" |
| A-03 | "Chọn tất cả" chỉ áp cho các dòng đang hiển thị của trang hiện tại, không phải toàn bộ kết quả lọc | high | yes | Nếu sai thì phải fetch id toàn bộ kết quả lọc rồi fetch lines hàng trăm phiếu — rủi ro treo trình duyệt | resolved | Đã hỏi người dùng 2026-08-27, chốt "Chỉ trang hiện tại, đổi trang GIỮ tick" |
| A-04 | Đổi bộ lọc hoặc bấm "Nạp" thì xóa tick; đổi trang thì giữ tick | medium | no | Người dùng in nhầm phiếu của bộ lọc cũ, hoặc mất công tick lại khi lật trang | resolved | Nằm trong lựa chọn A-03; giữ tick khi đổi trang là phần "gom qua nhiều trang", xóa khi đổi lọc là phần an toàn |
| A-05 | `GET /:id` trả về `lines` đầy đủ cho một phiếu, không phân trang, nên gom lines nhiều phiếu = N lần `GET /:id` là đủ | medium | no | Phiếu nhiều dòng bị cắt cụt khi in tem hàng loạt | resolved | Đọc code: toolbar "In tem mã" hiện tại đã đọc `selectedOrder.lines` từ `GET /goods-receipts/:id` (PurchaseOrdersPage.tsx:488) và `GET /inventory/goods-issues/:id` (GoodsIssuePage.tsx:463). `/:id/lines` phân trang chỉ phục vụ panel chi tiết cuộn vô hạn |
| A-06 | Số phiếu tick một lượt ở mức chục, không phải nghìn, nên N lần `GET /:id` tuần tự/song song có giới hạn là chấp nhận được | medium | no | Bấm in tem sau khi tick 500 phiếu làm nghẽn trình duyệt | resolved | Giới hạn tự nhiên: "Chọn tất cả" chỉ tick trang hiện tại (pageSize mặc định 20, tối đa theo picker). Gom qua nhiều trang cần thao tác thủ công từng trang. Vẫn chặn bằng giới hạn đồng thời khi fetch |
| A-07 | Panel "Chi tiết" của Chuyển kho đọc `lines` sẵn có trong row danh sách, không fetch — nên trang này vốn đã không fetch khi tick | high | no | Mô tả sai hiện trạng, làm thừa việc | resolved | Đọc code: `StockTransferPage.tsx:536` truyền `activeRecord` (row danh sách) thẳng vào `DetailPanel`, và `DetailPanel` đọc `transfer.lines` (dòng 607). Trang này chỉ cần phần multi-select |
| A-08 | Dòng đang xem cần dấu hiệu trực quan mới sau khi tách khỏi ô tick | high | no | Người dùng không biết panel chi tiết đang hiển thị phiếu nào | resolved | `BaseDataTable` không có prop nào tô dòng đang chọn (đọc phần render `<tr>`, dòng 598–607) — thêm prop `rowClassName` tùy chọn |
| A-09 | `apps/backoffice-web` có chỗ chạy unit test | low | no | Ticket đòi spec cho một util mà repo không chạy được test đó — spec thành code chết | resolved | Sai. `apps/backoffice-web/package.json` dòng 10 là `"test": "echo test"`, không có vitest/jest trong workspace, khớp với CLAUDE.md ("only @erp/api has real tests"). Bỏ spec khỏi T-02-01, chuyển kiểm chứng AC-14 sang demo trình duyệt |
| A-10 | `GET /:id` của phiếu nhập/xuất trả kèm `sellingPrice` trên quan hệ `item` | high | no | Nếu sai thì T-04-01 không tắt được nguồn phát, giá vẫn 0 và effect vẫn tra lại từng SKU | resolved | Đúng. `goods-receipt-line.entity.ts:66` khai `@ManyToOne(() => ItemEntity, { eager: true })` không kèm `select`, `item.entity.ts:49` có cột `selling_price`. Kiểm thực nghiệm bằng S16: giá 750.000 hiện ngay lúc mount |
| A-11 | Bỏ `sellingPrice: 0` ở hai trang nguồn là đủ để hết bùng nổ request | medium | no | Nguồn prefill khác (Chi tiết vị trí chế độ tổng quan) vẫn làm treo trình duyệt khi chọn nhiều | resolved | Không đủ. Effect trong `InventoryItemBarcodesPage` vẫn lặp theo từng dòng và `Promise.all` không giới hạn — bất kỳ nguồn nào thiếu giá đều tái diễn. T-04-02 gộp theo `itemId` và đặt trần đồng thời 6 |
| A-12 | Bốn tab kho trong phạm vi ban đầu là tất cả chỗ còn dính tick-là-fetch | medium | no | Người dùng vẫn gặp lại đúng triệu chứng ở trang khác | resolved | Không đúng. Rà `leadingColumn` toàn bộ `pages/`: Kiểm kê kho cũng tick-là-fetch (`selectStockTake` trong ô checkbox) và đã có multi-select để gộp phiếu — sửa ở UOW-05. Nhân viên / Thu chi tiền mặt / Thu chi tiền gửi cũng tick-là-fetch nhưng chỉ single-select, chưa có thao tác hàng loạt nên để nguyên. `TransferInPage` đã có multi-select riêng và không fetch khi tick |
