---
id: T-03-03
uow: UOW-03
title: Lệnh điều chuyển — multi-select và Chọn tất cả
layer: web
type: feature
estimate: 2h
status: done
depends_on: [T-01-04]
blocks: []
verifies: [AC-01, AC-03, AC-05]
touches:
  - apps/backoffice-web/src/pages/transfer-orders/TransferOrdersPage.tsx
assumptions: [A-01, A-04]
---

# T-03-03 — Lệnh điều chuyển — multi-select và Chọn tất cả

## Context

`TransferOrdersPage` cùng khuôn với `PurchaseOrdersPage`: ô tick từ `selectedId`
(dòng 556-573), `useQuery(["transfer-order", selectedId])` (dòng 305), `DetailPanel`
cuộn vô hạn `/inventory/transfer-orders/{id}/lines` (dòng 640). Nên tick ở đây là hai
request, đúng triệu chứng người dùng nêu.

Trang này **không** có nút "In tem mã" và ticket này **không** thêm (A-01).

## Implementation notes

- Y hệt T-01-04: `useRowMultiSelect`, `SelectAllCheckbox`, `RowSelectCheckbox`,
  `rowClassName`, effect xóa tick, "Nạp" gọi `clearChecked()`
- Effect xóa tick: đọc deps của `loadRecords` trong file này rồi trừ `pagination`
- **Không** thêm toolbar item nào

## Done when

- [x] Network lọc `transfer-orders`: tick 3 ô liên tiếp → 0 request
- [x] Click cột "Lý do" → có request, tick không đổi
- [x] Ô header Chọn tất cả: tick hết / sạch / indeterminate
- [x] Lật trang giữ tick; "Nạp" và đổi lọc sạch tick
- [x] `rtk proxy grep -n "In tem mã" TransferOrdersPage.tsx` → không có kết quả
- [x] `pnpm --filter @erp/backoffice-web build` xanh
