---
id: T-02-03
uow: UOW-02
title: cancel() phiếu nhập chạy qua engine chênh lệch
layer: service
type: refactor
estimate: 3h
status: done
depends_on: [T-02-02]
blocks: [T-02-04, T-03-02, T-03-01]
verifies: [AC-10]
touches:
  - apps/api/src/modules/inventory/goods-receipt/goods-receipt.service.ts
assumptions: [A-01, A-02, A-06]
---

# T-02-03 — Xoá là sửa về rỗng

## Context
ADR-02. `cancel()` hiện đảo sổ kho và xoá dòng nợ NCC nhưng bỏ quên bút toán — gọi lại engine
với `after = []` là cách làm cho lỗi đó không tái diễn được.

## Implementation notes
- Giữ nguyên đường dẫn, mã trạng thái và hành vi soft-delete + `status = CANCELLED`
- Thân xử lý: khoá row (đã có), `computeVoucherDelta(before, [])`, ghi chênh lệch sổ kho, rồi
  hạch toán chênh lệch tiền theo đúng nhánh của phiếu
- Bỏ nhánh xoá cứng `SupplierDebtEntity`: dòng nợ đi theo quy tắc của T-02-02
  (`originalAmount = 0`; còn `paidAmount` thì thành `overpaid`, không thì xoá dòng)
- Bỏ luôn guard "nợ NCC đã có thanh toán" theo A-02
- Giữ nguyên phần trả `TransferOrderEntity` về `IN_PROGRESS` đang có

## Done when
- [x] Xoá phiếu công nợ: INV-1, INV-2, INV-3 đều về 0
- [x] Xoá phiếu đã trả một phần: dòng nợ còn lại ở trạng thái `overpaid`, không bị mất dấu
- [x] Xoá phiếu điều chuyển vẫn trả lệnh về `IN_PROGRESS` như trước
- [x] Test xoá hai lần song song: chỉ đảo một lần
