---
id: T-04-01
uow: UOW-04
title: GoodsIssueService.update() ghi chênh lệch lên sổ kho, có khoá row
layer: service
type: feature
estimate: 4h
status: done
depends_on: [T-01-04]
blocks: [T-04-02, T-04-03, T-05-01]
verifies: [AC-12, AC-13]
touches:
  - apps/api/src/modules/inventory/goods-issue/goods-issue.service.ts
assumptions: [A-01, A-02, A-06, A-10]
---

# T-04-01 — Sửa phiếu xuất đã ghi sổ

## Context
Phiếu xuất hiện không có hàm `update` nào; dialog ở chế độ sửa đang tạo phiếu trùng.

## Implementation notes
- Hình dạng y hệt T-01-04: transaction → `SELECT … FOR UPDATE` → đọc lại status/revision →
  `computeVoucherDelta` → `recordBatchMovements` → ghi đè dòng hàng + header → `revision += 1`
- Dấu ngược với phiếu nhập: phiếu xuất giữ số lượng âm trên sổ, nên `quantityDelta > 0`
  (xuất thêm) tạo movement âm và ngược lại
- `referenceType = 'GOODS_ISSUE'`, `skipInactiveStorageGuard: true`
- Không chặn tồn âm (A-02)
- Giá vốn tạm lấy đơn giá đang lưu của dòng; quy tắc đầy đủ ở T-04-02

## Done when
- [x] Sửa tăng, sửa giảm, thêm dòng, xoá dòng đều ghi đúng một dòng chênh lệch mỗi cặp
- [x] Sửa phần header không sinh dòng ledger nào
- [x] Hai request sửa song song: một thành công, một 409
- [x] Test khẳng định INV-1 sau khi sửa
