---
id: T-04-04
uow: UOW-04
title: cancel() phiếu xuất chạy qua engine và khoá row
layer: service
type: refactor
estimate: 2h
status: done
depends_on: [T-04-02]
blocks: [T-04-05, T-05-02]
verifies: [AC-14]
touches:
  - apps/api/src/modules/inventory/goods-issue/goods-issue.service.ts
assumptions: [A-06]
---

# T-04-04 — Xoá phiếu xuất

## Context
Lỗi #5 của bản audit: `cancel()` kiểm tra trạng thái ngoài transaction nên hai request song song
cùng lọt và cộng đôi tồn kho.

## Implementation notes
- Khoá `SELECT … FOR UPDATE` trên `goods_issues`, đọc lại `status` trong transaction trước khi ghi
- Thân xử lý gọi engine với `after = []`, dùng đơn giá đã ghi của phiếu
- Giữ nguyên phần cascade sang lệnh điều chuyển (`assertExportIssueCanBeCancelled`,
  `cancelFromExportIssue`) và tuỳ chọn `cascadeTransferOrder`

## Done when
- [x] Xoá phiếu xuất: INV-1, INV-2 về 0
- [x] Hai request xoá song song: chỉ một lần đảo bút, request sau nhận 409
- [x] Luồng huỷ chân xuất của lệnh điều chuyển vẫn hoạt động y như trước
