/** Placeholder table for multiple providers per item (when backend supports it). */
export function ProvidersPlaceholderTable() {
  return (
    <div className="rounded-md border border-dashed border-border p-6 text-center text-sm text-muted-foreground">
      Cấu hình nhiều nhà cung cấp cho mặt hàng — sẽ khả dụng khi máy chủ hỗ trợ.
    </div>
  );
}
